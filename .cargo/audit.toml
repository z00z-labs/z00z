[advisories]
# bincode remains transitively required by the current workspace and Nova
# dependency graph. Track its unmaintained status as an explicit exception
# until those owners migrate to a maintained codec.
ignore = ["RUSTSEC-2025-0141"]
informational_warnings = ["unmaintained", "unsound"]
