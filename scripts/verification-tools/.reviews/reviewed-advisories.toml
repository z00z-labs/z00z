[[reviewed]]
id = "RUSTSEC-2025-0141"
crate = "bincode"
version = "2.0.1"
scope = "project"
decision = "temporary_exception"
reason = "The advisory is unmaintained-status only. The crate is transitively embedded in z00z_core, z00z_utils, and z00z_wallets and is used across the Phase 060 execution surface. No repository-local memory-safety or RCE claim is attached to this RustSec item, and replacing the serialization stack inside the current closeout slice would widen protocol and persistence scope."
action = "Keep the repository on one canonical reviewed exception path now; schedule replacement or removal in the next dependency-refresh slice that can touch cross-crate serialization contracts."
owner = "phase-060-supply-chain"
sunset = "before the next dependency-refresh milestone after Phase 060"
ancestry = "z00z_core -> z00z_storage -> z00z_wallets; z00z_utils -> z00z_crypto -> z00z_wallets"
criticality = "high transitively, because the crate fans into core, storage, wallet, and simulator paths"
replacement = "Evaluate a maintained bincode successor or reduce the dependency fan-out before upgrading"
temporary_exception_conditions = "Allowed only while the advisory remains classification-only, the dependency path stays understood, and a later dependency-refresh slice owns the replacement plan"

[[reviewed]]
id = "RUSTSEC-2024-0388"
crate = "derivative"
version = "2.2.0"
scope = "project"
decision = "temporary_exception"
reason = "The advisory is unmaintained-status only. The crate is a proc-macro dependency entering the tree through eframe and the z00z_wallets UI surface, not through consensus or settlement-critical runtime code."
action = "Keep as a reviewed temporary exception for the current closeout; remove or replace through the next wallet/UI dependency refresh."
owner = "phase-060-supply-chain"
sunset = "before the next wallet UI dependency-refresh milestone"
ancestry = "z00z_wallets -> eframe -> egui-winit -> accesskit_unix -> zbus -> derivative"
criticality = "medium, because it is project-owned but currently constrained to wallet/UI integration paths"
replacement = "Prefer upgrading or removing the upstream eframe/zbus chain that pulls derivative"
temporary_exception_conditions = "Allowed only while usage remains transitive through the UI stack and no maintained drop-in path is available inside Phase 060"

[[reviewed]]
id = "RUSTSEC-2024-0384"
crate = "instant"
version = "0.1.13"
scope = "project"
decision = "temporary_exception"
reason = "The advisory is unmaintained-status only. The crate is transitive through futures-lite and the wallet/UI async stack rather than through consensus or settlement-critical storage logic."
action = "Track it on the canonical repo-owned review path now and retire it during the next dependency-refresh slice for the wallet/UI stack."
owner = "phase-060-supply-chain"
sunset = "before the next wallet UI dependency-refresh milestone"
ancestry = "z00z_wallets -> eframe/accesskit_unix or zbus async path -> futures-lite -> instant"
criticality = "medium, because it is project-owned but primarily attached to the wallet/UI async surface"
replacement = "Prefer upstream removal through futures-lite or adjacent wallet/UI dependency upgrades"
temporary_exception_conditions = "Allowed only while the dependency remains transitive and the advisory stays classification-only rather than a concrete exploit"

[[reviewed]]
id = "RUSTSEC-2024-0436"
crate = "paste"
version = "1.0.15"
scope = "project"
decision = "temporary_exception"
reason = "The advisory is unmaintained-status only. The crate is a proc-macro dependency entering through the p3-field / p3-goldilocks chain into z00z_crypto and then fanning into core, storage, validator, watcher, and wallet consumers."
action = "Keep as an explicitly reviewed exception now and migrate away during the next cryptography dependency refresh that can safely touch the p3 stack."
owner = "phase-060-supply-chain"
sunset = "before the next crypto dependency-refresh milestone"
ancestry = "z00z_crypto <- p3-field / p3-goldilocks family <- paste; then fans into z00z_core, z00z_storage, z00z_wallets, z00z_rollup_node, and z00z_simulator"
criticality = "high, because the proc-macro enters the project-owned crypto tree and fans out broadly"
replacement = "Upgrade or replace the upstream p3 dependency family that still requires paste"
temporary_exception_conditions = "Allowed only while the advisory remains unmaintained-status only and the replacement is handled in a dedicated crypto dependency refresh rather than inside Phase 060 closeout"

[[reviewed]]
id = "RUSTSEC-2025-0141"
crate = "bincode"
version = "1.3.3"
scope = "vendor"
decision = "vendor_exception"
reason = "The advisory is unmaintained-status only and the finding is scoped to the protected vendor subtree under crates/z00z_crypto/tari/**. Phase 060 does not edit vendor code."
action = "Keep as an explicit vendor-scoped exception until upstream replacement or vendor refresh is intentionally scheduled."
owner = "phase-060-supply-chain"
sunset = "next intentional tari vendor refresh"
ancestry = "tari_bulletproofs_plus / tari_crypto / tari_utilities vendor subtree"
criticality = "vendor-scoped"
replacement = "Handle only in a dedicated vendor refresh or upstream sync"
temporary_exception_conditions = "Allowed only while the vendor subtree stays isolated and read-only in the repository"

[[reviewed]]
id = "RUSTSEC-2026-0194"
crate = "quick-xml"
version = "0.39.2"
scope = "project"
decision = "temporary_exception"
reason = "The vulnerable crate enters the project tree through wayland-scanner in the eframe/winit UI build stack rooted at z00z_wallets. The reported issue requires iterating untrusted runtime XML attributes, but the observed repository ancestry is build-time code generation rather than a project-owned runtime XML parser on protocol, storage, wallet-RPC, or simulator inputs."
action = "Keep this on an explicit reviewed exception path for Phase 065, and retire it by upgrading the wallet/UI stack to a quick-xml path >= 0.41.0 during the next UI dependency refresh."
owner = "phase-065-supply-chain"
sunset = "before the next wallet UI dependency-refresh milestone"
ancestry = "z00z_wallets -> eframe/egui-winit/winit -> smithay-client-toolkit/wayland-client -> wayland-scanner -> quick-xml"
criticality = "medium, because the finding is project-scoped in cargo metadata but the reachable repository path is build-time UI tooling rather than a project-owned runtime XML boundary"
replacement = "Upgrade the wallet/UI dependency chain so the wayland-scanner path resolves to quick-xml >= 0.41.0"
temporary_exception_conditions = "Allowed only while the dependency remains confined to the wallet/UI build stack and no project-owned runtime path parses untrusted XML through quick-xml"

[[reviewed]]
id = "RUSTSEC-2026-0195"
crate = "quick-xml"
version = "0.39.2"
scope = "project"
decision = "temporary_exception"
reason = "The vulnerable crate enters the project tree through wayland-scanner in the eframe/winit UI build stack rooted at z00z_wallets. The reported namespace-allocation issue in NsReader assumes a project-owned runtime XML parsing boundary, but the observed repository ancestry is build-time code generation rather than protocol, storage, wallet-RPC, or simulator handling of untrusted XML inputs."
action = "Keep this on an explicit reviewed exception path for Phase 065, and retire it by upgrading the wallet/UI stack to a quick-xml path >= 0.41.0 during the next UI dependency refresh."
owner = "phase-065-supply-chain"
sunset = "before the next wallet UI dependency-refresh milestone"
ancestry = "z00z_wallets -> eframe/egui-winit/winit -> smithay-client-toolkit/wayland-client -> wayland-scanner -> quick-xml"
criticality = "medium, because the finding is project-scoped in cargo metadata but the reachable repository path is build-time UI tooling rather than a project-owned runtime XML boundary"
replacement = "Upgrade the wallet/UI dependency chain so the wayland-scanner path resolves to quick-xml >= 0.41.0"
temporary_exception_conditions = "Allowed only while the dependency remains confined to the wallet/UI build stack and no project-owned runtime path parses untrusted XML through quick-xml"

[[reviewed]]
id = "RUSTSEC-2026-0192"
crate = "ttf-parser"
version = "0.25.1"
scope = "project"
decision = "temporary_exception"
reason = "The advisory is unmaintained-status only. The crate is transitive through ab_glyph and the eframe/egui font stack rooted at z00z_wallets, not through consensus, checkpoint, storage, wallet-RPC, or simulator verification logic."
action = "Keep as a reviewed temporary exception now and remove or replace it through the next wallet/UI dependency refresh."
owner = "phase-065-supply-chain"
sunset = "before the next wallet UI dependency-refresh milestone"
ancestry = "z00z_wallets -> eframe/egui -> epaint -> ab_glyph -> owned_ttf_parser -> ttf-parser"
criticality = "medium, because it is project-owned in cargo metadata but constrained to the wallet/UI font path"
replacement = "Upgrade or replace the upstream eframe/egui font stack that still pulls ttf-parser"
temporary_exception_conditions = "Allowed only while the advisory remains unmaintained-status only and the dependency stays confined to the wallet/UI rendering stack"
