---
phase: 065-Attack-Surface
plan: PLAN-065-G03
type: execute
wave: 3
depends_on:
  - PLAN-065-G02
files_modified:
  - crates/z00z_storage/src/settlement/hjmt_cache.rs
  - crates/z00z_storage/src/settlement/hjmt_scheduler.rs
  - crates/z00z_wallets/src/db/mod.rs
  - crates/z00z_wallets/src/wallet/mod.rs
  - crates/z00z_wallets/Cargo.toml
  - crates/z00z_simulator/Cargo.toml
  - scripts/audit/audit_release_feature_guards.sh
  - .github/workflows/release-safety-guards.yml
  - crates/z00z_wallets/tests/test_production_hardening.rs
  - crates/z00z_wallets/tests/test_live_boundary_claims.rs
requirements:
  - WS-03
autonomous: true
must_haves:
  truths:
    - "Release-capable builds must not carry weakened KDF settings, wallet secret debug exporters, or public corruption hooks."
    - "Debug-only surfaces must become test-only or explicitly internal-only."
  artifacts:
    - crates/z00z_wallets/Cargo.toml
    - crates/z00z_simulator/Cargo.toml
    - scripts/audit/audit_release_feature_guards.sh
  key_links:
    - "Scope: WS-03 release/build hardening for debug and test-only surfaces."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-065-G03 Plan: Release Build Hardening For Debug Surfaces

<objective>
Close `WS-03` by making debug and test-only surfaces impossible in
release-capable builds, moving remaining secret-export helpers and corruption
hooks behind test-only or internal-only boundaries, and adding an executable
release-feature guard matrix that fails closed.
</objective>

<context>
@.planning/phases/065-Attack-Surface/065-TODO.md
@.planning/phases/065-Attack-Surface/065-CONTEXT.md
@crates/z00z_storage/src/settlement/hjmt_cache.rs
@crates/z00z_storage/src/settlement/hjmt_scheduler.rs
@crates/z00z_wallets/src/db/mod.rs
@crates/z00z_wallets/src/wallet/mod.rs
@crates/z00z_wallets/Cargo.toml
@crates/z00z_simulator/Cargo.toml
@crates/z00z_wallets/tests/test_production_hardening.rs
@crates/z00z_wallets/tests/test_live_boundary_claims.rs
</context>

<coverage_contract>
This plan covers `WS-03`. Closure requires release-capable builds to reject
`test-params-fast`, `wallet_debug_tools`, and comparable debug/test-only
surfaces unless they are moved behind an explicitly internal-only build lane.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-065-G03`
task_ids:
- `WS-03`
copied_task_rows:
- "| WS-03 | Debug And Test-Only Surfaces Must Be Impossible In Release-Capable Builds | Sources: crate `AS-20260623-001`; crate `AS-20260623-003`; `AS-20260501-027`; `AS-20260501-031` | Close when release-capable builds cannot carry weakened KDF settings, plaintext secret export tools, or public cache/scheduler corruption hooks; the only remaining debug surfaces are test-only or explicitly internal-only. |"
source_refs:
- `WS-03`: `065-TODO.md` lines `442-470`
- `Active Disposition Map`: `065-TODO.md` lines `746`, `748`, `750`, `753`
- `crate AS-20260623-001`
- `crate AS-20260623-003`
- `AS-20260501-027`
- `AS-20260501-031`
inputs:
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_storage/src/settlement/hjmt_cache.rs`
- `crates/z00z_storage/src/settlement/hjmt_scheduler.rs`
- `crates/z00z_wallets/src/db/mod.rs`
- `crates/z00z_wallets/src/wallet/mod.rs`
- `crates/z00z_wallets/Cargo.toml`
- `crates/z00z_simulator/Cargo.toml`
outputs:
- `.planning/phases/065-Attack-Surface/065-03-SUMMARY.md`
- changed or created `crates/z00z_storage/src/settlement/hjmt_cache.rs`
- changed or created `crates/z00z_storage/src/settlement/hjmt_scheduler.rs`
- changed or created `crates/z00z_wallets/src/db/mod.rs`
- changed or created `crates/z00z_wallets/src/wallet/mod.rs`
- changed or created `crates/z00z_wallets/Cargo.toml`
- changed or created `crates/z00z_simulator/Cargo.toml`
- changed or created `scripts/audit/audit_release_feature_guards.sh`
- changed or created `.github/workflows/release-safety-guards.yml`
- changed or created `crates/z00z_wallets/tests/test_production_hardening.rs`
- changed or created `crates/z00z_wallets/tests/test_live_boundary_claims.rs`
dependencies:
- `PLAN-065-G02`
acceptance_tests:
- `cargo test --release -p z00z_wallets --test test_production_hardening -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_live_boundary_claims -- --nocapture`
- `bash scripts/audit/audit_release_feature_guards.sh`
- `cargo test --release`
simulation_gate:
- No protocol simulation is required. Prove closure with release build matrices, source audits, and targeted runtime tests only.
negative_tests:
- `cargo check -p z00z_wallets --release --features test-params-fast` must fail.
- `cargo check -p z00z_simulator --release --features wallet_debug_tools` must fail.
- `cargo check -p z00z_simulator --release --features test-params-fast` must fail unless the feature is moved into an internal-only lane that public release builds cannot select.
- public cache/scheduler corruption hooks must disappear from release-capable builds.
</plan_matrix>

<todo_exact_mirror>
This block mirrors the exact normative `WS-03` bullets from `065-TODO.md`. It
is a coverage mirror only and does not replace the TODO as authority.
Required implementation:
- Add workspace-level compile guards so `test-params-fast` cannot be used in
  release-capable builds outside explicitly internal-only tooling.
- Add workspace-level compile guards so `wallet_debug_tools` cannot be included
  in public or production-shaped release builds.
- Move `debug_export_wallet` and plaintext debug artifact emission into
  test-only modules or a separate internal-only tool surface.
- Gate settlement cache corruption and scheduler test controls behind
  `cfg(test)` or a dedicated internal test-only feature that downstream release
  crates cannot enable by accident.
- Remove release documentation that normalizes running production-shaped
  binaries with weakened KDF or secret-export features enabled.
Required tests:
- Expected-failure build checks such as:
  - `cargo check -p z00z_wallets --release --features test-params-fast`
  - `cargo check -p z00z_simulator --release --features wallet_debug_tools`
  - any equivalent workspace matrix that proves these feature combinations are
    rejected
- Source-audit tests proving release builds do not export settlement corruption
  hooks or wallet secret debug exporters.
Close when:
- release-capable builds cannot carry weakened KDF settings, plaintext secret
  export tools, or public cache/scheduler corruption hooks;
- the only remaining debug surfaces are test-only or explicitly internal-only.
</todo_exact_mirror>

<plan_artifacts>
- artifact_id: `release-feature-policy`
  path_or_api: `crates/z00z_wallets/Cargo.toml`
  kind: `config`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-03]
- artifact_id: `simulator-feature-policy`
  path_or_api: `crates/z00z_simulator/Cargo.toml`
  kind: `config`
  owner_crate: `z00z_simulator`
  created_or_modified: `modify`
  source_task_ids: [WS-03]
- artifact_id: `debug-export-surface`
  path_or_api: `crates/z00z_wallets/src/db/mod.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-03]
- artifact_id: `settlement-test-hooks`
  path_or_api: `crates/z00z_storage/src/settlement/hjmt_cache.rs`
  kind: `code`
  owner_crate: `z00z_storage`
  created_or_modified: `modify`
  source_task_ids: [WS-03]
- artifact_id: `release-feature-guard-audit`
  path_or_api: `scripts/audit/audit_release_feature_guards.sh`
  kind: `script`
  owner_crate: `repo`
  created_or_modified: `create`
  source_task_ids: [WS-03]
- artifact_id: `plan-065-g03-summary`
  path_or_api: `.planning/phases/065-Attack-Surface/065-03-SUMMARY.md`
  kind: `doc`
  owner_crate: `planning`
  created_or_modified: `create`
  source_task_ids: [WS-03]
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_wallets --test test_production_hardening -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_live_boundary_claims -- --nocapture`
- `bash scripts/audit/audit_release_feature_guards.sh`
- `cargo test --release`
</plan_tests>

<plan_results>
- `PLAN-065-G03` produces `.planning/phases/065-Attack-Surface/065-03-SUMMARY.md` with the feature-matrix policy, changed exports, and expected-failure build evidence.
- Group acceptance is proven only when forbidden release-feature combinations fail closed and secret-export/corruption hooks disappear from release-capable builds.
</plan_results>

<task_artifacts>
- task_id: `WS-03`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_storage/src/settlement/hjmt_cache.rs`
    - `crates/z00z_storage/src/settlement/hjmt_scheduler.rs`
    - `crates/z00z_wallets/src/db/mod.rs`
    - `crates/z00z_wallets/src/wallet/mod.rs`
    - `crates/z00z_wallets/Cargo.toml`
    - `crates/z00z_simulator/Cargo.toml`
    - `scripts/audit/audit_release_feature_guards.sh`
    - `.github/workflows/release-safety-guards.yml`
</task_artifacts>

<task_tests>
- task_id: `WS-03`
  commands:
    - `cargo test --release -p z00z_wallets --test test_production_hardening -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_live_boundary_claims -- --nocapture`
    - `bash scripts/audit/audit_release_feature_guards.sh`
  scenarios:
    - `forbidden release-feature combinations fail closed`
    - `debug secret export surfaces are not reachable in release-capable builds`
    - `settlement corruption and scheduler test hooks are test-only or internal-only`
</task_tests>

<task_results>
- task_id: `WS-03`
  expected_results:
    - `release-capable builds cannot carry weakened KDF settings, plaintext secret export tools, or public cache/scheduler corruption hooks`
    - `the only remaining debug surfaces are test-only or explicitly internal-only`
</task_results>

<anti_placeholder_gate>
- Comment-only warnings such as `SECURITY: NEVER enable in production builds` do not close this plan without compile guards.
- Feature flags remain phase-failing if public release builds can still enable them.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_storage/src/settlement/hjmt_cache.rs`
- `crates/z00z_storage/src/settlement/hjmt_scheduler.rs`
- `crates/z00z_wallets/src/db/mod.rs`
- `crates/z00z_wallets/src/wallet/mod.rs`
- `crates/z00z_simulator/Cargo.toml`
</current_code_refs>

<blockers>
- Any release-capable build that still compiles with forbidden debug/test features is phase-failing.
</blockers>

<evidence_gate>
- Completion evidence must include the exact expected-failure build commands or audit-script output proving those combinations are rejected.
</evidence_gate>

<not_recommendation_gate>
- This slice must add executable build policy, not prose asking developers to remember safe feature choices.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Release Build Hardening For Debug Surfaces</name>
<read_first>
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_storage/src/settlement/hjmt_cache.rs`
- `crates/z00z_storage/src/settlement/hjmt_scheduler.rs`
- `crates/z00z_wallets/src/db/mod.rs`
- `crates/z00z_wallets/src/wallet/mod.rs`
- `crates/z00z_wallets/Cargo.toml`
- `crates/z00z_simulator/Cargo.toml`
</read_first>
<files>
- `crates/z00z_storage/src/settlement/hjmt_cache.rs`
- `crates/z00z_storage/src/settlement/hjmt_scheduler.rs`
- `crates/z00z_wallets/src/db/mod.rs`
- `crates/z00z_wallets/src/wallet/mod.rs`
- `crates/z00z_wallets/Cargo.toml`
- `crates/z00z_simulator/Cargo.toml`
- `scripts/audit/audit_release_feature_guards.sh`
</files>
<action>
Execute `PLAN-065-G03` by making forbidden release-feature combinations fail
closed, moving debug wallet export and corruption hooks behind test-only or
internal-only boundaries, and locking those rules in scripts and CI.
</action>
<acceptance_criteria>
- `WS-03` closes only when release-capable builds cannot carry weakened KDF settings, plaintext secret export tools, or public cache/scheduler corruption hooks.
- The only remaining debug surfaces are test-only or explicitly internal-only.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_wallets --test test_production_hardening -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_live_boundary_claims -- --nocapture`
   - `bash scripts/audit/audit_release_feature_guards.sh`
   - `cargo test --release`
3. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
4. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
5. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
`WS-03` is closed only when forbidden release builds fail and the remaining
debug surfaces are visibly non-public and non-production.
</done>
</task>
</tasks>

<threat_model>
- Debug features can look harmless in Cargo metadata while still reaching release-capable binaries.
- Public re-exports can keep secret-export helpers reachable even after docs warn against them.
- Test corruption hooks can escape into release-capable code unless compile-gated.
</threat_model>

<verification>
This slice is valid only if release build policy is executable and fail-closed.
</verification>

<success_criteria>
- `WS-03` traceability stays attached to the named feature and debug-surface findings.
- Release feature policy is enforced by code and CI rather than convention.
</success_criteria>

<output>
Create `.planning/phases/065-Attack-Surface/065-03-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/065-Attack-Surface/065-03-SUMMARY.md`
- release-feature guard script and CI hook
- narrowed release-capable public surface

## 🔎 Coverage Appendix

| TASK-NNN | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `WS-03` (normative unit; `065-TODO.md` has no `TASK-NNN` rows) | `PLAN-065-G03` | `065-TODO.md` `442-470`; disposition rows `746-753` | TODO + CONTEXT + feature/debug anchors | Cargo features + wallet/storage exports + audit script/CI | `cargo test --release -p z00z_wallets --test test_production_hardening -- --nocapture`; `cargo test --release -p z00z_wallets --test test_live_boundary_claims -- --nocapture`; `bash scripts/audit/audit_release_feature_guards.sh` | forbidden debug/test-only surfaces disappear from release-capable builds | no protocol simulation; build matrix and source-audit only | expected-failure build evidence and changed export surfaces prove the guard is real | `Complete` |
