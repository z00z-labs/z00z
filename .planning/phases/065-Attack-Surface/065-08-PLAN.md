---
phase: 065-Attack-Surface
plan: PLAN-065-G08
type: execute
wave: 8
depends_on:
  - PLAN-065-G07
files_modified:
  - crates/z00z_wallets/src/services/chain_service.rs
  - crates/z00z_wallets/src/rpc/chain_rpc.rs
  - crates/z00z_wallets/src/rpc/chain_rpc_impl.rs
  - crates/z00z_wallets/src/rpc/chain_types.rs
  - crates/z00z_wallets/src/rpc/tx_types.rs
  - crates/z00z_wallets/src/rpc/tx_runtime_state.rs
  - crates/z00z_wallets/src/rpc/tx_rpc_admission.rs
  - crates/z00z_wallets/src/app/app_kernel.rs
  - crates/z00z_wallets/tests/test_rpc_truth.rs
  - crates/z00z_wallets/tests/test_rpc_types_serialization.rs
  - crates/z00z_wallets/tests/test_stub_behavior.rs
  - crates/z00z_wallets/tests/test_runtime_validation_result.rs
requirements:
  - WS-08
autonomous: true
must_haves:
  truths:
    - "Public RPCs must either become real local truths or stop looking production-ready."
    - "Production DTOs must not blur verified summaries, checkpoint/root evidence, and proof payloads."
  artifacts:
    - crates/z00z_wallets/src/services/chain_service.rs
    - crates/z00z_wallets/src/rpc/chain_rpc_impl.rs
    - crates/z00z_wallets/src/rpc/tx_types.rs
  key_links:
    - "Scope: WS-08 placeholder public RPC surfaces and DTO cleanup."
    - "Gate owners: G-08 and G-09."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-065-G08 Plan: Placeholder Public RPC And DTO Contract Cleanup

<objective>
Close `WS-08` by removing production-looking placeholder public RPC semantics
unless they become real durable local truths, and by cleaning DTO contracts so
production responses stop advertising placeholder proof fields or root-shaped
strings as if they were proof payloads.
</objective>

<context>
@.planning/phases/065-Attack-Surface/065-TODO.md
@.planning/phases/065-Attack-Surface/065-CONTEXT.md
@crates/z00z_wallets/src/services/chain_service.rs
@crates/z00z_wallets/src/rpc/chain_rpc.rs
@crates/z00z_wallets/src/rpc/chain_rpc_impl.rs
@crates/z00z_wallets/src/rpc/chain_types.rs
@crates/z00z_wallets/src/rpc/tx_types.rs
@crates/z00z_wallets/src/rpc/tx_runtime_state.rs
@crates/z00z_wallets/src/rpc/tx_rpc_admission.rs
@crates/z00z_wallets/src/app/app_kernel.rs
</context>

<coverage_contract>
This plan covers `WS-08`. The chosen closure mode is
`live-claim-removed` unless a surface is upgraded to durable local truth in the
same slice. Placeholder public RPC names, stub defaults, and proof-shaped DTO
fields must either become real local truths or leave the production-looking
contract.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-065-G08`
task_ids:
- `WS-08`
copied_task_rows:
- "| WS-08 | Placeholder Public RPC Surfaces And Stub DTOs Must Either Become Real Or Leave The Contract | Sources: `CF-004`; `CF-007`; `CF-008`; carried placeholder parts of `CF-010` | Close when public RPCs are either real or explicitly non-production; production DTOs do not advertise placeholder proof semantics as if they were finalized behavior. |"
source_refs:
- `Canonical Gate Inventory: Inputs, Outputs, Verification, Proofs`: `065-TODO.md` lines `325-377`
- `WS-08`: `065-TODO.md` lines `599-624`
- `G-08`: `065-TODO.md` lines `325-349`
- `G-09`: `065-TODO.md` lines `351-377`
- `Historical Or Narrowed Findings`: `065-TODO.md` lines `675-678`
- `Active Disposition Map`: `065-TODO.md` lines `761-764`
- `CF-004`
- `CF-007`
- `CF-008`
- `carried placeholder parts of CF-010`
inputs:
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_wallets/src/services/chain_service.rs`
- `crates/z00z_wallets/src/rpc/chain_rpc.rs`
- `crates/z00z_wallets/src/rpc/chain_rpc_impl.rs`
- `crates/z00z_wallets/src/rpc/chain_types.rs`
- `crates/z00z_wallets/src/rpc/tx_types.rs`
- `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
- `crates/z00z_wallets/src/rpc/tx_rpc_admission.rs`
- `crates/z00z_wallets/src/app/app_kernel.rs`
outputs:
- `.planning/phases/065-Attack-Surface/065-08-SUMMARY.md`
- changed or created `crates/z00z_wallets/src/services/chain_service.rs`
- changed or created `crates/z00z_wallets/src/rpc/chain_rpc.rs`
- changed or created `crates/z00z_wallets/src/rpc/chain_rpc_impl.rs`
- changed or created `crates/z00z_wallets/src/rpc/chain_types.rs`
- changed or created `crates/z00z_wallets/src/rpc/tx_types.rs`
- changed or created `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
- changed or created `crates/z00z_wallets/src/rpc/tx_rpc_admission.rs`
- changed or created `crates/z00z_wallets/src/app/app_kernel.rs`
- changed or created `crates/z00z_wallets/tests/test_rpc_truth.rs`
- changed or created `crates/z00z_wallets/tests/test_rpc_types_serialization.rs`
- changed or created `crates/z00z_wallets/tests/test_stub_behavior.rs`
- changed or created `crates/z00z_wallets/tests/test_runtime_validation_result.rs`
dependencies:
- `PLAN-065-G07`
acceptance_tests:
- `cargo test --release -p z00z_wallets --test test_rpc_truth -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_rpc_types_serialization -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_stub_behavior -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_runtime_validation_result -- --nocapture`
- `cargo test --release`
simulation_gate:
- Do not invent remote-backed chain truth. Either wire these surfaces to durable local truth in the same slice or demote/remove the production-looking claim.
negative_tests:
- Synthetic scan status and synthetic chain tip must not remain on a production-looking public contract.
- Placeholder proof fields must not survive in production DTO defaults.
- Production docs and route wiring must not describe placeholder paths as completed functionality.
</plan_matrix>

<todo_exact_mirror>
This block mirrors the exact normative `WS-08` bullets from `065-TODO.md`. It
is a coverage mirror only and does not replace the TODO as authority.
Required implementation:
- Either implement durable or remote-backed chain scan and chain tip behavior,
  or remove or rename these RPCs so they are not production-looking wallet
  truths.
- Remove verification-free placeholder proof fields from production DTOs
  unless they are explicitly compatibility wrappers with a documented
  unsupported state.
- Keep stub-only defaults out of production-facing types.
- If any unsupported memo or placeholder feature is still a product
  requirement, move it into a dedicated implementation spec instead of leaving
  it as an ambiguous trusted-path placeholder.
Required tests:
- RPC truth tests proving scan status and chain tip are not synthetic while
  claiming to be live network truth.
- Schema tests proving placeholder-only proof fields do not appear on
  production DTOs by default.
- Tests that production docs and route wiring do not describe placeholder paths
  as completed functionality.
Close when:
- public RPCs are either real or explicitly non-production;
- production DTOs do not advertise placeholder proof semantics as if they were
  finalized behavior.
</todo_exact_mirror>

<plan_artifacts>
- artifact_id: `chain-rpc-truth-contract`
  path_or_api: `crates/z00z_wallets/src/services/chain_service.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-08]
- artifact_id: `chain-rpc-surface`
  path_or_api: `crates/z00z_wallets/src/rpc/chain_rpc_impl.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-08]
- artifact_id: `receipt-dto-contract`
  path_or_api: `crates/z00z_wallets/src/rpc/tx_types.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-08]
- artifact_id: `receipt-projection-contract`
  path_or_api: `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-08]
- artifact_id: `receipt-admission-conversion`
  path_or_api: `crates/z00z_wallets/src/rpc/tx_rpc_admission.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-08]
- artifact_id: `plan-065-g08-summary`
  path_or_api: `.planning/phases/065-Attack-Surface/065-08-SUMMARY.md`
  kind: `doc`
  owner_crate: `planning`
  created_or_modified: `create`
  source_task_ids: [WS-08]
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_wallets --test test_rpc_truth -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_rpc_types_serialization -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_stub_behavior -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_runtime_validation_result -- --nocapture`
- `cargo test --release`
</plan_tests>

<plan_results>
- `PLAN-065-G08` produces `.planning/phases/065-Attack-Surface/065-08-SUMMARY.md` with public-RPC truth evidence and DTO-contract evidence.
- Group acceptance is proven only when production-looking placeholder surfaces are removed, renamed, or upgraded to real durable local truth in the same slice.
</plan_results>

<task_artifacts>
- task_id: `WS-08`
  implementation_depth: `live-claim-removed`
  artifacts:
    - `crates/z00z_wallets/src/services/chain_service.rs`
    - `crates/z00z_wallets/src/rpc/chain_rpc.rs`
    - `crates/z00z_wallets/src/rpc/chain_rpc_impl.rs`
    - `crates/z00z_wallets/src/rpc/chain_types.rs`
    - `crates/z00z_wallets/src/rpc/tx_types.rs`
    - `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
    - `crates/z00z_wallets/src/rpc/tx_rpc_admission.rs`
    - `crates/z00z_wallets/src/app/app_kernel.rs`
</task_artifacts>

<task_tests>
- task_id: `WS-08`
  commands:
    - `cargo test --release -p z00z_wallets --test test_rpc_truth -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_rpc_types_serialization -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_stub_behavior -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_runtime_validation_result -- --nocapture`
  scenarios:
    - `public chain scan and tip are either durable local truth or explicitly non-production`
    - `receipt DTOs stop treating root-like strings as proof payloads`
    - `production route wiring and docs do not present placeholder paths as finished`
</task_tests>

<task_results>
- task_id: `WS-08`
  expected_results:
    - `public RPCs are either real or explicitly non-production`
    - `production DTOs do not advertise placeholder proof semantics as if they were finalized behavior`
</task_results>

<anti_placeholder_gate>
- Renaming a stub without removing its production-looking contract is insufficient.
- Leaving `merkle_proof` or similar fields populated with root-style strings remains phase-failing.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_wallets/src/services/chain_service.rs`
- `crates/z00z_wallets/src/rpc/chain_rpc_impl.rs`
- `crates/z00z_wallets/src/rpc/tx_types.rs`
- `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
- `crates/z00z_wallets/src/rpc/tx_rpc_admission.rs`
</current_code_refs>

<blockers>
- Any production-looking public RPC that remains synthetic after this slice is phase-failing unless the contract is explicitly demoted in the same patch.
</blockers>

<evidence_gate>
- Completion evidence must include the exact public methods or DTO fields removed, renamed, or upgraded, plus tests proving placeholder semantics are gone from production defaults.
</evidence_gate>

<not_recommendation_gate>
- This slice must choose a real contract outcome for public RPC truth now; it cannot defer behind placeholder names.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Placeholder Public RPC And DTO Contract Cleanup</name>
<read_first>
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_wallets/src/services/chain_service.rs`
- `crates/z00z_wallets/src/rpc/chain_rpc.rs`
- `crates/z00z_wallets/src/rpc/chain_rpc_impl.rs`
- `crates/z00z_wallets/src/rpc/tx_types.rs`
- `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
- `crates/z00z_wallets/src/rpc/tx_rpc_admission.rs`
</read_first>
<files>
- `crates/z00z_wallets/src/services/chain_service.rs`
- `crates/z00z_wallets/src/rpc/chain_rpc.rs`
- `crates/z00z_wallets/src/rpc/chain_rpc_impl.rs`
- `crates/z00z_wallets/src/rpc/chain_types.rs`
- `crates/z00z_wallets/src/rpc/tx_types.rs`
- `crates/z00z_wallets/src/rpc/tx_runtime_state.rs`
- `crates/z00z_wallets/src/rpc/tx_rpc_admission.rs`
- `crates/z00z_wallets/src/app/app_kernel.rs`
</files>
<action>
Execute `PLAN-065-G08` by removing or demoting production-looking placeholder
public RPCs unless they become real durable local truths in the same slice, and
by cleaning receipt or verification DTOs so proof-bearing fields mean exactly
what their names claim.
</action>
<acceptance_criteria>
- `WS-08` closes only when public RPCs are either real or explicitly non-production.
- Production DTOs do not advertise placeholder proof semantics as if they were finalized behavior.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_wallets --test test_rpc_truth -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_rpc_types_serialization -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_stub_behavior -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_runtime_validation_result -- --nocapture`
   - `cargo test --release`
3. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
4. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
5. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
`WS-08` is closed only when placeholder public RPC truth and placeholder
proof-shaped DTO semantics are gone from the production-looking contract.
</done>
</task>
</tasks>

<threat_model>
- Placeholder RPC names can mislead users or downstream code into trusting synthetic local state.
- DTO field names can encode false proof guarantees when fed with root-like or compatibility values.
- Stub defaults can keep a production-looking contract alive long after the real behavior is known to be incomplete.
</threat_model>

<verification>
This slice is valid only if public route wiring, DTO serialization, and RPC truth tests agree on the same honest contract.
</verification>

<success_criteria>
- `WS-08` traceability stays attached to `G-08`, `G-09`, and the named source findings.
- Production-looking placeholder public contract surfaces disappear or become real in the same slice.
</success_criteria>

<output>
Create `.planning/phases/065-Attack-Surface/065-08-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/065-Attack-Surface/065-08-SUMMARY.md`
- honest public RPC truth contract
- narrowed production DTO proof vocabulary

## 🔎 Coverage Appendix

| TASK-NNN | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `WS-08` (normative unit; `065-TODO.md` has no `TASK-NNN` rows) | `PLAN-065-G08` | `065-TODO.md` `599-624`; `325-377`; disposition rows `761-764` | TODO + CONTEXT + chain/RPC/DTO anchors | chain service + RPC types + DTO conversions + tests | `cargo test --release -p z00z_wallets --test test_rpc_truth -- --nocapture`; `cargo test --release -p z00z_wallets --test test_rpc_types_serialization -- --nocapture`; `cargo test --release -p z00z_wallets --test test_stub_behavior -- --nocapture`; `cargo test --release -p z00z_wallets --test test_runtime_validation_result -- --nocapture` | public RPCs become real local truth or lose production-looking claims | do not invent remote-backed truth; use real local truth or remove claim | changed public routes/DTO fields plus serialization/runtime tests prove placeholder contract is gone | `Complete` |
