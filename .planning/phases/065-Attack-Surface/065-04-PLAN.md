---
phase: 065-Attack-Surface
plan: PLAN-065-G04
type: execute
wave: 4
depends_on:
  - PLAN-065-G03
files_modified:
  - crates/z00z_simulator/src/config.rs
  - crates/z00z_simulator/src/scenario_1/stage_12/mod.rs
  - crates/z00z_simulator/src/scenario_1/runtime_observability.rs
  - crates/z00z_simulator/tests/scenario_1/test_stage6_checkpoint_final_gate.rs
  - crates/z00z_simulator/tests/scenario_1/test_wallet_integration.rs
requirements:
  - WS-04
autonomous: true
must_haves:
  truths:
    - "Draft or debug simulator runs must not emit production-shaped checkpoint or publication evidence."
    - "Public packet consumers must distinguish debug output by type, not by convention."
  artifacts:
    - crates/z00z_simulator/src/config.rs
    - crates/z00z_simulator/src/scenario_1/stage_12/mod.rs
    - crates/z00z_simulator/src/scenario_1/runtime_observability.rs
  key_links:
    - "Scope: WS-04 draft/debug simulator evidence truth."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-065-G04 Plan: Draft And Debug Simulator Evidence Truth

<objective>
Close `WS-04` by preventing draft-only or debug simulator modes from emitting
checkpoint or publication evidence that looks like final canonical output. The
public lane must reference real finalized checkpoint ids only, while debug lanes
must stay on distinct schemas or paths.
</objective>

<context>
@.planning/phases/065-Attack-Surface/065-TODO.md
@.planning/phases/065-Attack-Surface/065-CONTEXT.md
@crates/z00z_simulator/src/config.rs
@crates/z00z_simulator/src/scenario_1/stage_12/mod.rs
@crates/z00z_simulator/src/scenario_1/runtime_observability.rs
@crates/z00z_simulator/tests/scenario_1/test_stage6_checkpoint_final_gate.rs
@crates/z00z_simulator/tests/scenario_1/test_wallet_integration.rs
</context>

<coverage_contract>
This plan covers `WS-04`. Closure requires `DraftOnly` to leave the public
release-packet contract, synthetic checkpoint ids to stop feeding
publication-binding evidence, and public packet verification to reject
draft-shaped evidence wherever final truth is claimed.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-065-G04`
task_ids:
- `WS-04`
copied_task_rows:
- "| WS-04 | Draft And Debug Simulator Paths Must Not Emit Production-Shaped Evidence | Sources: `AS-20260630-064-01`; `AS-20260630-064-07`; narrowed simulator remainder of old `CF-002` | Close when draft and debug runs cannot emit production-shaped checkpoint or publication evidence; public packet consumers can distinguish debug output from canonical final evidence by type, not by convention. |"
source_refs:
- `WS-04`: `065-TODO.md` lines `474-498`
- `Seal-Only Items That Must Keep Regression Coverage`: `065-TODO.md` line `657`
- `Active Disposition Map`: `065-TODO.md` lines `734`, `740`
- `AS-20260630-064-01`
- `AS-20260630-064-07`
- `narrowed simulator remainder of old CF-002`
inputs:
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_simulator/src/config.rs`
- `crates/z00z_simulator/src/scenario_1/stage_12/mod.rs`
- `crates/z00z_simulator/src/scenario_1/runtime_observability.rs`
outputs:
- `.planning/phases/065-Attack-Surface/065-04-SUMMARY.md`
- changed or created `crates/z00z_simulator/src/config.rs`
- changed or created `crates/z00z_simulator/src/scenario_1/stage_12/mod.rs`
- changed or created `crates/z00z_simulator/src/scenario_1/runtime_observability.rs`
- changed or created `crates/z00z_simulator/tests/scenario_1/test_stage6_checkpoint_final_gate.rs`
- changed or created `crates/z00z_simulator/tests/scenario_1/test_wallet_integration.rs`
dependencies:
- `PLAN-065-G03`
acceptance_tests:
- `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture`
- `cargo test --release -p z00z_storage --test test_checkpoint_finalization -- --nocapture`
- `cargo test --release`
simulation_gate:
- Use the canonical local simulator only. No remote transport or external DA simulation is needed for this slice.
negative_tests:
- Release-mode or public-lane runs must reject `DraftOnly`.
- Packet verification must reject synthetic checkpoint ids in public publication evidence.
- Default public lane must continue rejecting plaintext wallet-secret artifacts.
- Existing default-lane secret-lane regressions must remain green while debug and draft truth gaps are closed.
</plan_matrix>

<todo_exact_mirror>
This block mirrors the exact normative `WS-04` bullets from `065-TODO.md`. It
is a coverage mirror only and does not replace the TODO as authority.
Required implementation:
- Remove `DraftOnly` from any release or publication-packet contract, or move
  it into an explicitly debug-only packet schema that cannot be mistaken for
  final evidence.
- Require publication-binding evidence to reference a real finalized checkpoint
  id, not a synthetic stand-in derived from batch identity.
- Split public release artifacts from private debug artifacts at the schema and
  path level, not only by runtime convention.
- Make packet verification reject draft-shaped evidence wherever final
  publication or checkpoint truth is claimed.
Required tests:
- Simulator tests proving release-mode or public-lane runs reject `DraftOnly`.
- Packet verification tests that reject synthetic checkpoint ids and draft-only
  publication evidence.
- Anti-regression tests proving the default public lane never emits plaintext
  wallet-secret artifacts.
Close when:
- draft and debug runs cannot emit production-shaped checkpoint or publication
  evidence;
- public packet consumers can distinguish debug output from canonical final
  evidence by type, not by convention.
</todo_exact_mirror>

<plan_artifacts>
- artifact_id: `proof-mode-contract`
  path_or_api: `crates/z00z_simulator/src/config.rs`
  kind: `code`
  owner_crate: `z00z_simulator`
  created_or_modified: `modify`
  source_task_ids: [WS-04]
- artifact_id: `stage12-finalization-gate`
  path_or_api: `crates/z00z_simulator/src/scenario_1/stage_12/mod.rs`
  kind: `code`
  owner_crate: `z00z_simulator`
  created_or_modified: `modify`
  source_task_ids: [WS-04]
- artifact_id: `public-evidence-contract`
  path_or_api: `crates/z00z_simulator/src/scenario_1/runtime_observability.rs`
  kind: `code`
  owner_crate: `z00z_simulator`
  created_or_modified: `modify`
  source_task_ids: [WS-04]
- artifact_id: `stage6-final-gate-regression`
  path_or_api: `crates/z00z_simulator/tests/scenario_1/test_stage6_checkpoint_final_gate.rs`
  kind: `test`
  owner_crate: `z00z_simulator`
  created_or_modified: `modify`
  source_task_ids: [WS-04]
- artifact_id: `public-lane-secret-regression`
  path_or_api: `crates/z00z_simulator/tests/scenario_1/test_wallet_integration.rs`
  kind: `test`
  owner_crate: `z00z_simulator`
  created_or_modified: `modify`
  source_task_ids: [WS-04]
- artifact_id: `plan-065-g04-summary`
  path_or_api: `.planning/phases/065-Attack-Surface/065-04-SUMMARY.md`
  kind: `doc`
  owner_crate: `planning`
  created_or_modified: `create`
  source_task_ids: [WS-04]
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture`
- `cargo test --release -p z00z_storage --test test_checkpoint_finalization -- --nocapture`
- `cargo test --release`
</plan_tests>

<plan_results>
- `PLAN-065-G04` produces `.planning/phases/065-Attack-Surface/065-04-SUMMARY.md` with proof-mode, packet-schema, and public-lane anti-regression evidence.
- Group acceptance is proven only when public publication evidence references real finalized checkpoint ids and debug output stays visibly noncanonical.
</plan_results>

<task_artifacts>
- task_id: `WS-04`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_simulator/src/config.rs`
    - `crates/z00z_simulator/src/scenario_1/stage_12/mod.rs`
    - `crates/z00z_simulator/src/scenario_1/runtime_observability.rs`
    - `crates/z00z_simulator/tests/scenario_1/test_stage6_checkpoint_final_gate.rs`
    - `crates/z00z_simulator/tests/scenario_1/test_wallet_integration.rs`
</task_artifacts>

<task_tests>
- task_id: `WS-04`
  commands:
    - `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture`
    - `cargo test --release -p z00z_storage --test test_checkpoint_finalization -- --nocapture`
  scenarios:
    - `draft-only mode is not accepted on public or release-shaped packets`
    - `synthetic checkpoint ids do not feed publication binding evidence`
    - `default public lane keeps plaintext secret artifacts out of outputs`
</task_tests>

<task_results>
- task_id: `WS-04`
  expected_results:
    - `draft and debug runs cannot emit production-shaped checkpoint or publication evidence`
    - `public packet consumers can distinguish debug output from canonical final evidence by type`
</task_results>

<anti_placeholder_gate>
- Keeping `DraftOnly` alive under a renamed status string is insufficient.
- A packet format that still accepts synthetic checkpoint ids is insufficient even if docs warn that it is debug-only.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_simulator/src/config.rs`
- `crates/z00z_simulator/src/scenario_1/stage_12/mod.rs`
- `crates/z00z_simulator/src/scenario_1/runtime_observability.rs`
</current_code_refs>

<blockers>
- Any public packet consumer that still accepts synthetic checkpoint ids or draft-only evidence is phase-failing.
</blockers>

<evidence_gate>
- Completion evidence must cite the removed or narrowed `DraftOnly` public contract, the changed packet schema or route, and the negative tests proving synthetic evidence rejection.
</evidence_gate>

<not_recommendation_gate>
- This slice must change packet contracts or code paths now; it is not a note to revisit simulator wording later.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Draft And Debug Simulator Evidence Truth</name>
<read_first>
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_simulator/src/config.rs`
- `crates/z00z_simulator/src/scenario_1/stage_12/mod.rs`
- `crates/z00z_simulator/src/scenario_1/runtime_observability.rs`
</read_first>
<files>
- `crates/z00z_simulator/src/config.rs`
- `crates/z00z_simulator/src/scenario_1/stage_12/mod.rs`
- `crates/z00z_simulator/src/scenario_1/runtime_observability.rs`
- `crates/z00z_simulator/tests/scenario_1/test_stage6_checkpoint_final_gate.rs`
- `crates/z00z_simulator/tests/scenario_1/test_wallet_integration.rs`
</files>
<action>
Execute `PLAN-065-G04` by removing `DraftOnly` from public or release-shaped
evidence contracts, forcing publication-binding evidence to reference real final
checkpoint ids only, and making public packet verification reject draft-shaped
evidence.
</action>
<acceptance_criteria>
- `WS-04` closes only when draft and debug runs cannot emit production-shaped checkpoint or publication evidence.
- Public packet consumers can distinguish debug output from canonical final evidence by type, not by convention.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture`
   - `cargo test --release -p z00z_storage --test test_checkpoint_finalization -- --nocapture`
   - `cargo test --release`
3. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
4. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
5. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
`WS-04` is closed only when draft/debug evidence can no longer masquerade as final public checkpoint/publication truth.
</done>
</task>
</tasks>

<threat_model>
- Draft-only outputs can look final if they reuse the same packet shape and field names.
- Synthetic checkpoint ids can create a believable but false publication-binding story.
- Secret-lane regressions can slip back in if debug and public paths remain schema-identical.
</threat_model>

<verification>
This slice is valid only if public-lane packet verification, simulator tests, and storage finalization proofs all agree.
</verification>

<success_criteria>
- `WS-04` traceability stays attached to the named draft/debug findings.
- Public packet truth is type-level and fail-closed.
</success_criteria>

<output>
Create `.planning/phases/065-Attack-Surface/065-04-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/065-Attack-Surface/065-04-SUMMARY.md`
- narrowed simulator proof-mode contract
- public-lane packet truth regression tests

## 🔎 Coverage Appendix

| TASK-NNN | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `WS-04` (normative unit; `065-TODO.md` has no `TASK-NNN` rows) | `PLAN-065-G04` | `065-TODO.md` `474-498`; disposition rows `734/739` | TODO + CONTEXT + simulator evidence anchors | config + stage12 + runtime observability + scenario tests | `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture`; `cargo test --release -p z00z_storage --test test_checkpoint_finalization -- --nocapture` | draft/debug runs stop emitting production-shaped evidence | use canonical local simulator only | changed schema/code plus negative packet tests prove synthetic public evidence is rejected | `Complete` |
