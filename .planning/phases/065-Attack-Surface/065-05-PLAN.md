---
phase: 065-Attack-Surface
plan: PLAN-065-G05
type: execute
wave: 5
depends_on:
  - PLAN-065-G04
files_modified:
  - crates/z00z_wallets/src/services/wallet_session_runtime_limits.rs
  - crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs
  - crates/z00z_wallets/src/rpc/wallet_dispatcher_wiring.rs
  - crates/z00z_wallets/src/rpc/app_dispatcher_wiring.rs
  - crates/z00z_wallets/src/stealth/output.rs
  - crates/z00z_wallets/scripts/audit_rpc_method_wiring.py
  - crates/z00z_wallets/scripts/audit_rpc_method_wiring.sh
  - crates/z00z_wallets/tests/test_sensitive_rpc_session.rs
  - crates/z00z_wallets/tests/test_wallet_capability_matrix.rs
  - crates/z00z_wallets/tests/test_stealth_output.rs
  - crates/z00z_wallets/tests/test_rpc_route_coverage.rs
requirements:
  - WS-05
autonomous: true
must_haves:
  truths:
    - "Privileged wallet paths must accept typed verified capabilities, not raw session tokens."
    - "Capability truth must be explicit across native and wasm targets."
    - "Raw stealth builders must not look like canonical approval surfaces."
  artifacts:
    - crates/z00z_wallets/src/services/wallet_session_runtime_limits.rs
    - crates/z00z_wallets/src/stealth/output.rs
    - crates/z00z_wallets/scripts/audit_rpc_method_wiring.py
  key_links:
    - "Scope: WS-05 privileged wallet capability sealing."
    - "Gate owner: G-05 privileged session gate."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-065-G05 Plan: Capability-Typed Privileged Wallet Paths

<objective>
Close `WS-05` by replacing handler-convention session checks with typed
verified-capability objects, centralizing privileged-route registration so
unguarded additions fail CI, and making raw stealth builders visibly
noncanonical.
</objective>

<context>
@.planning/phases/065-Attack-Surface/065-TODO.md
@.planning/phases/065-Attack-Surface/065-CONTEXT.md
@crates/z00z_wallets/src/services/wallet_session_runtime_limits.rs
@crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs
@crates/z00z_wallets/src/rpc/wallet_dispatcher_wiring.rs
@crates/z00z_wallets/src/rpc/app_dispatcher_wiring.rs
@crates/z00z_wallets/src/stealth/output.rs
@crates/z00z_wallets/scripts/audit_rpc_method_wiring.py
@crates/z00z_wallets/tests/test_sensitive_rpc_session.rs
@crates/z00z_wallets/tests/test_wallet_capability_matrix.rs
</context>

<coverage_contract>
This plan covers `WS-05`. Closure requires privileged wallet code to stop
accepting raw session tokens directly, raw stealth builders to stop looking
canonical, and target capability truth to be explicit and test-enforced.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-065-G05`
task_ids:
- `WS-05`
copied_task_rows:
- "| WS-05 | Privileged Wallet Paths Need Capability Types, Not Only Handler Convention | Sources: `AS-20260630-064-03`; `AS-20260630-064-05`; `AS-20260630-064-06` | Close when privileged wallet code cannot compile without a verified capability object; the raw builder is visibly noncanonical by API shape; target capability truth is explicit and test-enforced. |"
source_refs:
- `Canonical Gate Inventory: Inputs, Outputs, Verification, Proofs`: `065-TODO.md` lines `245-268`
- `WS-05`: `065-TODO.md` lines `502-527`
- `G-05`: `065-TODO.md` lines `245-268`
- `Active Disposition Map`: `065-TODO.md` lines `736`, `738-739`
- `AS-20260630-064-03`
- `AS-20260630-064-05`
- `AS-20260630-064-06`
inputs:
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_wallets/src/services/wallet_session_runtime_limits.rs`
- `crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs`
- `crates/z00z_wallets/src/rpc/wallet_dispatcher_wiring.rs`
- `crates/z00z_wallets/src/rpc/app_dispatcher_wiring.rs`
- `crates/z00z_wallets/src/stealth/output.rs`
- `crates/z00z_wallets/scripts/audit_rpc_method_wiring.py`
outputs:
- `.planning/phases/065-Attack-Surface/065-05-SUMMARY.md`
- changed or created `crates/z00z_wallets/src/services/wallet_session_runtime_limits.rs`
- changed or created `crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs`
- changed or created `crates/z00z_wallets/src/rpc/wallet_dispatcher_wiring.rs`
- changed or created `crates/z00z_wallets/src/rpc/app_dispatcher_wiring.rs`
- changed or created `crates/z00z_wallets/src/stealth/output.rs`
- changed or created `crates/z00z_wallets/scripts/audit_rpc_method_wiring.py`
- changed or created `crates/z00z_wallets/scripts/audit_rpc_method_wiring.sh`
- changed or created `crates/z00z_wallets/tests/test_sensitive_rpc_session.rs`
- changed or created `crates/z00z_wallets/tests/test_wallet_capability_matrix.rs`
- changed or created `crates/z00z_wallets/tests/test_stealth_output.rs`
- changed or created `crates/z00z_wallets/tests/test_rpc_route_coverage.rs`
dependencies:
- `PLAN-065-G04`
acceptance_tests:
- `cargo test --release -p z00z_wallets --test test_sensitive_rpc_session -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_wallet_capability_matrix -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_stealth_output -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_rpc_route_coverage -- --nocapture`
- `bash crates/z00z_wallets/scripts/audit_rpc_method_wiring.sh`
- `cargo test --release`
simulation_gate:
- No external simulation is required. Cross-target capability truth may be proven through native/wasm build and targeted RPC tests.
negative_tests:
- Adding a privileged handler without a capability-typed guard must fail the route audit or build.
- Unsupported wasm capabilities must return explicit typed errors.
- Public production code paths must not use the raw stealth builder as an approval surface.
</plan_matrix>

<todo_exact_mirror>
This block mirrors the exact normative `WS-05` bullets from `065-TODO.md`. It
is a coverage mirror only and does not replace the TODO as authority.
Required implementation:
- Introduce typed `VerifiedSession` or `PrivilegedSession` capability objects
  and require secret-export, restore, backup, seed, rotate, and similar
  privileged services to accept those types instead of raw session tokens.
- Centralize sensitive-method registration so adding a privileged RPC without a
  guard fails build or CI.
- Rename the raw stealth builder to an explicitly unsafe or unvalidated API
  shape, or reduce it to an internal/test-oriented surface. The validated
  builder must become the clearly canonical public path.
- Publish and enforce a capability matrix for native and wasm targets.
  Unsupported capabilities must return explicit typed errors and must not be
  described as available.
Required tests:
- Source-audit tests that enumerate all privileged RPC methods and require a
  capability-typed guard path.
- Negative tests for newly added privileged handlers that omit the guard.
- API tests proving public production code paths do not use the raw stealth
  builder as an approval decision surface.
- Cross-target tests proving unsupported wasm capabilities fail explicitly.
Close when:
- privileged wallet code cannot compile without a verified capability object;
- the raw builder is visibly noncanonical by API shape;
- target capability truth is explicit and test-enforced.
</todo_exact_mirror>

<plan_artifacts>
- artifact_id: `verified-session-capability`
  path_or_api: `crates/z00z_wallets/src/services/wallet_session_runtime_limits.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-05]
- artifact_id: `privileged-route-registration`
  path_or_api: `crates/z00z_wallets/src/rpc/wallet_dispatcher_wiring.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-05]
- artifact_id: `app-route-capability-matrix`
  path_or_api: `crates/z00z_wallets/src/rpc/app_dispatcher_wiring.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-05]
- artifact_id: `stealth-builder-contract`
  path_or_api: `crates/z00z_wallets/src/stealth/output.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-05]
- artifact_id: `rpc-route-audit`
  path_or_api: `crates/z00z_wallets/scripts/audit_rpc_method_wiring.py`
  kind: `script`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-05]
- artifact_id: `plan-065-g05-summary`
  path_or_api: `.planning/phases/065-Attack-Surface/065-05-SUMMARY.md`
  kind: `doc`
  owner_crate: `planning`
  created_or_modified: `create`
  source_task_ids: [WS-05]
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_wallets --test test_sensitive_rpc_session -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_wallet_capability_matrix -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_stealth_output -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_rpc_route_coverage -- --nocapture`
- `bash crates/z00z_wallets/scripts/audit_rpc_method_wiring.sh`
- `cargo test --release`
</plan_tests>

<plan_results>
- `PLAN-065-G05` produces `.planning/phases/065-Attack-Surface/065-05-SUMMARY.md` with capability-type evidence, route audit evidence, and cross-target support or explicit-unsupported evidence.
- Group acceptance is proven only when privileged routes require typed capabilities and the route audit fails closed on missing guards.
</plan_results>

<task_artifacts>
- task_id: `WS-05`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_wallets/src/services/wallet_session_runtime_limits.rs`
    - `crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs`
    - `crates/z00z_wallets/src/rpc/wallet_dispatcher_wiring.rs`
    - `crates/z00z_wallets/src/rpc/app_dispatcher_wiring.rs`
    - `crates/z00z_wallets/src/stealth/output.rs`
    - `crates/z00z_wallets/scripts/audit_rpc_method_wiring.py`
    - `crates/z00z_wallets/scripts/audit_rpc_method_wiring.sh`
</task_artifacts>

<task_tests>
- task_id: `WS-05`
  commands:
    - `cargo test --release -p z00z_wallets --test test_sensitive_rpc_session -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_wallet_capability_matrix -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_stealth_output -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_rpc_route_coverage -- --nocapture`
    - `bash crates/z00z_wallets/scripts/audit_rpc_method_wiring.sh`
  scenarios:
    - `privileged RPC methods require capability-typed entry`
    - `new unguarded privileged handlers fail the audit`
    - `raw stealth builders are visibly noncanonical`
    - `unsupported wasm capabilities fail explicitly`
</task_tests>

<task_results>
- task_id: `WS-05`
  expected_results:
    - `privileged wallet code cannot compile without a verified capability object`
    - `the raw builder is visibly noncanonical by API shape`
    - `target capability truth is explicit and test-enforced`
</task_results>

<anti_placeholder_gate>
- Wrapping the same raw token in a renamed alias without changing call signatures is insufficient.
- A raw builder remains phase-failing if it still looks like the canonical public API for approval-sensitive paths.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_wallets/src/services/wallet_session_runtime_limits.rs`
- `crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs`
- `crates/z00z_wallets/src/rpc/wallet_dispatcher_wiring.rs`
- `crates/z00z_wallets/src/rpc/app_dispatcher_wiring.rs`
- `crates/z00z_wallets/src/stealth/output.rs`
</current_code_refs>

<blockers>
- Any privileged route that still accepts a raw session token after this slice is phase-failing.
</blockers>

<evidence_gate>
- Completion evidence must include the capability type, the route audit output, and the tests proving unsupported targets fail explicitly.
</evidence_gate>

<not_recommendation_gate>
- This slice must enforce capability types in signatures and route registration, not rely on reviewer memory.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Capability-Typed Privileged Wallet Paths</name>
<read_first>
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_wallets/src/services/wallet_session_runtime_limits.rs`
- `crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs`
- `crates/z00z_wallets/src/rpc/wallet_dispatcher_wiring.rs`
- `crates/z00z_wallets/src/rpc/app_dispatcher_wiring.rs`
- `crates/z00z_wallets/src/stealth/output.rs`
</read_first>
<files>
- `crates/z00z_wallets/src/services/wallet_session_runtime_limits.rs`
- `crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs`
- `crates/z00z_wallets/src/rpc/wallet_dispatcher_wiring.rs`
- `crates/z00z_wallets/src/rpc/app_dispatcher_wiring.rs`
- `crates/z00z_wallets/src/stealth/output.rs`
- `crates/z00z_wallets/scripts/audit_rpc_method_wiring.py`
</files>
<action>
Execute `PLAN-065-G05` by introducing typed verified-capability objects for
privileged wallet actions, centralizing privileged route registration so missing
guards fail CI, and demoting or renaming raw stealth builders so they cannot be
mistaken for canonical approval paths.
</action>
<acceptance_criteria>
- `WS-05` closes only when privileged wallet code cannot compile without a verified capability object.
- The raw builder is visibly noncanonical by API shape.
- Target capability truth is explicit and test-enforced.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_wallets --test test_sensitive_rpc_session -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_wallet_capability_matrix -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_stealth_output -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_rpc_route_coverage -- --nocapture`
   - `bash crates/z00z_wallets/scripts/audit_rpc_method_wiring.sh`
   - `cargo test --release`
3. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
4. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
5. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
`WS-05` is closed only when privileged routes are capability-typed, route
audits fail closed, and raw stealth builders are visibly noncanonical.
</done>
</task>
</tasks>

<threat_model>
- New privileged routes can silently bypass guards if route registration and guard proof stay disconnected.
- Wasm and native capability drift can create false availability claims even when wasm fail-closes.
- Raw builder naming can normalize unsafe or compatibility-only approval semantics.
</threat_model>

<verification>
This slice is valid only if signatures, route audits, and tests all enforce the same capability contract.
</verification>

<success_criteria>
- `WS-05` traceability stays attached to `G-05` and the named source findings.
- Privileged wallet access becomes type-driven and CI-enforced.
</success_criteria>

<output>
Create `.planning/phases/065-Attack-Surface/065-05-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/065-Attack-Surface/065-05-SUMMARY.md`
- capability-typed privileged route contract
- route-audit enforcement for sensitive RPC registration

## 🔎 Coverage Appendix

| TASK-NNN | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `WS-05` (normative unit; `065-TODO.md` has no `TASK-NNN` rows) | `PLAN-065-G05` | `065-TODO.md` `502-527`; `245-268`; disposition rows `736-739` | TODO + CONTEXT + wallet capability anchors | session/runtime limits + route wiring + stealth output + audit scripts | `cargo test --release -p z00z_wallets --test test_sensitive_rpc_session -- --nocapture`; `cargo test --release -p z00z_wallets --test test_wallet_capability_matrix -- --nocapture`; `cargo test --release -p z00z_wallets --test test_stealth_output -- --nocapture`; `bash crates/z00z_wallets/scripts/audit_rpc_method_wiring.sh` | privileged routes require typed capabilities and raw builders are visibly noncanonical | no external simulation; native/wasm truth via tests and build/audit | changed signatures plus route audit and negative tests prove guard omissions fail | `Complete` |
