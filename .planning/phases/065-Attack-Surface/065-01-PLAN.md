---
phase: 065-Attack-Surface
plan: PLAN-065-G01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/z00z_rollup_node/src/da.rs
  - crates/z00z_rollup_node/src/lib.rs
  - crates/z00z_runtime/validators/src/verdict.rs
  - crates/z00z_runtime/validators/src/checkpoint.rs
  - crates/z00z_runtime/validators/src/engine.rs
  - crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs
  - crates/z00z_rollup_node/tests/test_rollup_theorem_guard.rs
  - crates/z00z_simulator/tests/scenario_1/test_checkpoint_acceptance.rs
requirements:
  - WS-01
autonomous: true
must_haves:
  truths:
    - "Validator acceptance must require the same theorem bundle that the rollup theorem verifier already checks."
    - "No accepted path may rebuild checkpoint or publication truth from partial fields."
    - "Optional theorem data is forbidden on accepted paths."
  artifacts:
    - crates/z00z_rollup_node/src/da.rs
    - crates/z00z_runtime/validators/src/verdict.rs
    - crates/z00z_runtime/validators/src/checkpoint.rs
    - crates/z00z_runtime/validators/src/engine.rs
  key_links:
    - "Scope: WS-01 theorem-verified validator acceptance."
    - "Gate owners: G-01 verify_settlement_theorem and G-02 validator checkpoint/publication flow."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-065-G01 Plan: Theorem-Verified Validator Acceptance

<objective>
Close `WS-01` by making theorem-verified acceptance mandatory on the validator
path. The accepted-path bundle must carry the canonical checkpoint link,
execution-input identity, publication binding inputs, and rollup theorem
material as one typed validator-owned structure instead of partial fields that
can be inferred or rebuilt later.
</objective>

<context>
@.planning/phases/065-Attack-Surface/065-TODO.md
@.planning/phases/065-Attack-Surface/065-CONTEXT.md
@crates/z00z_rollup_node/src/lib.rs
@crates/z00z_rollup_node/src/da.rs
@crates/z00z_runtime/validators/src/verdict.rs
@crates/z00z_runtime/validators/src/checkpoint.rs
@crates/z00z_runtime/validators/src/engine.rs
@crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs
@crates/z00z_rollup_node/tests/test_rollup_theorem_guard.rs
@crates/z00z_simulator/tests/scenario_1/test_checkpoint_acceptance.rs
</context>

<coverage_contract>
This plan covers `WS-01`. `065-TODO.md` contains no canonical `TASK-NNN` rows,
so `WS-01` is the only valid normative execution unit for this slice.
Acceptance closes only when validator acceptance cannot compile or run without a
theorem bundle and no artifact-only `Accepted` lane remains.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-065-G01`
task_ids:
- `WS-01`
copied_task_rows:
- "| WS-01 | Theorem-Verified Validator Acceptance Must Be Mandatory | Sources: `AS-20260627-001`; `AS-20260630-064-11`; narrowed remainder of old `CF-006` | Close when validator acceptance cannot compile or run without the theorem input bundle; all accepted paths verify theorem, publication binding, and link coherence together; artifact-only acceptance no longer exists. |"
source_refs:
- `Canonical Gate Inventory: Inputs, Outputs, Verification, Proofs`: `065-TODO.md` lines `133-187`
- `WS-01`: `065-TODO.md` lines `379-404`
- `G-01`: `065-TODO.md` lines `133-161`
- `G-02`: `065-TODO.md` lines `163-187`
- `Active Disposition Map`: `065-TODO.md` lines `732`, `744`
- `AS-20260627-001`
- `AS-20260630-064-11`
- `narrowed remainder of old CF-006`
inputs:
- `.planning/phases/065-Attack-Surface/065-TODO.md` canonical `WS-01` and `G-01`/`G-02` contract
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md` coverage rule and current-code anchor table
- `crates/z00z_rollup_node/src/lib.rs` existing theorem verifier
- `crates/z00z_rollup_node/src/da.rs` current `ResolvedBatch` construction
- `crates/z00z_runtime/validators/src/verdict.rs` current `ResolvedBatch` shape
- `crates/z00z_runtime/validators/src/checkpoint.rs` publication-binding flow
- `crates/z00z_runtime/validators/src/engine.rs` `Accepted` verdict path
outputs:
- `.planning/phases/065-Attack-Surface/065-01-SUMMARY.md`
- changed or created `crates/z00z_rollup_node/src/da.rs`
- changed or created `crates/z00z_rollup_node/src/lib.rs`
- changed or created `crates/z00z_runtime/validators/src/verdict.rs`
- changed or created `crates/z00z_runtime/validators/src/checkpoint.rs`
- changed or created `crates/z00z_runtime/validators/src/engine.rs`
- changed or created `crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs`
- changed or created `crates/z00z_rollup_node/tests/test_rollup_theorem_guard.rs`
- changed or created `crates/z00z_simulator/tests/scenario_1/test_checkpoint_acceptance.rs`
dependencies:
- none
acceptance_tests:
- `cargo test --release -p z00z_validators --test test_hjmt_publication_contract -- --nocapture`
- `cargo test --release -p z00z_rollup_node --test test_rollup_theorem_guard -- --nocapture`
- `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture`
- `cargo test --release`
- `rg -n "verify_settlement_theorem|ResolvedBatch|verdict_for_batch" crates/z00z_rollup_node crates/z00z_runtime/validators`
simulation_gate:
- No external simulation is required. Use real local rollup, validator, storage, and simulator primitives only.
negative_tests:
- Wrong checkpoint link must reject.
- Wrong exec-input id must reject.
- Wrong prep-snapshot id must reject.
- Wrong route-table digest must reject.
- Detached or mismatched proof bytes must reject.
- `Accepted` must be unreachable when theorem inputs are absent.
</plan_matrix>

<todo_exact_mirror>
This block mirrors the exact normative `WS-01` bullets from `065-TODO.md`. It
is a coverage mirror only and does not replace the TODO as authority.
Required implementation:
- Extend the resolved-batch contract so validator-owned acceptance inputs
  include the canonical checkpoint link, execution-input identity, and the
  public theorem material required to prove one coherent publication story.
- Remove any acceptance path that can produce `Accepted` without theorem
  verification.
- Make DA resolve, validator acceptance, and publication-binding handling
  consume the same typed structure instead of rebuilding or re-inferring key
  public inputs in multiple places.
- Ensure publication binding, route-table digest, checkpoint id, and theorem
  inputs are checked together, not as loosely related post-fact fields.
- Keep missing theorem inputs fail-closed. "Optional" theorem data must not
  exist on the accepted path.
Required tests:
- Negative tests for wrong checkpoint link, wrong exec input id, wrong
  prep snapshot id, wrong route digest, detached proof blob, and mismatched
  publication binding.
- Cross-crate tests that one accepted batch has one theorem story and one
  publication binding digest.
- Validator tests proving `Accepted` is unreachable when theorem inputs are
  absent.
Close when:
- validator acceptance cannot compile or run without the theorem input bundle;
- all accepted paths verify theorem, publication binding, and link coherence
  together;
- artifact-only acceptance no longer exists.
</todo_exact_mirror>

<plan_artifacts>
- artifact_id: `validator-theorem-bundle`
  path_or_api: `crates/z00z_runtime/validators/src/verdict.rs`
  kind: `code`
  owner_crate: `z00z_validators`
  created_or_modified: `modify`
  source_task_ids: [WS-01]
- artifact_id: `validator-acceptance-gate`
  path_or_api: `crates/z00z_runtime/validators/src/engine.rs`
  kind: `code`
  owner_crate: `z00z_validators`
  created_or_modified: `modify`
  source_task_ids: [WS-01]
- artifact_id: `validator-checkpoint-flow`
  path_or_api: `crates/z00z_runtime/validators/src/checkpoint.rs`
  kind: `code`
  owner_crate: `z00z_validators`
  created_or_modified: `modify`
  source_task_ids: [WS-01]
- artifact_id: `da-resolve-bundle`
  path_or_api: `crates/z00z_rollup_node/src/da.rs`
  kind: `code`
  owner_crate: `z00z_rollup_node`
  created_or_modified: `modify`
  source_task_ids: [WS-01]
- artifact_id: `rollup-theorem-regression`
  path_or_api: `crates/z00z_rollup_node/tests/test_rollup_theorem_guard.rs`
  kind: `test`
  owner_crate: `z00z_rollup_node`
  created_or_modified: `modify-or-create`
  source_task_ids: [WS-01]
- artifact_id: `validator-publication-regression`
  path_or_api: `crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs`
  kind: `test`
  owner_crate: `z00z_validators`
  created_or_modified: `modify`
  source_task_ids: [WS-01]
- artifact_id: `plan-065-g01-summary`
  path_or_api: `.planning/phases/065-Attack-Surface/065-01-SUMMARY.md`
  kind: `doc`
  owner_crate: `planning`
  created_or_modified: `create`
  source_task_ids: [WS-01]
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_validators --test test_hjmt_publication_contract -- --nocapture`
- `cargo test --release -p z00z_rollup_node --test test_rollup_theorem_guard -- --nocapture`
- `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture`
- `cargo test --release`
</plan_tests>

<plan_results>
- `PLAN-065-G01` produces `.planning/phases/065-Attack-Surface/065-01-SUMMARY.md` with theorem-bundle traceability, changed files, and negative-proof evidence.
- Group acceptance is proven only when the validator accepted path consumes one typed theorem bundle and the negative theorem/publication drift cases fail closed.
</plan_results>

<task_artifacts>
- task_id: `WS-01`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_rollup_node/src/da.rs`
    - `crates/z00z_runtime/validators/src/verdict.rs`
    - `crates/z00z_runtime/validators/src/checkpoint.rs`
    - `crates/z00z_runtime/validators/src/engine.rs`
    - `crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs`
    - `crates/z00z_rollup_node/tests/test_rollup_theorem_guard.rs`
    - `crates/z00z_simulator/tests/scenario_1/test_checkpoint_acceptance.rs`
</task_artifacts>

<task_tests>
- task_id: `WS-01`
  commands:
    - `cargo test --release -p z00z_validators --test test_hjmt_publication_contract -- --nocapture`
    - `cargo test --release -p z00z_rollup_node --test test_rollup_theorem_guard -- --nocapture`
    - `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture`
  scenarios:
    - `one accepted batch has one theorem story and one publication binding digest`
    - `accepted verdict is impossible without the theorem input bundle`
    - `checkpoint-link, exec-input, snapshot, route-digest, and proof drift reject`
</task_tests>

<task_results>
- task_id: `WS-01`
  expected_results:
    - `validator acceptance cannot compile or run without the theorem input bundle`
    - `theorem, publication binding, and checkpoint-link coherence are verified together`
    - `artifact-only acceptance no longer exists`
</task_results>

<anti_placeholder_gate>
- `ResolvedBatch` or its replacement must carry real theorem-owned inputs, not `Option` placeholders on the accepted path.
- A doc string, helper wrapper, or post-verdict grep does not close this plan without runtime tests.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_rollup_node/src/lib.rs`
- `crates/z00z_rollup_node/src/da.rs`
- `crates/z00z_runtime/validators/src/verdict.rs`
- `crates/z00z_runtime/validators/src/checkpoint.rs`
- `crates/z00z_runtime/validators/src/engine.rs`
</current_code_refs>

<blockers>
- Any accepted path that still derives truth from artifact-only fields is phase-failing.
- Any theorem field left optional on the accepted path is phase-failing.
</blockers>

<evidence_gate>
- Completion evidence must cite the typed theorem bundle fields, the changed validator/rollup files, and the negative tests proving theorem-input absence rejects.
</evidence_gate>

<not_recommendation_gate>
- This slice is an implementation packet, not a recommendation to maybe add theorem checks later.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Theorem-Verified Validator Acceptance</name>
<read_first>
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_rollup_node/src/lib.rs`
- `crates/z00z_rollup_node/src/da.rs`
- `crates/z00z_runtime/validators/src/verdict.rs`
- `crates/z00z_runtime/validators/src/checkpoint.rs`
- `crates/z00z_runtime/validators/src/engine.rs`
</read_first>
<files>
- `crates/z00z_rollup_node/src/da.rs`
- `crates/z00z_runtime/validators/src/verdict.rs`
- `crates/z00z_runtime/validators/src/checkpoint.rs`
- `crates/z00z_runtime/validators/src/engine.rs`
- `crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs`
- `crates/z00z_rollup_node/tests/test_rollup_theorem_guard.rs`
</files>
<action>
Execute `PLAN-065-G01` by introducing one validator-owned theorem input bundle
that is constructed at DA resolve time and is mandatory for any accepted
verdict. The accepted path must verify theorem material, publication binding,
checkpoint id, snapshot id, exec-input id, and route digest together.
</action>
<acceptance_criteria>
- `WS-01` closes only when validator acceptance cannot compile or run without the theorem input bundle.
- All accepted paths verify theorem, publication binding, and link coherence together.
- Artifact-only acceptance no longer exists.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_validators --test test_hjmt_publication_contract -- --nocapture`
   - `cargo test --release -p z00z_rollup_node --test test_rollup_theorem_guard -- --nocapture`
   - `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture`
   - `cargo test --release`
3. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
4. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
5. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
`WS-01` is closed only when theorem-verified acceptance is mandatory and the evidence summary names the changed typed bundle, negative tests, and final verdict contract.
</done>
</task>
</tasks>

<threat_model>
- Acceptance drift could leave one theorem verifier in rollup while validators still accept partial bundles.
- Publication drift could keep binding checks disconnected from theorem and link checks.
- Placeholder drift could add a struct without actually making it mandatory on the accepted path.
</threat_model>

<verification>
This slice is valid only if the listed commands, negative cases, and anti-placeholder gate all pass together.
</verification>

<success_criteria>
- `WS-01` traceability stays attached to `G-01`, `G-02`, and the named source findings.
- The accepted path owns one theorem story, one publication binding digest, and one coherent link.
</success_criteria>

<output>
Create `.planning/phases/065-Attack-Surface/065-01-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/065-Attack-Surface/065-01-SUMMARY.md`
- typed theorem-bundle enforcement on validator acceptance
- updated rollup and validator regression tests

## 🔎 Coverage Appendix

| TASK-NNN | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `WS-01` (normative unit; `065-TODO.md` has no `TASK-NNN` rows) | `PLAN-065-G01` | `065-TODO.md` `379-404`; `133-187`; `732-744` | TODO + CONTEXT + rollup/validator anchors | rollup DA + validator verdict/checkpoint/engine + regression tests | `cargo test --release -p z00z_validators --test test_hjmt_publication_contract -- --nocapture`; `cargo test --release -p z00z_rollup_node --test test_rollup_theorem_guard -- --nocapture`; `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture` | accepted path requires one theorem bundle and artifact-only acceptance disappears | no external simulation; use real local validator/rollup/simulator primitives | changed code and negative runtime tests prove theorem-input absence rejects | `Complete` |
