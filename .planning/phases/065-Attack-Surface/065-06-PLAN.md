---
phase: 065-Attack-Surface
plan: PLAN-065-G06
type: execute
wave: 6
depends_on:
  - PLAN-065-G05
files_modified:
  - crates/z00z_wallets/src/rpc/asset_rpc_support_state.rs
  - crates/z00z_wallets/src/chain/broadcast_impl.rs
  - crates/z00z_wallets/src/persistence/tx_storage.rs
  - crates/z00z_wallets/src/persistence/tx_storage_impl.rs
  - crates/z00z_wallets/src/services/wallet_actions_backup.rs
  - crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs
  - crates/z00z_wallets/tests/test_asset_rpc_mutations.rs
  - crates/z00z_wallets/tests/test_wallet_restore_atomic.rs
  - crates/z00z_wallets/tests/test_chain_broadcast_retry.rs
  - crates/z00z_wallets/tests/test_tx_store_integration.rs
requirements:
  - WS-06
autonomous: true
must_haves:
  truths:
    - "One sealed service must own local mutation submission, broadcast persistence, tx-id truth, and durable lifecycle writes."
    - "Restore crash semantics must become explicit through a durable journal or transaction marker."
    - "Rotate-master-key wording must match the implemented persisted contract."
  artifacts:
    - crates/z00z_wallets/src/rpc/asset_rpc_support_state.rs
    - crates/z00z_wallets/src/chain/broadcast_impl.rs
    - crates/z00z_wallets/src/services/wallet_actions_backup.rs
  key_links:
    - "Scope: WS-06 wallet mutation truth and restore truth."
    - "Gate owners: G-06 mutation submission and G-07 atomic restore."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-065-G06 Plan: Canonical Wallet Mutation And Restore Ownership

<objective>
Close `WS-06` by introducing one canonical mutation executor for all local asset
mutation RPCs, moving tx-id and lifecycle truth under that executor plus
`TxStorage`, and adding an explicit durable restore journal or equivalent crash
marker for multi-step wallet restore.
</objective>

<context>
@.planning/phases/065-Attack-Surface/065-TODO.md
@.planning/phases/065-Attack-Surface/065-CONTEXT.md
@crates/z00z_wallets/src/rpc/asset_rpc_support_state.rs
@crates/z00z_wallets/src/chain/broadcast_impl.rs
@crates/z00z_wallets/src/persistence/tx_storage.rs
@crates/z00z_wallets/src/persistence/tx_storage_impl.rs
@crates/z00z_wallets/src/services/wallet_actions_backup.rs
@crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs
@crates/z00z_wallets/tests/test_asset_rpc_mutations.rs
@crates/z00z_wallets/tests/test_wallet_restore_atomic.rs
</context>

<coverage_contract>
This plan covers `WS-06`. Closure requires one sealed owner for mutation truth,
explicit restore retry or crash semantics across staged history, `.wlt`, and
publish, and rotate-master-key wording that matches the implemented persisted
behavior.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-065-G06`
task_ids:
- `WS-06`
copied_task_rows:
- "| WS-06 | Wallet Mutation Truth And Restore Truth Need One Canonical Owner | Sources: `AS-20260630-064-02`; `AS-20260630-064-04`; narrowed remainder of old rotate-master-key wording | Close when one sealed service owns mutation truth; restore has explicit retry semantics across crashes; persisted rotation behavior and method wording match each other. |"
source_refs:
- `Canonical Gate Inventory: Inputs, Outputs, Verification, Proofs`: `065-TODO.md` lines `270-323`
- `WS-06`: `065-TODO.md` lines `532-556`
- `G-06`: `065-TODO.md` lines `270-295`
- `G-07`: `065-TODO.md` lines `297-323`
- `Seal-Only Items That Must Keep Regression Coverage`: `065-TODO.md` lines `656`, `658`
- `Active Disposition Map`: `065-TODO.md` lines `735`, `737`, `745`
- `AS-20260630-064-02`
- `AS-20260630-064-04`
- `AS-20260630-064-12`
- `narrowed remainder of old rotate-master-key wording`
inputs:
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_wallets/src/rpc/asset_rpc_support_state.rs`
- `crates/z00z_wallets/src/chain/broadcast_impl.rs`
- `crates/z00z_wallets/src/persistence/tx_storage.rs`
- `crates/z00z_wallets/src/persistence/tx_storage_impl.rs`
- `crates/z00z_wallets/src/services/wallet_actions_backup.rs`
- `crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs`
outputs:
- `.planning/phases/065-Attack-Surface/065-06-SUMMARY.md`
- changed or created `crates/z00z_wallets/src/rpc/asset_rpc_support_state.rs`
- changed or created `crates/z00z_wallets/src/chain/broadcast_impl.rs`
- changed or created `crates/z00z_wallets/src/persistence/tx_storage.rs`
- changed or created `crates/z00z_wallets/src/persistence/tx_storage_impl.rs`
- changed or created `crates/z00z_wallets/src/services/wallet_actions_backup.rs`
- changed or created `crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs`
- changed or created `crates/z00z_wallets/tests/test_asset_rpc_mutations.rs`
- changed or created `crates/z00z_wallets/tests/test_wallet_restore_atomic.rs`
- changed or created `crates/z00z_wallets/tests/test_chain_broadcast_retry.rs`
- changed or created `crates/z00z_wallets/tests/test_tx_store_integration.rs`
dependencies:
- `PLAN-065-G05`
acceptance_tests:
- `cargo test --release -p z00z_wallets --test test_asset_rpc_mutations -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_wallet_restore_atomic -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_chain_broadcast_retry -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_tx_store_integration -- --nocapture`
- `cargo test --release`
simulation_gate:
- Use local deterministic failpoints and real wallet persistence primitives only. External chain transport remains adapter-only and must not become the owner of mutation truth.
negative_tests:
- `history_commit`, `.wlt` commit, publish, rollback failure, and retry paths must fail or recover deterministically.
- Every public local mutation RPC must route through the same executor.
- Tx-id truth must come from the canonical broadcast or persistence owner, not ad hoc string formatting in each RPC method.
- Recovery takeover or resume-ownership regressions must remain green, and rotate-master-key wording or receipt truth must not drift back to the retired placeholder claim.
</plan_matrix>

<todo_exact_mirror>
This block mirrors the exact normative `WS-06` bullets from `065-TODO.md`. It
is a coverage mirror only and does not replace the TODO as authority.
Required implementation:
- Introduce one canonical mutation executor that owns local mutation
  submission, broadcast persistence, tx-id truth, and durable lifecycle writes
  for all asset mutation RPCs.
- Route split, stake, swap, and any other local mutation entrypoints only
  through that executor.
- Add an explicit restore journal or transaction marker spanning staged tx
  history, `.wlt` writes, and in-memory publish so crash recovery and retry
  semantics are unambiguous.
- Keep restore retry/idempotency explicit after failures between file commits
  and publish.
- Audit the `rotate_master_key` API wording and receipt fields so they describe
  the implemented persisted rotation contract honestly.
Required tests:
- Failpoint or crash-matrix tests for `history_commit`, `.wlt` commit, publish,
  rollback failure, and retry.
- Routing tests proving every public asset mutation RPC uses the same executor.
- Tests proving tx storage and broadcast lifecycle remain coherent under
  repeated or partial failures.
Close when:
- one sealed service owns mutation truth;
- restore has explicit retry semantics across crashes;
- persisted rotation behavior and method wording match each other.
</todo_exact_mirror>

<plan_artifacts>
- artifact_id: `mutation-executor`
  path_or_api: `crates/z00z_wallets/src/rpc/asset_rpc_support_state.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-06]
- artifact_id: `broadcast-lifecycle-owner`
  path_or_api: `crates/z00z_wallets/src/chain/broadcast_impl.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-06]
- artifact_id: `tx-storage-contract`
  path_or_api: `crates/z00z_wallets/src/persistence/tx_storage.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-06]
- artifact_id: `restore-journal`
  path_or_api: `crates/z00z_wallets/src/services/wallet_actions_backup.rs`
  kind: `code`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-06]
- artifact_id: `restore-crash-regression`
  path_or_api: `crates/z00z_wallets/tests/test_wallet_restore_atomic.rs`
  kind: `test`
  owner_crate: `z00z_wallets`
  created_or_modified: `modify`
  source_task_ids: [WS-06]
- artifact_id: `plan-065-g06-summary`
  path_or_api: `.planning/phases/065-Attack-Surface/065-06-SUMMARY.md`
  kind: `doc`
  owner_crate: `planning`
  created_or_modified: `create`
  source_task_ids: [WS-06]
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_wallets --test test_asset_rpc_mutations -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_wallet_restore_atomic -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_chain_broadcast_retry -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_tx_store_integration -- --nocapture`
- `cargo test --release`
</plan_tests>

<plan_results>
- `PLAN-065-G06` produces `.planning/phases/065-Attack-Surface/065-06-SUMMARY.md` with one-owner mutation truth evidence, restore crash-matrix evidence, and wording-truth evidence for rotate-master-key.
- Group acceptance is proven only when all local mutation entrypoints route through one executor, restore retry semantics remain explicit after partial failures, and the current recovery or persisted-rotation regressions stay preserved.
</plan_results>

<task_artifacts>
- task_id: `WS-06`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_wallets/src/rpc/asset_rpc_support_state.rs`
    - `crates/z00z_wallets/src/chain/broadcast_impl.rs`
    - `crates/z00z_wallets/src/persistence/tx_storage.rs`
    - `crates/z00z_wallets/src/persistence/tx_storage_impl.rs`
    - `crates/z00z_wallets/src/services/wallet_actions_backup.rs`
    - `crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs`
    - `crates/z00z_wallets/tests/test_asset_rpc_mutations.rs`
    - `crates/z00z_wallets/tests/test_wallet_restore_atomic.rs`
    - `crates/z00z_wallets/tests/test_chain_broadcast_retry.rs`
    - `crates/z00z_wallets/tests/test_tx_store_integration.rs`
</task_artifacts>

<task_tests>
- task_id: `WS-06`
  commands:
    - `cargo test --release -p z00z_wallets --test test_asset_rpc_mutations -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_wallet_restore_atomic -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_chain_broadcast_retry -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_tx_store_integration -- --nocapture`
  scenarios:
    - `every public local mutation RPC routes through the same executor`
    - `history_commit, wlt_commit, publish, rollback-failure, and retry paths stay coherent`
    - `tx-id and lifecycle truth remain canonical under repeated or partial failures`
</task_tests>

<task_results>
- task_id: `WS-06`
  expected_results:
    - `one sealed service owns mutation truth`
    - `restore has explicit retry semantics across crashes`
    - `persisted rotation behavior and method wording match each other`
</task_results>

<anti_placeholder_gate>
- A helper that still formats tx ids ad hoc or bypasses the executor is insufficient.
- Restore rollback code without a durable journal or explicit transaction marker is insufficient.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_wallets/src/rpc/asset_rpc_support_state.rs`
- `crates/z00z_wallets/src/chain/broadcast_impl.rs`
- `crates/z00z_wallets/src/persistence/tx_storage.rs`
- `crates/z00z_wallets/src/services/wallet_actions_backup.rs`
- `crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs`
</current_code_refs>

<blockers>
- Any public asset mutation entrypoint that still bypasses the canonical executor is phase-failing.
- Any restore path that leaves crash or retry semantics ambiguous is phase-failing.
</blockers>

<evidence_gate>
- Completion evidence must include routing proof, failpoint/crash-matrix proof, and wording-truth proof for rotate-master-key.
</evidence_gate>

<not_recommendation_gate>
- This slice must pick one canonical owner path and make every public local mutation follow it.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Canonical Wallet Mutation And Restore Ownership</name>
<read_first>
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_wallets/src/rpc/asset_rpc_support_state.rs`
- `crates/z00z_wallets/src/chain/broadcast_impl.rs`
- `crates/z00z_wallets/src/persistence/tx_storage.rs`
- `crates/z00z_wallets/src/services/wallet_actions_backup.rs`
- `crates/z00z_wallets/src/rpc/key_rpc_server_admin.rs`
</read_first>
<files>
- `crates/z00z_wallets/src/rpc/asset_rpc_support_state.rs`
- `crates/z00z_wallets/src/chain/broadcast_impl.rs`
- `crates/z00z_wallets/src/persistence/tx_storage.rs`
- `crates/z00z_wallets/src/persistence/tx_storage_impl.rs`
- `crates/z00z_wallets/src/services/wallet_actions_backup.rs`
- `crates/z00z_wallets/tests/test_wallet_restore_atomic.rs`
</files>
<action>
Execute `PLAN-065-G06` by introducing one canonical mutation executor for all
local asset-mutation RPCs, making `TxStorage` the durable lifecycle owner,
adding an explicit restore journal or transaction marker across staged history,
`.wlt`, and publish, and aligning rotate-master-key wording with the real
persisted contract.
</action>
<acceptance_criteria>
- `WS-06` closes only when one sealed service owns mutation truth.
- Restore has explicit retry semantics across crashes.
- Persisted rotation behavior and method wording match each other.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_wallets --test test_asset_rpc_mutations -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_wallet_restore_atomic -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_chain_broadcast_retry -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_tx_store_integration -- --nocapture`
   - `cargo test --release`
3. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
4. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
5. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
`WS-06` is closed only when mutation truth and restore truth each have one
explicit owner and one crash-proof evidence story.
</done>
</task>
</tasks>

<threat_model>
- Helper composition can silently fork tx-id truth across RPC methods.
- Restore rollback can look careful while still leaving no durable restart marker.
- Wording drift around rotate-master-key can misrepresent what is actually persisted.
</threat_model>

<verification>
This slice is valid only if routing, lifecycle persistence, and restore crash handling are proven by real wallet tests and failpoints.
</verification>

<success_criteria>
- `WS-06` traceability stays attached to `G-06`, `G-07`, and the named source findings.
- One sealed owner path governs both local mutation truth and restore durability truth.
</success_criteria>

<output>
Create `.planning/phases/065-Attack-Surface/065-06-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/065-Attack-Surface/065-06-SUMMARY.md`
- canonical mutation executor contract
- explicit restore crash/retry evidence

## 🔎 Coverage Appendix

| TASK-NNN | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `WS-06` (normative unit; `065-TODO.md` has no `TASK-NNN` rows) | `PLAN-065-G06` | `065-TODO.md` `532-556`; `270-323`; disposition rows `735/737` | TODO + CONTEXT + wallet mutation/restore anchors | mutation executor + broadcast/tx storage + restore journal/tests | `cargo test --release -p z00z_wallets --test test_asset_rpc_mutations -- --nocapture`; `cargo test --release -p z00z_wallets --test test_wallet_restore_atomic -- --nocapture`; `cargo test --release -p z00z_wallets --test test_chain_broadcast_retry -- --nocapture`; `cargo test --release -p z00z_wallets --test test_tx_store_integration -- --nocapture` | one sealed owner governs mutation truth and restore retry semantics | use local deterministic failpoints and real wallet persistence only | changed routing/lifecycle code plus crash-matrix tests prove no ad hoc mutation or restore truth remains | `Complete` |
