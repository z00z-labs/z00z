---
phase: 065-Attack-Surface
plan: PLAN-065-G07
type: execute
wave: 7
depends_on:
  - PLAN-065-G06
files_modified:
  - crates/z00z_storage/src/settlement/store.rs
  - crates/z00z_networks/rpc/src/wasm_client.rs
  - crates/z00z_networks/rpc/tests/test_wasm_client_redaction.rs
  - crates/z00z_wallets/tests/test_rpc_logging_acceptance.rs
  - crates/z00z_wallets/tests/test_rpc_logging_risk_policy.rs
  - crates/z00z_storage/tests/test_live_guardrails.rs
  - scripts/audit/audit_secret_type_hygiene.sh
  - scripts/audit/audit_secret_eq_hygiene.sh
  - scripts/audit/audit_crypto_rng_hygiene.sh
  - scripts/audit/audit_boundary_panic_hygiene.sh
  - scripts/audit/audit_log_redaction_hygiene.sh
  - .github/workflows/security-hygiene-guards.yml
requirements:
  - WS-07
autonomous: true
must_haves:
  truths:
    - "Security-boundary construction must fail closed and use fallible APIs."
    - "Transport logging must emit only redacted summaries."
    - "The permanent Phase 065 meta-gates must be executable CI or source-audit checks, not prose."
  artifacts:
    - crates/z00z_storage/src/settlement/store.rs
    - crates/z00z_networks/rpc/src/wasm_client.rs
    - scripts/audit/audit_boundary_panic_hygiene.sh
  key_links:
    - "Scope: WS-07 fail-closed construction, logging redaction, and repository-wide meta-gates."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-065-G07 Plan: Fail-Closed Construction, Redaction, And Meta-Gates

<objective>
Close `WS-07` by removing panicking constructors and raw transport logging from
security boundaries, then locking the broader Phase 065 meta-gates into
repository-executable source-audit or CI checks for secret-bearing type hygiene,
secret equality hygiene, RNG hygiene, panic hygiene, and operator-visible log
redaction.
</objective>

<context>
@.planning/phases/065-Attack-Surface/065-TODO.md
@.planning/phases/065-Attack-Surface/065-CONTEXT.md
@crates/z00z_storage/src/settlement/store.rs
@crates/z00z_networks/rpc/src/wasm_client.rs
@crates/z00z_wallets/tests/test_rpc_logging_acceptance.rs
@crates/z00z_wallets/tests/test_rpc_logging_risk_policy.rs
@crates/z00z_storage/tests/test_live_guardrails.rs
</context>

<coverage_contract>
This plan covers `WS-07` plus the permanent repository-wide meta-gates named in
`065-TODO.md`. Closure requires no boundary constructor panic on open or
config-load failure, no raw secret-bearing transport logs, and executable
audits for the absorbed policy-gate classes.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-065-G07`
task_ids:
- `WS-07`
copied_task_rows:
- "| WS-07 | Security Boundary Construction And Logging Must Fail Closed | Sources: `AS-20260627-002`; crate `AS-20260623-002`; legacy logging and panic cluster `AS-20260501-002/004/007/011/020/023/024/025/026` | Close when no security-boundary constructor panics on open/load/config failure; transport logs never emit raw secret-bearing JSON; panic and redaction rules are enforced by CI rather than memory. |"
source_refs:
- `WS-07`: `065-TODO.md` lines `561-594`
- `Permanent Repository-Wide Meta-Gates From The Legacy Catalog`: `065-TODO.md` lines `679-724`
- `Seal-Only Items That Must Keep Regression Coverage`: `065-TODO.md` line `655`
- `Active Disposition Map`: `065-TODO.md` lines `733`, `742`, `747`, `754-758`
- `AS-20260627-002`
- `AS-20260630-064-09`
- `crate AS-20260623-002`
- `legacy logging and panic cluster:`
- `AS-20260501-002`
- `AS-20260501-004`
- `AS-20260501-007`
- `AS-20260501-011`
- `AS-20260501-020`
- `AS-20260501-023`
- `AS-20260501-024`
- `AS-20260501-025`
- `AS-20260501-026`
inputs:
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_storage/src/settlement/store.rs`
- `crates/z00z_networks/rpc/src/wasm_client.rs`
- `crates/z00z_wallets/tests/test_rpc_logging_acceptance.rs`
- `crates/z00z_wallets/tests/test_rpc_logging_risk_policy.rs`
- `crates/z00z_storage/tests/test_live_guardrails.rs`
outputs:
- `.planning/phases/065-Attack-Surface/065-07-SUMMARY.md`
- changed or created `crates/z00z_storage/src/settlement/store.rs`
- changed or created `crates/z00z_networks/rpc/src/wasm_client.rs`
- changed or created `crates/z00z_networks/rpc/tests/test_wasm_client_redaction.rs`
- changed or created `crates/z00z_wallets/tests/test_rpc_logging_acceptance.rs`
- changed or created `crates/z00z_wallets/tests/test_rpc_logging_risk_policy.rs`
- changed or created `crates/z00z_storage/tests/test_live_guardrails.rs`
- changed or created `scripts/audit/audit_secret_type_hygiene.sh`
- changed or created `scripts/audit/audit_secret_eq_hygiene.sh`
- changed or created `scripts/audit/audit_crypto_rng_hygiene.sh`
- changed or created `scripts/audit/audit_boundary_panic_hygiene.sh`
- changed or created `scripts/audit/audit_log_redaction_hygiene.sh`
- changed or created `.github/workflows/security-hygiene-guards.yml`
dependencies:
- `PLAN-065-G06`
acceptance_tests:
- `cargo test --release -p z00z_storage --test test_live_guardrails -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_rpc_logging_acceptance -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_rpc_logging_risk_policy -- --nocapture`
- `cargo test --release -p z00z_networks_rpc -- --nocapture`
- `bash scripts/audit/audit_secret_type_hygiene.sh`
- `bash scripts/audit/audit_secret_eq_hygiene.sh`
- `bash scripts/audit/audit_crypto_rng_hygiene.sh`
- `bash scripts/audit/audit_boundary_panic_hygiene.sh`
- `bash scripts/audit/audit_log_redaction_hygiene.sh`
- `cargo test --release`
simulation_gate:
- No protocol simulation is required. Prove closure with fallible constructors, logger tests, and repository source-audit scripts only.
negative_tests:
- Raw params and raw responses must never be logged from transport layers.
- New panic, unwrap, or expect at auth/open/load/restore/proof boundaries must fail the panic-hygiene audit.
- New direct equality on secret material or non-cryptographic RNG near crypto must fail the relevant meta-gate audit.
- Existing object reject-code contract regressions across storage, wallet, and rollup must remain green while fail-closed boundary policy is tightened.
</plan_matrix>

<todo_exact_mirror>
This block mirrors the exact normative `WS-07` bullets from `065-TODO.md`. It
is a coverage mirror only and does not replace the TODO as authority.
Required implementation:
- Deprecate or internalize panicking constructors on security-relevant
  open/load boundaries. Public callers must use fallible constructors.
- Sweep auth, storage open/load, restore, wallet-key, and proof-verification
  seams for `unwrap`, `expect`, and `panic!` and replace them with typed errors
  where the failure crosses a trust boundary.
- Remove raw param/response logging from transport layers. Transport logging
  may emit method names and already-redacted summaries only.
- Add one sanitization policy for logs and user/operator-visible errors: no
  plaintext seed phrases, no raw secret material, no wallet ids unless
  explicitly classified safe, no filesystem paths unless explicitly classified
  safe, no raw internal storage state dumps.
- Keep a small allowlist instead of broad exceptions.
Required tests:
- Native and wasm logging tests that prove sensitive methods never print raw
  params or raw responses.
- Source-audit tests for panic patterns in boundary modules.
- Tests that assert high-risk wallet RPC methods use redacted summaries only.
Close when:
- no security-boundary constructor panics on open/load/config failure;
- transport logs never emit raw secret-bearing JSON;
- panic and redaction rules are enforced by CI rather than memory.
</todo_exact_mirror>

<plan_artifacts>
- artifact_id: `settlement-fallible-constructor`
  path_or_api: `crates/z00z_storage/src/settlement/store.rs`
  kind: `code`
  owner_crate: `z00z_storage`
  created_or_modified: `modify`
  source_task_ids: [WS-07]
- artifact_id: `wasm-rpc-redaction`
  path_or_api: `crates/z00z_networks/rpc/src/wasm_client.rs`
  kind: `code`
  owner_crate: `z00z_networks_rpc`
  created_or_modified: `modify`
  source_task_ids: [WS-07]
- artifact_id: `secret-type-audit`
  path_or_api: `scripts/audit/audit_secret_type_hygiene.sh`
  kind: `script`
  owner_crate: `repo`
  created_or_modified: `create`
  source_task_ids: [WS-07]
- artifact_id: `secret-eq-audit`
  path_or_api: `scripts/audit/audit_secret_eq_hygiene.sh`
  kind: `script`
  owner_crate: `repo`
  created_or_modified: `create`
  source_task_ids: [WS-07]
- artifact_id: `crypto-rng-audit`
  path_or_api: `scripts/audit/audit_crypto_rng_hygiene.sh`
  kind: `script`
  owner_crate: `repo`
  created_or_modified: `create`
  source_task_ids: [WS-07]
- artifact_id: `boundary-panic-audit`
  path_or_api: `scripts/audit/audit_boundary_panic_hygiene.sh`
  kind: `script`
  owner_crate: `repo`
  created_or_modified: `create`
  source_task_ids: [WS-07]
- artifact_id: `log-redaction-audit`
  path_or_api: `scripts/audit/audit_log_redaction_hygiene.sh`
  kind: `script`
  owner_crate: `repo`
  created_or_modified: `create`
  source_task_ids: [WS-07]
- artifact_id: `plan-065-g07-summary`
  path_or_api: `.planning/phases/065-Attack-Surface/065-07-SUMMARY.md`
  kind: `doc`
  owner_crate: `planning`
  created_or_modified: `create`
  source_task_ids: [WS-07]
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_storage --test test_live_guardrails -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_rpc_logging_acceptance -- --nocapture`
- `cargo test --release -p z00z_wallets --test test_rpc_logging_risk_policy -- --nocapture`
- `cargo test --release -p z00z_networks_rpc -- --nocapture`
- `bash scripts/audit/audit_secret_type_hygiene.sh`
- `bash scripts/audit/audit_secret_eq_hygiene.sh`
- `bash scripts/audit/audit_crypto_rng_hygiene.sh`
- `bash scripts/audit/audit_boundary_panic_hygiene.sh`
- `bash scripts/audit/audit_log_redaction_hygiene.sh`
- `cargo test --release`
</plan_tests>

<plan_results>
- `PLAN-065-G07` produces `.planning/phases/065-Attack-Surface/065-07-SUMMARY.md` with fallible-constructor, redaction, and meta-gate CI evidence.
- Group acceptance is proven only when panic/logging findings are fixed and the five permanent meta-gate classes each have an executable guard.
</plan_results>

<task_artifacts>
- task_id: `WS-07`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_storage/src/settlement/store.rs`
    - `crates/z00z_networks/rpc/src/wasm_client.rs`
    - `crates/z00z_networks/rpc/tests/test_wasm_client_redaction.rs`
    - `crates/z00z_wallets/tests/test_rpc_logging_acceptance.rs`
    - `crates/z00z_wallets/tests/test_rpc_logging_risk_policy.rs`
    - `crates/z00z_storage/tests/test_live_guardrails.rs`
    - `scripts/audit/audit_secret_type_hygiene.sh`
    - `scripts/audit/audit_secret_eq_hygiene.sh`
    - `scripts/audit/audit_crypto_rng_hygiene.sh`
    - `scripts/audit/audit_boundary_panic_hygiene.sh`
    - `scripts/audit/audit_log_redaction_hygiene.sh`
    - `.github/workflows/security-hygiene-guards.yml`
</task_artifacts>

<task_tests>
- task_id: `WS-07`
  commands:
    - `cargo test --release -p z00z_storage --test test_live_guardrails -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_rpc_logging_acceptance -- --nocapture`
    - `cargo test --release -p z00z_wallets --test test_rpc_logging_risk_policy -- --nocapture`
    - `cargo test --release -p z00z_networks_rpc -- --nocapture`
    - `bash scripts/audit/audit_secret_type_hygiene.sh`
    - `bash scripts/audit/audit_secret_eq_hygiene.sh`
    - `bash scripts/audit/audit_crypto_rng_hygiene.sh`
    - `bash scripts/audit/audit_boundary_panic_hygiene.sh`
    - `bash scripts/audit/audit_log_redaction_hygiene.sh`
  scenarios:
    - `security-boundary constructors fail closed on open/load/config failure`
    - `transport logs emit redacted summaries only`
    - `new secret-type, secret-equality, RNG, panic, or log-redaction regressions fail CI`
</task_tests>

<task_results>
- task_id: `WS-07`
  expected_results:
    - `no security-boundary constructor panics on open/load/config failure`
    - `transport logs never emit raw secret-bearing JSON`
    - `panic and redaction rules are enforced by CI rather than memory`
</task_results>

<anti_placeholder_gate>
- A redaction comment or allowlist-free grep note does not close the meta-gates.
- Panicking constructors remain phase-failing even if tests only cover the happy path.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_storage/src/settlement/store.rs`
- `crates/z00z_networks/rpc/src/wasm_client.rs`
- `crates/z00z_wallets/tests/test_rpc_logging_acceptance.rs`
- `crates/z00z_wallets/tests/test_rpc_logging_risk_policy.rs`
- `crates/z00z_storage/tests/test_live_guardrails.rs`
</current_code_refs>

<blockers>
- Missing source-audit coverage for any permanent meta-gate class is phase-failing.
- Any public constructor that still panics on open/load/config failure is phase-failing.
</blockers>

<evidence_gate>
- Completion evidence must include the new fallible constructor path, the logger redaction proof, and the five executable meta-gate scripts or CI hooks.
</evidence_gate>

<not_recommendation_gate>
- The permanent meta-gates are mandatory closure scope, not optional repo hygiene ideas.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Fail-Closed Construction, Redaction, And Meta-Gates</name>
<read_first>
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_storage/src/settlement/store.rs`
- `crates/z00z_networks/rpc/src/wasm_client.rs`
- `crates/z00z_wallets/tests/test_rpc_logging_acceptance.rs`
- `crates/z00z_wallets/tests/test_rpc_logging_risk_policy.rs`
</read_first>
<files>
- `crates/z00z_storage/src/settlement/store.rs`
- `crates/z00z_networks/rpc/src/wasm_client.rs`
- `crates/z00z_networks/rpc/tests/test_wasm_client_redaction.rs`
- `scripts/audit/audit_secret_type_hygiene.sh`
- `scripts/audit/audit_secret_eq_hygiene.sh`
- `scripts/audit/audit_crypto_rng_hygiene.sh`
- `scripts/audit/audit_boundary_panic_hygiene.sh`
- `scripts/audit/audit_log_redaction_hygiene.sh`
</files>
<action>
Execute `PLAN-065-G07` by replacing panicking construction on security
boundaries with fallible APIs, removing raw transport logging, and installing
one executable meta-gate per absorbed policy class from the legacy attack
surface catalog.
</action>
<acceptance_criteria>
- `WS-07` closes only when no security-boundary constructor panics on open/load/config failure.
- Transport logs never emit raw secret-bearing JSON.
- Panic and redaction rules are enforced by CI rather than memory.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_storage --test test_live_guardrails -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_rpc_logging_acceptance -- --nocapture`
   - `cargo test --release -p z00z_wallets --test test_rpc_logging_risk_policy -- --nocapture`
   - `cargo test --release -p z00z_networks_rpc -- --nocapture`
   - `bash scripts/audit/audit_secret_type_hygiene.sh`
   - `bash scripts/audit/audit_secret_eq_hygiene.sh`
   - `bash scripts/audit/audit_crypto_rng_hygiene.sh`
   - `bash scripts/audit/audit_boundary_panic_hygiene.sh`
   - `bash scripts/audit/audit_log_redaction_hygiene.sh`
   - `cargo test --release`
3. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
4. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
5. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
`WS-07` is closed only when boundary failures are fallible, transport logs are
redacted, and all five meta-gate classes have executable enforcement.
</done>
</task>
</tasks>

<threat_model>
- Boundary panics can turn configuration or open failures into denial-of-service or partial-state bugs.
- Raw transport logging can leak secrets even when higher layers redact logs correctly.
- Generic security classes recur unless repository-wide guards exist.
</threat_model>

<verification>
This slice is valid only if boundary code, tests, and audit scripts agree on the same fail-closed policy.
</verification>

<success_criteria>
- `WS-07` traceability stays attached to the named panic/logging findings and policy-gate rows.
- The permanent meta-gates become executable repository policy.
</success_criteria>

<output>
Create `.planning/phases/065-Attack-Surface/065-07-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/065-Attack-Surface/065-07-SUMMARY.md`
- fallible-constructor and redaction fixes
- repository-wide meta-gate audit scripts and CI hook

## 🔎 Coverage Appendix

| TASK-NNN | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `WS-07` (normative unit; `065-TODO.md` has no `TASK-NNN` rows) | `PLAN-065-G07` | `065-TODO.md` `561-594`; `689-724`; disposition rows `733/747/755/756` | TODO + CONTEXT + boundary/logging anchors | fallible constructor + wasm log redaction + five audit scripts/CI | `cargo test --release -p z00z_storage --test test_live_guardrails -- --nocapture`; `cargo test --release -p z00z_wallets --test test_rpc_logging_acceptance -- --nocapture`; `cargo test --release -p z00z_networks_rpc -- --nocapture`; audit scripts | boundary failures are fallible and meta-gates are executable | no simulation; tests and source-audit only | changed code and executable audits prove no memory-only policy remains | `Complete` |
