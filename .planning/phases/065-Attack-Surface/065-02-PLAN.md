---
phase: 065-Attack-Surface
plan: PLAN-065-G02
type: execute
wave: 2
depends_on:
  - PLAN-065-G01
files_modified:
  - crates/z00z_storage/src/checkpoint/store.rs
  - crates/z00z_storage/src/checkpoint/link.rs
  - crates/z00z_storage/tests/test_checkpoint_store.rs
  - crates/z00z_storage/tests/test_checkpoint_finalization.rs
  - crates/z00z_storage/tests/test_checkpoint_link_injective.rs
  - crates/z00z_simulator/src/scenario_1/stage_12/finalize_flow.rs
  - crates/z00z_simulator/tests/scenario_1/test_stage6_checkpoint_storage_bridge.rs
requirements:
  - WS-02
autonomous: true
must_haves:
  truths:
    - "Canonical checkpoint artifacts must be born only through `seal_artifact()`."
    - "Raw artifact export lanes must stay visibly noncanonical."
    - "Compatibility proof bytes must not share canonical proof semantics."
  artifacts:
    - crates/z00z_storage/src/checkpoint/store.rs
    - crates/z00z_storage/src/checkpoint/link.rs
    - crates/z00z_storage/tests/test_checkpoint_store.rs
  key_links:
    - "Scope: WS-02 raw-vs-canonical checkpoint authority closure."
    - "Gate owners: G-03 checkpoint seal gate and G-04 link bind/codec gate."
user_setup: none
---

<!-- markdownlint-disable MD022 MD031 MD032 MD033 MD041 MD047 MD055 MD056 MD060 -->

# PLAN-065-G02 Plan: Canonical Checkpoint Persistence

<objective>
Close `WS-02` by making canonical checkpoint persistence one-way through the
seal path, demoting raw artifact write lanes into clearly noncanonical export
surfaces, and splitting compatibility proof semantics away from final attested
checkpoint proof contracts.
</objective>

<context>
@.planning/phases/065-Attack-Surface/065-TODO.md
@.planning/phases/065-Attack-Surface/065-CONTEXT.md
@crates/z00z_storage/src/checkpoint/store.rs
@crates/z00z_storage/src/checkpoint/link.rs
@crates/z00z_storage/tests/test_checkpoint_store.rs
@crates/z00z_storage/tests/test_checkpoint_finalization.rs
@crates/z00z_storage/tests/test_checkpoint_link_injective.rs
@crates/z00z_simulator/src/scenario_1/stage_12/finalize_flow.rs
</context>

<coverage_contract>
This plan covers `WS-02`. The closure contract is: canonical checkpoint
artifacts can only be produced via `seal_artifact()`, compatibility proof bytes
cannot be mistaken for canonical proofs, and raw persistence survives only as an
explicitly quarantined export surface.
</coverage_contract>

<plan_matrix>
plan_id:
- `PLAN-065-G02`
task_ids:
- `WS-02`
copied_task_rows:
- "| WS-02 | Canonical Checkpoint Persistence Must Be One-Way, And Raw Lanes Must Be Quarantine-Only | Sources: `AS-20260630-064-10`; `AS-20260501-029`; `AS-20260501-030`; `CF-002`; carried parts of `CF-010` | Close when canonical checkpoint artifacts can only be born through `seal_artifact()`; compatibility proof bytes are no longer semantically confusable with canonical proofs; raw persistence survives only as an explicitly quarantined export surface. |"
source_refs:
- `Canonical Gate Inventory: Inputs, Outputs, Verification, Proofs`: `065-TODO.md` lines `189-243`
- `WS-02`: `065-TODO.md` lines `409-437`
- `G-03`: `065-TODO.md` lines `189-214`
- `G-04`: `065-TODO.md` lines `216-243`
- `Seal-Only Items That Must Keep Regression Coverage`: `065-TODO.md` lines `653-654`
- `Active Disposition Map`: `065-TODO.md` lines `741`, `743`, `751`, `752`, `759`, `760`, `764`
- `AS-20260630-064-10`
- `AS-20260630-064-08`
- `AS-20260501-029`
- `AS-20260501-030`
- `CF-001`
- `CF-002`
- `carried parts of CF-010`
inputs:
- `.planning/phases/065-Attack-Surface/065-TODO.md` canonical `WS-02` and `G-03`/`G-04` contract
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_storage/src/checkpoint/store.rs`
- `crates/z00z_storage/src/checkpoint/link.rs`
- `crates/z00z_simulator/src/scenario_1/stage_12/finalize_flow.rs`
outputs:
- `.planning/phases/065-Attack-Surface/065-02-SUMMARY.md`
- changed or created `crates/z00z_storage/src/checkpoint/store.rs`
- changed or created `crates/z00z_storage/src/checkpoint/link.rs`
- changed or created `crates/z00z_storage/tests/test_checkpoint_store.rs`
- changed or created `crates/z00z_storage/tests/test_checkpoint_finalization.rs`
- changed or created `crates/z00z_storage/tests/test_checkpoint_link_injective.rs`
- changed or created `crates/z00z_simulator/src/scenario_1/stage_12/finalize_flow.rs`
- changed or created `crates/z00z_simulator/tests/scenario_1/test_stage6_checkpoint_storage_bridge.rs`
dependencies:
- `PLAN-065-G01`
acceptance_tests:
- `cargo test --release -p z00z_storage --test test_checkpoint_store -- --nocapture`
- `cargo test --release -p z00z_storage --test test_checkpoint_finalization -- --nocapture`
- `cargo test --release -p z00z_storage --test test_checkpoint_link_injective -- --nocapture`
- `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture`
- `cargo test --release`
simulation_gate:
- No remote or external simulation is required. Use the real checkpoint store, link codec, and local simulator finalization path only.
negative_tests:
- `save_link()` before evidence rows exist must fail.
- Mismatched snapshot id or exec-input id must fail at write time.
- Mismatched statement/proof pairs must fail.
- Raw-export artifacts must not load as canonical final artifacts.
- Stage-12 finalization and validator acceptance must reject raw-lane checkpoint artifacts.
- Existing claim-source continuity and quarantine-promotion regressions must remain green while the canonical lane is narrowed.
</plan_matrix>

<todo_exact_mirror>
This block mirrors the exact normative `WS-02` bullets from `065-TODO.md`. It
is a coverage mirror only and does not replace the TODO as authority.
Required implementation:
- Move the raw artifact lane behind a clearly named noncanonical export
  capability or internal module. Canonical callers must not see it as a peer
  API.
- Make `save_link()` fail if replay-evidence rows are not already present and
  coherent at write time.
- Split canonical attested proof types from compatibility or draft proof bytes.
  Do not keep one ambiguous field name for both.
- Rename compatibility-only proof fields and helper types so downstream code
  cannot mistake them for a final theorem-bearing checkpoint proof.
- Add provenance or lane markers so downstream code can reject raw-export
  artifacts when a canonical sealed artifact is required.
- Rewrite tests that currently normalize the raw lane as a canonical
  persistence pattern. Keep raw-lane tests only where the surface is explicitly
  noncanonical.
Required tests:
- Negative tests for "link before evidence row exists", mismatched snapshot or
  exec ids, mismatched statement/proof pairs, and raw artifact reuse on
  canonical paths.
- Tests proving stage-12 finalization, validator acceptance, and publication
  export use only the seal path.
- Tests proving a raw-export artifact cannot be loaded as if it were a
  canonical final artifact.
Close when:
- canonical checkpoint artifacts can only be born through `seal_artifact()`;
- compatibility proof bytes are no longer semantically confusable with
  canonical proofs;
- raw persistence survives only as an explicitly quarantined export surface.
</todo_exact_mirror>

<plan_artifacts>
- artifact_id: `checkpoint-store-authority`
  path_or_api: `crates/z00z_storage/src/checkpoint/store.rs`
  kind: `code`
  owner_crate: `z00z_storage`
  created_or_modified: `modify`
  source_task_ids: [WS-02]
- artifact_id: `checkpoint-link-bind`
  path_or_api: `crates/z00z_storage/src/checkpoint/link.rs`
  kind: `code`
  owner_crate: `z00z_storage`
  created_or_modified: `modify`
  source_task_ids: [WS-02]
- artifact_id: `checkpoint-store-regression`
  path_or_api: `crates/z00z_storage/tests/test_checkpoint_store.rs`
  kind: `test`
  owner_crate: `z00z_storage`
  created_or_modified: `modify`
  source_task_ids: [WS-02]
- artifact_id: `checkpoint-finalization-regression`
  path_or_api: `crates/z00z_storage/tests/test_checkpoint_finalization.rs`
  kind: `test`
  owner_crate: `z00z_storage`
  created_or_modified: `modify`
  source_task_ids: [WS-02]
- artifact_id: `checkpoint-link-regression`
  path_or_api: `crates/z00z_storage/tests/test_checkpoint_link_injective.rs`
  kind: `test`
  owner_crate: `z00z_storage`
  created_or_modified: `modify`
  source_task_ids: [WS-02]
- artifact_id: `sim-stage12-seal-usage`
  path_or_api: `crates/z00z_simulator/src/scenario_1/stage_12/finalize_flow.rs`
  kind: `code`
  owner_crate: `z00z_simulator`
  created_or_modified: `modify`
  source_task_ids: [WS-02]
- artifact_id: `plan-065-g02-summary`
  path_or_api: `.planning/phases/065-Attack-Surface/065-02-SUMMARY.md`
  kind: `doc`
  owner_crate: `planning`
  created_or_modified: `create`
  source_task_ids: [WS-02]
</plan_artifacts>

<plan_tests>
- `cargo test --release -p z00z_storage --test test_checkpoint_store -- --nocapture`
- `cargo test --release -p z00z_storage --test test_checkpoint_finalization -- --nocapture`
- `cargo test --release -p z00z_storage --test test_checkpoint_link_injective -- --nocapture`
- `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture`
- `cargo test --release`
</plan_tests>

<plan_results>
- `PLAN-065-G02` produces `.planning/phases/065-Attack-Surface/065-02-SUMMARY.md` with raw-lane quarantine evidence, canonical seal-path evidence, and negative proof for link/evidence drift.
- Group acceptance is proven only when canonical finalization and canonical load paths reject raw or compatibility-only checkpoint material and the already-strong claim-source or quarantine regressions stay preserved.
</plan_results>

<task_artifacts>
- task_id: `WS-02`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_storage/src/checkpoint/store.rs`
    - `crates/z00z_storage/src/checkpoint/link.rs`
    - `crates/z00z_storage/tests/test_checkpoint_store.rs`
    - `crates/z00z_storage/tests/test_checkpoint_finalization.rs`
    - `crates/z00z_storage/tests/test_checkpoint_link_injective.rs`
    - `crates/z00z_simulator/src/scenario_1/stage_12/finalize_flow.rs`
    - `crates/z00z_simulator/tests/scenario_1/test_stage6_checkpoint_storage_bridge.rs`
</task_artifacts>

<task_tests>
- task_id: `WS-02`
  commands:
    - `cargo test --release -p z00z_storage --test test_checkpoint_store -- --nocapture`
    - `cargo test --release -p z00z_storage --test test_checkpoint_finalization -- --nocapture`
    - `cargo test --release -p z00z_storage --test test_checkpoint_link_injective -- --nocapture`
    - `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture`
  scenarios:
    - `canonical checkpoint artifacts are born only through the seal path`
    - `raw-export artifacts cannot re-enter canonical finalization or load paths`
    - `link, snapshot, exec-input, and proof mismatches fail at write time`
</task_tests>

<task_results>
- task_id: `WS-02`
  expected_results:
    - `canonical checkpoint artifacts can only be born through seal_artifact()`
    - `compatibility proof bytes are no longer semantically confusable with canonical proofs`
    - `raw persistence survives only as an explicitly quarantined export surface`
</task_results>

<anti_placeholder_gate>
- Renaming fields without narrowing their semantics is insufficient.
- A raw save lane hidden only by comments or doc wording is insufficient.
</anti_placeholder_gate>

<current_code_refs>
- `crates/z00z_storage/src/checkpoint/store.rs`
- `crates/z00z_storage/src/checkpoint/link.rs`
- `crates/z00z_simulator/src/scenario_1/stage_12/finalize_flow.rs`
</current_code_refs>

<blockers>
- Any path that still allows raw artifact or raw link material onto canonical loads or canonical finalization is phase-failing.
</blockers>

<evidence_gate>
- Completion evidence must show the canonical seal path, the quarantined raw lane, and negative tests for evidence-row or proof drift.
</evidence_gate>

<not_recommendation_gate>
- This slice must implement or remove ambiguous authority lanes now; it is not a recommendation to revisit checkpoint semantics later.
</not_recommendation_gate>

<tasks>
<task type="auto">
<name>Canonical Checkpoint Persistence</name>
<read_first>
- `.planning/phases/065-Attack-Surface/065-TODO.md`
- `.planning/phases/065-Attack-Surface/065-CONTEXT.md`
- `crates/z00z_storage/src/checkpoint/store.rs`
- `crates/z00z_storage/src/checkpoint/link.rs`
- `crates/z00z_simulator/src/scenario_1/stage_12/finalize_flow.rs`
</read_first>
<files>
- `crates/z00z_storage/src/checkpoint/store.rs`
- `crates/z00z_storage/src/checkpoint/link.rs`
- `crates/z00z_storage/tests/test_checkpoint_store.rs`
- `crates/z00z_storage/tests/test_checkpoint_finalization.rs`
- `crates/z00z_storage/tests/test_checkpoint_link_injective.rs`
</files>
<action>
Execute `PLAN-065-G02` by making canonical checkpoint persistence one-way
through `seal_artifact()`, forcing write-time evidence coherence for links, and
splitting compatibility proof semantics away from canonical attested proof
types.
</action>
<acceptance_criteria>
- `WS-02` closes only when canonical checkpoint artifacts can only be born through `seal_artifact()`.
- Compatibility proof bytes are no longer semantically confusable with canonical proofs.
- Raw persistence survives only as an explicitly quarantined export surface.
</acceptance_criteria>
<verify>
1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` first as the mandatory fail-fast gate. If it fails, stop, fix the issues, rerun it, and only then continue.
2. Run the slice-specific commands below:
   - `cargo test --release -p z00z_storage --test test_checkpoint_store -- --nocapture`
   - `cargo test --release -p z00z_storage --test test_checkpoint_finalization -- --nocapture`
   - `cargo test --release -p z00z_storage --test test_checkpoint_link_injective -- --nocapture`
   - `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture`
   - `cargo test --release`
3. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times. Fix all issues and warnings and continue until at least 2 consecutive runs show no significant code issues.
4. If this slice needs a commit after validation passes, use `/z00z-git-versioning`.
5. Use nested `.github/` skills, prompts, and gates as needed, especially `doublecheck`, smart tests, spec-to-code compliance, and the relevant Z00Z verification gates.
</verify>
<done>
`WS-02` is closed only when canonical finalization paths reject raw or
compatibility-only checkpoint material and the summary captures those proofs.
</done>
</task>
</tasks>

<threat_model>
- Raw-write proximity could let downstream code treat export artifacts as canonical final artifacts.
- Ambiguous proof naming could keep compatibility bytes alive as theorem-bearing proof claims.
- Load-time-only coherence checks could allow inconsistent link rows to persist.
</threat_model>

<verification>
This slice is valid only if store-time write checks, simulator integration, and raw-lane negative tests all pass together.
</verification>

<success_criteria>
- `WS-02` traceability stays attached to `G-03`, `G-04`, and the named source findings.
- Canonical checkpoint authority is singular and explicit in both code shape and tests.
</success_criteria>

<output>
Create `.planning/phases/065-Attack-Surface/065-02-SUMMARY.md` when done.
</output>

## 📌 Artifacts This Phase Produces

- `.planning/phases/065-Attack-Surface/065-02-SUMMARY.md`
- narrowed checkpoint authority surface
- updated storage and simulator checkpoint regressions

## 🔎 Coverage Appendix

| TASK-NNN | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `WS-02` (normative unit; `065-TODO.md` has no `TASK-NNN` rows) | `PLAN-065-G02` | `065-TODO.md` `409-437`; `189-243`; disposition rows `743/751/752/760/764` | TODO + CONTEXT + checkpoint store/link anchors | checkpoint store/link + storage tests + stage-12 seal usage | `cargo test --release -p z00z_storage --test test_checkpoint_store -- --nocapture`; `cargo test --release -p z00z_storage --test test_checkpoint_finalization -- --nocapture`; `cargo test --release -p z00z_storage --test test_checkpoint_link_injective -- --nocapture`; `cargo test --release -p z00z_simulator --test scenario_1 -- --nocapture` | canonical artifacts are seal-only and raw lanes stay quarantined | no external simulation; use real checkpoint store and local simulator | changed code plus negative write/load tests prove raw-lane misuse is blocked | `Complete` |
