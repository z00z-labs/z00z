---
phase: 066
plan: 066-01
plan_id: 066-01
title: Scope Safety And Rules Of Engagement
type: implementation
wave: 1
depends_on: []
task_ids: [WS-01]
files_modified:
  - .security/scope.yaml
  - .security/allowed-targets.txt
  - .security/denied-tools.txt
  - .github/skills/pentest-local-orchestrator/references/safety-policy.md
  - scripts/penetration/validate_scope.py
  - tests/penetration/test_scope_validation.py
autonomous: true
requirements:
  - REQ-001
  - REQ-006
  - REQ-007
  - REQ-012
  - REQ-015
---

# 066-01 Scope Safety And Rules Of Engagement

<objective>
Create the local-only authorization boundary that every Phase 066 skill,
script, agent, and DAST runner must enforce before any dynamic scan can run.
</objective>

<plan_fields>
plan_id: `066-01`
task_ids: `WS-01`
source_refs: `.planning/phases/066-Strix/066-TODO.md`, `.planning/phases/066-Strix/066-CONTEXT.md`, `.planning/phases/066-Strix/066-COVERAGE.md`
inputs: local-only scope policy, denied tool list, accepted localhost targets, rejected public targets
outputs: `.security/scope.yaml`, `.security/allowed-targets.txt`, `.security/denied-tools.txt`, `scripts/penetration/validate_scope.py`, safety reference, scope tests
dependencies: none
acceptance_tests: `python3 scripts/penetration/validate_scope.py .security/scope.yaml`, `python3 -m pytest tests/penetration/test_scope_validation.py` or unittest fallback
simulation_gate: deterministic local fixture scopes must cover localhost accepted, public DNS rejected, public IP rejected, wildcard rejected, broad CIDR rejected, denied tool rejected, and empty DAST target skip decision
negative_tests: `https://example.com`, `8.8.8.8`, `*.example.com`, `0.0.0.0/0`, and denied tools must fail before any network call
plan_artifacts: this plan plus `066-COVERAGE.md`
plan_tests: plan-field grep and coverage mapping checks
plan_results: one authoritative safety contract with testable parser behavior
task_artifacts: exact files listed in `files_modified`
task_tests: scope validation unit tests plus denied-tool grep
task_results: validated local-only scope and fail-closed rejection artifacts
anti_placeholder_gate: validator must parse YAML, normalize targets, return structured errors, and be covered by tests; no TODO-only, panic-only, string-only, or no-op implementation counts
current_code_refs: `.github/skills/attack-surfaces-create/SKILL.md`, `.github/skills/attack-surfaces-create/REFERENCE.md`, `.github/copilot-instructions.md`, `.github/requirements/Z00Z_DESIGN_FOUNDATION.md`
blockers: missing YAML parser dependency must be resolved with standard library or existing repo dependency, not bypassed
evidence_gate: each accepted or rejected scope fixture writes a machine-checkable result in test output or artifact JSON
not_recommendation_gate: this plan is complete only when executable files and tests exist; policy prose alone is insufficient
</plan_fields>

<todo_global_mirror>
This section mirrors the global normative bullets from `066-TODO.md` for
plan-corpus traceability only. `066-TODO.md` remains the authority if wording
ever diverges.

This phase must preserve the useful value of both upstream repositories:

- Strix value to preserve: source-aware white-box methodology, skill taxonomy, SAST-to-dynamic-validation workflow, and bounded CLI playbooks.
- Strix value to preserve beyond tools: framework, protocol, technology, vulnerability, and scan-mode playbooks as a reference library and routing matrix.
- HexStrike value to preserve: tool inventory, category taxonomy, process/artifact discipline, timeout awareness, and local tool execution patterns.
- Strix value not to preserve: Strix runtime, Docker sandbox product flow, provider configuration, `openai-agents[litellm]`, Caido SDK runtime coupling, external API-key model.
- HexStrike value not to preserve: MCP server/client, Flask API server, arbitrary command wrapper, file operation wrappers, credential attacks, password cracking, exploit generation, payload generation, public recon defaults, AI evasion/payload generation, and any default workflow that can affect non-owned targets.

## 🛑 Non-Negotiable Constraints

- `REQ-001`: All repository artifacts created by this phase must be English-only.
- `REQ-002`: New third-party penetration tools, cloned upstream sources, local tool caches, and tool wrappers must live under `tools/penetration/`.
- `REQ-003`: Reusable skills must live under `.github/skills/`; no new canonical skill root may be introduced.
- `REQ-004`: `.codex/skills`, `.codex/agents`, `.codex/prompts`, `.codex/hooks`, `.codex/instructions`, `.codex/requirements`, `.codex/scripts`, and `.codex/plugins` must remain symlink compatibility surfaces for `.github/*`.
- `REQ-005`: No MCP server, no HexStrike server, no Strix runtime, and no external LLM API keys may be required for the default workflow.
- `REQ-006`: Dynamic testing must be local-only by default and must read allowed targets from a scope file.
- `REQ-007`: Scanner output is only a hypothesis until linked to source evidence, controlled local reproduction, regression test, or explicit false-positive classification.
- `REQ-008`: Any copied upstream reference must preserve license/provenance metadata and must be marked as reference-only when it must not be executed.
- `REQ-009`: `crates/z00z_crypto/tari/**` remains read-only and out of scope for modification.
- `REQ-010`: All install scripts must avoid global tool pollution where feasible and must install new tools into `tools/penetration/`.
- `REQ-011`: The workflow must be reusable outside Z00Z through generic `pentest-*` skills, with Z00Z-specific audit logic isolated in a profile skill.
- `REQ-012`: Every task below is incomplete until its acceptance checks and doublecheck evidence are recorded.
- `REQ-013`: Do not flatten Strix into tool wrappers only; preserve framework, protocol, technology, vulnerability, and scan-mode playbooks as reference-only knowledge and routing guidance.
- `REQ-014`: Keep the default active skill surface small: target seven generic `pentest-*` skills plus one project profile. Extra upstream playbooks belong under `references/`, not as dozens of auto-triggering skills.
- `REQ-015`: Any grep-based safety check must distinguish active execution paths from reference-only upstream material and must not fail merely because a forbidden tool is named in a denylist or provenance note.
- `REQ-016`: Pentest Docker workflows must not install, bootstrap, or run formal-verification tooling. Do not call `scripts/install-verification-tools.sh`, `tools/formal_verification/**`, `z00z-full-verify-gate`, or verification-orchestrator report flows from the pentest Docker path.
- `REQ-017`: `./z00z_penetration_tests.sh --docker-sandbox` must be the canonical Docker entrypoint for penetration tests. It must be purpose-built for pentests and must not delegate to `unpack_z00z_project.sh --docker-sandbox`.
- `REQ-018`: Docker pentests must run from the same portable tarball artifact produced by `pack_z00z_project.sh`. The Docker path must not implicitly use mutable live checkout state as its source of truth.
- `REQ-019`: Final human-facing pentest reports must be generated under `reports/z00z-pentests_report-<timestamp>/` on the host. `.security-artifacts/<timestamp>/` remains the machine-readable artifact tree and must reference the exported host report location.
- `REQ-020`: Docker pentest runs must stream container stdout/stderr to the invoking terminal and must copy every report they generate back to the matching host `reports/z00z-pentests_report-<timestamp>/` directory before exit.
</todo_global_mirror>

<copied_task_rows>
Goal:
Define the local-only authorized testing boundary that every skill, script, and
agent must enforce.

Required files:

- `.security/scope.yaml`
- `.security/allowed-targets.txt`
- `.security/denied-tools.txt`
- `.github/skills/pentest-local-orchestrator/references/safety-policy.md`
- `scripts/penetration/validate_scope.py`

Implementation requirements:

- `scope.yaml` must include `mode`, `allowed_paths`, `excluded_paths`, `allowed_hosts`, `allowed_urls`, `forbidden`, `rate_limits`, and `evidence_required`.
- Default `allowed_hosts` must be only `127.0.0.1` and `localhost`.
- Default `allowed_urls` must be empty or localhost URLs only.
- `validate_scope.py` must reject public IPs, public DNS names, empty scope for DAST, wildcard hosts, CIDR ranges broader than loopback, and denied tools.
- Dynamic scan scripts must call `validate_scope.py` before running.
- The safety policy must state that source scanning may run without local service targets, but DAST must skip when no valid local targets exist.

Acceptance checks:

- `python3 scripts/penetration/validate_scope.py .security/scope.yaml` returns success for local-only scope.
- A test scope containing `https://example.com` fails unless an explicit future authorization mode is added.
- `rg -n "hydra|john|hashcat|medusa|patator|metasploit|msfvenom|pacu|responder|netexec|crackmapexec|commix|tplmap" scripts/penetration .github/skills/pentest-*/SKILL.md .github/prompts/pentest-*.prompt.md .security/denied-tools.txt` shows those names only in denylist/safety text and never in an active execution command.
</copied_task_rows>

<task_artifacts>
For `WS-01` create or modify:
- `.security/scope.yaml` with the default local-only scope.
- `.security/allowed-targets.txt` with localhost defaults.
- `.security/denied-tools.txt` with forbidden tools from `066-TODO.md`.
- `.github/skills/pentest-local-orchestrator/references/safety-policy.md`.
- `scripts/penetration/validate_scope.py`.
- `tests/penetration/test_scope_validation.py`.
</task_artifacts>

<task_tests>
- `python3 scripts/penetration/validate_scope.py .security/scope.yaml`.
- `python3 -m pytest tests/penetration/test_scope_validation.py` when pytest is available.
- `python3 -m unittest discover tests/penetration` fallback when pytest is unavailable.
- `rg -n "hydra|john|hashcat|medusa|patator|metasploit|msfvenom|pacu|responder|netexec|crackmapexec|commix|tplmap" scripts/penetration .github/skills/pentest-*/SKILL.md .github/prompts/pentest-*.prompt.md .security/denied-tools.txt`.
</task_tests>

<task_results>
- `implementation_depth`: `full`.
- Local-only scope passes.
- Public and wildcard targets fail before network execution.
- Denied tools are accepted only as denylist or safety-policy text.
- DAST with no allowed local target records a skip reason rather than running.
</task_results>

<shared_artifact_boundaries>
- `scripts/penetration/validate_scope.py` owns scope normalization and
  rejection semantics here; later plans must call it rather than reimplement
  target parsing.
- `tests/penetration/test_scope_validation.py` is seeded here as validator
  proof and expanded by `066-12-PLAN.md`, not forked into a second validator
  contract.
</shared_artifact_boundaries>

<tasks>
<task type="auto" id="066-01-T1">
  <read_first>
  - `.planning/phases/066-Strix/066-TODO.md`
  - `.planning/phases/066-Strix/066-CONTEXT.md`
  - `.github/copilot-instructions.md`
  - `.github/requirements/Z00Z_DESIGN_FOUNDATION.md`
  - `.github/skills/attack-surfaces-create/SKILL.md`
  - `.github/skills/attack-surfaces-create/REFERENCE.md`
  </read_first>
  <action>
  Implement the scope files, safety policy, YAML scope validator, and deterministic scope tests listed above. The validator must expose a CLI that accepts a scope path, validates local-only DAST authorization, rejects denied tools, and writes clear non-secret errors.
  </action>
  <acceptance_criteria>
  - `.security/scope.yaml` contains every required key from `066-TODO.md`.
  - `validate_scope.py` rejects public hosts and broad CIDRs before any network calls.
  - tests cover accepted localhost and all listed negative targets.
  - denied tool matches are not active execution commands.
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, and rerun before continuing.
  2. Run `python3 scripts/penetration/validate_scope.py .security/scope.yaml`.
  3. Run `python3 -m pytest tests/penetration/test_scope_validation.py` or `python3 -m unittest discover tests/penetration`.
  4. Run the denied-tool `rg` audit and inspect every match.
  5. If Rust or release-test-affecting changes were made, run `cargo test --release`.
  6. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/066-Strix/066-01-PLAN.md current_task="WS-01"` in YOLO mode at least 3 times via the active agent runtime; if the prompt runner is unavailable, perform an equivalent workspace-first manual review and record the exact blocker plus 2 consecutive clean reruns in the summary.
  7. Use nested `.github` skills as needed: `doublecheck`, `spec-to-code-compliance`, `smart-tests-bootstrap`, and Z00Z gates.
  8. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
- Required bootstrap gate is first.
- Scope validator behavior is proven by positive and negative local fixtures.
- Forbidden names are audited for active command usage.
</verification>

<must_haves>
truths:
- statement: `WS-01` maps only to `066-01-PLAN.md`.
  verification: `rg -n "WS-01" .planning/phases/066-Strix/066-*-PLAN.md`
- statement: scanner safety is enforced by code before dynamic execution.
  verification: scope tests fail rejected targets before DAST scripts can run.
prohibitions:
- statement: docs-only scope policy cannot close `WS-01`.
  verification: executable validator and tests must exist.
</must_haves>

## Artifacts This Phase Produces

- `.security/scope.yaml`
- `.security/allowed-targets.txt`
- `.security/denied-tools.txt`
- `.github/skills/pentest-local-orchestrator/references/safety-policy.md`
- `scripts/penetration/validate_scope.py`
- `tests/penetration/test_scope_validation.py`

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | WS-01 | 066-01 | `066-TODO.md`, `066-CONTEXT.md` | scope policy | scope files and validator | scope tests and grep audit | local-only enforcement | local/public target fixtures | real parser and tests | planned |
