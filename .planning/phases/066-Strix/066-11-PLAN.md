---
phase: 066
plan: 066-11
plan_id: 066-11
title: Docker Decision And Isolation Model
type: implementation
wave: 6
depends_on: [066-10]
task_ids: [WS-11]
files_modified:
  - tools/penetration/docker/README.md
  - tools/penetration/docker/run_pentest_container.sh
  - tools/penetration/docker/Dockerfile
  - z00z_penetration_tests.sh
autonomous: true
requirements:
  - REQ-016
  - REQ-017
  - REQ-018
  - REQ-019
  - REQ-020
---

# 066-11 Docker Decision And Isolation Model

<objective>
Implement and document the optional pentest-only Docker isolation model:
archive-driven input, read-only scan source, live terminal output, host report
export, and no formal-verification or public-recon defaults.
</objective>

<plan_fields>
plan_id: `066-11`
task_ids: `WS-11`
source_refs: `.planning/phases/066-Strix/066-TODO.md`, `tools/penetration/docker/`, `z00z_penetration_tests.sh`, `pack_z00z_project.sh`
inputs: portable archive, Docker image settings, mode flags, host report directory
outputs: Docker README, container runner, optional Dockerfile, entrypoint integration
dependencies: `066-10`
acceptance_tests: local static check works without Docker; Docker check-only works with archive or records exact blocker; logs and reports are visible on host
simulation_gate: container check-only mode verifies archive extraction, symlinks, tool status, and report copy-back without scanning public targets
negative_tests: no privileged default, no host Docker socket mount, no detached-only logs, no formal-verification execution, no MCP server, no credential attack tools
plan_artifacts: this plan and coverage row
plan_tests: Docker check-only, grep audits, local static no-Docker command
plan_results: optional reproducible Docker path
task_artifacts: exact files listed above
task_tests: commands from `WS-11` acceptance checks
task_results: reports copied to host under `reports/z00z-pentests_report-<timestamp>/`
anti_placeholder_gate: Docker wrapper must perform archive extraction and report export; README-only Docker design fails
current_code_refs: `pack_z00z_project.sh`, `z00z_penetration_tests.sh`, `tools/penetration/docker/`
blockers: Docker not installed is a local environment blocker only for Docker tests; local static path must still pass
evidence_gate: logs show attached output and manifest records host report path
not_recommendation_gate: Docker cannot be marked complete without executable wrapper behavior or explicit local Docker blocker evidence
</plan_fields>

<copied_task_rows>
Goal:
Use Docker where it adds safety and reproducibility for penetration testing,
without making Docker the only path or pulling in formal-verification setup.

Decision:

- Do not use `unpack_z00z_project.sh --docker-sandbox` as-is for pentest work; it is a full project restore verifier, not a pentest sandbox.
- Reuse only safe pieces from pack/unpack: archive extraction model, non-root container user, `.codex` symlink checks, and minimal package bootstrap.
- Do not require Docker for normal local static scans.
- Make Docker optional for dynamic local service testing and heavy toolchain verification.
- For pentest Docker, "heavy toolchain verification" means penetration tool availability and artifact/report checks only, not formal-verification toolchain verification.
- Pentest Docker must be archive-driven: the input is the portable `.tar.gz` produced by `pack_z00z_project.sh`, and the extracted container workspace is the scan root.

Implementation requirements:

- Add `./z00z_penetration_tests.sh` with `--docker-sandbox` support specifically for penetration tests.
- Add `--archive <path>` support to `./z00z_penetration_tests.sh --docker-sandbox`.
- When `--docker-sandbox` is used without `--archive`, `./z00z_penetration_tests.sh` must call `./pack_z00z_project.sh --output <tmp-artifact>` first, then run Docker from that artifact.
- `tools/penetration/docker/run_pentest_container.sh` must require an explicit archive path and must reject direct live-checkout scan mode unless a future `--unsafe-live-checkout` debug flag is added.
- Add `tools/penetration/docker/README.md` describing when to use Docker.
- Add `tools/penetration/docker/run_pentest_container.sh` for a pentest-only container entrypoint.
- Add optional `tools/penetration/docker/Dockerfile` if implementers need a pinned pentest environment after the base local workflow exists.
- `./z00z_penetration_tests.sh --docker-sandbox` must mount the packed artifact read-only, extract it inside the container, and write results only to the configured `.security-artifacts/<timestamp>/` output mount.
- `./z00z_penetration_tests.sh --docker-sandbox` must also mount the paired host report directory `reports/z00z-pentests_report-<timestamp>/` and copy all generated reports there before the container exits.
- Docker container stdout/stderr must remain visible in the host terminal for the full run; detached-only execution is not acceptable for the default workflow.
- Any Docker image must mount the repository read-only for scan-only modes unless a remediation mode is explicitly requested.
- Do not run privileged containers by default.
- Do not mount host Docker socket into the pentest container by default.
- Do not install or execute formal-verification tooling in the pentest Docker image or entrypoint.

Acceptance checks:

- Static scan works without Docker.
- `./z00z_penetration_tests.sh --mode quick --static-only --check-only` works without Docker.
- `./z00z_penetration_tests.sh --docker-sandbox --archive /tmp/z00z-pentest-plan-check.tar.gz --mode quick --static-only --check-only` works with the pentest-only container path.
- `./z00z_penetration_tests.sh --docker-sandbox --mode quick --static-only --check-only` produces a temporary packed artifact and records its path in the run manifest.
- Pentest Docker check mode can run `scripts/penetration/check_pentest_tools.sh` and verify symlinks.
- Pentest Docker logs contain no `install-verification-tools`, `tools/formal_verification`, `full_verify.sh`, or `verification-orchestrator` execution.
- Pentest Docker output is visible in the invoking terminal and generated reports are present on the host under `reports/z00z-pentests_report-<timestamp>/`.
- Any Dockerfile has no default public recon, no credential attack tools, and no MCP server.
</copied_task_rows>

<task_artifacts>
- `tools/penetration/docker/README.md`.
- `tools/penetration/docker/run_pentest_container.sh`.
- Optional `tools/penetration/docker/Dockerfile`.
- `z00z_penetration_tests.sh` Docker-only hardening updates needed to keep
  archive input, visible logs, and host report export aligned with `WS-11`.
</task_artifacts>

<task_tests>
- `./z00z_penetration_tests.sh --mode quick --static-only --check-only`.
- `./z00z_penetration_tests.sh --docker-sandbox --archive /tmp/z00z-pentest-plan-check.tar.gz --mode quick --static-only --check-only`.
- `./z00z_penetration_tests.sh --docker-sandbox --mode quick --static-only --check-only`.
- `python3 scripts/penetration/validate_pentest_docker_scope.py tools/penetration/docker scripts/penetration`.
- grep Docker files for forbidden public recon, credential attack, MCP, privileged, and Docker socket defaults.
</task_tests>

<task_results>
- `implementation_depth`: `simulated-full` if Docker is unavailable; `full` when Docker can run.
- Docker path is optional and archive-driven.
- Host report export is proven.
- Formal-verification exclusion is proven.
</task_results>

<shared_artifact_boundaries>
- This plan owns Docker-only hardening, isolation rules, optional image
  definition, and operator-facing Docker documentation.
- `066-10-PLAN.md` owns the archive-production and CLI forwarding contract in
  `z00z_penetration_tests.sh`; this plan must refine Docker behavior without
  redefining the canonical entrypoint semantics.
</shared_artifact_boundaries>

<tasks>
<task type="auto" id="066-11-T1">
  <read_first>
  - `.planning/phases/066-Strix/066-TODO.md`
  - `z00z_penetration_tests.sh`
  - `tools/penetration/docker/run_pentest_container.sh`
  - `pack_z00z_project.sh`
  </read_first>
  <action>
  Implement the Docker isolation model and docs. Make Docker optional, archive-driven, report-exporting, and forbidden-path validated.
  </action>
  <acceptance_criteria>
  - local static check works without Docker.
  - Docker check-only path uses archive input.
  - Docker reports appear on host and logs stream to terminal.
  - no formal-verification or public recon defaults exist.
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, and rerun.
  2. Run local static check-only command.
  3. Run Docker check-only commands when Docker is available; otherwise record exact environment blocker.
  4. Run Docker scope validator and forbidden grep audits.
  5. Run `cargo test --release` when Rust/test-affecting changes are relevant.
  6. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/066-Strix/066-11-PLAN.md current_task="WS-11"` in YOLO mode at least 3 times via the active agent runtime; if the prompt runner is unavailable, perform an equivalent workspace-first manual review and record the exact blocker plus 2 consecutive clean reruns in the summary.
  7. Use `docker-expert`, `doublecheck`, `spec-to-code-compliance`, and Z00Z gates as needed.
  8. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
Docker isolation must be proven by executable check-only behavior and
forbidden-path validation.
</verification>

<must_haves>
truths:
- statement: Docker is optional and archive-driven.
  verification: local no-Docker check and Docker archive check.
prohibitions:
- statement: Docker does not use formal-verification tooling, host Docker socket, privileged mode, or detached-only default output.
  verification: validator and grep audits.
</must_haves>

## Artifacts This Phase Produces

- `tools/penetration/docker/README.md`
- `tools/penetration/docker/run_pentest_container.sh`
- optional `tools/penetration/docker/Dockerfile`
- `z00z_penetration_tests.sh` Docker integration updates

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | WS-11 | 066-11 | `066-TODO.md`, Docker files | archive/report dir | Docker wrapper/docs | local/docker checks | optional isolation | check-only container | archive extraction/report export | planned |
