---
phase: 066
plan: 066-06
plan_id: 066-06
title: Local Script Orchestration
type: implementation
wave: 4
depends_on: [066-01, 066-02, 066-04, 066-05]
task_ids: [WS-06]
files_modified:
  - scripts/penetration/run_local_pentest.sh
  - scripts/penetration/run_parallel_static.sh
  - scripts/penetration/run_source_sast.sh
  - scripts/penetration/run_rust_security.sh
  - scripts/penetration/run_secrets_supply_chain.sh
  - scripts/penetration/run_local_dast.sh
  - scripts/penetration/build_pentest_report.py
  - scripts/penetration/validate_artifacts.py
autonomous: true
requirements:
  - REQ-006
  - REQ-007
  - REQ-019
  - REQ-020
---

# 066-06 Local Script Orchestration

<objective>
Implement deterministic local script entrypoints that skills and humans can
run directly, with one shared timestamp, one machine-readable artifact tree,
and one paired human-facing report directory.
</objective>

<plan_fields>
plan_id: `066-06`
task_ids: `WS-06`
source_refs: `.planning/phases/066-Strix/066-TODO.md`, `scripts/`, `.github/skills/pentest-*`, `.github/skills/z00z-pentest-profile/SKILL.md`
inputs: mode flags, scope path, profile, tool status, local scan commands
outputs: local runner scripts, report builder hook, artifact validator hook
dependencies: `066-01`, `066-02`, `066-04`, `066-05`
acceptance_tests: shell syntax, Python compile, quick static-only generic run creates artifact tree and host report dir
simulation_gate: missing security tools and no local DAST target must be represented as structured artifacts, not swallowed
negative_tests: tool failures must write `.exit`, `.out`, `.err`, or JSON status files and must not mean no issue
plan_artifacts: this plan and coverage row
plan_tests: command syntax and artifact creation checks
plan_results: deterministic local orchestration path
task_artifacts: exact runner and Python files listed above
task_tests: `bash -n`, `python3 -m py_compile`, static-only quick run
task_results: `.security-artifacts/<timestamp>/` and `reports/z00z-pentests_report-<timestamp>/` created for the same timestamp
anti_placeholder_gate: scripts must create real directories, preserve exit codes, and write status files; empty success scripts fail
current_code_refs: `scripts/`, `.security/`, `.github/skills/pentest-*`, `.github/skills/z00z-pentest-profile/SKILL.md`
blockers: absent optional tools must be status entries, not fatal blockers for check-only or static-only baseline
evidence_gate: manifest records exact commands, exits, outputs, and paired host report path
not_recommendation_gate: runner plans are complete only when scripts execute and produce artifacts
</plan_fields>

<copied_task_rows>
Goal:
Provide deterministic local commands that skills can invoke and that humans can
run directly.

Required scripts:

- `scripts/penetration/run_local_pentest.sh`
- `scripts/penetration/run_parallel_static.sh`
- `scripts/penetration/run_source_sast.sh`
- `scripts/penetration/run_rust_security.sh`
- `scripts/penetration/run_secrets_supply_chain.sh`
- `scripts/penetration/run_local_dast.sh`
- `scripts/penetration/build_pentest_report.py`
- `scripts/penetration/validate_artifacts.py`

Implementation requirements:

- Scripts must use `set -euo pipefail` where shell is used.
- Scripts must create `.security-artifacts/<timestamp>/` with subdirectories: `sast`, `rust`, `secrets`, `dast`, `report`, `logs`, `raw`, and `normalized`.
- Scripts must derive one shared run timestamp and use it for both `.security-artifacts/<timestamp>/` and `reports/z00z-pentests_report-<timestamp>/`.
- Scripts must never treat tool failure as proof of no issue; they must write `.exit`, `.out`, `.err`, or JSON status files.
- Static runners may run in parallel; DAST must stay bounded and scope-validated.
- `run_local_pentest.sh` must accept `--mode quick|standard|deep`, `--scope .security/scope.yaml`, `--no-dast`, `--static-only`, `--profile generic|z00z`, and `--artifact-dir`.
- `run_parallel_static.sh` must wait for all child processes and preserve each exit code.
- `build_pentest_report.py` must create a preliminary report without exposing secrets and must write the final report set under `reports/z00z-pentests_report-<timestamp>/`.

Acceptance checks:

- `bash -n scripts/penetration/*.sh` passes.
- `python3 -m py_compile scripts/penetration/*.py` passes.
- `scripts/penetration/run_local_pentest.sh --static-only --mode quick --profile generic` creates an artifact tree even when some tools are missing.
- `scripts/penetration/run_local_pentest.sh --static-only --mode quick --profile generic` also creates `reports/z00z-pentests_report-<timestamp>/` for the same run.
</copied_task_rows>

<task_artifacts>
- `scripts/penetration/run_local_pentest.sh`.
- `scripts/penetration/run_parallel_static.sh`.
- `scripts/penetration/run_source_sast.sh`.
- `scripts/penetration/run_rust_security.sh`.
- `scripts/penetration/run_secrets_supply_chain.sh`.
- `scripts/penetration/run_local_dast.sh`.
- `scripts/penetration/build_pentest_report.py`.
- `scripts/penetration/validate_artifacts.py`.
- Shared artifact-root and host-report-root timestamp contract inside the
  runner family and generated `manifest.json`.
</task_artifacts>

<task_tests>
- `bash -n scripts/penetration/*.sh`.
- `python3 -m py_compile scripts/penetration/*.py`.
- `scripts/penetration/run_local_pentest.sh --static-only --mode quick --profile generic`.
- Validate created `.security-artifacts/<timestamp>/manifest.json`.
- Validate created `reports/z00z-pentests_report-<timestamp>/`.
</task_tests>

<task_results>
- `implementation_depth`: `full`.
- Static-only generic run completes with structured missing-tool statuses.
- DAST is skipped with an explicit artifact when scope has no target.
- Host report and machine artifact roots share the same timestamp.
</task_results>

<shared_artifact_boundaries>
- This plan owns runner orchestration, shared timestamp derivation, manifest
  wiring, exit-status recording, and invocation order across the runner family.
- `066-07-PLAN.md` owns the report and artifact schema semantics inside
  `build_pentest_report.py` and `validate_artifacts.py`; this plan must call
  those surfaces, not invent a second evidence model.
- `066-08-PLAN.md` owns the bounded command set and safety policy inside
  `run_local_dast.sh`; this plan invokes DAST only after validation and mode
  gating.
</shared_artifact_boundaries>

<tasks>
<task type="auto" id="066-06-T1">
  <read_first>
  - `.planning/phases/066-Strix/066-TODO.md`
  - `.security/scope.yaml`
  - `scripts/penetration/validate_scope.py`
  - `scripts/penetration/check_pentest_tools.sh`
  </read_first>
  <action>
  Implement the local script orchestrator and runner family. Ensure `run_local_pentest.sh` validates scope, creates the artifact tree, invokes static runners, optionally invokes DAST, records command exits, and calls the report builder.
  </action>
  <acceptance_criteria>
  - all scripts pass syntax checks.
  - quick static-only generic mode creates both output roots.
  - child process exit codes are preserved in artifacts.
  - no DAST command runs before scope validation.
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, and rerun.
  2. Run `bash -n scripts/penetration/*.sh`.
  3. Run `python3 -m py_compile scripts/penetration/*.py`.
  4. Run `scripts/penetration/run_local_pentest.sh --static-only --mode quick --profile generic`.
  5. Run `python3 scripts/penetration/validate_artifacts.py .security-artifacts/<timestamp>` for the created run.
  6. Run `cargo test --release` when Rust/test-affecting changes are relevant.
  7. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/066-Strix/066-06-PLAN.md current_task="WS-06"` in YOLO mode at least 3 times via the active agent runtime; if the prompt runner is unavailable, perform an equivalent workspace-first manual review and record the exact blocker plus 2 consecutive clean reruns in the summary.
  8. Use nested `.github` skills as needed, especially `doublecheck`, `spec-to-code-compliance`, `smart-tests-bootstrap`, and Z00Z gates.
  9. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
Local runner completion is proven by real artifact creation and validator
acceptance, not by command availability alone.
</verification>

<must_haves>
truths:
- statement: every tool run records command, exit, stdout, stderr, or JSON status.
  verification: artifact manifest and raw/log directories.
prohibitions:
- statement: scanner failure cannot be interpreted as no findings.
  verification: status model distinguishes missing, failed, skipped, and passed.
</must_haves>

## Artifacts This Phase Produces

- `scripts/penetration/run_local_pentest.sh`
- `scripts/penetration/run_parallel_static.sh`
- `scripts/penetration/run_source_sast.sh`
- `scripts/penetration/run_rust_security.sh`
- `scripts/penetration/run_secrets_supply_chain.sh`
- `scripts/penetration/run_local_dast.sh`
- `scripts/penetration/build_pentest_report.py`
- `scripts/penetration/validate_artifacts.py`

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | WS-06 | 066-06 | `066-TODO.md`, scripts | mode/scope/profile | runner scripts | syntax and quick run | artifact/report dirs | missing tools/skipped DAST | real status files | planned |
