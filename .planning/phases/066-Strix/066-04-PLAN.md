---
phase: 066
plan: 066-04
plan_id: 066-04
title: Generic Skill Family
type: implementation
wave: 3
depends_on: [066-01, 066-02, 066-03]
task_ids: [WS-04]
files_modified:
  - .github/skills/pentest-local-orchestrator/SKILL.md
  - .github/skills/pentest-source-aware-sast/SKILL.md
  - .github/skills/pentest-rust-security/SKILL.md
  - .github/skills/pentest-secrets-supply-chain/SKILL.md
  - .github/skills/pentest-local-dast/SKILL.md
  - .github/skills/pentest-report/SKILL.md
  - .github/skills/pentest-tool-installer/SKILL.md
autonomous: true
requirements:
  - REQ-003
  - REQ-011
  - REQ-014
---

# 066-04 Generic Skill Family

<objective>
Create the reusable generic `pentest-*` skill family that orchestrates local
source-aware SAST, Rust security checks, secrets and supply-chain checks,
local-only DAST, reporting, and tool installation without Z00Z-specific
assumptions.
</objective>

<plan_fields>
plan_id: `066-04`
task_ids: `WS-04`
source_refs: `.planning/phases/066-Strix/066-TODO.md`, `.github/skills/attack-surfaces-create/SKILL.md`, `.github/skills/smart-tests-bootstrap/SKILL.md`
inputs: generic skill names, safety boundaries, script paths, artifact root
outputs: seven `.github/skills/pentest-*` directories with complete `SKILL.md` files
dependencies: `066-01`, `066-02`, `066-03`
acceptance_tests: skill count, active skill directory count, safety grep, artifact path grep
simulation_gate: documentation-only dry run over a temporary artifact directory proves skills route to scripts without running scanners
negative_tests: skills mention no default MCP, external API key, public target, or scanner-output-as-confirmed-finding path
plan_artifacts: this plan and coverage row
plan_tests: skill file presence and grep checks
plan_results: small generic skill surface with routed references
task_artifacts: seven skill directories
task_tests: `find` and `rg` checks from `066-TODO.md`
task_results: skills callable by Codex/Copilot and generic by default
anti_placeholder_gate: every `SKILL.md` must include concrete invocation paths, outputs, safety rules, and evidence requirements
current_code_refs: `.github/skills/`, `.github/skills/attack-surfaces-create/`, `.github/skills/smart-tests-bootstrap/`
blockers: if scripts from later plans are not implemented yet, skills must name expected script paths and fail closed when absent
evidence_gate: skill grep checks and dry-run text prove script paths and artifact roots are explicit
not_recommendation_gate: empty skill directories or README-only skill stubs do not count
</plan_fields>

<copied_task_rows>
Goal:
Create reusable Codex/Copilot skills that can migrate to other projects
without Z00Z-specific assumptions.

Required skill directories:

- `.github/skills/pentest-local-orchestrator/`
- `.github/skills/pentest-source-aware-sast/`
- `.github/skills/pentest-rust-security/`
- `.github/skills/pentest-secrets-supply-chain/`
- `.github/skills/pentest-local-dast/`
- `.github/skills/pentest-report/`
- `.github/skills/pentest-tool-installer/`

Implementation requirements:

- Every skill must have a complete `SKILL.md` with YAML front matter.
- Every skill description must include trigger language and safety boundaries.
- Skills must call scripts by path rather than retyping long command blocks.
- Skills must write artifacts under `.security-artifacts/<timestamp>/`.
- Skills must be generic by default; any Z00Z-only logic belongs in `z00z-pentest-profile`.
- Do not create a separate active skill for each Strix framework, technology, or vulnerability file. Keep those files as routed references to avoid context bloat and trigger ambiguity.
- `pentest-local-orchestrator` must define quick, standard, and deep modes.
- `pentest-source-aware-sast` must adapt Strix source-aware SAST workflow: Semgrep, deterministic AST targets from Semgrep scope, `sg` or `tree-sitter`, Gitleaks, TruffleHog, and Trivy.
- `pentest-local-dast` must use scoped local-only DAST with bounded `nmap`, `nuclei`, `httpx`, `katana`, `ffuf`, and `gobuster`.
- `pentest-report` must deduplicate scanner results and require evidence mapping before final findings.

Acceptance checks:

- `find .github/skills/pentest-* -maxdepth 2 -name SKILL.md | wc -l` reports at least `7`.
- `find .github/skills -maxdepth 1 -name 'pentest-*' -type d | wc -l` stays intentionally small; any expansion beyond `10` directories requires a written rationale.
- `rg -n "MCP|API key|public target|scanner output" .github/skills/pentest-*` finds explicit safety and evidence rules.
- `rg -n "tools/penetration|scripts/penetration|.security-artifacts" .github/skills/pentest-*` finds consistent paths.
</copied_task_rows>

<task_artifacts>
- `.github/skills/pentest-local-orchestrator/SKILL.md`.
- `.github/skills/pentest-source-aware-sast/SKILL.md`.
- `.github/skills/pentest-rust-security/SKILL.md`.
- `.github/skills/pentest-secrets-supply-chain/SKILL.md`.
- `.github/skills/pentest-local-dast/SKILL.md`.
- `.github/skills/pentest-report/SKILL.md`.
- `.github/skills/pentest-tool-installer/SKILL.md`.
- Any base reference indexes or readme fragments required inside those
  directories to keep one canonical `pentest-*` skill root.
</task_artifacts>

<task_tests>
- `find .github/skills/pentest-* -maxdepth 2 -name SKILL.md | wc -l`.
- `find .github/skills -maxdepth 1 -name 'pentest-*' -type d | wc -l`.
- `rg -n "MCP|API key|public target|scanner output" .github/skills/pentest-*`.
- `rg -n "tools/penetration|scripts/penetration|.security-artifacts" .github/skills/pentest-*`.
</task_tests>

<task_results>
- `implementation_depth`: `full`.
- Seven generic skill directories exist.
- Skills are generic and call local scripts by path.
- Default active skill surface remains intentionally small.
</task_results>

<shared_artifact_boundaries>
- This plan owns the existence, front matter, base trigger contracts, and
  fail-closed behavior of the seven generic `pentest-*` skills.
- Later plans may deepen skill-specific content, such as bounded DAST behavior
  in `066-08-PLAN.md` or report-evidence semantics in `066-07-PLAN.md`, but
  must not create alternate skill roots or duplicate active skills.
</shared_artifact_boundaries>

<tasks>
<task type="auto" id="066-04-T1">
  <read_first>
  - `.planning/phases/066-Strix/066-TODO.md`
  - `.planning/phases/066-Strix/066-CONTEXT.md`
  - `.github/skills/attack-surfaces-create/SKILL.md`
  - `.github/skills/smart-tests-bootstrap/SKILL.md`
  </read_first>
  <action>
  Implement the seven generic `pentest-*` skills with YAML front matter, trigger descriptions, safety boundaries, script invocations, artifact paths, and evidence mapping requirements. Keep upstream Strix material as routed references rather than one active skill per playbook.
  </action>
  <acceptance_criteria>
  - all seven `SKILL.md` files exist.
  - skills call `scripts/penetration/*` and write under `.security-artifacts/<timestamp>/`.
  - `pentest-report` requires evidence mapping before final findings.
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, and rerun.
  2. Run all `find` and `rg` checks in `<task_tests>`.
  3. Run `cargo test --release` when Rust/test-affecting changes are relevant.
  4. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/066-Strix/066-04-PLAN.md current_task="WS-04"` in YOLO mode at least 3 times via the active agent runtime; if the prompt runner is unavailable, perform an equivalent workspace-first manual review and record the exact blocker plus 2 consecutive clean reruns in the summary.
  5. Use `doublecheck`, `spec-to-code-compliance`, and Z00Z gates as needed.
  6. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
The generic skill surface must be complete enough for script-backed execution
but small enough to avoid one active skill per upstream playbook.
</verification>

<must_haves>
truths:
- statement: seven generic `pentest-*` skills exist and are generic by default.
  verification: `find` count and skill content grep.
prohibitions:
- statement: Z00Z-specific logic must not live in generic skill bodies.
  verification: Z00Z routing appears only as generic mention or in profile plan.
</must_haves>

## Artifacts This Phase Produces

- `.github/skills/pentest-local-orchestrator/SKILL.md`
- `.github/skills/pentest-source-aware-sast/SKILL.md`
- `.github/skills/pentest-rust-security/SKILL.md`
- `.github/skills/pentest-secrets-supply-chain/SKILL.md`
- `.github/skills/pentest-local-dast/SKILL.md`
- `.github/skills/pentest-report/SKILL.md`
- `.github/skills/pentest-tool-installer/SKILL.md`

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | WS-04 | 066-04 | `066-TODO.md`, existing skill patterns | skill contracts | seven skills | find/rg checks | generic callable skills | dry-run routing | complete SKILL.md files | planned |
