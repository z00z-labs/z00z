---
phase: 066
plan: 066-12
plan_id: 066-12
title: Regression Tests And Self Tests
type: implementation
wave: 7
depends_on: [066-01, 066-02, 066-06, 066-07, 066-08, 066-09]
task_ids: [WS-12]
files_modified:
  - tests/penetration/test_scope_validation.py
  - tests/penetration/test_artifact_schema.py
  - tests/penetration/test_report_builder.py
  - tests/penetration/test_tool_manifest.py
  - tests/penetration/fixtures/scope/
  - tests/penetration/fixtures/tool_status/
  - tests/penetration/fixtures/scanner_outputs/
  - tests/penetration/fixtures/reports/
autonomous: true
requirements:
  - REQ-012
---

# 066-12 Regression Tests And Self Tests

<objective>
Add deterministic regression tests and self-tests that prove the orchestration
layer is safe before it is used for real audits.
</objective>

<plan_fields>
plan_id: `066-12`
task_ids: `WS-12`
source_refs: `.planning/phases/066-Strix/066-TODO.md`, `scripts/penetration/*.sh`, `scripts/penetration/*.py`, `.security/*`
inputs: fixtures for scopes, tool status, scanner output, report findings, symlinks
outputs: penetration test suite and fixtures
dependencies: `066-01`, `066-02`, `066-06`, `066-07`, `066-08`, `066-09`
acceptance_tests: pytest or unittest fallback, shell syntax, Python compile
simulation_gate: tests use fixtures and fake external tool outputs while using real validators, report builder, artifact validator, symlink checks, and script syntax
negative_tests: public scope, denied tool, missing tools, skipped DAST, unredacted secret report, broken symlink, and scanner-only confirmed finding must fail
plan_artifacts: this plan and coverage row
plan_tests: full `tests/penetration` run
plan_results: repeatable offline regression suite
task_artifacts: exact tests and fixtures listed above
task_tests: pytest/unittest plus syntax/compile checks
task_results: safety and report behavior covered without internet or installed third-party tools
anti_placeholder_gate: tests must assert behavior and fail against no-op validators or static report builders
current_code_refs: `scripts/penetration/`, `.security/`, `.codex` symlinks
blockers: if pytest is unavailable, unittest fallback must cover core validation behavior
evidence_gate: CI-local test output proves positive and negative cases
not_recommendation_gate: test plan text without executable tests does not close `WS-12`
</plan_fields>

<copied_task_rows>
Goal:
Prove that the orchestration layer behaves safely before it is used for real
audits.

Required tests:

- `tests/penetration/test_scope_validation.py`
- `tests/penetration/test_artifact_schema.py`
- `tests/penetration/test_report_builder.py`
- `tests/penetration/test_tool_manifest.py`
- optional shell tests for `scripts/penetration/*.sh`

Implementation requirements:

- Tests must cover local scope accepted, public scope rejected, denied tool rejected, missing tools recorded, skipped DAST recorded, report redaction, and symlink presence.
- Tests must not require internet access or installed third-party security tools.
- Tests must use fixtures under `tests/penetration/fixtures/`.

Acceptance checks:

- `python3 -m pytest tests/penetration` passes when pytest is available.
- If pytest is unavailable, `python3 -m unittest discover tests/penetration` or a documented fallback runs the core validation tests.
- `bash -n scripts/penetration/*.sh` and `python3 -m py_compile scripts/penetration/*.py` pass.
</copied_task_rows>

<task_artifacts>
- `tests/penetration/test_scope_validation.py`.
- `tests/penetration/test_artifact_schema.py`.
- `tests/penetration/test_report_builder.py`.
- `tests/penetration/test_tool_manifest.py`.
- `tests/penetration/fixtures/scope/`.
- `tests/penetration/fixtures/tool_status/`.
- `tests/penetration/fixtures/scanner_outputs/`.
- `tests/penetration/fixtures/reports/`.
- Any optional shell-test harness under `tests/penetration/` required to prove
  `scripts/penetration/*.sh` behavior without internet access.
</task_artifacts>

<task_tests>
- `python3 -m pytest tests/penetration`.
- `python3 -m unittest discover tests/penetration` fallback.
- `bash -n scripts/penetration/*.sh`.
- `python3 -m py_compile scripts/penetration/*.py`.
</task_tests>

<task_results>
- `implementation_depth`: `full`.
- Tests run offline.
- Tests fail against no-op validators, no-op report builder, or broken symlink assumptions.
- Missing tools are recorded without requiring installation.
</task_results>

<shared_artifact_boundaries>
- This plan owns cross-plan regression coverage and fixture depth for Phase
  066.
- `066-01-PLAN.md` seeds validator semantics and
  `066-07-PLAN.md` seeds report/artifact semantics; this plan extends their
  tests and fixtures rather than forking those contracts behind mocks or a
  second validator/report model.
</shared_artifact_boundaries>

<tasks>
<task type="auto" id="066-12-T1">
  <read_first>
  - `.planning/phases/066-Strix/066-TODO.md`
  - `scripts/penetration/validate_scope.py`
  - `scripts/penetration/build_pentest_report.py`
  - `scripts/penetration/validate_artifacts.py`
  - `scripts/penetration/check_pentest_tools.sh`
  </read_first>
  <action>
  Implement the offline penetration regression tests and fixtures. Cover the required positive and negative cases and keep fallback execution available when pytest is absent.
  </action>
  <acceptance_criteria>
  - pytest or unittest fallback passes.
  - shell and Python syntax checks pass.
  - tests include negative cases for public target, denied tool, missing tools, skipped DAST, redaction, broken symlink, and scanner-only confirmed finding.
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, and rerun.
  2. Run `python3 -m pytest tests/penetration` or fallback unittest.
  3. Run `bash -n scripts/penetration/*.sh`.
  4. Run `python3 -m py_compile scripts/penetration/*.py`.
  5. Run `cargo test --release` when Rust/test-affecting changes are relevant.
  6. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/066-Strix/066-12-PLAN.md current_task="WS-12"` in YOLO mode at least 3 times via the active agent runtime; if the prompt runner is unavailable, perform an equivalent workspace-first manual review and record the exact blocker plus 2 consecutive clean reruns in the summary.
  7. Use `doublecheck`, `spec-to-code-compliance`, `smart-tests-bootstrap`, and Z00Z gates as needed.
  8. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
The test suite is the anti-placeholder guard for all earlier Phase 066 scripts
and report contracts.
</verification>

<must_haves>
truths:
- statement: safety behavior is covered offline.
  verification: penetration test suite.
prohibitions:
- statement: tests must not require internet or installed third-party tools.
  verification: fixtures and missing-tool status tests.
</must_haves>

## Artifacts This Phase Produces

- `tests/penetration/test_scope_validation.py`
- `tests/penetration/test_artifact_schema.py`
- `tests/penetration/test_report_builder.py`
- `tests/penetration/test_tool_manifest.py`
- `tests/penetration/fixtures/`

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | WS-12 | 066-12 | `066-TODO.md`, scripts | fixtures | tests | pytest/unittest | offline safety suite | fake tools/real validators | tests fail no-ops | planned |
