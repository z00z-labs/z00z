---
phase: 066
plan: 066-10
plan_id: 066-10
title: Portable Pack Unpack Integration
type: implementation
wave: 6
depends_on: [066-02, 066-06, 066-07, 066-09]
task_ids: [WS-10]
files_modified:
  - z00z_penetration_tests.sh
  - pack_z00z_project.sh
  - unpack_z00z_project.sh
  - scripts/penetration/check_pentest_tools.sh
  - scripts/penetration/validate_pentest_docker_scope.py
  - tools/penetration/docker/run_pentest_container.sh
  - tools/penetration/manifests/tool-versions.lock
autonomous: true
requirements:
  - REQ-016
  - REQ-017
  - REQ-018
  - REQ-019
  - REQ-020
---

# 066-10 Portable Pack Unpack Integration

<objective>
Make penetration tooling portable through the existing archive workflow while
keeping pentest Docker separate from the full restore and formal-verification
path.
</objective>

<plan_fields>
plan_id: `066-10`
task_ids: `WS-10`
source_refs: `.planning/phases/066-Strix/066-TODO.md`, `pack_z00z_project.sh`, `unpack_z00z_project.sh`, `scripts/install-verification-tools.sh`, `scripts/penetration/build_pentest_report.py`, `scripts/penetration/validate_artifacts.py`, `tools/formal_verification/`
inputs: portable archive path, pentest mode flags, symlink manifest, tool status
outputs: canonical `z00z_penetration_tests.sh`, pack updates, Docker runner, Docker-scope validator
dependencies: `066-02`, `066-06`, `066-07`, `066-09`
acceptance_tests: pack includes pentest sources and excludes caches; Docker check-only runs from archive; formal-verification forbidden grep passes
simulation_gate: check-only Docker path verifies extraction, symlinks, tool status, and host report directory without running scans
negative_tests: pentest Docker path must not invoke `unpack_z00z_project.sh --docker-sandbox`, `install-verification-tools`, `tools/formal_verification`, `full_verify.sh`, or verification orchestrator
plan_artifacts: this plan and coverage row
plan_tests: pack check, Docker check-only, forbidden executable-line grep
plan_results: archive-driven pentest entrypoint
task_artifacts: exact files listed above
task_tests: commands from `WS-10` acceptance checks
task_results: packed artifact drives pentest check-only Docker path and report export
anti_placeholder_gate: `z00z_penetration_tests.sh` must parse and forward real flags; Docker wrapper must require archive and write artifacts
current_code_refs: `pack_z00z_project.sh`, `unpack_z00z_project.sh`, `scripts/penetration/build_pentest_report.py`, `scripts/penetration/validate_artifacts.py`, `.codex` symlinks, `tools/formal_verification/`
blockers: Docker unavailable may block Docker execution but not local pack validation; record exact blocker
evidence_gate: run manifest records archive path, extraction path, report dir, and formal-verification exclusion audit
not_recommendation_gate: Docker design docs alone cannot close this plan
</plan_fields>

<copied_task_rows>
Goal:
Make penetration tooling portable across machines without breaking existing
archive behavior or invoking unrelated formal-verification setup.

Required changes:

- `z00z_penetration_tests.sh`
- `pack_z00z_project.sh`
- `unpack_z00z_project.sh`
- `scripts/penetration/check_pentest_tools.sh`
- `scripts/penetration/validate_pentest_docker_scope.py`
- `tools/penetration/docker/run_pentest_container.sh`
- `tools/penetration/manifests/tool-versions.lock`

Implementation requirements:

- Do not pack large generated caches by default: exclude `tools/penetration/cache`, heavy downloaded DBs, and tool build artifacts unless a future `--with-pentest-cache` flag is explicitly added.
- Pack source scripts, skills, prompts, agents, manifests, rules, templates, and lightweight wrappers.
- Preserve symlink manifest behavior for `.codex/*`.
- Add optional restore hook that runs `scripts/penetration/check_pentest_tools.sh` after extraction and reports missing tools without failing the whole restore unless a strict flag is added.
- Add `./z00z_penetration_tests.sh` as the canonical local and Docker entrypoint for penetration tests.
- `./z00z_penetration_tests.sh --docker-sandbox` must call the pentest-only Docker path, not `unpack_z00z_project.sh --docker-sandbox`.
- `./z00z_penetration_tests.sh --docker-sandbox` must create or accept a `pack_z00z_project.sh` tarball artifact and pass that archive into `tools/penetration/docker/run_pentest_container.sh`.
- Supported Docker modes must include `--archive <path>` to reuse an existing packed artifact and `--pack` or default auto-pack behavior to create a fresh artifact before container launch.
- The Docker container must extract the packed artifact into an internal workspace and run pentest checks against that extracted workspace, not against the host checkout mounted as the source tree.
- Docker runs must allocate or accept the paired host report directory `reports/z00z-pentests_report-<timestamp>/`, mount it for output, and copy every report generated in the container into that directory before exit.
- Docker runs must keep container stdout/stderr attached to the invoking terminal so tool progress, warnings, and report-generation messages are visible live on the host.
- `./z00z_penetration_tests.sh` must forward common flags to `scripts/penetration/run_local_pentest.sh`: `--mode quick|standard|deep`, `--scope`, `--static-only`, `--no-dast`, `--profile generic|z00z`, `--artifact-dir`, and `--check-only`.
- Do not use `unpack_z00z_project.sh --docker-sandbox` as the pentest Docker runner because that path runs formal-verification installer and full verification gates.
- Either add an explicit `--profile pentest` or `--skip-formal-verification` restore mode, or create a dedicated `tools/penetration/docker/run_pentest_container.sh` wrapper that reuses only safe extraction, symlink verification, and penetration-tool checks.
- The pentest restore/container path must skip `scripts/install-verification-tools.sh`, `tools/formal_verification/**`, `.github/skills/z00z-full-verify-gate/scripts/full_verify.sh`, and `.github/skills/z00z-verification-orchestrator/scripts/orchestrate.sh`.
- If a dedicated pentest Docker image is needed, define it under `tools/penetration/docker/` and make it optional.

Acceptance checks:

- `./pack_z00z_project.sh --output /tmp/z00z-pentest-plan-check.tar.gz --keep-tmp` includes new skills/scripts/manifests and excludes heavy caches.
- `./z00z_penetration_tests.sh --docker-sandbox --archive /tmp/z00z-pentest-plan-check.tar.gz --mode quick --static-only --check-only` runs the pentest-only Docker check path from the packed artifact.
- `./z00z_penetration_tests.sh --docker-sandbox --mode quick --static-only --check-only` creates a fresh packed artifact before launching Docker when `--archive` is not provided.
- `tools/penetration/docker/run_pentest_container.sh --archive /tmp/z00z-pentest-plan-check.tar.gz --mode check-only` verifies `.codex` symlinks and reports penetration tool status without running formal verification.
- Docker report runs leave exported host reports under `reports/z00z-pentests_report-<timestamp>/`.
- `python3 scripts/penetration/validate_pentest_docker_scope.py tools/penetration/docker scripts/penetration` fails if executable script lines invoke `install-verification-tools`, `tools/formal_verification`, `z00z-full-verify-gate`, or `z00z-verification-orchestrator`.
- `tools/penetration/cache` is not present in packed output unless explicitly requested by a future flag.
</copied_task_rows>

<task_artifacts>
- `z00z_penetration_tests.sh`.
- `pack_z00z_project.sh` updates for pentest source inclusion and heavy-cache
  exclusion.
- `unpack_z00z_project.sh` pentest-safe restore integration, if needed for the
  selected wrapper strategy.
- `scripts/penetration/check_pentest_tools.sh` Docker/check-only integration
  updates.
- `scripts/penetration/validate_pentest_docker_scope.py`.
- `tools/penetration/docker/run_pentest_container.sh`.
- `tools/penetration/manifests/tool-versions.lock` updates required by the
  portable pentest path.
- Archive manifest/report-path fields proving packed-artifact origin and host
  report export.
</task_artifacts>

<task_tests>
- `./pack_z00z_project.sh --output /tmp/z00z-pentest-plan-check.tar.gz --keep-tmp`.
- `./z00z_penetration_tests.sh --docker-sandbox --archive /tmp/z00z-pentest-plan-check.tar.gz --mode quick --static-only --check-only`.
- `./z00z_penetration_tests.sh --docker-sandbox --mode quick --static-only --check-only`.
- `tools/penetration/docker/run_pentest_container.sh --archive /tmp/z00z-pentest-plan-check.tar.gz --mode check-only`.
- `python3 scripts/penetration/validate_pentest_docker_scope.py tools/penetration/docker scripts/penetration`.
</task_tests>

<task_results>
- `implementation_depth`: `simulated-full` when Docker is unavailable; `full` when Docker can run locally.
- Archive-driven Docker check-only path works or records exact Docker blocker.
- Formal-verification calls are excluded by validator.
</task_results>

<shared_artifact_boundaries>
- This plan owns the archive-driven CLI contract in
  `z00z_penetration_tests.sh`, pack/unpack integration, and the rule that the
  pentest Docker path cannot delegate to full restore verification.
- `066-11-PLAN.md` may harden Docker-only behavior and image/docs details, but
  must not redesign flag forwarding, archive selection, or report-export
  semantics owned here.
- `scripts/penetration/check_pentest_tools.sh` and `tool-versions.lock` are
  rooted in `066-02-PLAN.md`; this plan may extend them only for check-only
  portability evidence, not redefine tool-root semantics.
</shared_artifact_boundaries>

<tasks>
<task type="auto" id="066-10-T1">
  <read_first>
  - `.planning/phases/066-Strix/066-TODO.md`
  - `pack_z00z_project.sh`
  - `unpack_z00z_project.sh`
  - `scripts/install-verification-tools.sh`
  - `tools/formal_verification/`
  </read_first>
  <action>
  Add `z00z_penetration_tests.sh`, update pack behavior for pentest sources and cache exclusions, add the pentest-only Docker wrapper, and implement `validate_pentest_docker_scope.py`.
  </action>
  <acceptance_criteria>
  - entrypoint forwards common flags to `run_local_pentest.sh`.
  - `--docker-sandbox` uses an archive and pentest-only Docker wrapper.
  - formal-verification forbidden-call validator passes.
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, and rerun.
  2. Run pack and check-only commands where Docker is available; otherwise record Docker blocker and run non-Docker validator.
  3. Run `python3 scripts/penetration/validate_pentest_docker_scope.py tools/penetration/docker scripts/penetration`.
  4. Run `cargo test --release` when Rust/test-affecting changes are relevant.
  5. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/066-Strix/066-10-PLAN.md current_task="WS-10"` in YOLO mode at least 3 times via the active agent runtime; if the prompt runner is unavailable, perform an equivalent workspace-first manual review and record the exact blocker plus 2 consecutive clean reruns in the summary.
  6. Use `docker-expert`, `doublecheck`, `spec-to-code-compliance`, and Z00Z gates as needed.
  7. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
Portability is proven from packed archive behavior and forbidden-path
validation, not live checkout assumptions.
</verification>

<must_haves>
truths:
- statement: pentest Docker runs from `pack_z00z_project.sh` archive.
  verification: check-only archive command and manifest.
prohibitions:
- statement: pentest Docker must not invoke full restore or formal-verification flows.
  verification: `validate_pentest_docker_scope.py`.
</must_haves>

## Artifacts This Phase Produces

- `z00z_penetration_tests.sh`
- `scripts/penetration/validate_pentest_docker_scope.py`
- `tools/penetration/docker/run_pentest_container.sh`
- pack/unpack integration changes
- updated `tools/penetration/manifests/tool-versions.lock`

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | WS-10 | 066-10 | `066-TODO.md`, pack/unpack | archive flags | entrypoint/wrapper | pack/docker checks | archive-driven pentest | check-only container | forbidden formal path validator | planned |
