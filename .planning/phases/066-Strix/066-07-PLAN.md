---
phase: 066
plan: 066-07
plan_id: 066-07
title: Artifact Schema And Report Contract
type: implementation
wave: 4
depends_on: [066-01, 066-04, 066-06]
task_ids: [WS-07]
files_modified:
  - .security/report-template.md
  - .github/skills/pentest-report/references/report-schema.md
  - scripts/penetration/build_pentest_report.py
  - scripts/penetration/validate_artifacts.py
  - tests/penetration/test_artifact_schema.py
  - tests/penetration/test_report_builder.py
autonomous: true
requirements:
  - REQ-007
  - REQ-019
---

# 066-07 Artifact Schema And Report Contract

<objective>
Define and enforce the machine-readable artifact schema and human-facing report
contract so findings are evidence-mapped and usable without reading raw scanner
noise.
</objective>

<plan_fields>
plan_id: `066-07`
task_ids: `WS-07`
source_refs: `.planning/phases/066-Strix/066-TODO.md`, `.security/report-template.md`, `.github/skills/pentest-report/SKILL.md`, `.github/skills/pentest-report/references/report-schema.md`
inputs: raw scanner statuses, normalized findings, scope, tool versions, evidence links
outputs: report template, schema reference, report builder, artifact validator, report tests
dependencies: `066-01`, `066-04`, `066-06`
acceptance_tests: artifact validator passes created run, manifest records host report path, scanner-only hit cannot be confirmed without evidence mapping
simulation_gate: fixture scanner hits must produce confirmed, false-positive, skipped, or unconfirmed report entries based on evidence fields
negative_tests: report builder must reject confirmed finding with no source file, scanner artifact, reproduction/proof, or false-positive classification
plan_artifacts: this plan and coverage row
plan_tests: report-builder tests and artifact validator
plan_results: report contract with severity summary and evidence requirements
task_artifacts: exact files listed above
task_tests: pytest/unittest report builder tests, artifact validation
task_results: `security-report.md` and host report directory are generated deterministically
anti_placeholder_gate: report builder must parse normalized input and enforce evidence; static Markdown template alone fails
current_code_refs: `scripts/penetration/build_pentest_report.py`, `scripts/penetration/validate_artifacts.py`, `.security-artifacts/`
blockers: missing optional scanner output must be reported as skipped or missing, not hidden
evidence_gate: every confirmed finding row contains scope, command, source evidence, confidence, fix, and regression test
not_recommendation_gate: recommendations without proof artifacts do not count as confirmed findings
</plan_fields>

<copied_task_rows>
Goal:
Make outputs usable by Codex, Copilot, humans, and future automation without
reading raw scanner noise.

Required files:

- `.security/report-template.md`
- `.github/skills/pentest-report/references/report-schema.md`
- `scripts/penetration/build_pentest_report.py`
- `scripts/penetration/validate_artifacts.py`

Required artifact shape:

```text
.security-artifacts/<timestamp>/
  manifest.json
  scope.normalized.json
  tool-status.json
  sast/
  rust/
  secrets/
  dast/
  raw/
  normalized/
  report/security-report.md
  logs/
```

Canonical host report root:

```text
reports/z00z-pentests_report-<timestamp>/
  security-report.md
  ...
```

Report requirements:

- The canonical host report root is `reports/z00z-pentests_report-<timestamp>/`; every generated Markdown, HTML, JSON, or other human-facing report artifact must be written there.
- Scope and authorization.
- Tool versions and exact commands.
- Findings summary ordered by severity.
- Per-finding evidence: source file, scanner artifact, local reproduction or proof, confidence, false-positive status.
- Fix recommendation and required regression tests.
- Redaction notes for secrets.
- Open questions and skipped scans with explicit reason.
- Doublecheck section.

Acceptance checks:

- `python3 scripts/penetration/validate_artifacts.py .security-artifacts/<timestamp>` validates manifest, tool status, and report path.
- The artifact manifest records the paired host report directory `reports/z00z-pentests_report-<timestamp>/`.
- A report cannot classify a scanner hit as confirmed unless it has evidence mapping.
</copied_task_rows>

<task_artifacts>
- `.security/report-template.md`.
- `.github/skills/pentest-report/references/report-schema.md`.
- `scripts/penetration/build_pentest_report.py`.
- `scripts/penetration/validate_artifacts.py`.
- `tests/penetration/test_artifact_schema.py`.
- `tests/penetration/test_report_builder.py`.
- Normalized finding schema fields and host-report-path manifest contract used
  by those files.
</task_artifacts>

<task_tests>
- `python3 -m pytest tests/penetration/test_artifact_schema.py tests/penetration/test_report_builder.py` or unittest fallback.
- `python3 scripts/penetration/validate_artifacts.py .security-artifacts/<timestamp>`.
- fixture test where scanner-only finding cannot be confirmed.
</task_tests>

<task_results>
- `implementation_depth`: `full`.
- Host reports are written under `reports/z00z-pentests_report-<timestamp>/`.
- Machine artifacts reference the paired host report directory.
- Report contains severity summary and doublecheck section.
</task_results>

<shared_artifact_boundaries>
- This plan owns the evidence model, confirmed-finding rules, report shape,
  and artifact validation semantics inside `build_pentest_report.py` and
  `validate_artifacts.py`.
- `066-06-PLAN.md` owns invocation order, timestamping, and manifest/report-dir
  wiring for those shared files, while `066-12-PLAN.md` extends their tests
  rather than forking validator or report semantics.
</shared_artifact_boundaries>

<tasks>
<task type="auto" id="066-07-T1">
  <read_first>
  - `.planning/phases/066-Strix/066-TODO.md`
  - `scripts/penetration/build_pentest_report.py`
  - `scripts/penetration/validate_artifacts.py`
  </read_first>
  <action>
  Implement the report schema, report builder, validator, and tests. Enforce that confirmed findings require evidence mapping and that every human-facing report artifact is written under the host report root.
  </action>
  <acceptance_criteria>
  - `security-report.md` exists in the host report root.
  - manifest records paired host report directory.
  - confirmed scanner hit without evidence fails validation.
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, and rerun.
  2. Run report-builder and artifact-schema tests.
  3. Run `python3 scripts/penetration/validate_artifacts.py .security-artifacts/<timestamp>`.
  4. Run `cargo test --release` when Rust/test-affecting changes are relevant.
  5. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/066-Strix/066-07-PLAN.md current_task="WS-07"` in YOLO mode at least 3 times via the active agent runtime; if the prompt runner is unavailable, perform an equivalent workspace-first manual review and record the exact blocker plus 2 consecutive clean reruns in the summary.
  6. Use `doublecheck`, `spec-to-code-compliance`, and Z00Z gates as needed.
  7. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
Reports are valid only when artifacts, manifest, and evidence mapping agree.
</verification>

<must_haves>
truths:
- statement: final human-facing reports are under `reports/z00z-pentests_report-<timestamp>/`.
  verification: report builder tests and manifest validation.
prohibitions:
- statement: scanner output alone cannot be a confirmed finding.
  verification: negative report-builder fixture.
</must_haves>

## Artifacts This Phase Produces

- `.security/report-template.md`
- `.github/skills/pentest-report/references/report-schema.md`
- `scripts/penetration/build_pentest_report.py`
- `scripts/penetration/validate_artifacts.py`
- `tests/penetration/test_artifact_schema.py`
- `tests/penetration/test_report_builder.py`

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | WS-07 | 066-07 | `066-TODO.md`, report schema | normalized findings | report builder/schema | validator/report tests | evidence-mapped report | fixture scanner hits | confirmed requires evidence | planned |
