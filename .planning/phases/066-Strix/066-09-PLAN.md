---
phase: 066
plan: 066-09
plan_id: 066-09
title: Codex And Copilot Surface Wiring
type: implementation
wave: 5
depends_on: [066-04, 066-05, 066-13]
task_ids: [WS-09]
files_modified:
  - .github/agents/pentest-rust-reviewer.agent.md
  - .github/agents/pentest-crypto-reviewer.agent.md
  - .github/agents/pentest-storage-reviewer.agent.md
  - .github/agents/pentest-rpc-dast-reviewer.agent.md
  - .github/agents/pentest-supply-chain-reviewer.agent.md
  - .github/prompts/pentest-local.prompt.md
  - .github/prompts/pentest-parallel-review.prompt.md
  - .github/prompts/pentest-report-doublecheck.prompt.md
autonomous: true
requirements:
  - REQ-003
  - REQ-004
---

# 066-09 Codex And Copilot Surface Wiring

<objective>
Expose the Phase 066 skills, agents, prompts, and scripts consistently through
`.github` canonical paths and existing `.codex` symlink compatibility
surfaces.
</objective>

<plan_fields>
plan_id: `066-09`
task_ids: `WS-09`
source_refs: `.planning/phases/066-Strix/066-TODO.md`, `.codex/*` symlink targets, `.github/agents/`, `.github/prompts/`
inputs: agent responsibilities, prompt contracts, symlink state
outputs: five pentest agents, three generic prompts, symlink verification docs if needed
dependencies: `066-04`, `066-05`, `066-13`
acceptance_tests: symlink tests, agent count, prompt grep checks
simulation_gate: prompt dry run merges fixture findings from multiple lanes and deduplicates them
negative_tests: agents must not claim direct tool execution by default; `.codex` must not become duplicate real directories
plan_artifacts: this plan and coverage row
plan_tests: symlink and prompt/agent greps
plan_results: stable Codex/Copilot invocation surface
task_artifacts: exact agent and prompt files listed above
task_tests: symlink checks, agent count, prompt behavior grep
task_results: canonical files under `.github`, symlink exposure under `.codex`
anti_placeholder_gate: agent files must define bounded scope and output contract; prompt files must define invocation and merge behavior
current_code_refs: `.codex/skills`, `.codex/agents`, `.codex/prompts`, `.github/agents/`, `.github/prompts/`
blockers: if a `.codex` symlink is broken, fix the symlink or stop; do not create a duplicate directory
evidence_gate: shell tests prove symlink targets and prompt/agent counts
not_recommendation_gate: prompt examples without files do not count
</plan_fields>

<copied_task_rows>
Goal:
Expose the new skills, agents, prompts, and scripts consistently through
`.github` and `.codex`.

Required files:

- `.github/agents/pentest-rust-reviewer.agent.md`
- `.github/agents/pentest-crypto-reviewer.agent.md`
- `.github/agents/pentest-storage-reviewer.agent.md`
- `.github/agents/pentest-rpc-dast-reviewer.agent.md`
- `.github/agents/pentest-supply-chain-reviewer.agent.md`
- `.github/prompts/pentest-local.prompt.md`
- `.github/prompts/pentest-parallel-review.prompt.md`
- `.github/prompts/pentest-report-doublecheck.prompt.md`
- `.codex` symlink verification documentation, if not already sufficient.

Implementation requirements:

- Keep canonical files under `.github/*`.
- Preserve `.codex/skills -> ../.github/skills`, `.codex/agents -> ../.github/agents`, and `.codex/prompts -> ../.github/prompts`.
- Preserve existing `.codex/hooks`, `.codex/instructions`, `.codex/requirements`, `.codex/scripts`, and `.codex/plugins` symlinks when adding new surfaces.
- Do not create duplicate real directories under `.codex` for surfaces already symlinked.
- Add prompt examples for generic repo mode and Z00Z profile mode.
- Agents must have bounded responsibilities and must not run tools directly unless instructed by the orchestrator prompt.

Acceptance checks:

- `test -L .codex/skills && test "$(readlink .codex/skills)" = "../.github/skills"`.
- `test -L .codex/agents && test "$(readlink .codex/agents)" = "../.github/agents"`.
- `test -L .codex/prompts && test "$(readlink .codex/prompts)" = "../.github/prompts"`.
- `test -L .codex/hooks && test -L .codex/instructions && test -L .codex/requirements && test -L .codex/scripts && test -L .codex/plugins`.
- `find .github/agents -maxdepth 1 -name 'pentest-*.agent.md' | wc -l` reports at least `5`.
</copied_task_rows>

<task_artifacts>
- `.github/agents/pentest-rust-reviewer.agent.md`.
- `.github/agents/pentest-crypto-reviewer.agent.md`.
- `.github/agents/pentest-storage-reviewer.agent.md`.
- `.github/agents/pentest-rpc-dast-reviewer.agent.md`.
- `.github/agents/pentest-supply-chain-reviewer.agent.md`.
- `.github/prompts/pentest-local.prompt.md`.
- `.github/prompts/pentest-parallel-review.prompt.md`.
- `.github/prompts/pentest-report-doublecheck.prompt.md`.
- Existing `.codex/*` symlink verification notes, or one additional local doc
  only if current repo docs are not already sufficient.
</task_artifacts>

<task_tests>
- `test -L .codex/skills && test "$(readlink .codex/skills)" = "../.github/skills"`.
- `test -L .codex/agents && test "$(readlink .codex/agents)" = "../.github/agents"`.
- `test -L .codex/prompts && test "$(readlink .codex/prompts)" = "../.github/prompts"`.
- `test -L .codex/hooks && test -L .codex/instructions && test -L .codex/requirements && test -L .codex/scripts && test -L .codex/plugins`.
- `find .github/agents -maxdepth 1 -name 'pentest-*.agent.md' | wc -l`.
</task_tests>

<task_results>
- `implementation_depth`: `full`.
- Canonical `.github` files exist.
- `.codex` remains symlink-only for these surfaces.
- Agents and prompts have bounded roles and merge contracts.
</task_results>

<shared_artifact_boundaries>
- This plan owns pentest agent files, `.codex` symlink integrity, and the fact
  that the generic prompt files are exposed canonically under `.github`.
- `066-13-PLAN.md` owns the semantic content of the shared prompt files; this
  plan may verify and surface them, but must not invent a second prompt
  contract that diverges from `WS-13`.
</shared_artifact_boundaries>

<tasks>
<task type="auto" id="066-09-T1">
  <read_first>
  - `.planning/phases/066-Strix/066-TODO.md`
  - `.codex/`
  - `.github/agents/`
  - `.github/prompts/`
  </read_first>
  <action>
  Add the pentest agent and prompt surfaces under `.github`, preserve `.codex` symlinks, and document prompt examples for generic and Z00Z profile modes.
  </action>
  <acceptance_criteria>
  - all symlink checks pass.
  - at least five pentest agents exist.
  - prompts instruct waiting for parallel lanes and deduping findings.
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, and rerun.
  2. Run all symlink and agent-count checks.
  3. Run prompt grep checks from `WS-13` after prompt files exist.
  4. Run `cargo test --release` when Rust/test-affecting changes are relevant.
  5. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/066-Strix/066-09-PLAN.md current_task="WS-09"` in YOLO mode at least 3 times via the active agent runtime; if the prompt runner is unavailable, perform an equivalent workspace-first manual review and record the exact blocker plus 2 consecutive clean reruns in the summary.
  6. Use `doublecheck`, `spec-to-code-compliance`, and Z00Z gates as needed.
  7. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
Surface wiring is correct only when canonical `.github` files and `.codex`
symlink exposure both pass.
</verification>

<must_haves>
truths:
- statement: `.codex` remains compatibility symlinks.
  verification: symlink tests.
prohibitions:
- statement: no duplicate real `.codex` skill/agent/prompt directories.
  verification: symlink checks and directory audit.
</must_haves>

## Artifacts This Phase Produces

- `.github/agents/pentest-rust-reviewer.agent.md`
- `.github/agents/pentest-crypto-reviewer.agent.md`
- `.github/agents/pentest-storage-reviewer.agent.md`
- `.github/agents/pentest-rpc-dast-reviewer.agent.md`
- `.github/agents/pentest-supply-chain-reviewer.agent.md`
- `.github/prompts/pentest-local.prompt.md`
- `.github/prompts/pentest-parallel-review.prompt.md`
- `.github/prompts/pentest-report-doublecheck.prompt.md`

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | WS-09 | 066-09 | `066-TODO.md`, `.codex` symlinks | agents/prompts | surfaces | symlink/count checks | canonical exposure | fixture merge prompt | bounded roles | planned |
