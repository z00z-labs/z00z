---
phase: 066
plan: 066-02
plan_id: 066-02
title: Tool Root And Installation Model
type: implementation
wave: 1
depends_on: [066-01]
task_ids: [WS-02]
files_modified:
  - tools/penetration/README.md
  - tools/penetration/manifests/tool-versions.lock
  - tools/penetration/manifests/upstream-sources.lock
  - tools/penetration/manifests/checksums.sha256
  - scripts/penetration/install_pentest_tools.sh
  - scripts/penetration/check_pentest_tools.sh
  - .github/skills/pentest-tool-installer/SKILL.md
autonomous: true
requirements:
  - REQ-002
  - REQ-010
  - REQ-012
---

# 066-02 Tool Root And Installation Model

<objective>
Create the repository-local penetration tool root and installer/checker model
without contaminating global tool homes or the existing formal-verification
toolchain.
</objective>

<plan_fields>
plan_id: `066-02`
task_ids: `WS-02`
source_refs: `.planning/phases/066-Strix/066-TODO.md`, `tools/formal_verification/`, `scripts/install-verification-tools.sh`
inputs: desired tool directories, local environment variable contract, allowed default tool inventory
outputs: local tool root directories, tool manifests, installer, checker, installer skill
dependencies: `066-01`
acceptance_tests: `bash scripts/penetration/check_pentest_tools.sh --json`, `find tools/penetration -maxdepth 3`, path audit for local wrappers
simulation_gate: run checker with no tools installed and prove it records missing tools as status, not success or failure concealment
negative_tests: ensure `tools/formal_verification/**` and global cache paths are not used as pentest tool homes
plan_artifacts: this plan and coverage row
plan_tests: manifest and path grep checks
plan_results: reproducible local tool status model
task_artifacts: exact installer/checker/manifests listed above
task_tests: shell syntax, JSON output, local-path assertions
task_results: tool status JSON and local wrapper discovery
anti_placeholder_gate: checker must inspect actual executable paths and versions where available; no static fake all-green JSON
current_code_refs: `scripts/install-verification-tools.sh`, `tools/formal_verification/`, `pack_z00z_project.sh`
blockers: missing optional tools cannot block baseline check mode; they must be reported in JSON
evidence_gate: `tool-status.json` or command output must show each checked tool status and source path
not_recommendation_gate: tool policy text alone is insufficient; scripts and manifests must exist
</plan_fields>

<copied_task_rows>
Goal:
Install and manage all new third-party security tools under
`tools/penetration/` without contaminating the host or the existing
formal-verification tool root.

Required files:

- `tools/penetration/README.md`
- `tools/penetration/manifests/tool-versions.lock`
- `tools/penetration/manifests/upstream-sources.lock`
- `tools/penetration/manifests/checksums.sha256`
- `scripts/penetration/install_pentest_tools.sh`
- `scripts/penetration/check_pentest_tools.sh`
- `.github/skills/pentest-tool-installer/SKILL.md`

Implementation requirements:

- Use `TOOLS_DIR="${Z00Z_PENTEST_TOOLS_DIR:-$ROOT/tools/penetration}"`.
- Create local tool homes: `bin/`, `cargo/`, `go/`, `python/`, `cache/`, `rules/`, `templates/`, `wordlists/`, `upstream/`, and `manifests/`.
- Set `CARGO_HOME`, `GOBIN`, `GOMODCACHE`, `UV_TOOL_DIR`, `UV_TOOL_BIN_DIR`, `PIPX_HOME`, `PIPX_BIN_DIR`, `TRIVY_CACHE_DIR`, and `NUCLEI_TEMPLATES_DIR` under `tools/penetration/`.
- Prefer repository-local wrappers in `tools/penetration/bin/`.
- Record every installed tool version in `tool-versions.lock`.
- Do not use destructive delete commands. Use safe replacement through staging directories and backup suffixes for text manifest rewrites when full overwrite is required.
- System package installation may install minimal prerequisites only when needed, but tool payloads and caches must stay under `tools/penetration/`.

Acceptance checks:

- `bash scripts/penetration/check_pentest_tools.sh --json` writes a machine-readable status artifact.
- `find tools/penetration -maxdepth 3 \( -type f -o -type l \)` shows local tool/wrapper state after install.
- `which semgrep` is not required to point to a global path when `tools/penetration/bin/semgrep` exists.
</copied_task_rows>

<task_artifacts>
- `tools/penetration/README.md`.
- `tools/penetration/manifests/tool-versions.lock`.
- `tools/penetration/manifests/upstream-sources.lock`.
- `tools/penetration/manifests/checksums.sha256`.
- `scripts/penetration/install_pentest_tools.sh`.
- `scripts/penetration/check_pentest_tools.sh`.
- `.github/skills/pentest-tool-installer/SKILL.md`.
- Repository-local tool homes under `tools/penetration/`: `bin/`, `cargo/`,
  `go/`, `python/`, `cache/`, `rules/`, `templates/`, `wordlists/`,
  `upstream/`, and `manifests/`.
</task_artifacts>

<task_tests>
- `bash -n scripts/penetration/install_pentest_tools.sh scripts/penetration/check_pentest_tools.sh`.
- `bash scripts/penetration/check_pentest_tools.sh --json`.
- `find tools/penetration -maxdepth 3 \( -type f -o -type l \)`.
- grep audit that no pentest install script writes tool payloads under `tools/formal_verification/`.
</task_tests>

<task_results>
- `implementation_depth`: `full`.
- `check_pentest_tools.sh --json` reports present, missing, skipped, and path fields.
- Manifests exist and are updated through safe staging or targeted patching.
- Missing tools are explicit, not hidden as passed checks.
</task_results>

<shared_artifact_boundaries>
- The `tools/penetration/` root contract, installer, checker, and
  `tool-versions.lock` semantics are owned here; later plans may extend them
  for provenance or Docker portability but must not move payloads outside
  `tools/penetration/`.
- `tools/penetration/manifests/upstream-sources.lock` is created here as a
  canonical manifest path, while provenance contents are owned by
  `066-03-PLAN.md`.
- `tools/penetration/README.md` establishes root layout and local-tool-home
  rules here; `066-14-PLAN.md` may extend operator and migration guidance
  without redefining the root contract.
</shared_artifact_boundaries>

<tasks>
<task type="auto" id="066-02-T1">
  <read_first>
  - `.planning/phases/066-Strix/066-TODO.md`
  - `scripts/install-verification-tools.sh`
  - `pack_z00z_project.sh`
  - `.github/copilot-instructions.md`
  </read_first>
  <action>
  Implement `tools/penetration/` as the only default pentest tool root, add installer and checker scripts, configure local tool homes, and create the `pentest-tool-installer` skill that calls scripts by path.
  </action>
  <acceptance_criteria>
  - `tools/penetration/manifests/tool-versions.lock` exists.
  - `check_pentest_tools.sh --json` exits successfully and emits structured status even when optional tools are missing.
  - no active install command targets `tools/formal_verification`.
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, and rerun.
  2. Run `bash -n scripts/penetration/install_pentest_tools.sh scripts/penetration/check_pentest_tools.sh`.
  3. Run `bash scripts/penetration/check_pentest_tools.sh --json`.
  4. Run local path audits for `tools/penetration` and against `tools/formal_verification`.
  5. Run `cargo test --release` when Rust/test-affecting changes are relevant.
  6. Run `/GSD-Review-Tasks-Execution current_spec=.planning/phases/066-Strix/066-02-PLAN.md current_task="WS-02"` in YOLO mode at least 3 times via the active agent runtime; if the prompt runner is unavailable, perform an equivalent workspace-first manual review and record the exact blocker plus 2 consecutive clean reruns in the summary.
  7. Use `doublecheck`, `spec-to-code-compliance`, `smart-tests-bootstrap`, and Z00Z gates as needed.
  8. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
Tool status must be generated from real filesystem and executable checks.
</verification>

<must_haves>
truths:
- statement: all new tool payload and cache paths default under `tools/penetration/`.
  verification: script env vars and checker JSON paths.
prohibitions:
- statement: pentest scripts must not install into `tools/formal_verification/`.
  verification: grep audit over executable script lines.
</must_haves>

## Artifacts This Phase Produces

- `tools/penetration/README.md`
- `tools/penetration/bin/`
- `tools/penetration/{cargo,go,python,cache,rules,templates,wordlists,upstream,manifests}/`
- `scripts/penetration/install_pentest_tools.sh`
- `scripts/penetration/check_pentest_tools.sh`
- `.github/skills/pentest-tool-installer/SKILL.md`

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | WS-02 | 066-02 | `066-TODO.md`, `tools/formal_verification/` | tool-root contract | tool root and scripts | checker JSON and path audits | local tool status | missing-tool fixture | real path/version checks | planned |
