---
phase: 069
plan: 069-07
plan_id: 069-07
title: Real Plonky3 Base STARK
type: implementation
status: complete
completed: 2026-07-28
next_plan: 069-08
wave: 6
depends_on: [069-04, 069-051]
task_ids: []
copied_task_rows: []
files_modified:
  - .github/skills/smart-tests-bootstrap/scripts/plonky3_resource_worker.sh
  - .planning/phases/069-Recursive-Proof/069-TODO.md
  - .planning/phases/069-Recursive-Proof/069-07-STOP-SPLIT.md
  - Cargo.toml
  - crates/z00z_storage/Cargo.toml
  - crates/z00z_storage/src/checkpoint/checkpoint_contract.yaml
  - crates/z00z_storage/src/checkpoint/contract_config_v3.rs
  - crates/z00z_storage/src/checkpoint/mod.rs
  - crates/z00z_storage/src/checkpoint/plonky3_binary_fri_fold.rs
  - crates/z00z_storage/src/checkpoint/plonky3_binary_hash.rs
  - crates/z00z_storage/src/checkpoint/plonky3_binary_mmcs.rs
  - crates/z00z_storage/src/checkpoint/plonky3_binary_pcs.rs
  - crates/z00z_storage/src/checkpoint/plonky3.rs
  - crates/z00z_storage/src/checkpoint/plonky3_root_statement.rs
  - crates/z00z_storage/src/checkpoint/plonky3_root_statement_air.rs
  - crates/z00z_storage/src/checkpoint/recursive_v2.rs
  - crates/z00z_storage/src/checkpoint/adapter.rs
  - crates/z00z_storage/src/checkpoint/receipt.rs
  - crates/z00z_storage/src/checkpoint/recursive_reject.rs
  - crates/z00z_storage/src/checkpoint/version_registry.rs
  - crates/z00z_storage/src/checkpoint/authority_artifacts.rs
  - crates/z00z_storage/tests/test_checkpoint_contract_config.rs
  - crates/z00z_storage/tests/test_recursive_v2_plonky3_base.rs
  - crates/z00z_utils/src/os_hardening.rs
autonomous: true
requirements: [RCP-069]
must_haves:
  truths: ["A real Plonky3 base proof enforces the full frozen predicate.", "One complete Plan-07 base-theorem envelope targets at most 2 MiB, may publish verified degraded evidence through 4 MiB, and rejects above the 16 MiB ingress ceiling; Plan 08 applies the same contract to exactly 2000 finalized-block transitions."]
  artifacts:
    - path: crates/z00z_storage/src/checkpoint/plonky3.rs
      provides: "Single private facade and owner for the full frozen checkpoint predicate, trace, parameters, and Plonky3 backend."
    - path: crates/z00z_storage/src/checkpoint/plonky3_binary_pcs.rs
      provides: "Private generation-bound binary-W32 PCS adapter within the canonical Plonky3 subsystem."
    - path: crates/z00z_storage/src/checkpoint/plonky3_root_statement.rs
      provides: "Canonical recursive public-statement model and field layout."
    - path: crates/z00z_storage/src/checkpoint/plonky3_root_statement_air.rs
      provides: "Root-statement NPO, AIR, preprocessing, and table-prover binding."
    - path: crates/z00z_storage/src/checkpoint/plonky3_binary_mmcs.rs
      provides: "Private binary-W32 authentication constraints separated from PCS delegation."
    - path: crates/z00z_storage/src/checkpoint/plonky3_binary_hash.rs
      provides: "Private binary-W32 hash and cap-selection circuit primitives."
    - path: crates/z00z_storage/tests/test_recursive_v2_plonky3_base.rs
      provides: "Actual Plonky3 verifier differential and mutation evidence."
  key_links:
    - from: .planning/phases/069-Recursive-Proof/069-PREDICATE.md
      to: crates/z00z_storage/src/checkpoint/plonky3.rs
      via: "Frozen shared constraint map and executable vector corpus."
    - from: crates/z00z_storage/src/checkpoint/plonky3.rs
      to: crates/z00z_storage/src/checkpoint/recursive_v2.rs
      via: "Private backend is reachable only through the canonical public V2 facade."
    - from: crates/z00z_storage/src/checkpoint/adapter.rs
      to: crates/z00z_storage/src/checkpoint/receipt.rs
      via: "Actual verifier success creates the storage-owned typed receipt."
---

# 069-07 Real Plonky3 Base STARK

<objective>
Implement and verify a real pinned Plonky3 base STARK/AIR for the exact frozen
`CheckpointTransitionConsistencyV2` predicate before recursive aggregation,
with native/Nova/Plonky3 differential parity and a quantum-aware, FRI/hash-only
base profile suitable as the sole leaf type of the end-to-end PQ recursion
chain, without upgrading classical transaction primitives.
</objective>

## plan_id

`069-07`

## task_ids

[]

## copied_task_rows

[]

## source_refs

- `.planning/phases/069-Recursive-Proof/069-TODO.md` proof semantics/threat model/parameter manifest, `Version Registry And Migration Contract`, `Plonky3 Epoch Proof Contract`, backend/PQ red lines, integration tests, `RCP-AC-035`, `RCP-AC-037`, `RCP-AC-038`, `RCP-AC-042`
- `.planning/phases/069-Recursive-Proof/069-CONTEXT.md` backend and security decisions
- `.planning/phases/069-Recursive-Proof/069-PATTERNS.md` Pattern Families A/C and `069-07`
- Generated predecessor dependencies:
  `.planning/phases/069-Recursive-Proof/069-FEASIBILITY.md`,
  `.planning/phases/069-Recursive-Proof/069-PREDICATE.md`, and Plan 04/069-051 outputs.

## inputs

- Exact compatible `p3-*` pins, KoalaBear/Poseidon2/FRI manifest, AIR map, bounded trace/witness, canonical vectors.

## outputs

- Private checkpoint AIR/trace/parameter implementation and real base prover/verifier adapter with typed receipt.
- `RecursiveSecurityBudgetManifestV2` with exact conservative dyadic error
  bounds, finite generation budget, inherited loss, and residual-security gate.
- Bounded internal base-proof leaves for Plan 08. Base proofs are proving
  intermediates, not consensus messages, wallet inputs, or permanent archive
  objects.

## canonical_production_epoch_contract

- Phase 069 has one production Plonky3 publication path: for each exact
  consecutive range of `2000` finalized checkpoint blocks, emit exactly one
  canonical recursive epoch proof envelope. Plan 07 supplies the full base
  theorem and bounded recursive building block; Plan 08 owns the exact
  `2000`-block epoch aggregation and inherits this contract without
  reinterpretation.
- Plan 07 applies the same `2 MiB` target to one complete canonical
  single-transition base-theorem envelope as a prerequisite. Plan 08 MUST
  recursively aggregate exactly `2000` such transitions into a final
  per-epoch payload that also targets at most `2 MiB`; the allowance never
  multiplies by the leaf count.
- The production size target is `2,097,152` bytes (`2 MiB`) for the complete
  canonical encoded per-epoch Plonky3 payload. The measurement includes every
  transcript-separated root/replica, recursive proof body, public input,
  manifest field, and required framing. It is not a separate allowance for
  each root, leaf, chunk, or history proof body, and transport compression does
  not count toward compliance.
- The finite publication cap is `4,194,304` bytes (`4 MiB`) for that same
  complete payload. A verified envelope in `(2 MiB, 4 MiB]` is publishable
  degraded evidence with
  `Plonky3ProofSizeStatusV2::TargetMissed`; it preserves recursive/history
  liveness but cannot satisfy the Plan-07 or Plan-08 size acceptance gate.
- Raw base proofs, chunk proofs, and intermediate recursion nodes are bounded
  ephemeral prover artifacts. They MUST NOT be published as the epoch result,
  retained as permanent history, or used to split one epoch into multiple
  final envelopes to evade the `4 MiB` publication cap. An internal node may exceed
  `2 MiB` within its separate resource-safety bound and continue through
  additional sound recursion; the production limit is evaluated only on the
  one complete canonical output.
- `16 MiB` is only the untrusted-input parser/allocation and DoS emergency
  ceiling before the input type is trusted. Once identified as a canonical
  Plonky3 V2 proof, any payload larger than `4 MiB` is rejected before
  proof-specific allocation, deserialization, or verification as
  `ProofSizeBudgetExceeded`; a payload above `16 MiB` rejects at the generic
  ingress guard.
- The Phase-069 production acceptance fixture is exactly `2000` finalized
  blocks (`10,000` seconds, or `2 h 46 min 40 s`, at the configured five-second
  finalization interval). A smaller smoke-test range, a different cadence, weaker security
  parameters, or multiple published proof objects cannot be used to claim
  production acceptance. If the real proof cannot satisfy the theorem,
  security, memory, and total encoded-size limits together, the owning plan
  remains `BLOCKED` and records stop/split evidence.
- `ProofSizeBudgetExceeded` above `4 MiB` is fail-closed for the Plonky3 publication branch,
  not a process panic or canonical-chain failure. Because this branch is
  asynchronous and non-authoritative, canonical checkpoint finality and Nova
  folding continue while the epoch remains in the bounded pending-PQ queue.
  Once the configured pending limit is reached, new compression/publication
  work stops according to
  `stop_compression_publication_keep_fold_finality`; the implementation MUST
  not drop epochs, bypass the size cap, or reinterpret an oversized object as
  verified evidence.

## current_implementation_boundary

- The active Plan-07 prover materializes every bounded AIR chunk for one
  `CanonicalCheckpointTransitionV2` and only then constructs its recursive
  tree. Its current "leaves" are domain-specific AIR chunks across the three
  physical FRI replicas; they are neither HJMT leaves nor already persisted
  per-block leaves of a `2000`-block production epoch.
- Base-chunk admission is currently sequential. One bounded twelve-thread
  Rayon pool serves at most five ordered chunk coordinates / fifteen physical
  replica proofs before it is dropped; an exact one-slot prepared-shape cache
  is reused only inside that batch. Recursive sibling pairs are evaluated by
  the bounded local `2x12` aggregation schedule and merged in deterministic
  order. This is thread-level parallelism inside one trusted prover process,
  not cross-aggregator proof sharding.
- The configured `2000`-block asynchronous cadence is declarative while
  `is_live_cadence_enforced=false`. Current code does not continuously prove
  each finalized block into a durable epoch frontier, and epoch close does not
  yet consist only of the remaining upper-tree folds.
- The production successor MUST persist an accepted per-block epoch leaf and
  immediately merge equal-height frontier nodes. At the exact epoch boundary,
  only the deterministic bounded frontier and ordered replica folds may remain.
  Plan 08 owns this live frontier, recovery, cadence, and exact `2000`-block
  acceptance evidence.
- Cross-machine proving MUST use authenticated, generation-bound, idempotent
  leaf/node jobs with exact ordered ranges, dependency-proof digests, lease
  identity, independent worker resource limits, canonical result encoding,
  and actual-pinned-verifier admission. Base jobs containing private witness
  material may run only on trusted witness-owning prover workers. Proof-only
  recursive-parent jobs may be distributed more broadly. Plan 10 owns this
  typed local/remote prover-job protocol and its restart/lease/resource
  lifecycle; Plan 08 owns the durable per-block epoch frontier consumed by
  those jobs. The existing runtime transaction/HJMT shard dispatcher is not
  this protocol and MUST NOT be treated as implementation evidence.
- A supported `32 GiB` dedicated prover host runs only one heavyweight proof
  process under the Plan-07 limits. Multiple full local prover processes MUST
  NOT share the `24 GiB` emergency ceiling; scale-out uses independently
  isolated workers and a separately measured coordinator/network/cache budget.

## dependencies

- `069-04` and `069-051`. The accepted V2 Nova implementation supplies the live
  differential corpus required here; `069-06` and `069-07` may execute in
  parallel only after `069-051` passes.

## acceptance_tests

- Real base proof verifies the full transition predicate.
- The complete canonical single-transition base-theorem envelope, including all
  roots/replicas and framing, measures at most `2,097,152` encoded bytes.
- Native evaluator, Nova R1CS, and Plonky3 AIR agree on the shared corpus.
- Trace/witness/public input/context/Poseidon2/FRI/parameter/proof mutations reject in the actual verifier.
- The root verifier-common authority is not fixture-specific: either every
  supported canonical transition shape derives the same pinned outer common
  digest, demonstrated by minimum/maximum/boundary shape tests, or the verifier
  independently derives the exact expected digest from a bounded,
  generation-bound canonical shape manifest. A proof-supplied digest or an
  unbounded allowlist cannot satisfy this gate.
- The Fiat-Shamir transcript and receipt bind network/genesis/config/predicate,
  statement and ordered row/range identity, AIR/trace shape, field/hash/FRI
  parameters, algorithm/version, proof bytes, and public inputs before every
  derived challenge.
- The security manifest derives rather than copies the nominal bit target and
  rejects any configured accepted-epoch horizon whose conservative composed
  failure bound exceeds `2^-100`.
- The base-proof/receipt profile is registry-complete and transcript-bound;
  wrong type/wire/domain/public-input encoding/parameter generation rejects in
  the actual verifier before a receipt is created.

## simulation_gate

- No fake crypto; deterministic trace inputs are allowed only for tests and vectors.

## negative_tests

- Public-root-only trace, missing row/path/nullifier/delta/journal constraint,
  wrong field limbs, context replay, transcript field omission/reordering,
  parameter downgrade, proof mutation, base-proof public broadcast/permanent
  retention, and classical-nested-to-end-to-end-PQ claim.
- API-suffix dispatch, cross-type or downgraded proof bytes, fallback decode,
  mixed transcript/public-input/parameter generations, and a receipt whose
  declared profile differs from the verifier-selected profile.

## plan_artifacts

- `CheckpointTransitionAirV2`
- `CheckpointTraceV2`
- `Plonky3BaseAdapterV2`
- Base proof/receipt and differential mutation corpus.

## plan_tests

- Bounded smoke/codec/parser/manifest/fail-closed assertions run separately
  from real prover acceptance and remain below the 4 GiB process budget.
- Each real prover assertion is launched alone, with `--test-threads=1`, by
  `.github/skills/smart-tests-bootstrap/scripts/plonky3_resource_worker.sh`.
- The isolated real-proof targets include the exact named
  `test_predicate_differential` assertion and the actual-verifier mutation
  assertion; zero-match filters cannot report success.

## plan_results

- Actual base STARK verification proves the same scoped predicate as native replay/Nova.
- The measured complete Plan-07 envelope is at most `2 MiB`; Plan 08 inherits
  the non-multiplying `2 MiB` total for its exact `2000`-block epoch output.

## task_artifacts

- No literal `TASK-NNN`; group artifacts are the AIR, trace, adapter, proof/receipt, and differential evidence.

## task_tests

- No literal `TASK-NNN`; group tests call real pinned Plonky3 prover/verifier.

## task_results

- No literal `TASK-NNN`; expected result is parity plus mutation rejection.

## anti_placeholder_gate

- Compile-only, root-equality-only, fake trace, digest/boolean-only, or Nova-verifier wrapper proof fails.

## current_code_refs

- `crates/z00z_storage/src/checkpoint/artifact_stmt.rs`
- `crates/z00z_storage/src/checkpoint/exec_input.rs`
- `crates/z00z_storage/src/checkpoint/recursive_v2.rs`
- `crates/z00z_storage/src/checkpoint/recursive_statement.rs`
- `crates/z00z_storage/src/checkpoint/recursive_predicate.rs`
- `crates/z00z_storage/src/checkpoint/adapter.rs`
- `crates/z00z_storage/src/checkpoint/receipt.rs`
- `crates/z00z_storage/src/settlement/hjmt_journal.rs`
- `crates/z00z_storage/src/settlement/hjmt_proof.rs`
- `crates/z00z_storage/src/settlement/proof.rs`
- `crates/z00z_storage/src/settlement/store.rs`
- `crates/z00z_storage/tests/test_checkpoint_statement_vectors.rs`

## blockers

- Any predicate constraint without a sound AIR/hash-gadget/translation proof triggers stop/split evidence.
- OOM, SIGKILL, cgroup `MemoryMax`, or resource timeout is a terminal blocker
  for the current implementation. The unchanged command MUST NOT be retried,
  broad tests MUST NOT run, and typed resource-exhaustion evidence MUST be
  appended to the Plan-07 stop/split record.

## evidence_gate

- Differential matrix and receipt must identify actual verifier calls, exact parameters, and every mutated constraint family.

## version_boundary_contract

- Plan 07 MUST consume `CheckpointVersionRegistryV2` and register every
  persisted base-proof/receipt/profile artifact. A private trace/AIR subrecord
  MAY use local codec version 1 only when the selected grammar/profile digest
  binds it; that local choice is not public wire-V1 compatibility.
- Backend construction MUST bind the activated ConfigV3 digest/generation.
  ConfigV2 is offline migration input only and MUST NOT select a proof grammar,
  transcript, cap, parameter set, or decoder after activation.
- Before parameters or transcript state are constructed, ConfigV3 and the
  registry MUST select the same runtime profile identifier, generation, and
  authority-pinned canonical manifest digest. Reusing an identifier/generation
  with altered manifest bytes/digest MUST reject as profile substitution.
- The fixed artifact preheader selects the exact type, wire codec, cap, and
  activated authority generation before allocation. The verifier profile then
  selects the cryptographic-domain, transcript, public-input encoding, AIR,
  hash, FRI, and parameter generations. No integer equality or `V2` suffix may
  substitute for those labeled binds.
- The actual Fiat-Shamir transcript and verification receipt MUST bind the
  registry digest and exact runtime-profile-manifest digest. Unknown, cross-
  type, downgraded, cross-generation,
  or post-activation-old writes MUST reject; decode or verifier failure MUST
  NOT fall back to another codec or parameter set.
- Registry completeness, old/new Cartesian decoder/verifier vectors, wrong-
  profile mutations, and activation-boundary tests MUST be release gates.

## not_recommendation_gate

- A proposed AIR or successful trace generation without proof verification does not close.

## base_proof_lifecycle_contract

- The AIR MUST prove the storage-owned transition predicate independently. A
  Nova receipt, Nova proof digest, or equality of public roots MAY be auxiliary
  differential evidence but MUST NOT satisfy any Plonky3 constraint.
- Canonical serialization MUST be finite and versioned; parsers MUST enforce all
  nested counts and byte caps before allocation or hashing and reject trailing,
  duplicate, non-canonical, wrong-endian, or field-alias encodings.
- The transcript MUST absorb the complete domain/version/role, chain context,
  predicate and AIR digests, trace shape, all public inputs, MMCS/PCS/FRI/hash
  parameter digests, and commitments before challenges. No commit-then-reveal
  field may remain outside the transcript.
- FRI security MUST be derived from the concrete field/extension, blowup,
  queries, grinding, hash assumptions, recursion depth, and composition loss.
  The configured security-bit literal alone is not evidence.
- Soundness bounds MUST be encoded canonically as upward-rounded dyadic
  rationals and composed with checked integer arithmetic; floating point,
  wraparound, downward rounding, or a parameter-rotation reset is forbidden.
  The manifest MUST bind parameter generation, derivation inputs, per-proof
  bound, finite accepted-epoch budget, inherited bound, composition rule, and
  minimum residual target for Plan 08.
- Base proof bodies SHOULD be deleted after Plan 08 has atomically committed and
  re-verified the enclosing epoch proof and its recovery window has elapsed.
  Their digests remain in the epoch manifest/challenge pack for the bounded
  Plan 09 window. Ordinary validators and wallets MUST NOT download them.
- Any selected `p3-*` revision, parameter set, or transcript change creates a new
  generation and requires complete positive, differential, mutation, and
  cross-generation rejection evidence.

## 🚨 Resource Safety and Memory Acceptance Contract

This contract is a mandatory Plan-07 acceptance boundary, not an optional
profiling recommendation.

### Incident authority

- The real `test_recursive_v2_plonky3_base` process reached approximately
  `51.7 GiB` anonymous RSS and was killed by the Linux OOM killer.
- The process ran inside the VS Code systemd scope whose `OOMPolicy=stop` and
  `KillMode=control-group` terminated the whole editor.
- The current host has no swap.
- `24 GiB` is an emergency safety ceiling. It is neither an allocation target
  nor permission to require a 64 GiB host.

### Mandatory execution boundary

1. An unchanged command that reaches OOM, `SIGKILL`, cgroup `MemoryMax`, or the
   resource timeout MUST NOT be rerun automatically. The run ends the current
   implementation attempt and emits typed `resource_exhaustion` evidence.
2. The executor MUST NOT create or enable swapfile, zram, zswap, change sysctl
   settings, change the VS Code scope, or modify global systemd OOM policy.
   Swap is diagnostic-only after explicit user approval, must be encrypted and
   finitely capped, and a swap-assisted run can never satisfy Plan 07.
3. Every real Plonky3 prover test MUST run in a separate transient worker unit
   outside the VS Code application scope. On systemd/cgroup-v2 hosts the worker
   uses `OOMPolicy=continue`, `MemoryHigh=16G`, `MemoryMax=24G`, and
   `MemorySwapMax=0`. If these controls are unavailable, an equivalent
   container/CI cgroup is required; the executor MUST fail closed rather than
   silently run without isolation. The heavy unit MUST be detached from the
   invoking editor/exec client: launcher termination cannot stop the unit or
   erase its terminal evidence. A durable `run-state.json` is written before
   launch, the worker writes its own `resource-evidence.json`, and `--status`
   or `--status-latest` can recover the result after launcher loss.
4. Only one named heavy proof test may run at a time, always with
   `--test-threads=1`. Always-on bounded smoke, codec, parser, manifest,
   mutation, and fail-closed tests remain separate from real heavyweight prover
   acceptance. Both real targets are `#[ignore]` in ordinary Cargo suites and
   the isolated worker selects one exact ignored test explicitly.
5. Every worker records wall time, peak RSS, cgroup `memory.events`, exit
   reason, fixture identity, backend/parameter digests, trace dimensions, and
   phase-level memory high-water marks under
   `crates/z00z_storage/outputs/checkpoint/069-07/`. Witness bytes MUST NOT be
   logged.
6. Resource budgets are:
   - regular smoke-test process: at most `4 GiB` peak RSS;
   - one real base-proof target: at most `16 GiB` peak RSS;
   - absolute emergency ceiling: `24 GiB`;
   - supported dedicated prover host: `32 GiB` RAM with OS/service headroom.
   The real prover also has a non-blocking optimization objective of at most
   `4 GiB` peak RSS. This is a desired production-efficiency target, not a
   reason to reject an otherwise accepted proof below `16 GiB`, and never
   authorizes weaker security parameters, an incomplete theorem, or hidden
   swap/out-of-cgroup memory.
   The complete canonical Plan-07 single-transition recursive base-theorem
   envelope has a production acceptance target of `2,097,152` bytes (`2 MiB`)
   under
   `canonical_production_epoch_contract`; verified `(2 MiB, 4 MiB]` envelopes
   are publishable degraded evidence but cannot close the plan. The separate
   `16 MiB` parser/DoS ceiling remains a fail-closed emergency bound, not an
   acceptable production proof size.
   Validators and wallets MUST NOT generate base proofs.
7. A real proof above the `16 GiB` target MUST be profiled and optimized in
   this order:
   - remove unnecessary clones and overlapping circuit/trace/prover lifetimes;
   - release intermediate buffers immediately after their final consumer;
   - replace per-`u16` 16-bit boolean expansion with an audited packed
     limb/range-check or lookup construction where sound;
   - split the predicate into bounded domain-specific KoalaBear AIR tables and
     micro-op trace chunks;
   - recursively aggregate bounded chunk proofs;
   - evaluate explicit streaming/out-of-core trace construction only when the
     backend supports it without weakening witness secrecy or soundness.
8. FRI, hash, grinding, query, field, transcript, and recursion security
   parameters MUST NOT be weakened to meet the memory budget. Any parameter
   change recomputes the complete quantum-aware security manifest and preserves
   at least `100` configured residual bits.
9. Prover liveness is part of the resource contract. A named chunk stage that
   emits no progress for `900` seconds MUST terminate as typed
   `resource_timeout`, record the last non-secret chunk coordinates and trace
   dimensions, and forbid an unchanged retry. The total real-target limit
   remains `7,200` seconds; high CPU utilization is not progress by itself.
10. The generation after command digest `a7d267acc332...` MUST replace the
    unbounded-latency `54` FRI queries plus `16` query-grinding bits with `62`
    FRI queries and zero query-grinding bits. With log blowup `2`, both
    allocations derive exactly `124` physical classical FRI bits:
    `2 * 54 + 16 = 2 * 62 + 0`. The conservative physical quantum-search
    bound remains `62` bits, the three-replica composed bounds and configured
    lifetime residual remain unchanged, and the complete manifest, parameter,
    registry, config, circuit, aggregation, and cache generations MUST rotate.
    This is a liveness redistribution, not a security downgrade. Its larger
    query/opening payload MUST still satisfy the complete `2 MiB` envelope
    acceptance target without transport compression.
10a. Command digest `f733545815d6...` proves that eliminating query grinding
    did not by itself bound leaf-prover latency. It terminated through the
    `900`-second watchdog at hash leaf `44/72` with `4,158,065` witnesses and
    `3,709,877` circuit operations, while memory remained below the target and
    swap/cgroup memory events remained zero. The next generation MUST split
    the manifest-bound hash capacity from `12` to `6` SHA blocks per leaf,
    rotate predicate-packing, cache, and aggregation identities, and retain the
    exact `62`-query/zero-grinding security configuration. Six is selected
    because it lowers the packed ALU table by one power-of-two domain; `8`
    would not, and `4` would add aggregation leaves without another domain
    reduction. Before another complete theorem run, bounded evidence MUST show
    a material reduction from the failed hash-leaf trace high-water marks.
    Exact terminal evidence is
    `crates/z00z_storage/outputs/checkpoint/069-07/task-1/resource-worker/20260727T013629434604725Z-predicate_differential-f733545815d6/resource-evidence.json`.
    The bounded successor is accepted for a complete-theorem attempt only
    after command digest `be5f855b8116...` proved and actual-verified one
    six-block hash leaf in `8.08` seconds with `2,079,575` witnesses,
    `1,855,058` operations, `1,853,809` ALU rows, `4,756,869,120` bytes peak
    cgroup memory, zero swap, and zero memory-event deltas. Its exact evidence
    is
    `crates/z00z_storage/outputs/checkpoint/069-07/task-1/resource-worker/20260727T023313844584211Z-test_real_hash_chunk_batch_stark_roundtrip-be5f855b8116/resource-evidence.json`.
10b. The first complete-theorem command using the bounded six-block topology,
    digest `18d614dca0a9...`, is terminal and MUST NOT be rerun unchanged. The
    first structural leaf completed in `15.2` seconds, but the next,
    smaller leaf remained in `stark_prove` until the `900`-second watchdog.
    Its peak cgroup memory was `9,380,147,200` bytes, swap and all memory-event
    deltas were zero, so this is a repeated fresh-pool lifecycle/liveness
    blocker rather than an OOM or trace-size regression. The material
    successor MUST use one bounded 16-thread Rayon pool for the complete
    sequential leaf-materialization pass, while dropping circuit, trace,
    prover-data, and proof buffers and trimming the allocator after every
    cache write. It MUST prove and actual-verify two consecutive bounded
    leaves in that same pool through an isolated named diagnostic before the
    next full theorem attempt. This scheduling change MUST NOT change proof
    bytes, transcript, parameters, security manifest, or cache format. Exact
    terminal evidence is
    `crates/z00z_storage/outputs/checkpoint/069-07/task-1/resource-worker/20260727T023622579554400Z-predicate_differential-18d614dca0a9/resource-evidence.json`.
    The required shared-pool gate subsequently passed as command digest
    `248e6653756e...`: two consecutive real hash leaves actual-verified at
    `7.94` and `15.83` seconds, peak cgroup memory was `4,752,605,184` bytes,
    and swap/memory-event deltas remained zero. Exact evidence is
    `crates/z00z_storage/outputs/checkpoint/069-07/task-1/resource-worker/20260727T025848322936788Z-test_real_hash_chunk_batch_stark_roundtrip-248e6653756e/resource-evidence.json`.
10c. The first complete-theorem command after the shared-pool gate, digest
    `e9b3934df9db...`, is terminal and MUST NOT be rerun unchanged. It verified
    or materialized `281` cache leaves, including every replica-0 domain and
    replica-1 structural leaf, before hash leaf
    `replica-1/index-11/count-143` remained in `stark_prove` for the complete
    `900`-second stage limit. The ten immediately preceding same-shape hash
    leaves each completed in approximately `5.5` seconds. Terminal wall time
    was `3,112,303 ms`; peak cgroup memory was `10,059,333,632` bytes,
    observed process peak RSS was `9,246,844 KiB`, swap was zero, and every
    cgroup high/max/OOM/OOM-kill delta was zero. This proves a repeated
    high-CPU backend scheduling tail rather than memory exhaustion or an
    oversized AIR leaf. The material successor MUST reduce the one shared
    bounded Rayon pool from `16` to `12` workers, retain strictly sequential
    leaf proofs, and expose non-witness `p3-batch-stark`, PCS/FRI, and DFT span
    boundaries in resource logs. Before another complete theorem attempt, one
    isolated diagnostic MUST prove and actual-verify at least `16` consecutive
    bounded hash leaves in that same pool. Scheduling telemetry MUST NOT alter
    proof bytes, transcript, cache format, theorem, or any security parameter.
    Existing cached leaves MAY be retained only because they are
    generation-bound and actual-verified before aggregation. Exact evidence is
    `crates/z00z_storage/outputs/checkpoint/069-07/task-1/resource-worker/20260727T030621640523802Z-predicate_differential-e9b3934df9db/resource-evidence.json`.
    The required twelve-worker sequential gate passed under command digest
    `b94a7d4346a2...`: all `16/16` real hash leaves completed
    `prove_all_tables` and the actual pinned verifier in one shared pool in
    `145.66` seconds of test time. Peak cgroup memory was `5,048,766,464`
    bytes, observed process peak RSS was `4,708,548 KiB`, swap was zero, and
    every memory-event delta was zero. Exact evidence is
    `crates/z00z_storage/outputs/checkpoint/069-07/task-1/resource-worker/20260727T041505797215369Z-test_real_hash_chunk_batch_stark_roundtrip-b94a7d4346a2/resource-evidence.json`.
10d. Command digest `3b5a508acef7...` is terminal and MUST NOT be rerun
    unchanged. It actual-verified or materialized `194` new leaves before a
    small source leaf entered the repeated twelve-thread scheduling tail; the
    `900`-second stage watchdog ended the worker with typed
    `resource_timeout`, `9,459,527,680` bytes peak cgroup memory, zero swap,
    and zero memory-event deltas. The current material successor supersedes
    the historical six-block/shared-pool topology: it restores `12` SHA blocks
    per hash chunk to avoid doubling the recursive leaf count, recycles the
    bounded twelve-thread pool after at most five coordinates / fifteen
    three-replica proofs, binds the global Rayon pool to twelve threads, binds
    the HJMT scheduler to one thread, and rotates predicate-packing, chunk-cache,
    and aggregation generations to `7`, `16`, and `13`.

    The generation-rotated isolated hash diagnostic
    `982760aeb812...` actual-proved and verified all `16/16` leaves with exactly
    one prepared-shape miss and fifteen hits. The miss reached trace build in
    `6,252 ms`; every hit reached it in `24..29 ms`. Test time fell from
    `326.04 s` under baseline digest `b146b16583e1...` to `227.19 s`
    (`30.32%`), and worker wall time fell from `399,219 ms` to `293,913 ms`
    (`26.38%`). Peak cgroup memory increased from `9,484,861,440` to
    `9,821,720,576` bytes, still below the `16 GiB` acceptance target; swap and
    all memory-event deltas remained zero. During the prover phase, the
    inventory peaked at eighteen threads: twelve named bounded prover workers
    plus the six launcher/test infrastructure threads. The apparent
    twenty-eight-thread launch peak consisted of release compiler/LTO threads,
    not an HJMT or overlapping prover pool. No `z00z-hjmt-worker` was observed.
10e. Recursive-node/root restart caching and aggregation scheduling are part
    of the same required successor. The bounded generation-`1` restart cache
    binds exact ordered child-proof digests, statements, relation/range/depth,
    parameter and verifier authority, and circuit/cache/aggregation
    generations. It stores canonical proof bytes only, never witness bytes;
    every load canonical-decodes and passes the actual pinned aggregation
    verifier before admission.

    Four isolated actual-proof schedule diagnostics produced the identical
    root digest
    `641369a6ab91c9779a83d65b8bf8420a24f76a77c55e99d6448a7cb2b0d9fdc0`
    with zero swap and zero memory-event deltas:

    | schedule | wall | user CPU | effective CPU | process peak RSS | cgroup peak | aggregation threads |
    | --- | ---: | ---: | ---: | ---: | ---: | ---: |
    | `1x12` | `22.377 s` | `204.77 s` | `936%` | `5,925,960 KiB` | `6,279,270,400 B` | `19` |
    | `2x8` | `23.056 s` | `210.75 s` | `936%` | `6,617,116 KiB` | `6,985,457,664 B` | `24` |
    | `2x10` | `21.129 s` | `229.73 s` | `1108%` | `6,597,444 KiB` | `6,980,796,416 B` | `28` |
    | `2x12` | `19.527 s` | `253.80 s` | `1340%` | `6,642,864 KiB` | `7,034,417,152 B` | `32` |

    `2x12` is therefore the current measured default: it is fastest on the
    declared host and remains below the hard memory target. The desired
    `4 GiB` objective is not met by recursive aggregation and remains an
    optimization target, not an acceptance claim.

    The exact pinned `p3-* = 0.6.1` stack exposes CPU parallelism through
    `p3-maybe-rayon/parallel`; it exposes no CUDA, OpenCL, WGPU, or other GPU
    proving feature. A GPU could in principle accelerate measured DFT/FFT,
    FRI commitment, Merkle/MMCS, or Poseidon work, but it cannot accelerate
    circuit/AIR construction merely by being present. GPU use is a separate
    audited backend-development successor and requires host/device-transfer
    and VRAM profiling, deterministic CPU/GPU proof verification, complete
    security-manifest preservation, and an independently isolated worker
    budget. It is not an available Plan-07 optimization today.
10f. The first full-theorem command using the prepared-shape/restart-cache
    successor, digest `03ba28c7b413...`, is terminal and MUST NOT be rerun
    unchanged. It completed and actual-verified all `492/492` current-generation
    base leaves, completed the entire `246`-node first recursive wave, and
    saved `36` additional second-wave nodes before the fixed `7,200`-second
    total gate. The terminal record is typed
    `resource_exhaustion/resource_timeout`; no root, proof size, independent
    root-common authority, or final verifier receipt was reached. Peak process
    RSS was `13,962,292 KiB`, cgroup peak was `15,328,071,680` bytes, swap was
    zero, and every high/max/OOM/OOM-kill event delta was zero. The cache now
    contains `1,673` canonical proof files / `1,954,520,480` bytes: `899`
    historical base files, `492` current base files, and `282` current
    generation-bound recursive nodes.

    The exact thread inventory identifies the remaining process-global
    fallback pool. During base proving, the phase peak of `31` consisted of
    twelve explicit base workers, twelve idle global Rayon threads, one HJMT
    worker, test/main threads, and launcher infrastructure. During aggregation,
    the phase peak of `45` consisted of twenty-four explicit `2x12` workers,
    the same twelve idle global Rayon threads, two aggregation controller
    threads, one HJMT worker, test/main threads, and launcher infrastructure.
    The materially changed successor therefore reduces only
    `RAYON_NUM_THREADS` for the unused process-global fallback from `12` to
    `1`. Explicit twelve-thread base pools and the measured `2x12` aggregation
    schedule remain unchanged. This removes eleven idle threads without
    changing theorem, proof bytes, transcript, security/parameter manifests,
    cache identities, or verifier behavior. A new command may reuse the
    generation-bound base and recursive caches only after canonical decode and
    actual pinned-verifier admission.
10g. The materially changed full-theorem successor, command digest
    `b09825a64cdf...`, is terminal. It actual-verified all `492` cached
    current-generation base leaves, completed all `491` recursive nodes
    (`163` per replica plus two ordered replica folds), produced one canonical
    envelope of `1,333,743` bytes (`1.27 MiB`), and passed the actual pinned
    Plonky3 cryptographic constraint verifier. The envelope is below the
    mandatory `2 MiB` production target. The run then rejected fail-closed at
    the independent root-common authority gate because
    `ACTIVE_PLONKY3_ROOT_COMMON_DIGEST_V2` remains zero; the observed
    non-secret candidate
    `068d54c5426fe95c7a07d6c6d1366a4cca412e650e2a1b3dd7da840c5bbe938b`
    is evidence, not self-authorizing release authority.

    Wall time was `3,403,815 ms` (`56:43.815`), `/usr/bin/time` recorded
    `13,699,084 KiB` maximum RSS, cgroup peak was `14,595,211,264` bytes
    (`13.59 GiB`), and aggregation high-water was `14,528,155,648` bytes.
    Swap and every cgroup high/max/OOM/OOM-kill delta were zero. The complete
    generation-bound cache contains `1,882` proof files /
    `2,230,806,589` bytes: `899` historical base files, `492` current base
    files, and all `491` current recursive nodes. Exact evidence is
    `crates/z00z_storage/outputs/checkpoint/069-07/task-1/resource-worker/20260727T170024589480673Z-predicate_differential-b09825a64cdf/resource-evidence.json`.

    The new run's aggregation phase peaked at `34` cgroup threads. The exact
    test process had one main thread plus `29` background threads: twenty-four
    explicit aggregation workers, two aggregation controllers, one
    process-global Rayon fallback, one bounded HJMT worker, and one libtest
    test thread. There is no unidentified or second twenty-eight-thread HJMT
    pool. The root-common candidate MUST NOT be copied into the authority pin
    until the minimum/maximum/boundary shape gate proves invariance for every
    supported canonical transition shape or the verifier implements the
    bounded generation-bound shape manifest required by acceptance. At that
    checkpoint, Plan 07 therefore remained `BLOCKED`; no completion summary or
    Plan-08 transition was permitted.

    Host inventory also confirms an NVIDIA GeForce RTX 4060 Ti with
    `16,380 MiB` VRAM. The full theorem created no NVIDIA compute context; only
    desktop graphics processes were present. The pinned `p3` backend remains
    CPU-only, so this GPU is capacity for a future audited backend, not
    acceleration evidence for this acceptance run.

    After removing `/proc/<tid>` disappearance noise from thread sampling, the
    successor isolation preflight passed under digest `f1283f5e82f9...` and
    the mandatory isolated release bootstrap passed under digest
    `b007a03016b1...`. Bootstrap process peak RSS was `4,100,768 KiB`, cgroup
    peak was `4,306,894,848` bytes, wall time was `309,074 ms`, and swap plus
    all memory-event deltas were zero. No thread-sampling warnings remained.
10h. The first recursive-cache authority inventory actual-verified every one
    of the `491` current generation-bound recursive nodes but was terminally
    misclassified as `isolation_unavailable` under command digest
    `f3eefb76cb71...`; that unchanged digest remains retry-forbidden. The
    materially changed worker defines a typed `authority_inventory` evidence
    schema, requires the reported verified-node total to equal the exact
    recursive-cache file count, and does not substitute inventory evidence for
    proof acceptance.

    After isolation preflight `b061bc9d7d02...` and mandatory release
    bootstrap `9813df803e3c...`, command digest `7d8b165c15a1...` passed with
    typed `authority_inventory/success`. It canonical-decoded, re-encoded, and
    actual-verified `491/491` cached recursive nodes, covering `11`
    relation/depth/leaf-count groups and `33` distinct common-data digests.
    Within the current `164`-leaf topology, relation-`0` common data converges
    to one digest per group from depth `4` onward; the two ordered
    replica-fold groups also each contain one digest. This proves cache
    integrity and within-topology convergence only. It does not establish
    minimum/maximum/boundary transition-shape invariance and MUST NOT
    self-authorize the fixture-derived final root digest.

    The accepted inventory evidence is
    `crates/z00z_storage/outputs/checkpoint/069-07/task-1/resource-worker/20260727T182802336214165Z-test_recursive_cache_authority_inventory-7d8b165c15a1/resource-evidence.json`.
    Its worker wall time was `88,278 ms`, observed process peak RSS was
    `4,039,132 KiB`, cgroup peak was `4,094,648,320` bytes, swap was zero, and
    every high/max/OOM/OOM-kill delta was zero. The preceding bootstrap peaked
    at `4,044,320 KiB` observed process RSS and `4,275,990,528` cgroup bytes
    with zero swap/events. At that checkpoint, the remaining authority
    successor therefore had to prove final-common invariance across all bounded
    canonical shapes or implement the generation-bound bounded shape manifest;
    Plan 07 remained `BLOCKED`.
10i. A materially changed inventory emitted the exact non-secret common-data
    digests after mandatory bootstrap `44436c3f9e7a...`. Command digest
    `37d0b21a4ef6...` again canonical-decoded, re-encoded, and actual-verified
    `491/491` nodes. Every relation-`0` group from depth `4` through depth `9`
    has the same fixed-point digest
    `29978135eb63f02873a9643d968cecc1acf4ee8579f8950cfee0f869f1c1ba87`.
    The first replica fold has
    `1bd565b5be029f08e0223bb87b37cb5a621558645b2e35ab01abfa16dc8d5040`;
    the final fold has the previously observed candidate
    `068d54c5426fe95c7a07d6c6d1366a4cca412e650e2a1b3dd7da840c5bbe938b`.

    Exact evidence is
    `crates/z00z_storage/outputs/checkpoint/069-07/task-1/resource-worker/20260727T183914086738740Z-test_recursive_cache_authority_inventory-37d0b21a4ef6/resource-evidence.json`.
    Worker wall time was `88,285 ms`, observed process peak RSS was
    `4,059,012 KiB`, cgroup peak was `4,064,571,392` bytes, swap was zero, and
    every high/max/OOM/OOM-kill delta was zero. The fixed point is established
    only for all ten consecutive sixteen-leaf subtrees and later merges
    present in the current canonical fixture. It does not authorize unseen
    minimum, maximum, or boundary leaf shapes and therefore does not change
    the fail-closed Plan-07 status.
11. Generic Pallas-R1CS-to-KoalaBear translation is not the default successor.
   The bounded domain-specific KoalaBear AIR from `069-07-STOP-SPLIT.md` is the
   live theorem implementation. Its pinned binary-W32 recursion-profile
   mismatch must be resolved without reverting to a generic translation. Any
   backend generation change and theorem/trace split preserves the complete
   `CheckpointTransitionConsistencyV2` relation and actual-verifier mutation
   coverage.
12. Plan 07 closes only when the full real theorem verifies without swap,
    below the declared memory cap, emits one complete canonical
    single-transition base-theorem envelope no larger than `2,097,152` bytes,
    and passes the actual pinned Plonky3 verifier. The size is measured over
    the complete canonical encoded payload, not per root and not after
    transport compression. An envelope in `(2 MiB, 4 MiB]` is a publishable
    target miss and production acceptance failure; `(4 MiB, 16 MiB]` rejects
    as `ProofSizeBudgetExceeded`. If either target
    cannot be met, Plan 07 remains `BLOCKED`, stop/split evidence is updated,
    broad tests do not run, and no completion summary is emitted.

### Typed resource evidence

Each isolated run emits a canonical JSON record with a versioned schema and an
exit reason selected from `success`, `test_failure`, `resource_oom`,
`resource_sigkill`, `resource_memory_max`, `resource_timeout`, or
`isolation_unavailable`. The record includes exact command/test identity,
source/fixture/backend/parameter digests, trace dimensions, unit/cgroup path,
configured limits, wall time, peak RSS, `memory.current`, `memory.peak`,
`memory.events` before/after deltas, swap use, and the previous command digest.
For resource exits, `is_retry_forbidden=true`; the runner rejects an unchanged
command digest on the next invocation. The evidence also records the
generation-bound canonical checkpoint authority-source digest and the
`900`-second stage-stall limit so a config or registry change cannot retain the
same worker command identity.

## execution_status

Plan 07 has passed the current-source real-theorem and actual-verifier mutation
gates on worker source digest
`f8dcf51aca0da0220c776eca053466653e23a7978a724ac06636b25a4b29755e`.
The mandatory isolated bootstrap
`20260728T123505437735755Z-bootstrap-460ce54db836` passed in `298,045 ms`;
observed process peak RSS was `4,009,416 KiB`, cgroup peak was
`4,273,115,136` bytes, swap and all memory-event deltas were zero, and no
named real prover started.

The materially changed exact isolated `test_predicate_differential` command
`731ffd506883cef13b376bd83feb21089e2410f8db37e0b693344baddde11558`
actual-verified all `492` base leaves and `491` recursive nodes, emitted one
`1,556,272`-byte canonical envelope, and passed the pinned outer verifier in
`1,324,921 ms`. Observed process peak RSS was `13,390,036 KiB`, cgroup peak
was `14,164,029,440` bytes, aggregation high-water was `14,110,322,688`
bytes, and swap plus every high/max/OOM/OOM-kill delta was zero.

The separately isolated `test_transcript_mutations_reject` command
`ab52ee7d8d71d130b5389646c5506977ba84ea5f79b3a66c163b344ee6c8d842`
passed in `637,528 ms` at `4,839,559,168` cgroup bytes with zero swap/events.
The root-opening mutation reached the actual pinned verifier and rejected with
`CapMismatch`.

The successor removes only duplicate canonical reserialization for exact
same-run bytes already canonical-serialized and actual-verified. A restart,
different cache key, or any byte mutation retains strict decode/re-encode and
actual-verifier admission. The complete transition relation, cache identities,
proof bytes, transcript, and security parameters are unchanged. The preceding
terminal timeout `e5161ce4515a…` remains retry-forbidden historical evidence.

The production size policy remains `2 MiB` target, `4 MiB` finite publication
cap, and `16 MiB` untrusted ingress/parser cap. FRI, hash, grinding, query,
field, transcript, recursion, three-replica, and residual-security parameters
were not reduced.

Closure gates passed. The exact current-source workspace release run recorded
`5,124` passed, `0` failed, and `0` warnings with zero swap/OOM events.
Release all-target build, release feature guard, production clippy with
`-D warnings`, and all-target clippy with the one explicitly documented
proof-bound Nova test-source exception passed. Six YOLO execution reviews
ended in two consecutive significant-clean passes, and both final
`/doublecheck` passes found no unresolved issue. Atomic coverage is
`1,345/1,345` with drift `0`. Plan 07 is summary-backed complete on
`.planning/phases/069-Recursive-Proof/069-07-SUMMARY.md`; Plan 08 is active.

## atomic_coverage_contract

- Gating source: `.planning/phases/069-Recursive-Proof/069-COVERAGE.md` rows selected by
  `primary_plan=069-07`; section-level mappings are navigation only.
- Task/action location: every selected atomic ID is owned by
  `069-07-T1/<action>`; the executor must load the exact source row/hash and
  implement or explicitly disposition the full atom without semantic merging.
- Test location: `069-07-T1/<tests>` must name a deterministic assertion and
  command for every selected ID; a shared command is allowed only when its
  output enumerates each covered ID independently.
- Result location: `069-07-T1/<results>` and
  `.planning/phases/069-Recursive-Proof/069-07-SUMMARY.md` must emit one
  pass/reject/disposition row per selected ID.
- Evidence location: this plan's `evidence_gate` requires an artifact/test/result
  pointer for every selected ID; missing, duplicate, untested, recommendation-
  only, or silently deferred atoms fail the plan.
- Audit command: `python3 .planning/phases/069-Recursive-Proof/069-COVERAGE-AUDIT.py`.

<execution_context>
Use the exact pinned Plonky3 family and frozen predicate. Treat upstream active-
development/unaudited status as a promotion red line, not permission to mock.
</execution_context>

<must_haves>
<truths>

- Base AIR proves transition consistency, not public-root equality or Nova acceptance.
- The base STARK uses only FRI plus hash/Merkle commitments and records
  quantum-aware hash, Fiat-Shamir, FRI, grinding, query, and finite-horizon
  security inputs; it contains no EC/pairing PCS or wrapper.
</truths>

<artifacts>
- AIR, trace, params, real prover/verifier, receipt, differential/mutation evidence.
</artifacts>
<key_links>
- Frozen witness/vector -> Plonky3 trace/AIR -> actual verifier -> receipt.
</key_links>
<prohibitions>
- No omitted constraint, incomplete transcript, ad hoc Poseidon2 parameters,
  weak FRI substitution, classical-only hash/Fiat-Shamir budget, fake proof,
  permanent base-proof archive, EC/pairing PCS or wrapper, suffix/fallback
  dispatch, mixed generation, or whole-checkpoint PQ overclaim.
</prohibitions>
</must_haves>

<threat_model>
Block AIR underconstraint, trace forgery, hash/field mismatch, FRI downgrade,
cross-context replay, malicious proof, transcript ambiguity, parameter rotation,
witness leakage, and PQ overclaim. Any unresolved high/critical issue blocks.
</threat_model>

<tasks>
<task type="auto">
  <name>Implement and adversarially verify the real Plonky3 base AIR</name>
  <id>069-07-T1</id>
  <read_first>
  - `.planning/phases/069-Recursive-Proof/069-PREDICATE.md`,
    `.planning/phases/069-Recursive-Proof/069-FEASIBILITY.md`, and
    `.planning/phases/069-Recursive-Proof/069-051-SUMMARY.md`
  - storage predicate vectors and native evaluator
  - pinned Plonky3 source/API used by the probe
  - backend receipt boundary
  </read_first>
  <files>
  - `Cargo.toml` and `crates/z00z_storage/Cargo.toml`
  - `crates/z00z_storage/src/checkpoint/{mod,plonky3,recursive_v2,adapter,receipt,recursive_reject,version_registry,authority_artifacts}.rs`
  - `crates/z00z_storage/tests/test_recursive_v2_plonky3_base.rs`
  </files>
  <action>
Implement the complete frozen predicate as Plonky3 AIR with bounded trace,
canonical limb encoding, pinned Poseidon2/MMCS/PCS/FRI/challenger parameters,
a fully bound Fiat-Shamir transcript, and real prove/verify paths. Execute the
same differential vectors as native and the completed real Nova evaluator/
verifier, mutate every constraint, transcript, encoding, and manifest family,
derive and record the concrete security budget, and create receipts only from
actual verifier success. Expose base proofs only to the bounded Plan 08
aggregation interface.
Derive the security budget against quantum collision/search and Fiat-Shamir
adversaries rather than treating a classical collision literal as PQ strength.
Fail closed unless the concrete base parameters and finite recursion horizon
preserve the configured residual target. Assert by dependency/type/config scans
that the base PCS and exported proof have no Nova, Groth16, KZG, IPA, pairing,
or elliptic-curve wrapper.
Keep one private `checkpoint::plonky3` subsystem and one public facade path.
`checkpoint/plonky3.rs` remains the owner/facade for the theorem, trace,
parameters, and backend orchestration. The audited, generation-bound binary-W32
PCS adapter lives in `checkpoint/plonky3_binary_pcs.rs`; opening reductions,
FRI folding, authentication constraints, and hash/cap primitives live directly
in `checkpoint/plonky3_binary_mmcs.rs`,
`checkpoint/plonky3_binary_fri_fold.rs`, and
`checkpoint/plonky3_binary_hash.rs`. The recursive public statement and its AIR
live directly in `checkpoint/plonky3_root_statement.rs` and
`checkpoint/plonky3_root_statement_air.rs`. These seams MUST call the pinned
recursive FRI arithmetic verifier, authenticate the same opened values through
the native binary W32 commitment geometry, expose no public dependency types,
create no compatibility module, and create no second proof path. Export the public API only through
`checkpoint/recursive_v2.rs`; adapter and receipt types remain owned by
`checkpoint/adapter.rs` and `checkpoint/receipt.rs` respectively.
Implement and use the canonical isolated worker required by the Resource Safety
and Memory Acceptance Contract before executing any real prover. Preserve the
security manifest while reducing peak memory through the mandated optimization
order. Apply the relevant measurement, bounded-parallelism, cache, and
memory-first/checkpoint-second requirements from
`.planning/phases/profiling-comprehensive.md` to this prover path. Do not treat
that full-gate profiling prompt as a blind instruction to add concurrency:
CPU-bound proof work uses measured bounded worker pools, asynchronous work is
limited to independently measured I/O orchestration, and separate proof
processes are permitted only when every process has an independent isolated
resource budget and their combined host budget is proven safe. Record why any
candidate optimization is used, rejected, or deferred. Do not retry an
unchanged resource-exhausted command.
  </action>
  <artifacts>
  - Real base proof/receipt fixtures, `RecursiveSecurityBudgetManifestV2`, and
    complete differential mutation matrix.
  </artifacts>
  <tests>
  - Run bounded non-prover assertions separately with a `4 GiB` process cap.
  - Run each real named prover assertion through
    `.github/skills/smart-tests-bootstrap/scripts/plonky3_resource_worker.sh`
    using `--run <exact-name>` (or `--start` plus `--status`) with
    `--test-threads=1`, `MemoryHigh=16G`, `MemoryMax=24G`,
    `MemorySwapMax=0`, `OOMPolicy=continue`, and a detached durable
    `run-state.json`.
  - The isolated targets MUST execute the exact `test_predicate_differential`
    and `test_transcript_mutations_reject` assertions; target and exact-name
    checks prevent a zero-match success.
  - Transcript-pruning/order tests remove or move each context, statement,
    commitment, public-input, shape, and parameter field independently and
    require actual-verifier rejection.
  - Security derivation tests independently mutate every FRI/hash/grinding/
    recursion input and reject missing generation binding, downward rounding,
    zero/overflowed lifetime budget, checked-sum overflow, inherited-loss reset,
    and any residual bound above `2^-100`.
  - Lifecycle tests prove base proofs are absent from validator/wallet/network
    payloads and become eligible for deletion only after a verified epoch
    aggregate commit.
  </tests>
  <results>
  - Valid base proof verifies; every constrained mutation and parameter/context drift rejects.
  </results>
  <implementation_depth>full</implementation_depth>
  <verify>
  1. First, run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` as the fail-fast gate; on failure stop, fix, and rerun it before any broader validation.
  2. Before any real prover, verify the transient-worker isolation controls and canonical typed-evidence writer. Run one named heavy test at a time with `--test-threads=1`; never execute a real prover in the VS Code scope.
  3. Treat OOM, `SIGKILL`, cgroup `MemoryMax`, resource timeout, missing isolation, peak RSS above `16 GiB`, nonzero swap, or missing resource metadata as a terminal Plan-07 blocker. Do not retry the unchanged command, do not run broad tests, update `069-07-STOP-SPLIT.md`, and emit no completion summary.
  3a. Before pinning a root-common candidate, prove that it is valid for every
  supported canonical transition shape or implement the bounded reproducible
  authority derivation described in `acceptance_tests`; a digest observed from
  only the acceptance fixture remains candidate evidence, not release
  authority.
  4. Only after every named real prover target verifies without swap below `16 GiB`, run the remaining plan-specific release tests and then `cargo test --release`; every other relevant broad Cargo command must also use `--release`.
  4a. Before final acceptance, produce a stage-level prover profile covering
  cold and cache-assisted leaf materialization, recursive aggregation, actual
  verification, wall/CPU time, peak memory, cache hit/miss/write cost, process
  count, thread-pool sizes, effective CPU utilization, and I/O volume. Audit
  every relevant recommendation in
  `.planning/phases/profiling-comprehensive.md`; classify it as implemented,
  inapplicable, unsafe under the memory contract, or still required. Measure
  before enabling extra processes, threads, async tasks, or cache layers.
  Prove cache identity/invalidation and actual-verifier checks, and never cache
  or log witness bytes. The report must state both the hard `16 GiB`
  acceptance result and progress toward the desired `4 GiB` prover objective.
  4b. Treat the following measured performance work as mandatory before the
  final complete-theorem attempt:
   - eliminate the redundant `28`-thread HJMT scheduler pool from the prover
     worker and account by name, owner, purpose, and lifetime for every one of
     the other `29` background threads observed in the process; an unknown
     thread is a profiling blocker, not free parallel capacity;
   - add a bounded generation-bound cache for prepared base-circuit shapes.
     Its identity MUST bind the complete circuit/parameter/profile authority,
     it MUST contain no witness/private-input bytes, and it MUST remove or
     materially reduce the currently measured median `2.65 s` circuit/AIR
     preparation cost (approximately `28%` of one leaf);
   - add a generation-bound restart cache for recursive nodes and root proofs,
     keyed by the exact child-proof digests, statements, relation, parameter
     manifest, circuit/aggregation generation, and verifier authority. Every
     loaded artifact MUST canonical-decode and pass the actual pinned verifier;
     no witness bytes may be cached;
   - benchmark aggregation schedules `1x12`, `2x8`, `2x10`, and `2x12`
     separately, one named isolated target at a time. Record wall/CPU time,
     utilization, peak process/cgroup memory, memory events, and output
     identity, then select the fastest schedule that remains below the hard
     process cap with host headroom. Do not assume that more threads or workers
     are faster;
   - evaluate distributed proving by replica/chunk as the explicit
     multi-machine successor for order-of-magnitude latency reduction. Each
     worker MUST have an independent authenticated job identity, isolated
     cgroup or dedicated-host budget, no witness logging, deterministic result
     ordering, actual-verifier admission, and a proven combined coordinator
     memory/network budget. Do not launch multiple heavyweight local prover
     processes under one shared `24 GiB` emergency ceiling.
  4c. Evaluate GPU acceleration against the exact pinned backend. The report
  MUST identify which measured stages, if any, have an implemented and
  auditable GPU kernel (for example field FFT/DFT, Merkle/MMCS, or hashing),
  include host-device transfer and GPU-memory costs, preserve deterministic
  transcript/proof identity and the complete security manifest, and fail
  closed on CPU/GPU differential mismatches. The absence of a GPU execution
  path in the pinned Plonky3/circuit-prover stack MUST be reported as a
  separate backend-development requirement; it MUST NOT be represented as an
  available Plan-07 optimization.
  5. Run the canonical inline `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least three times, fix every issue and warning, and stop only after at least two consecutive runs have no significant code issues; record evidence in `.planning/phases/069-Recursive-Proof/069-07-SUMMARY.md`.
  6. Execute two `/doublecheck` passes on AIR, params, receipts, resource isolation/evidence, and claims—one with the spec-to-code, crypto/security/constant-time, and Z00Z gates, then one after the final fixes—and resolve every finding from both passes.
  7. Contain all Phase 069 raw logs, PID/time/exit files, cgroup events, fuzz outputs, measurements, proof material, and scenario packets fail-closed under `crates/z00z_storage/outputs/checkpoint/069-07/task-1/...`; assert that the repository-root `test-results` tree does not exist.
  8. If committing, use `/z00z-git-versioning` only.
  </verify>
  <acceptance_criteria>
  - Real base proof verifies the full same-theorem predicate.
  - The complete canonical Plan-07 base-theorem envelope is no larger than
    `2,097,152` encoded bytes before transport compression; the acceptance
    target is not
    applied separately per root or replica.
  - Each real proof verifies without swap through the actual pinned verifier at
    or below `16 GiB` peak RSS in the isolated capped worker; typed evidence is
    complete and no validator or wallet prover path exists.
  - Differential and mutation tests prove sound constraints.
  - Transcript completeness, concrete FRI security derivation, bounded parser
    behavior, and internal-only base-proof lifecycle are evidenced.
  - The FRI/hash-only base profile and quantum-aware residual are exact; only
    the recursive-proof chain, not classical transaction primitives, may later
    receive an end-to-end PQ label.
  </acceptance_criteria>
  <blockers>Any AIR/hash underconstraint, parameter ambiguity, or missing actual verifier path blocks recursion.</blockers>
  <evidence_gate>Differential matrix records exact parameters, actual verifier calls, and every constraint-family mutation.</evidence_gate>
  <anti_placeholder_gate>Root-only AIR, fake trace/proof, Nova-wrapper theorem, or compile-only evidence fails.</anti_placeholder_gate>
  <not_recommendation_gate>Only real Plonky3 prove/verify differential evidence closes.</not_recommendation_gate>
  <done>A real Plonky3 base proof verifies the full predicate, the finite
  security budget is derivation-bound, and all proof/manifest mutations reject.</done>
</task>
</tasks>

## Artifacts this phase produces

- New: checkpoint AIR, trace, Plonky3 params/adapter, base proof/receipt fixtures.
- Existing, not new: canonical theorem, native evaluator, Nova adapter/receipt contract.

<verification>
- Bootstrap, real Plonky3 release/differential tests, full release, repeated crypto/security/spec audits.
</verification>

<success_criteria>

- One real base STARK proves the complete shared predicate, emits one complete
  canonical envelope no larger than `2,097,152` encoded bytes, and rejects all
  constrained mutations.
</success_criteria>

## Coverage Appendix

| TASK-NNN | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| N/A — PRD contains no TASK-NNN | `069-07` | TODO semantics/Plonky/params/PQ/AC sections | exact pins and frozen predicate | real AIR/trace/adapter/proof/receipt | actual verifier differential/mutations | same predicate verifies | real crypto; deterministic trace input only | no root-only/Nova-wrapper proof | pending |
