---
phase: 069
plan: 069-13
plan_id: 069-13
title: Documentation Security And Claim Closeout
type: implementation
wave: 12
depends_on: [069-12]
task_ids: []
copied_task_rows: []
files_modified:
  - crates/z00z_storage/src/checkpoint/documentation_packet.rs
  - crates/z00z_storage/src/checkpoint/mod.rs
  - crates/z00z_storage/tests/test_recursive_docs.rs
  - scripts/audit/audit_069_recursive_v2.sh
  - .planning/phases/069-Recursive-Proof/069-SECURITY.md
  - .planning/phases/069-Recursive-Proof/069-VERIFICATION.md
autonomous: true
requirements: [RCP-069]
must_haves:
  truths: ["Every Phase-069 normative clause has evidence; current-state, archive, backup sharding, and seed recovery have executable parity; the Phase-071 mailbox handoff has exact config/registry/no-online-path parity without a delivery claim; no unsupported mailbox or full-replica-fallback claim survives."]
  artifacts:
    - path: crates/z00z_storage/src/checkpoint/documentation_packet.rs
      provides: "Generated evidence-bound recursive documentation packet."
    - path: .planning/phases/069-Recursive-Proof/069-VERIFICATION.md
      provides: "Atomic obligation, security, review, and command-result closeout."
  key_links:
    - from: .planning/phases/069-Recursive-Proof/069-COVERAGE.md
      to: .planning/phases/069-Recursive-Proof/069-VERIFICATION.md
      via: "One-primary-owner atomic IDs resolved to persisted artifacts, tests, and results."
---

# 069-13 Documentation Security And Claim Closeout

<objective>
Generate the recursive documentation packet from persisted evidence and close
dependency, parameter, cryptographic-security, client-centric retention,
mandatory current-state/RS/archive/backup sharding, seed recovery, rolling-
history, Phase-071 offline-delivery handoff, wallet custody, atomicity, capacity, source-disposition,
coverage, spec-to-code, and overclaim audits only after every TODO invariant,
acceptance criterion, Done Definition bullet, handoff gate, test family, and
artifact has executable evidence.
</objective>

## plan_id

`069-13`

## task_ids

[]

## copied_task_rows

[]

## source_refs

- Entire `.planning/phases/069-Recursive-Proof/069-TODO.md`, especially source disposition, backend/PQ policy, documentation packet, required artifacts, non-negotiable rejections, Done Definition, and Planning Handoff Checklist
- `.planning/phases/069-Recursive-Proof/069-CONTEXT.md`, `.planning/phases/069-Recursive-Proof/069-PATTERNS.md`, `.planning/phases/069-Recursive-Proof/069-COVERAGE.md`
- Phase 068 canonical spec/verification, all Phase 069 plans/summaries/evidence, repository rules and required review/verification prompts.
- `.planning/phases/071-Request-Bound-Inbox/071-Request-Bound-Inbox-Spec.md` and `Request-Bound-Inbox-Config.yaml` as the mailbox implementation owner contract.

## inputs

- Persisted schemas/vectors/proofs/receipts/chains/history transitions,
  measurements/cadence/archive/audit/snapshot/pruning/wallet/atomicity/simulator
  evidence and all dependency/parameter manifests.

## outputs

- `RecursiveCheckpointDocumentationPacketV2` generator/codec and persisted packet.
- Rejected-claim/source-disposition/dependency/parameter/security/retention/
  rolling-history/wallet/atomicity/capacity audit packets and final
  `069-VERIFICATION.md`.

## dependencies

- `069-12` complete integrated evidence; every prior plan summary must exist and pass.

## acceptance_tests

- Packet is generated from persisted artifacts and every referenced digest/path exists and verifies.
- Coverage audit reports 0 TASK IDs, 13/13 owner groups, 25/25 invariants,
  47/47 ACs, 26/26 Done bullets, and 14/14 handoff items with no duplicate
  primary owner. The packet inventory explicitly records 14 plan packets:
  historical/superseded `069-05`, active corrective overlay `069-051`, and the
  other twelve owner-group plans; superseded evidence MUST NOT be mistaken for
  the active Group 05 contract.
- Planning-time atomic audit reports `1319/1319` current TODO atoms with one
  primary owner and concrete task/action/test/result/evidence locations, all
  generated SHOULD/MAY dispositions enforced, and zero missing/duplicate/
  drifted rows. If the TODO changes, the regenerated count replaces `1319` and
  must pass before closeout.
- All forbidden authority, finality, PQ, DA/archive, storage, snapshot, pruning,
  proof-size, production, retention, wallet-custody, atomicity, capacity, and
  audit claims reject.
- Documentation explicitly distinguishes prover-local PP/PK, finite
  generation-pinned verifier VK cache, and per-snapshot proof envelope; it states
  the rotation/archive/deletion owner and that a wallet verifies finality
  certificate + committed root + HJMT membership rather than a recursive proof.
- Documentation includes the generated `CheckpointVersionRegistryV2` and labels
  API suffix, type ID/wire version, cryptographic/transcript/public-input/root-
  encoding generations, config schema generation, runtime profile identifier/
  generation/authority-pinned manifest digest, and authority/parameter
  generation separately.
  Every public/persisted/networked family is classified exactly once as live,
  bounded migration-only, eradicated, or current writer; no prose infers wire
  semantics from `V1`/`V2` names.
- The client-centric retention matrix proves: current state/recovery data stay
  network-available through the mandatory 16-shard distributed-HJMT route,
  per-shard replicas and root-verified membership/seed recovery; compact
  certified anchors/MMR plus minimal committee
  keyset/threshold/rotation manifests are
  permanent; canonical raw transaction packages/non-derivable witnesses/deltas/
  exact transaction-proof bytes are bounded by the 1,555,200-block challenge
  window and verified-history gates; compressed Nova proof bodies instead follow
  the separate 16-body/2-MiB/eight-pending-epoch `NovaRetentionStateV2` policy;
  full Plonky3 bodies follow their rolling-successor/window policy;
  wallet receipt/history/backups are wallet-owned. Any wording that makes all
  raw data or all proof bodies permanent MUST fail the audit.
- The offline-recipient handoff matrix proves Phase 069 implements only current-
  unspent seed recovery, `ReservedUnreachable` type/domain rows, ConfigV3
  `declared_only`/cap zero, generic finalized-output/outbox seams, and no online
  mailbox path. It points all codec/crypto/provider/replication/activation/ACK/
  GC/measurement/delivery gates to Phase 071. Any Phase-069 claim that encrypted
  delivery, 90-day mailbox plateau, provider metadata handling, or ACK/GC
  executed MUST fail the audit.
- Active ConfigV3/docs/code/scenario parity proves no one-shard or full-state-
  replica current-state launch and no full-replica challenge-archive fallback.
  Challenge retention uses measured RS(10,16); missing feasibility/placement
  blocks promotion. Reshard uses COW copy/root verification/ordered delta catch-
  up/canonical CAS through existing HJMT owners and never creates a second root.
- Seed recovery restores current spendable outputs by scanning all active
  shards, locally authenticating/decrypting the bounded live-leaf capsule and
  verifying a fresh current-root witness. `tag16` is not ownership proof and
  full personal history still comes only from wallet backup.
- A late evidence/PQ anchor is only local pending data after its proof/archive
  join; network promotion and retention deletion require typed evidence-MMR
  inclusion in a later quorum-certified close. Treating an unauthenticated
  digest as network-notarized evidence MUST fail.
- Epoch-close evidence proves height 1000 is canonical after inherited DA-ready/
  QC gates without waiting for Nova/Plonky3/PQ/additional evidence publication.
  `PqAnchorMissing` can block recursive-evidence promotion
  or pruning authorization only; using it for canonical admission MUST fail.
- Rolling-history evidence verifies base, successor, and rotation bridge with
  actual predecessor-proof verification. Digest-only history chaining or an
  unbridged parameter/VK transition MUST fail.
- Security evidence derives a finite lifetime budget from concrete backend
  parameters and conservatively composes per-proof plus inherited error across
  accepted epochs and rotations. A fixed `124 bits forever` claim, count
  rollback/overflow, recursion past budget, or rotation that resets inherited
  loss MUST fail; canonical admission remains independent if recursive evidence
  must stop.
- Atomicity evidence covers canonical, recursive-evidence, and wallet-backup
  state machines independently. The packet MUST NOT claim a cross-provider
  transaction; it MUST prove digest/idempotency-key-bound durable outbox/saga
  recovery instead. Canonical evidence includes touched-shard prepare/quorum,
  route/shard-manifest/head/outbox boundaries.
  Recursive evidence includes ticket/disposition/retirement-head-before-unlink
  Nova body GC and separate active-plus-two local recovery-snapshot rotation; it
  MUST NOT infer deletion from directory contents, age, or current config.
  The Phase-071 handoff MUST represent future mailbox activation/ACK/GC as
  subordinate outbox sagas in those three authority domains, never as a fourth
  transaction or cross-node atomic commit; Phase 069 contains no such live saga.
- Capacity evidence separates static PP/PK/VK, transient proof bodies, bounded
  challenge bytes, permanent compact history, live state, role-specific network
  traffic, and wallet-owned backups. It proves each anchor and epoch-close
  certificate <= 4 KiB, permanent
  compact historical growth <= 100 KiB/day, and retained challenge bytes
  plateau after the configured window.
  It separately proves Nova retained bodies plateau at 16/2 MiB under a nine-
  epoch PQ outage, the ninth epoch backpressures only compression/publication,
  and production activation uses a measured positive finite hot-recovery cap.
  Mailbox measurement rows are explicitly `not_measured_phase071`, nonnumeric,
  inactive, and cap zero. Phase 071 owns object/traffic/skew/plateau measurements
  and a later new-generation positive aggregate cap; 2 KiB/8 KiB DoS caps are
  never capacity estimates.
- Wallet Backup V5 evidence proves canonical plaintext is encrypted before
  erasure coding, providers receive only ciphertext shards, `k`-of-`n` exact
  reconstruction/decrypt/readback precedes head CAS, and the latest three
  logical generations remain independently reconstructible. A one-provider
  backup is reported as one failure domain, not trusted durability.
- Canonical max-safe Z00Z verification and two consecutive clean review passes after the last fix succeed.

## simulation_gate

- No simulation closes documentation/code claims. Packet generation consumes
  real persisted evidence. Plan 12's accelerated height/time is admissible only
  for retention-state evolution and byte accounting, never for proof-throughput
  or cryptographic-soundness claims; unsupported end-to-end PQ validity is
  `live-claim-removed`.
- Local/test retention-ticket execution MUST NOT be laundered into a production
  pruning claim; production remains `declared_only` pending the separate
  retention-promotion gate.

## negative_tests

- Missing/tampered artifact/digest/pin/parameter/link/vector/reject/measurement/
  cadence/retention/snapshot/pruning/wallet/atomicity/review evidence; PP/PK
  presented as verifier/wallet/network payload; permanent raw/witness/full-proof
  retention; proof/PQ-gated canonical epoch close; digest-only history chaining;
  unbridged verifier-generation rotation; unverified wallet backup head;
  backup nonce/key reuse or unauthenticated/downgraded/unbounded KDF header;
  cross-provider atomic-transaction claims; Plonky unaudited risk omission;
  Nova/Pedersen/classical-as-PQ; Plonky Nova-only; DA/retrieval-as-validity;
  snapshot trust; recursive proof as storage; accelerated retention presented as
  proof throughput; `VERIFIED`/production/finality claims; coverage omission or
  duplicate; unclassified recursive V1/V2 or config V1/V2/V3 suffix;
  suffix/backend-label dispatch; fallback
  decode; cross-type/downgrade acceptance; post-cutover old writer; partial or
  non-atomic migration; domain/digest reuse across changed semantics.
- Any Phase-069 mailbox codec/store/provider/reader/writer/activation/ACK/GC,
  positive cap, same-generation activation, delivery/plateau evidence, or
  network lookup from request id/local route bucket/`tag16`; any handoff that
  permits permanent/authoritative/listable/public-DHT storage, history/DA/
  challenge-archive use, sender-until-ACK, ACK-before-wallet-commit, mailbox-
  dependent finality/seed recovery, or cross-namespace GC.
- One-shard/full-state current-state launch, missing shard-manifest/root binding,
  partial/dual-writer reshard, skipped-shard or `tag16`-trusted seed recovery,
  full-replica archive fallback, invalid RS placement/reconstruction, backup
  plaintext/key sharding, head CAS before reconstruct/decrypt/readback, or a
  protected backup generation below `k`.

## plan_artifacts

- `RecursiveCheckpointDocumentationPacketV2`
- `069-SECURITY.md`, rejected-claim/source-disposition register,
  client-centric retention, Phase-071 mailbox-handoff/no-live-path, and capacity matrices, rolling-history/rotation
  theorem register, three-state-machine crash matrix, wallet custody/restore
  register, audit script, and final verification packet.
- Generated version registry, old/new Cartesian compatibility matrix,
  activation/minimum-version ledger, ConfigV2 parity plus ConfigV3 activation
  evidence, and complete migration/cutover crash matrix.
- Registry/code/docs/scenario parity MUST also cover each row's dispatch framing
  mode. `TypedLegacyAdapter` is valid only for an enumerated immutable inherited
  grammar, preserves its canonical bytes/digest, has one typed route and one
  capped decoder, and is absent from every newly introduced Phase 069 family.

## plan_tests

- `cargo test --release -p z00z_storage --test test_recursive_docs -- --nocapture`
- `bash scripts/audit/audit_069_recursive_v2.sh`
- `bash scripts/audit/audit_release_feature_guards.sh`
- `./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run`

## plan_results

- Phase closure is evidence-backed, complete, non-authoritative, and free of
  unsupported PQ/finality/production/storage/retention/wallet/atomicity claims,
  PP/VK/bundle size misclassification, or unbounded historical-growth defaults.

## task_artifacts

- No literal `TASK-NNN`; group artifacts are the generated docs/security/audit/verification packet.

## task_tests

- No literal `TASK-NNN`; group tests verify every packet reference and source/code claim.

## task_results

- No literal `TASK-NNN`; expected result is a complete pass or explicit blocked closeout with no hidden gap.

## anti_placeholder_gate

- Handwritten packet, missing artifact link, docs-only code claim, unchecked coverage row, placeholder/TODO/panic/no-op, or single clean review pass fails.

## current_code_refs

- `.github/skills/z00z-full-verify-gate/scripts/full_verify.sh`
- `.github/prompts/gsd-review-tasks-execution.prompt.md`
- `docs/tech-papers/Recursive-Ready-Checkpoint-Contract.md`
- `crates/z00z_storage/src/checkpoint/recursive_checkpoint.rs`
- `crates/z00z_storage/src/checkpoint/contract_config.rs`

The generated baseline, Phase 069 implementation/test outputs, and Scenario 069
artifacts are dependencies/evidence inputs, not current-code anchors.

## blockers

- Any missing/failed evidence, unresolved high/critical threat, unaccounted TODO clause, unsupported claim, or missing consecutive clean review blocks phase completion.

## evidence_gate

- `069-VERIFICATION.md` must link exact commands/results, artifact digests,
  coverage rows, review passes, threats/dispositions, limitations, and the
  PP/PK/VK/proof size, transport, rotation, archive, deletion, and wallet-boundary
  classification.

## version_boundary_closeout_contract

- Closeout MUST regenerate and compare the implementation registry, codec/cap
  dispatch tables, golden/fuzz vectors, migration entry points, docs table, and
  persisted scenario evidence from one declarative source or prove exact parity.
- It MUST report zero unclassified recursive V1/V2 and config V1/V2/V3
  occurrences after distinguishing
  historical completed plans, the superseded Plan 05 packet, live inherited V1
  canonical/availability contracts, bounded migration-only V1 contracts,
  eradicated recursive-proof V1 bytes, public V2 wire-2 objects, and private V2
  local codec-1 subrecords; plus the pre-cutover misnamed ConfigV1 API owner,
  exact migration-only ConfigV2 owner, and active ConfigV3 owner.
- Wallet documentation MUST show the explicit V5 backup/head/shard-manifest
  registry rows, independent KDF/encryption/erasure/head-policy generations,
  bounded V4-read/V5-write migration, and last-V4-copy retention gate; it MUST
  NOT infer wire or policy from `V5`, relabel V4 bytes, or count V4 as one of
  the three protected V5 generations.
- It MUST prove the `CheckpointContractConfigV1` to
  `CheckpointContractConfigV2` change is an API-owner/caller cutover over the
  existing schema-2 bytes with identical canonical config digest. A schema-1
  migrator is forbidden unless an actual registered schema-1 fixture exists.
- It MUST separately prove exhaustive `ConfigV3RenameLedger` coverage and the
  atomic ConfigV2-to-ConfigV3 transform, including distinct source/destination
  bytes/domains/digests, failpoint recovery to one complete generation,
  ConfigV3-only online reachability after activation, and rejection of every
  missing/default/alias/unknown-field or downgrade path. Config schema 3 MUST
  NOT be reported as recursive-object wire version 3.
- It MUST prove ConfigV3 cannot authorize its own activation: the certificate
  verifies under the pre-cutover trust root or pinned release manifest and
  binds both config digests, registry/ledger digests, runtime profile identifier/
  generation/authority-pinned manifest digest, schema, pre-cutover signer-
  authority generation, activated authority/parameter generation, distinct
  monotonic config generation, activation boundary, network/chain, and rollback
  floor. The same binding is
  mandatory in a local authenticated release-manifest activation record.
- It MUST prove the digest dependency DAG is acyclic: registry digest excludes
  active config digest; the bounded canonical runtime-profile manifest excludes
  registry/config/head/migration/activation digests; the registry binds the
  profile-manifest digest; ConfigV3 binds registry/profile/ledger declarations;
  the activation record then binds all of them. Direct/transitive self-reference
  and same-ID/different-manifest substitution reject.
- `RCP-INV-023` and `RCP-AC-042` require complete positive rows, invalid old/new
  Cartesian rejection before allocation, no suffix/fallback dispatch, bounded
  dual-read/single-write entry points, activation/minimum-version enforcement,
  idempotent failpoint recovery, and no old online writer after cutover.
- Any implementation/docs/plan disagreement, shared semantic domain across
  changed bytes, unbounded old decoder, or missing registry row is an unresolved
  high-risk version-boundary finding and blocks Phase 069 closeout.

## not_recommendation_gate

- Recommendations cannot substitute for missing local correctness, code, tests, proof artifacts, or deterministic simulations.

## atomic_coverage_contract

- Gating source: `.planning/phases/069-Recursive-Proof/069-COVERAGE.md` rows selected by
  `primary_plan=069-13`; section-level mappings are navigation only.
- Task/action location: every selected atomic ID is owned by
  `069-13-T1/<action>`; the executor must load the exact source row/hash and
  implement or explicitly disposition the full atom without semantic merging.
- Test location: `069-13-T1/<tests>` must name a deterministic assertion and
  command for every selected ID; a shared command is allowed only when its
  output enumerates each covered ID independently.
- Result location: `069-13-T1/<results>` and `.planning/phases/069-Recursive-Proof/069-13-SUMMARY.md` must emit one
  pass/reject/disposition row per selected ID.
- Evidence location: this plan's `evidence_gate` requires an artifact/test/result
  pointer for every selected ID; missing, duplicate, untested, recommendation-
  only, or silently deferred atoms fail the plan.
- Audit command: `python3 .planning/phases/069-Recursive-Proof/069-COVERAGE-AUDIT.py`.

<execution_context>
Re-read the entire normative PRD and all plans/summaries. Run workspace-first
doublecheck and spec-to-code compliance before asserting closure.
</execution_context>

<must_haves>
<truths>

- Every normative clause has executable evidence or the phase remains blocked.
- Recursive evidence stays non-authoritative; Plonky outer evidence is not end-to-end PQ validity.
- Canonical epoch closure is independent of recursive proving and publication availability.
- Archive/retrieval/snapshot/pruning claims preserve Phase 068 ownership and trust boundaries while allowing only ticket-authorized bounded-window pruning.
- Network, wallet, and prover/verifier storage ownership and traffic are explicit and capacity-checked.
</truths>

<artifacts>
- Generated docs packet, audits/registers, security and final verification packet.
</artifacts>
<key_links>
- Every closeout claim -> exact persisted artifact/test/review result -> coverage primary owner.
</key_links>
<prohibitions>
- No unsupported authority/finality/PQ/production/size/storage/retention/wallet/
  atomicity claim, permanent raw/full-proof default, proof-gated finality,
  missing evidence, silent deferral, parallel owner, unclassified/mixed version
  boundary, suffix/fallback dispatch, or placeholder closure.
</prohibitions>
</must_haves>

<threat_model>
Block claim laundering, evidence substitution, stale/tampered packet links,
dependency/parameter omission, unaudited-backend promotion, secret leakage,
retention/trust drift, digest-only recursive history, unsafe key rotation,
wallet backup rollback/fork, partial-commit laundering, storage amplification,
source/spec omission, and false completion. Any unresolved
high/critical issue or missing mandatory evidence blocks closure.
</threat_model>

<tasks>
<task type="auto">
  <name>Generate evidence packet and execute final coverage/security/claim audit</name>
  <id>069-13-T1</id>
  <read_first>
  - Entire `.planning/phases/069-Recursive-Proof/069-TODO.md`, `.planning/phases/069-Recursive-Proof/069-CONTEXT.md`, `.planning/phases/069-Recursive-Proof/069-PATTERNS.md`, `.planning/phases/069-Recursive-Proof/069-COVERAGE.md`
  - All thirteen owner groups and fourteen plan packets/summaries/evidence,
    including historical `069-05` and its active `069-051` corrective overlay
  - Phase 068 verification and repository verification/review instructions
  </read_first>
  <files>
  - `crates/z00z_storage/src/checkpoint/documentation_packet.rs`
  - `crates/z00z_storage/src/checkpoint/mod.rs`
  - `crates/z00z_storage/tests/test_recursive_docs.rs`
  - `scripts/audit/audit_069_recursive_v2.sh`
  - `.planning/phases/069-Recursive-Proof/069-SECURITY.md`
  - `.planning/phases/069-Recursive-Proof/069-VERIFICATION.md`
  </files>
  <action>
Generate the strict documentation packet from persisted artifacts, validate all
cross-digests/paths/pins/parameters/vectors/receipts/measurements/cadence/archive/
snapshot/pruning/history/wallet/atomicity/capacity evidence, and run a
machine-checkable source/claim/coverage
audit. Reconcile all 1319 planning-time atomic obligations plus the 112 stable
numbered obligation-family rows and every required test/artifact;
reject forbidden wording and placeholders. Run final security/spec-to-code/
doublecheck/full-verify/review gates and leave the phase blocked if any evidence,
threat, warning, or unsupported claim remains. Static/promotion/closeout defects
return `BackendClaimUnsupported`, `Plonky3UnauditedPromotion`,
`CanonicalAdmissionAttempt`, or `RecursiveDocumentationIncomplete`; these
claim-only outcomes never substitute for runtime/backend rejection evidence.
The audit MUST additionally prove all of the following:

1. Height 1000 closes canonically after inherited DA readiness while recursive/
   PQ/evidence-publication workers are stopped;
   later evidence attachment cannot change the committed root or certificate.
2. Exact-epoch proof and rolling-history proof are distinct statements; every
   successor verifies its predecessor proof, and every parameter/VK change has
   an accepted `HistoryRotationBridgeV2`.
3. `RetentionTicketV2` is the only deletion authority for immutable persisted
   challenge/proof bodies and its scopes are non-interchangeable. Current state
   is never pruned; the 90-day challenge ring plateaus under the full window/
   history/snapshot/RS/audit scope; Nova bodies separately plateau at 16/2 MiB,
   use independent-PQ-plus-two-certified-epoch normal retirement or permanent-
   gap abandonment, and cannot address challenge/state/recovery/key/anchor
   namespaces. Ticket/disposition/head commits precede physical unlink. Permanent
   anchor/MMR history satisfies the 4 KiB/item and 100 KiB/day caps. The active
   challenge archive is measured RS(10,16) with no full-replica fallback.
4. Wallet receipts/history/backups remain client-owned; receipt inclusion,
   trust-anchor-to-keyset/threshold/rotation and certificate verification,
   signer uniqueness/weight plus the selected scheme's canonical-encoding,
   subgroup/PoP/rogue-key defenses,
   all-shard seed recovery and fresh current-root spending witness, immutable
   encrypt-before-erasure backup, ciphertext-shard reconstruction/decrypt/
   readback, CAS head, last-three-under-`k` GC protection, rollback/fork
   rejection, and offline restore all
   have persisted evidence. Restore without a previously trusted finalized
   anchor is documented as weak-subjectivity/bootstrap, never trustless recovery.
   The companion offline-recipient evidence proves current-unspent seed fallback
   with no mailbox implementation/data and full/spent history only from backup.
   The handoff points pre-expiry delivery, sender disappearance, quorum/readback,
   expiry, and all-provider-loss execution to Phase 071 without claiming success.
5. Canonical, recursive-evidence, and wallet-backup crash matrices each cover
   every durable boundary and recover idempotently without claiming one atomic
   transaction across DB, filesystem, shard nodes, DA, or backup provider. The
   canonical matrix includes touched-shard prepare/quorum receipts, active route
   generation, `CurrentStateShardManifestV2`, head, marker, and outbox/promote.
6. Role traffic reports prove ordinary validators and wallets receive no PP/PK
   and no per-block recursive bundle; verifier roles obtain a generation-pinned
   VK by content-addressed pull/cache, while proof publication follows the
   measured epoch cadence.
7. The generated version registry and scenario evidence classify every
   recursive V1/V2 and config V1/V2/V3
   family, pass every declared tuple, reject the invalid Cartesian product
   before allocation, prove byte-identical ConfigV2 owner correction plus the
   exhaustive atomic ConfigV2-to-ConfigV3 transform, and recover at every
   migration/activation failpoint without fallback/default/alias or old online
   writer.
8. Config/code/docs/evidence parity proves the first active current-state profile
   has 16 complete non-overlapping shards, three distinct-domain replicas/write
   quorum two per shard, no full-state placement, one global root, and COW copy/
   root-verify/ordered-delta-catch-up/CAS resharding through the existing HJMT
   owners. A one-shard/default route, parallel state model, skipped recovery
   shard, or partial route activation blocks closeout.
9. Mailbox handoff parity proves exact Phase-071 semantic ownership, unique
   `ReservedUnreachable` rows, `declared_only`/cap zero, generic finalized-output
   outbox context, no Phase-069 mailbox module/online path/benchmark/scenario,
   request-id/local-route-bucket/`tag16` lookup prohibition, and mailbox-absent
   seed recovery. It separately proves the Phase-071 spec/config contains the
   acyclic digest, ECDH-secret locator, admission, opaque-provider, replication,
   ACK/GC, measurement, privacy, and day-100 gates—without laundering them into
   Phase-069 executable evidence.
  </action>
  <artifacts>
  - Generated documentation packet and complete security/claim/source/coverage/verification evidence.
  - Typed `BackendClaimUnsupported`, `Plonky3UnauditedPromotion`,
    `CanonicalAdmissionAttempt`, and `RecursiveDocumentationIncomplete` evidence.
  - Registry inventory, compatibility/cutover matrix, config parity, and
    version-boundary crash evidence closing `RCP-INV-023`/`RCP-AC-042`.
  - Normal-PQ and capped-outage Nova retention/GC evidence closing
    `RCP-INV-025`/`RCP-AC-047` without borrowing the challenge-window clock.
  </artifacts>
  <tests>
  - `cargo test --release -p z00z_storage --test test_recursive_docs -- --nocapture`
  - `bash scripts/audit/audit_069_recursive_v2.sh`
  - `bash scripts/audit/audit_release_feature_guards.sh`
  - `./.github/skills/z00z-full-verify-gate/scripts/full_verify.sh --max-safe-run`
  - `python3 .planning/phases/069-Recursive-Proof/069-COVERAGE-AUDIT.py`
  - Static claim mutations assert exact `BackendClaimUnsupported`,
    `Plonky3UnauditedPromotion`, `CanonicalAdmissionAttempt`, and
    `RecursiveDocumentationIncomplete`.
  - Documentation audit rejects permanent raw/witness/full-proof defaults,
    proof/PQ-gated height-1000 admission, digest-only history, unbridged
    rotation, fixed-lifetime-security claims or reset inherited soundness loss,
    ticketless/scope-confused deletion, unbounded Nova pending-PQ/body retention,
    Nova unlink-before-retirement-head, false promotion of an abandoned body,
    network-owned wallet history, unverified
    backup head, nonce/key reuse, unauthenticated/downgraded/unbounded backup
    KDF parameters, cross-provider atomicity, and accelerated-time performance
    claims.
  - Mailbox-handoff audit rejects any Phase-069 codec/store/provider/outbox/
    activation/ACK/GC/benchmark/delivery scenario, nonzero or same-generation
    activation, numeric mailbox capacity result, suffix confusion, request-id/
    local-route-bucket/`tag16` network lookup, missing Phase-071 links, or any
    handoff that permits plaintext/permanent/authoritative/listable/public-DHT/
    cross-namespace behavior or suppresses current-unspent seed recovery.
  - Sharding audit rejects one-shard/full-state launch, parallel HJMT, missing
    range/replica/quorum/manifest binding, partial reshard, skipped-shard or
    `tag16`-trusted seed recovery, full-replica archive fallback, invalid
    RS(10,16) reconstruction/placement, backup plaintext/key sharding, head CAS
    before reconstruct/decrypt/readback, and protected generation below `k`.
  - Capacity audit cross-checks configured block/epoch/window constants against
    17,280 blocks/day, 17.28 epochs/day, per-role traffic, retained-window
    plateau, compact permanent-growth caps, checked `2 * 8 = 16` Nova bodies/
    `16 * 131072 = 2097152` bytes, ninth-epoch backpressure, and the measured
    positive hot-recovery cap.
  - Version audit proves zero unclassified family, zero suffix/backend-label or
    fallback dispatch, exact preheader/cap routing, full old/new Cartesian
    rejection, bounded offline migration entry points, and no old writer after
    activation.
  </tests>
  <results>
  - Every mandatory clause/evidence link passes and no unsupported claim survives, or closeout remains blocked explicitly.
  - The client-centric retention, rolling-history, wallet custody, capacity,
    mailbox-handoff, and three-state-machine matrices are complete and mutually consistent.
  - Static defects return `BackendClaimUnsupported`, `Plonky3UnauditedPromotion`,
    `CanonicalAdmissionAttempt`, or `RecursiveDocumentationIncomplete` exactly.
  - Registry/docs/code/scenario parity is exact and all version cutovers recover
    to one complete legal generation.
  </results>
  <implementation_depth>live-claim-removed</implementation_depth>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; on failure stop, fix, rerun it, and only then continue.
  2. Run all targeted audits/tests, `cargo test --release`, and the max-safe full verification command.
  3. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) inline/local in YOLO mode at least three times, fix every issue/warning, and continue until at least two consecutive runs after the final fix have no significant code issues; record each pass path/result in `.planning/phases/069-Recursive-Proof/069-13-SUMMARY.md` and `069-VERIFICATION.md`.
  4. Run a first full `doublecheck` plus `spec-to-code-compliance`, crypto/security/threat-model and Z00Z gates; reconcile findings. Then run the required second `doublecheck` directly against `.planning/phases/069-Recursive-Proof/069-TODO.md` and all plans/coverage to confirm zero missing/duplicate obligations.
  5. If committing, use `/z00z-git-versioning` only.
  </verify>
  <acceptance_criteria>
  - Generated packet and all 1319 atomic links plus 112 stable numbered-family
    links are complete and valid; a TODO edit requires a regenerated passing
    inventory rather than preserving a stale count.
  - Max-safe verification and repeated review stop conditions pass.
  - No authority/PQ/finality/production/storage/retention overclaim or placeholder survives.
  - No permanent raw/full-proof default, proof-gated epoch close, digest-only
    history, unsafe rotation, ticketless deletion, wallet-custody/mailbox-handoff drift,
    cross-provider atomicity claim, one-shard/full-state launch, full-replica
    archive fallback, unsafe reshard/seed recovery/backup sharding, or capacity-
    budget breach survives.
  - `BackendClaimUnsupported`, `Plonky3UnauditedPromotion`,
    `CanonicalAdmissionAttempt`, and `RecursiveDocumentationIncomplete` are exact typed outcomes.
  </acceptance_criteria>
  <blockers>Any uncovered atom, stale pointer, unsupported claim, one-shard/full-state launch, incomplete route/reshard/seed-recovery evidence, missing/stale 069→071 handoff, any reachable or claimed Phase-069 mailbox implementation/activation/benchmark/delivery result, nonzero/same-generation mailbox cap, mailbox-dependent finality or seed recovery, invalid RS placement/reconstruction or full-replica fallback, unsafe/under-`k` backup sharding, unbounded/scope-confused Nova retention, missing ninth-epoch backpressure, unsafe Nova unlink ordering, unclassified/mixed version family, suffix/fallback dispatch, incomplete migration/cutover evidence, retention/history/wallet/atomicity/capacity inconsistency, or failed final gate blocks closeout.</blockers>
  <evidence_gate>Closeout cross-links every atom/claim, current-state sharding/reshard/seed-recovery row, mailbox `ReservedUnreachable`/no-online-path/Phase-071-transfer row, Nova lifecycle/cap/scope/GC row, RS retention row, ciphertext-shard backup row, retention/history/wallet/atomicity/capacity row, and exact `BackendClaimUnsupported`, `Plonky3UnauditedPromotion`, `CanonicalAdmissionAttempt`, `RecursiveDocumentationIncomplete` result to live evidence.</evidence_gate>
  <anti_placeholder_gate>Docs-only assertions, recommendations, static reports, or suppressed warnings fail.</anti_placeholder_gate>
  <not_recommendation_gate>Closeout requires executable evidence and two clean consecutive final reviews/doublechecks.</not_recommendation_gate>
  <done>All 1319 current atomic obligations, all 112 stable numbered-family
  obligations and exact `BackendClaimUnsupported`, `Plonky3UnauditedPromotion`,
  `CanonicalAdmissionAttempt`, `RecursiveDocumentationIncomplete` claim gates
  pass, all 14 plan packets are correctly classified under 13 owner groups, and
  every current-state-sharding/offline-mailbox-handoff/RS-retention/history/wallet-backup/atomicity/
  capacity matrix is complete.</done>
</task>
</tasks>

## Artifacts this phase produces

- New: generated recursive documentation packet, security/source/rejected-claim audit and final verification packet.
- Existing, not new: all proof/storage/simulator evidence generated by earlier plans.

<verification>
- Bootstrap, targeted audits/tests, max-safe full verify, at least three reviews with two consecutive clean after fixes, first and second doublecheck, spec-to-code/security/crypto/design gates.
</verification>

<success_criteria>

- All 13 owner groups and 14 plan packets, all 1319 current atomic obligations,
  and all 112 stable numbered-family obligations have exact evidence; no
  blocker or unsupported claim remains.
</success_criteria>

## Coverage Appendix

| TASK-NNN | PLAN id | source refs | inputs | artifacts | tests | expected results | simulation requirement | anti-placeholder proof | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| N/A — PRD contains no TASK-NNN | `069-13` | entire TODO/context/patterns/coverage/plans | all persisted evidence | docs/security/audit/verification packet | targeted audits + max-safe + repeated reviews | complete honest closure or explicit block | real evidence only; unsupported claim removed | machine coverage and placeholder audits | pending |
