## Conclusion

`069-051-T2` remains complete on its closure source identity
`302bf11cf4516acacaf31e1f8c591c2b21e5f6dbacfeb7f5ec4b3257a0aa79ef`.
The semantic relation is unchanged in active candidate
`1da05771ae22d8da4b8e8693954540f468708be47f25f7dc654a0f7f9df4c4e3`,
whose terminal source-bound evidence is being regenerated; the older identity
is provenance, not current whole-plan acceptance.
The one private Nova owner proves the complete semantic relation, consumes the
one bounded HJMT segment grammar, verifies hierarchy operations incrementally,
and uses the authority-selected fixed production profile: 1 MiB segments, one
HJMT worker, 2 MiB result-byte admission, a separate 64 MiB input/snapshot
reservation, one prover, SHA width `k=1`, a 1 GiB single-entry private PP/PK
cache, and deterministic replay recovery. There is no production resident
fallback, runtime selector, second envelope codec, second HJMT owner, or
parallel accumulator path.

Authority accepted DC2-F12/F23/F24, the exact conditional A-17 premises, and
the numeric tuple under decision reference
`phase-069-t2-interactive-authority-2026-07-20`. The operational verifier-
attempt bound is `q_V=1,048,576`, the polynomial-depth/cumulative-step bound is
`N=4,294,967,296`, and the
challenge window is 1,555,200 five-second blocks (90 days), explicitly distinct
from later Nova proof retention. Final proof/RSS values and the version-managed
commit/tag are recorded in the retained evidence and `069-051-BENCHMARKS.md`.
T3 previously passed on one continuous 1/3/5-block accumulator and must now be
reproduced for the active candidate; T4 and the plan summary own final closure.

### Historical T2 proof and verifier evidence

The cache-bypassing release harness passed on the exact T2-close identities.
Its original raw path was removed by the required old-output cleanup; the
historical hashes and measurements below remain provenance only. Current raw
evidence is retained under `outputs/checkpoint/069-051/final/`.
Its measurement-bundle SHA-256 is
`465fe1322894af2ecc49d084932b5a20a462b6a92b2e6c8150124fb032a82136`.
All 1,727 folds, accumulator verification, compression, keyless envelope,
clean verifier-only child, exact endpoints and independently recomputed Model C
passed. The full invocation took 2,317.436 s at 6,564,720,640 B worker peak
RSS; Model C took 1,075.488 s and rejected the changed target. The compressed
proof is 122,288 B, the envelope 342,353 B, and the authority-distributed
verifier bundle 47,008,185 B. Clean verification took 29.340 s with
3,056,861,184 B sampled VmRSS and kernel peak VmHWM, below the active 4 GiB
cap by 1,238,106,112 B.

The final-source validation sequence passed: bootstrap first at
`3:09.07`/`3,073,912 KiB`; semantic R1CS 36/36 at
`1:09:45`/`6,565,988 KiB`; TestCS 1,727/1,727 at
`4:23.55`/`5,039,244 KiB`; artifacts 3/3 at
`7:48.43`/`7,884,496 KiB`; all-target workspace build at
`2:16.60`/`3,502,916 KiB`; and full workspace release tests at
`30:44.10`/`4,167,768 KiB`. Two clean-clone-only planning dependencies were
then removed without weakening their guards. Patch release `v1.14.1` at
`37ece6c797d3807283eaea611252e657e10faad2` passed bootstrap first
(`2:55.62`/`3,062,652 KiB`), curated (`1:41.07`/`3,054,092 KiB`), all-target
build (`2:21.51`/`3,365,040 KiB`) and 350 workspace result blocks with zero
failures (`44:37.30`/`4,076,048 KiB`) in a clean detached clone.

Review pass 1 fixed streaming retention, hierarchy-map lifetime, cache
corruption/symlink/total-cap handling and project-owned buffer zeroization.
Pass 2 corrected one stale harness comment. Final-source passes 7–9 were clean,
and the post-tag fixture review ended with two consecutive significant-clean
passes. Doublechecks 5–6 and the two post-fix doublechecks passed the
theorem/constraint/stream/concurrency audit; Doublecheck 2 passed genuine
Model-C rejection plus Model A/B, cache poison/rollback, recovery/cutover and
secret-lifecycle falsification. No new remote timing oracle was found: new
branches depend on public framing or local witness control, and no constant-
time claim is made for dependency proof generation.

## Live reconciliation (final T2 closure 2026-07-20)

| Obligation | Live status |
| --- | --- |
| SourceMemoryWrite → TraceChunk → FIPS SHA | complete; endpoint-free Poseidon path remains deleted |
| Full uniqueness and semantic Net | complete in native/R1CS; Models A/B/C reject |
| Net mutation → terminal JMT operation | complete with two products over the exact 132-byte row plus exact count |
| Six-case JMT and hierarchy/key/root induction | complete in the sole micro-op path and incremental hierarchy verifier |
| Settlement roots and four typed commitments | complete and `X_h`-bound |
| `X_h`/prior IVC/exact successor | complete in the typed public-state/envelope path |
| Bounded HJMT streaming | complete: versioned 1 MiB segments, strict ordinals/digests/caps, one private 0600 spool, immediate per-operation verification/drop |
| Hierarchy-aware scheduling/backpressure | complete: canonical level order, one selected HJMT worker, 2 MiB result admission and separate 64 MiB input/snapshot reservation |
| PP/PK setup cache and recovery boundary | complete: identity-keyed atomic 0700/0600 single-entry cache plus deterministic replay; cache stores no proof/verdict/VK-distribution state |
| Authority decisions | accepted: F12/F23/F24, conditional A-17, selected tuple, `q_V`, `N`, cache/recovery/cancellation bounds |
| Final proof/artifacts/reviews | complete on the identities and exact measurements recorded in the final evidence section |
| T2/T3/T4 | T2 complete; T3 complete on the continuous 1/3/5-block accumulator; T4 final-source release/profile closeout active |

### Final threat/evidence mapping

| Threat | Final control and evidence |
| --- | --- |
| Malicious segment omission/duplication/reorder/substitution | Version/context/height/root/tree/ordinal/count/length and per-segment/whole-stream digests reject before Nova; exact-cap and mutation corpus passes. |
| Cross-level or unused-child promotion | Terminal → bucket → serial → definition barriers plus exact used-child consumption; maps are cleared only after their parent barrier. |
| Resource exhaustion/oversubscription | Allocation is preceded by 1 MiB segment, 2 MiB result, 64 MiB separate input/snapshot, one-worker, one-prover and 1 GiB cache checks. |
| Partial spool/cache write or cancellation | Private 0700/0600 temp files, bounded writes, sync plus no-clobber atomic seal, strict reopen and controlled cleanup corpus. |
| Cache poison/mix/rollback | Full authority/source/profile/spec/grammar/shape/lock/manifest/`k` identity, canonical re-encode, exclusive lock, one-entry pruning and corruption/symlink regeneration tests. |
| Recovery replay/stale predecessor | Deterministic replay of the sealed context/digest/next height only; stale height/generation/source/prior state rejects and no verdict/accumulator image is stored. |
| Project-owned secret retention | Segment/value/key/leaf/internal/sibling buffers zeroize on success and early return; dependency hard-kill allocation zeroization remains the accepted F24 non-claim. |
| Timing disclosure | New branches depend on canonical/public framing or local witness control; no remote timing oracle or constant-time claim for dependency proof generation is introduced. |

## 📋 Canonical A–Z closure checklist

This table is the controlling closure view. `DONE/FROZEN` means the implementation
and retained evidence exist and the item must not be reopened merely because a
later operational task is unfinished. `BASELINE PASS` means the work is
implemented but its evidence must be regenerated after the final production
diff. `PARTIAL` and `OPEN` are T2 work. `EXTERNAL` requires an authority record
and cannot be checked by code. Detailed numbered items below are normative and
cannot weaken this table.

| ID | Gate | Live status | Exact closure condition |
| --- | --- | --- | --- |
| A | Scope, threat model, and authority boundaries | **COMPLETE** | Final mapping covers malicious prover, partial/reordered segments, resource exhaustion, crash/cancel, cache poison/rollback, replay and secret cleanup without widening public API. |
| B | One canonical module and owner | **COMPLETE** | `z00z_storage::checkpoint::nova` is the sole circuit/bundle owner; no recursive V1, nested `recursive_v2::nova`, public Nova type, duplicate envelope, runtime backend selector, or second HJMT walker exists. |
| C | SourceMemoryWrite → TraceChunk → FIPS SHA | **COMPLETE** | One stack-local canonical chunk feeds direct byte equality and both SHA contexts; endpoint-free Poseidon memory remains deleted. |
| D | Full uniqueness | **COMPLETE** | Original/sorted rows, strict global/set order, four list commitments, four SHA-derived challenges, two product pairs per set, exact counts and disjointness are native/R1CS-bound. |
| E | Semantic Net | **COMPLETE** | Delete/insert/replace/unchanged/close consume the exact authenticated rows; effects, counts and precommit/challenges are constrained. |
| F | Per-tree JMT relation | **COMPLETE** | All six mutation cases, old/new leaf/value bytes, siblings, split/coalesce rules, raw-SHA parents and per-operation old-root continuity are constrained. |
| G | HJMT hierarchy induction | **COMPLETE** | Terminal → bucket → serial → definition → optional path-index order, child-root promotion, exact keys, one consumer per child and unused-child rejection are constrained. |
| H | Settlement roots and commitments | **COMPLETE** | Pre/post SettlementV2 roots and four typed checkpoint commitments bind the same authenticated transition. |
| I | `X_h`, prior IVC, and exact successor | **COMPLETE** | Complete public identity, predecessor, prior accumulator and independently derived final `z_N` are compared limb-for-limb. |
| J | Fixed shape, counters, and finalization | **COMPLETE** | Uniform shape, exact opcode/counter accounting, inactive-lane zeroing and `FinalizeBlock` as the only `done=1` transition are proved. |
| K | Mutation ledger and Models A/B/C | **COMPLETE** | All security-critical mutants are killed; recomputed Model C reaches the unchanged verifier and all-limb target comparator. |
| L | Current proof and strict artifacts | **COMPLETE** | Final-source 1,727-step proof, strict PP/PK/VK corpus, clean verifier and artifact/source-binding workers pass. |
| M | Native HJMT segmented stream | **COMPLETE** | Production retains one capped segment and compact hierarchy summaries; the resident path is test-oracle-only. |
| N | Deterministic HJMT segment grammar | **COMPLETE** | Versioned identity/context/ordinal/root/digest framing rejects missing, duplicate, reordered, trailing, oversized and cross-generation input. |
| O | Streaming native hierarchy verifier | **COMPLETE** | Operations verify and drop on arrival; child maps clear at their exact parent barriers and exact consumption is enforced. |
| P | Hierarchy-aware parallel scheduler | **COMPLETE** | Canonical level order and bounded ordered delivery are fixed; the selected production thread cap is one. |
| Q | Byte-weighted backpressure and resource reservations | **COMPLETE** | 2 MiB results and separate 64 MiB input/snapshot reservations are checked before allocation and released on every controlled exit. |
| R | Crash, cancellation, and secret lifecycle for streaming | **COMPLETE** | Partial spool/cache/cancel corpus, atomic sealing, 0700/0600 permissions and project-owned buffer zeroization pass. |
| S | Identity-bound PP/PK setup cache | **COMPLETE** | The sole 1 GiB private single-entry cache validates its full identity, cap and canonical encoding under an exclusive lock; corruption/mix/rollback regenerate safely. |
| T | Recovery-compatible Nova fold boundary | **COMPLETE** | Deterministic replay from sealed canonical segments is the sole recovery choice; no acceptance/verdict or accumulator image is cached. |
| U | Production concurrency/fanout policy | **COMPLETE** | One sequential accumulator, one prover and one HJMT worker are authority-bound; parallel folds and oversubscription selectors are absent. |
| V | Full-cap candidate benchmarks | **COMPLETE** | Finite 1/4/8 MiB and 1/2/4-thread candidates were measured; exactly the 1 MiB/one-worker/`k=1` tuple was selected. |
| W | F12/F23/F24 and A-17 decisions | **COMPLETE** | Authority accepted all three residuals and conditional A-17 under the recorded EAGM/GZT, Pallas/Vesta DL and separate compression premises. |
| X | Numeric authority tuple and candidate selection | **COMPLETE** | Generation, identities, selected caps, `q_V`, `N`, challenge window, cache, recovery and concurrency are bound by the active decision record. |
| Y | Final release, review, and Git reproducibility | **COMPLETE** | Final proof/Model C, bootstrap-first release gates, three reviews ending two clean, two doublechecks, scoped versioning and clean-clone verification pass. |
| Z | Close T2 and unlock T3 | **COMPLETE** | A–Y and GAP-01…20 are closed, evidence surfaces are synchronized and T3 alone is opened next. |

Current A–Z status count: `26 COMPLETE`, `0 OPEN`. The detailed ledger has all
`T2-GAP-01…20` and `T2-CLOSE-21` closed on one final identity. Historical
baseline text below is retained as chronology and cannot override this closure.

**Production boundary retained:** live Celestia publication, publication cadence,
receipts, retention/GC, and continuous per-block runner activation remain T3 or
later integration work. T2 now owns the stable streaming, cache, recovery
identity, concurrency and resource-control primitives those phases must reuse;
later plans may integrate them but must not introduce a second wire format,
state identity, scheduler, cache, or recovery path. None of A–Z may set
`CheckpointProofSystem::VERIFIED`; that remains gated by T3/T4/promotion.

### Non-negotiable production invariants

- **REQ-T2-001 — One representation:** one canonical HJMT segment grammar is
  the only native/evaluator/Nova source; no digest-only, native-verdict,
  resident fallback or alternate event tape may accept.
- **REQ-T2-002 — HJMT, not flat JMT:** the scheduler and verifier preserve the
  exact terminal → bucket → serial → definition → optional path-index DAG. A
  parent job cannot start before the relevant child level is verified/sealed.
- **REQ-T2-003 — Determinism under concurrency:** thread count, timing,
  cancellation points and worker completion order cannot change canonical
  segment bytes, source/global SHA, update-trace digest, roots, event count,
  `z_N` or verifier decision.
- **REQ-T2-004 — Sequential Nova chain:** one accumulator for one chain is
  folded strictly by event ordinal. Parallelism is restricted to disjoint HJMT
  jobs inside one level or separately authority-admitted proof workers.
- **REQ-T2-005 — Bound before allocation:** every length, count, segment,
  queue, spool, cache and artifact allocation is checked against an identity-
  bound numeric cap before reserve/read/decode/dispatch.
- **REQ-T2-006 — Fixed production choice:** segment cap, HJMT thread cap,
  in-flight byte cap, prover concurrency, recovery model and SHA width `k` have
  exactly one production value after selection; no runtime experimentation
  selector remains.
- **SEC-T2-001 — Malicious prover:** missing, duplicated, reordered, truncated,
  trailing, cross-level, cross-tree, cross-height, cross-generation and
  wrong-root segments reject before influencing accepted final state.
- **SEC-T2-002 — Cache poisoning/rollback:** PP/PK or recovery material cannot
  cross authority generation, source/profile/spec/grammar/shape/`k`, height,
  predecessor, source-prefix or prior-IVC identity; cache data is never proof
  acceptance evidence.
- **SEC-T2-003 — Crash consistency:** an interrupted segment/cache/recovery
  write is never loadable; live settlement state is complete-old or
  complete-new; retry is exactly once; a post-commit error cannot fabricate a
  source for a different root.
- **SEC-T2-004 — Secret lifecycle:** project-owned prior/new values, witness
  records, segments and accumulator recovery material are private, non-debug,
  non-telemetry, permission-restricted and zeroized whenever execution retains
  control. Dependency-owned hard-kill zeroization remains a non-claim.
- **SEC-T2-005 — Cryptographic continuity:** streaming/concurrency/cache work
  must not weaken uniqueness, Net, six-case JMT, HJMT promotion, root,
  commitment, `X_h`, prior-IVC, successor, transcript, entropy or A-17 premises.
- **OPS-T2-001 — Backpressure:** queue admission reserves CPU/memory/disk budget;
  exhaustion produces a typed fail-closed result, never allocator-driven abort,
  unbounded blocking or silent work drop.
- **OPS-T2-002 — Single-flight ownership:** locks are generation/source-bound,
  have PID/start-time or equivalent stale-owner protection, and are released by
  controlled cleanup; hard-kill cleanup is performed and verified by the
  parent/restart path.
- **OPS-T2-003 — Measurement honesty:** authority evidence is release-only,
  cache-bypassing where setup cost is measured, source/host/config-bound and
  reports RSS plus address space, not one metric relabelled as the other.
- **OPS-T2-004 — Later-phase reuse:** T3 may activate continuous folding and
  T3/Plans 06/09/10/12 may publish/recover/retain, but must consume these exact
  identities and formats without a compatibility lane.

### Security/failure findings that the open work must eliminate

| Severity | Risk | Required disposition |
| --- | --- | --- |
| **S1** | Cross-level parallelism can promote a non-final or wrong child root into a parent HJMT leaf. | Hard level barriers plus exact child-consumption proof; no speculative parent commit. |
| **S1** | Two production resident/streaming codecs can diverge while both appear valid. | Resident path is an equivalence oracle only and is removed/unreachable before closure. |
| **S1** | Out-of-order result emission changes the authenticated trace while preserving individually valid JMT proofs. | Preassigned canonical ordinals and one ordered sink; skew tests compare exact bytes/digests/endpoints. |
| **S2** | Unbounded HJMT/prover fanout converts valid workloads into memory/address-space DoS. | Byte-weighted reservations, queue caps, authority concurrency and fail-before-allocation. |
| **S2** | PP/PK or folding cache mix/rollback can reuse material for the wrong relation or predecessor. | Full identity key, atomic private storage, monotonic generation/height and strict load/re-encode/revalidation. |
| **S2** | Partial spool/cache/recovery files can leak witness bytes or resume an unauthenticated prefix. | Atomic sealing, permissions, AEAD where witness-bearing, digest/footer verification, cleanup and crash corpus. |

No new primitive or change to the cryptographic statement is selected here.
The plan is **Risky but salvageable** until S1 rows are eliminated by GAP-01…05;
after their negative/equivalence tests pass, the design may be classified
`Safe enough` subject to external authority limits and A-17 acceptance.

## Detailed open work items

This is the exhaustive production-first T2 closure checklist as of 2026-07-20.
A checked implementation item stays checked unless the final diff changes its
owner/relation or a required test disproves it. A retained baseline proof does
not need semantic reimplementation, but it must be regenerated after the open
identity-bearing work. No item may be closed from a debug run, cache hit,
diagnostic cap, native-only assertion, or unsigned authority proposal.

### Completed and frozen implementation (do not redo)

- [x] **T2-DONE-01 — One canonical Nova owner and path.** The owner is
  `crates/z00z_storage/src/checkpoint/nova.rs`; the former nested owner is
  deleted; public callers receive no Nova dependency type; no V1 selector,
  compatibility shim, second circuit, or runtime SHA-width selector exists.
- [x] **T2-DONE-02 — Authenticated source and SHA relation.** The single
  `SourceMemoryWrite`/`TraceChunk` path proves exact bytes, metadata, ordinal,
  zero tail, FIPS padding/chaining and source/global endpoints. The former
  Poseidon root/frontier path is deleted.
- [x] **T2-DONE-03 — Full uniqueness.** Original/sorted codecs, adjacency to
  replay, order/disjointness, commitments, SHA-derived challenges, product
  pairs and exact cardinalities are constrained and mutation-tested.
- [x] **T2-DONE-04 — Semantic Net and Net→JMT.** Exact effect rows,
  unchanged/change semantics, close anchors, mutation count and two product
  pairs over the 132-byte terminal-operation row are constrained.
- [x] **T2-DONE-05 — Six-case JMT relation.** Insert/update/split and all three
  delete cases, value/leaf/parent hashes, siblings, old/new roots and operation
  chaining are constrained.
- [x] **T2-DONE-06 — HJMT hierarchy and SettlementV2 roots.** Terminal,
  bucket, serial, definition and path-index stages, exact promotion keys,
  child-root consumption and settlement-root derivation are constrained.
- [x] **T2-DONE-07 — Typed commitments and endpoint theorem.** Four ordered
  commitments, `X_h`, prior IVC, exact final successor, counters, mutation
  ledger and Models A/B/C are implemented.
- [x] **T2-DONE-08 — Strict artifact formats.** Private PP/PK material,
  authority-distributed VK bundle and keyless proof envelope have capped,
  canonical, identity-bound codecs and invalid-key rejection. Their final
  bytes must be regenerated, not redesigned.
- [x] **T2-DONE-09 — Current baseline proof.** The retained 1,727-step
  complete-relation proof, clean verifier, Model C and artifact corpus pass for
  source revision `d7980118…fc06`. This is comparison evidence only after the
  production-first refactor.
- [x] **T2-DONE-10 — Current owned-boundary durability corpus.** Five abrupt
  cutover seams reopen to complete old or complete new state with exactly-once
  retry. Only redb-internal equivalence acceptance remains external.
- [x] **T2-DONE-11 — Current project-owned secret corpus.** Six process
  outcomes, logging/telemetry scans, core policy, private permissions and
  project-owned zeroization pass. Extend the same owner to new segment buffers;
  do not recreate a second harness.
- [x] **T2-DONE-12 — Workspace owner repairs.** The wallet 79-row golden and
  egui archive provenance have single inline owners; legacy paths are forbidden;
  focused guards and the all-target release build pass.

### Production-first implementation gaps

- [x] **T2-GAP-01 — Replace the resident HJMT envelope with one bounded
  segmented source.**
  - **Owners:** `settlement/proof_batch.rs::{JmtUpdateTraceV2,
    SettlementUpdateTraceEnvelopeV2, visit_circuit_micro_operations}` and
    `settlement/hjmt_store.rs::commit_snap_with_update_trace`.
  - **Implementation:** introduce one private versioned segment grammar and one
    visitor/reader API. Split a large per-tree operation batch deterministically
    before proof/value allocation can exceed the selected segment cap. Each
    segment binds transition identity, level, canonical tree role/id, segment
    ordinal/count, operation ordinal/count, old/new version/root, exact byte
    length and digest. The global stream binds total segments/updates/
    operations/bytes and the existing trace digest.
  - **Single-path rule:** the old resident `Vec<JmtUpdateTraceV2>` path may be
    used only by an equivalence test during migration. Delete or compile it out
    of production after equivalence; do not add a runtime selector or second
    envelope type exposed outside storage.
  - **Done:** maximum resident proof/value payload is one authority-selected
    segment plus fixed parser state; oversized length/count is rejected before
    allocation; canonical bytes and final HJMT roots match the baseline oracle
    across all six JMT cases and multi-operation chains.
  - **Estimate:** `8–14 engineer-hours`.

- [x] **T2-GAP-02 — Stream HJMT commits in dependency order without retaining
  all `TreeOut`/trace payloads.**
  - **Owners:** `settlement/hjmt_commit.rs::{commit_hjmt_plan_at_with_update_traces,
    commit_terminal_batches, commit_bucket_batches, commit_serial_batches,
    commit_definition_batch, commit_tree_jobs}`.
  - **Implementation:** preserve the exact level DAG `Terminal → Bucket →
    Serial → Definition → PathIndex`. Assign canonical job ordinals before
    dispatch. Stream each completed tree trace to the sole ordered segment sink;
    update `next_roots`; retain only compact root transitions. A level barrier
    must complete, verify and seal before parent jobs are constructed. Preserve
    the existing isolated preflight-before-live-commit safety rule; do not emit
    fallible source bytes after durable state advances.
  - **Done:** peak trace memory is independent of total envelope bytes; every
    child root is consumed exactly once by the proper parent level; failure at
    any barrier restores the complete pre-state; output is byte-identical under
    adversarial worker completion order.
  - **Estimate:** `6–10 engineer-hours`.

- [x] **T2-GAP-03 — Replace full-envelope native hierarchy reconstruction with
  a bounded streaming verifier.**
  - **Owners:** `settlement/proof_batch.rs::{SettlementUpdateTraceCircuitDecoderV2,
    verify_hierarchy_semantics}` and
    `checkpoint/recursive_predicate.rs::verify_jmt_envelope`.
  - **Implementation:** verify each segment/update/operation on arrival; carry
    only current JMT root state and bounded maps of `(tree id, old root, new
    root, consumed)` for the active child level. At each barrier, require exact
    counts and complete child consumption, then erase/drop the prior level.
    Bind the final definition transition to the immutable pre-definition root
    and derived pre/post SettlementV2 roots.
  - **Done:** native verification rejects missing/duplicate/reordered/trailing/
    cross-level/cross-generation segments and reaches exactly the same final
    roots/counts/digests as the current verifier without storing proof/value
    payloads from completed updates.
  - **Estimate:** `4–8 engineer-hours`.

- [x] **T2-GAP-04 — Feed the same sealed segment stream to the canonical trace
  source and Nova without a second byte representation.**
  - **Owners:** `checkpoint/canonical_transition.rs::{from_exec,
    canonical_events}`, `checkpoint/recursive_trace.rs`, and the existing
    `checkpoint/nova.rs::synthesize_jmt_hierarchy_payload` owner.
  - **Implementation:** emit the existing 39-byte JMT header and existing
    bounded `JmtMicroOp` records directly from the sealed segment reader. Keep
    the R1CS grammar and O(1) circuit state unless equivalence tests prove an
    unavoidable version change. The exact same records must feed source/global
    SHA, native evaluator and Nova folds.
  - **Done:** one canonical stream and one digest owner remain; no opaque
    envelope body, alternate event tape, native-verdict shortcut or changed
    theorem endpoint exists; complete TestCS and all prior mutants pass.
  - **Estimate:** `3–6 engineer-hours`.

- [x] **T2-GAP-05 — Make HJMT concurrency deterministic and byte-bounded.**
  - **Owners:** `settlement/hjmt_scheduler.rs::ForestScheduler` and the sole
    HJMT commit owner.
  - **Implementation:** retain Rayon parallelism only for disjoint trees inside
    one level. Add a byte-weighted reservation/semaphore covering input snapshot,
    proof/value segment, pending ordered result and spool write. Cap CPU threads,
    in-flight jobs, queued jobs, buffered result bytes and total spool bytes.
    Results commit by canonical ordinal, never completion order. Parallel work
    must stop at each hierarchy barrier. One Nova accumulator remains strictly
    sequential; parallel `prove_step` calls for one chain are forbidden.
  - **Done:** seeded scheduling-skew tests produce identical segment bytes,
    trace digest, roots, `z_N`, proof acceptance and cleanup for thread counts
    in the finite benchmark set; oversubscription fails before allocation; no
    deadlock occurs when the first ordinal is the slowest job.
  - **Estimate:** `5–10 engineer-hours`.

- [x] **T2-GAP-06 — Close streaming crash/cancellation/secret lifecycle.**
  - **Owners:** extend the existing F23/F24 child-process owners; do not create
    parallel harnesses.
  - **Implementation:** inject termination before segment creation, during
    write, before/after seal, while producer or ordered consumer is blocked,
    at every level barrier, before live commit, after live commit/before
    readback and during cache write. Use private directories/files, atomic
    temp→rename publication, exact generation/digest manifests, fail-closed
    core/non-dumpable policy and zeroization for project-owned value/segment
    buffers. Dependency-owned hard-kill zeroization remains a named non-claim.
  - **Done:** reopen observes only complete old or complete new durable state;
    partial segments/cache entries never load; no canary enters logs, core or
    artifacts; all byte reservations/locks/temp files are released on every
    controlled exit and parent cleanup after hard kill.
  - **Estimate:** `3–6 engineer-hours`.

- [x] **T2-GAP-07 — Implement the sole safe PP/PK setup cache.**
  - **Owner:** private code under `checkpoint/nova.rs` using the existing
    `NovaProverMaterialV2` canonical codec; no public cache type or second
    material codec.
  - **Identity key:** authority generation, suite/backend revision, Cargo
    feature and lock digests, source revision, predicate/profile/spec/grammar/
    shape digests, selected `k`, PP generation and exact payload digests.
  - **Storage:** private `0700/0600`, capped decode before allocation,
    canonical re-encode, digest verification, atomic write, single-writer lock,
    stale-generation and rollback rejection. Cache only PP/PK material; never
    cache proof verdicts, verifier acceptance, source/witness bytes or
    authority approval. Corruption is a miss plus quarantine/delete, never a
    fallback acceptance.
  - **Done:** cold generate/store/load, warm hit, interrupted write, truncation,
    bit flip, swapped PP/PK, wrong `k`, wrong source/profile/generation, replay,
    permission and concurrent-writer tests pass. All acceptance benchmarks
    explicitly bypass cache; separate production latency reports show cold and
    warm modes.
  - **Estimate:** `5–8 engineer-hours`.

- [x] **T2-GAP-08 — Freeze one recovery-compatible Nova folding boundary so
  T3 does not redesign it.**
  - **Owner:** private `checkpoint/nova.rs` types adjacent to
    `NovaCompressedSnapshotV2`; no second proof wrapper.
  - **Decision rule:** prefer deterministic replay from sealed canonical
    segments. Persist an accumulator blob only if the pinned Nova type provides
    a bounded canonical serialization whose full identity, initial/current
    public state, step ordinal, source-prefix digest and prior-IVC binding can
    be independently revalidated before reuse. Select exactly one production
    model; delete the other production path.
  - **Security:** AEAD-protect any witness-bearing accumulator payload with
    key/version/domain separation and rollback-resistant generation/height;
    atomic private writes; no cached verdict; restart always verifies the
    recovered prefix and final proof normally. If canonical accumulator restore
    cannot be proved, record deterministic replay as the sole recovery model.
  - **Done:** roundtrip/restart, stale height, wrong predecessor, wrong source
    prefix, wrong authority generation, rollback, corruption, truncation,
    cancellation and deterministic replay-equivalence tests pass. T3 may call
    this boundary but may not introduce a new format.
  - **Estimate:** `5–10 engineer-hours`.

- [x] **T2-GAP-09 — Measure and select the complete production resource
  profile.**
  - **Candidates:** authority supplies finite candidate sets for segment bytes,
    HJMT worker threads, in-flight byte budget, independent prover concurrency
    and SHA width `k`. Repository measurements may recommend values but may not
    activate them.
  - **Repository measurement matrix when authority has not supplied a narrower
    set:** segment cap `{1, 4, 8 MiB}`; HJMT worker threads `{1, 2, 4}`;
    independent prover concurrency `{1}`; SHA width `{k=1}`; in-flight result
    budget `2 × worker_threads × segment_cap` with a separately capped input/
    snapshot reservation. Authority activated exactly the 1 MiB/one-worker/
    2 MiB-results/separate-64 MiB-input/one-prover/`k=1` tuple; a candidate that
    cannot hold one maximum canonical operation rejects rather than silently
    enlarging itself.
  - **Modes:** resident oracle versus streaming; minimum/typical/full-cap HJMT;
    1 and multiple disjoint trees per hierarchy level; cold/warm PP/PK cache;
    cache-bypassing fresh setup; proof, compression, clean verifier, restart and
    cancellation under contention.
  - **Capture:** exact source/host/config identities, elapsed and CPU time,
    per-stage latency, throughput, queue wait, VmRSS/VmHWM/address space, disk
    read/write/temp peak, segment/cache/artifact sizes, cleanup and exit codes.
  - **Done:** one candidate satisfies the signed tuple with safety headroom;
    exactly one segment cap/thread cap/byte budget/prover concurrency/`k` is
    production-reachable; resident oracle and every unselected selector are
    removed from production. Update `RecursiveCircuitProfileV2` resident
    accounting from complete-envelope bytes to the exact selected streaming
    equation.
  - **Estimate:** `4–8 elapsed/engineer-hours` after implementation.

### External decisions and final closure

- [x] **T2-GAP-10 — Obtain final DC2-F12 authority acceptance.** Replace the
  historical `69,337,178 B` resident-envelope proposal with the selected
  streaming equation and full-cap measured VmRSS/VmHWM/address-space evidence.
  Authority must approve or reject exact segment, scheduler, cache, prover and
  concurrency limits. If rejected, execute the signed no-candidate action;
  implementation must not silently enlarge a cap. Estimate: `0.5–1 hour` to
  record a supplied decision; external response time is unbounded.

- [x] **T2-GAP-11 — Obtain DC2-F23 owned-boundary equivalence acceptance.** The
  existing five-seam corpus is complete; add the streaming/cache seams from
  GAP-06 and obtain an explicit accept/reject decision for the redb
  library-boundary equivalence residual. No internal fsync hook may be claimed
  unless the dependency actually exposes and tests it. Estimate: `0.5–1 hour`
  after the extended corpus passes.

- [x] **T2-GAP-12 — Complete project-owned DC2-F24 code work.** The existing
  success/failure/panic/timeout/cancel/hard-kill corpus, logging scans,
  permissions and core policy pass. GAP-06 extends the same corpus to new
  streaming/cache buffers; dependency-owned Nova allocation zeroization remains
  external and must not be represented as project-owned work.

- [x] **T2-GAP-13 — Obtain F24 residual and conditional A-17 authority
  decisions.** Require authority identity, decision reference,
  signature/attestation and timestamp. A-17 may be approved only conditionally
  with explicit EAGM/GZT/compression and Pallas/Vesta discrete-log premises;
  no document may claim unconditional cumulative 128-bit IVC security.
  Estimate: `0.5–1 hour` to bind supplied decisions; external response time is
  unbounded.

- [x] **T2-GAP-14 — Obtain the complete numeric authority tuple.** Bind the
  final source/profile/spec/grammar/shape/material/bundle/cache/recovery
  identities; finite candidate sets; selected segment/thread/in-flight-byte/
  prover-concurrency/`k`; operational `q_V`; maximum cumulative steps `N`;
  setup/fold/compression/verifier/native-evaluator latency and RSS; all artifact,
  spool, cache and recovery sizes; queue/fanout/cancellation/hard-kill/restart/
  cold-load policies; activation interval; and no-candidate decision. Every
  canonical `AUTHORITY_REQUIRED_*` field must be filled and signed. Estimate:
  `0.5–1 hour` to validate a supplied tuple; external response is unbounded.

- [x] **T2-GAP-15 — Freeze exactly one `k` and all operational candidates.**
  Evaluate every authority-permitted `k` with cache bypass. Bind the selected
  `k` and the selected streaming/concurrency tuple into profile/spec/material/
  bundle/cache/recovery identities. Remove all runtime selectors and make
  unselected candidates test-only or unreachable. If none passes, execute the
  signed no-candidate rule and keep T2/T3 blocked. Estimate: `1–2 hours` for
  already-supported `k=1`; add `1.5–3 hours` per additional `k`.

- [x] **T2-GAP-16 — Regenerate final artifacts, proof and adversarial evidence.**
  Generate authoritative PP/PK/VK/bundle identities, a fresh cache-bypassing
  complete mixed proof, uncompressed verification, compression, clean VK-only
  verification, exact endpoints, keyless envelope and independently recomputed
  Model C. Rerun strict invalid-key/material/cache/recovery/segment parsers and
  every mutation. Record exact timings, RSS and sizes. The retained
  `d7980118…fc06` proof is baseline only. Estimate: `2–4 elapsed hours`.

- [x] **T2-GAP-17 — Synchronize every evidence and authority surface.** Update
  T1 DC2, theorem/code, mutation and benchmark ledgers; Plan live status;
  `STATE.md`; `ROADMAP.md`; interim report; T2 report; authority and A-17
  packets; coverage inputs; and versioning manifest to the exact final digests.
  Bind the scoped code manifest to exactly 35 tracked paths, five new paths and
  the version-manager-owned `versions.yaml`; retain planning/evidence as
  local-only because `commit_docs=false`, and replace stale
  `GAP-01…08/CLOSE-09` references with this checklist. Estimate: `1–2 hours`.

- [x] **T2-GAP-18 — Run the unchanged-final-tree release gate.** First run
  `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` as the
  mandatory fail-fast gate. If it fails, stop, fix and restart final-tree
  convergence. Then run focused T1/T2/streaming/concurrency/cache/recovery
  release suites, curated Nova milestone, full `cargo build --workspace
  --all-targets --release`, full `cargo test --release`, complete mixed
  TestCS/proof/Model C and all crypto/security/spec/dependency/canonical-path
  scans. All Rust executions are release mode. Capture commands, exit codes,
  source digests, timings, RSS, artifacts and cleanup. Estimate: `2–4 elapsed
  hours` without defects.

- [x] **T2-GAP-19 — Obtain final review convergence.** On the unchanged tree,
  run `/.github/prompts/gsd-review-tasks-execution.prompt.md`
  (`/GSD-Review-Tasks-Execution`) at least three times in YOLO mode, fix every
  issue and warning, and continue until at least two consecutive runs have no
  significant issue. Any substantive fix resets GAP-18 and review convergence.
  Then run two independent `doublecheck` passes: theorem/constraint/streaming/
  concurrency coverage and Model A/B/C/cache/recovery falsification. Estimate:
  `2–4 hours` without findings.

- [x] **T2-GAP-20 — Make the exact passing tree reproducible.** Refresh the
  scoped manifest, stage only the authorized Phase-051 paths, and never use
  `--stage-all` in the unrelated dirty tree. Use `/z00z-git-versioning` only
  after explicit release-bump/tag/push authorization and only in an isolated
  worktree because the version manager overwrites `versions.yaml.bak`. Verify
  all manifest paths/exclusions, size caps and the owner move; perform real
  versioning; then rerun bootstrap, curated and broad release gates from a clean
  clone of the resulting commit/tag. Estimate: `2–4 elapsed hours` plus
  authorization time.

- [x] **T2-CLOSE-21 — Close T2 and unlock T3.** Require A–Y and GAP-01…20 all
  checked with final-source, signed-external and clean-clone evidence. Mark T2
  complete in Plan/STATE/ROADMAP/reports, replace the blocked conclusion with
  final measurements and explicitly unlock T3. Do not set
  `CheckpointProofSystem::VERIFIED`; later T3/T4/promotion gates own that state.
  Estimate: `0.5 hour`.

### Dependency order and total estimate

`GAP-01 → GAP-02 → GAP-03 → GAP-04`; GAP-05 and GAP-06 then close
deterministic scheduling/lifecycle; GAP-07 and GAP-08 reuse the frozen identities;
GAP-09 produces the candidate evidence; GAP-10/11/13/14/15 bind authority and
selection; GAP-16/17/18/19/20 execute strictly on the selected final tree;
`T2-CLOSE-21` is last.

Production-first completion is estimated at **`55–100 sequential
engineering/compute hours`**, excluding unbounded external authority and Git
authorization response time and assuming no new cryptographic defect. An
additional permitted `k` adds `1.5–3 hours`; a finding that changes the R1CS
relation or selected wire format resets artifact, proof, release and review
evidence. This range supersedes the former resident-path `19–37 hour` estimate.

### Canonical owner and test matrix

No new public module or parallel owner is permitted. New private helpers remain
under the owners below unless file size alone requires a private child module;
if split, `checkpoint::nova` and `settlement::proof_batch` remain the only
export/semantic owners and guards must forbid direct downstream imports.

| Surface | Sole owner/change location | Required verification owner |
| --- | --- | --- |
| HJMT update/segment grammar and strict reader | `crates/z00z_storage/src/settlement/proof_batch.rs` | existing unit corpus in `proof_batch.rs`; `tests/test_recursive_v2_trace.rs` for live roundtrip/rejects |
| Per-tree JMT proof production | `crates/z00z_storage/src/settlement/hjmt_store.rs::commit_snap_with_update_trace` | existing six-case/two-operation HJMT tests; add max-single-operation and deterministic segmentation boundaries |
| HJMT level commit/order | `crates/z00z_storage/src/settlement/hjmt_commit.rs` | existing HJMT commit tests plus thread-count/skew/level-barrier equivalence cases |
| Parallelism/backpressure | `crates/z00z_storage/src/settlement/hjmt_scheduler.rs::ForestScheduler` | existing scheduler tests/bench lane; add byte reservation, slow-first, cancel and oversubscription rejects |
| Preflight/live cutover | `crates/z00z_storage/src/checkpoint/canonical_transition.rs` | `tests/test_recursive_v2_cutover.rs` and existing F23 child/reopen owner |
| Native streamed evaluator | `crates/z00z_storage/src/checkpoint/recursive_predicate.rs` | `tests/test_recursive_v2_trace.rs`; exact resident-oracle equivalence until oracle removal |
| Profile/resource equation | `crates/z00z_storage/src/checkpoint/recursive_circuit.rs::RecursiveCircuitProfileV2` | focused profile boundary tests plus full-cap external RSS/address-space worker |
| R1CS/Nova/cache/recovery identity | `crates/z00z_storage/src/checkpoint/nova.rs` | `tests/test_recursive_v2_nova_step.rs` and `tests/test_recursive_v2_nova_adversarial.rs`; no third Nova integration owner |
| Milestone orchestration | `.github/skills/smart-tests-bootstrap/scripts/nova_milestone_tests.sh` | guard one canonical owner, zero legacy owner, required ignored-heavy cases and exact execution markers |
| Verifier RSS/process cleanup | `.github/skills/smart-tests-bootstrap/scripts/nova_verifier_rss_measurement.sh` | terminal exit, VmHWM/VmRSS, process-group cleanup and free single-flight lock |
| Mandatory fail-fast | `.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh` | terminal `BOOTSTRAP COMPLETE` before broader release validation |

### Required streaming/concurrency/cache test matrix

- **Boundary sizes:** zero, one byte below cap, exact cap, cap plus one, one
  maximum-size operation, multiple segments, maximum segment/update/tree counts,
  arithmetic-overflow lengths, empty mutating envelope and non-empty no-op.
- **Canonical grammar:** wrong version/role/level/tree id/ordinal/count/length/
  digest/root/version; missing/duplicate/reordered segment; trailing bytes;
  cross-transition/generation/height segment substitution.
- **HJMT dependency:** every six-case JMT mutation at every hierarchy level;
  duplicate/unused child; parent before child barrier; wrong child old/new root;
  missing definition update; path-index before definition; exact final root.
- **Scheduling:** thread counts from the finite candidate set; randomized and
  worst-case completion skew; slow ordinal zero; queue full; byte budget exact/
  plus one; cancellation while producer/consumer/reservation holder blocks;
  panic/error reservation release; no deadlock or starvation.
- **Equivalence:** resident test oracle versus streaming for exact canonical
  micro-op bytes, trace/source/global digests, declared/consumed counts,
  pre/post roots, `X_h`, `z_N`, TestCS satisfaction and unchanged verifier
  decision. Proof bytes need not be asserted equal if prover randomness differs.
- **Cache:** cold/warm, concurrent writers/readers, interrupted atomic write,
  truncation/bit flip/trailing bytes, wrong permissions, swapped PP/PK, wrong
  generation/source/profile/spec/grammar/shape/`k`, stale lock, rollback and cap
  rejection before allocation/proof decode.
- **Recovery:** selected model roundtrip; stale height/predecessor/prior-IVC/
  source-prefix; wrong authority generation; partial/corrupt/rolled-back image;
  restart equivalence; normal final proof verification after recovery; no cached
  verdict or bypass.
- **Process secrecy/crash:** success, explicit error, sanitized panic, timeout,
  cooperative cancel and hard kill at segment/cache/recovery/barrier/cutover
  seams; canary-free stdout/stderr/artifacts/core; `0700/0600`; temp and lock
  cleanup; complete-old/complete-new durable state.
- **Performance:** minimum/typical/full-cap workloads; cold and warm setup;
  cache bypass; every segment/thread/concurrency/`k` candidate; isolated HJMT,
  setup, fold, compression, verifier load/check and end-to-end runs; report CPU,
  wall, throughput, queue wait, RSS/VmHWM/address space, disk/temp peak and bytes.
- **Final theorem:** fixed ShapeCS across opcode/JMT/segment boundaries; every
  existing mutation remains killed; Models A/B/C reach the unchanged real
  verifier and all-limb comparator; proof/envelope/VK contain no PP/PK or
  witness/recovery bytes.

## ✅ Rechecked exclusions

The following are not open T2 implementation tasks: the full uniqueness/Net/
per-tree-JMT/HJMT-hierarchy/Settlement/commitment/`X_h`/prior-IVC/successor
relations; their parsers; Models A/B/C; and the existing mutation ledger. They
remain final-rerun obligations, not redesign work. A-17 also does not need a new
unconditional reduction: the explicit residual-assumption branch is permitted
provided no report claims unconditional 128-bit cumulative IVC security.

Live Celestia publication/cadence, receipt distribution, retention/physical GC,
and activation of the continuous per-block runner remain T3 or later. The
formats and primitives they consume are no longer deferred: bounded HJMT
streaming, setup cache identity, folding recovery boundary and concurrency/
resource controls are mandatory T2 work in GAP-01…09.

## Historical pre-closure assessment (superseded 2026-07-20)

This section through “Historical documentation reconciliation” records the
2026-07-19 pre-closure assessment. It is retained only to explain how the
checked GAP rows were derived. It does not define current status, owners,
identities, caps, authority decisions or required work; the Conclusion, live
reconciliation, A–Z checklist, checked GAP list and final evidence above are
canonical.

At that checkpoint T2 formally consumed a completed T1, while STATE had
reopened T1 for the residuals below. Final closure resolved those residuals.

The following T1 residuals had to be completed before T2 could close:

1. **Obtain durable-cutover equivalence acceptance.**
   Durable CAS, manifest, and reload are implemented in
   [store.rs (line 747)](/home/vadim/Projects/z00z/crates/z00z_storage/src/settlement/store.rs:747),
   and the five-seam abrupt child/reopen corpus now passes with atomic stale-CAS,
   exactly-once/reload, and clean-process verification. redb does not expose
   each internal write/fsync/directory-fsync boundary, so the ledger kept this
   row partial pending an external equivalence decision instead of inventing a
   per-fsync hook:
   [DC2 ledger](/home/vadim/Projects/z00z/.planning/phases/069-Recursive-Proof/069-051-T1-DC2-RESOLUTION-LEDGER.md).
2. **Secret-boundary residual.**
   Replay/JMT payload types remain private and without payload-bearing `Debug`;
   secret-canary plus in-place zeroization tests cover trace payloads, private
   spool reads, and JMT prior/new-value buffers. The six-outcome project-owned
   process corpus now covers logs/telemetry, clean-process core policy, private
   artifacts, cancellation and hard kill. Only dependency-owned Nova allocation
   zeroization remains an explicit upstream non-claim.
3. **Replace and approve native-residency accounting.**
   The SHA-role and exact declared/emitted/consumed work equation already close
   in the 20-row registry and retained 1,727-step proof. The historical
   `69,337,178 B` complete-envelope equation and `574,040 KiB` one-real-HJMT
   workload RSS remain baseline measurements only. GAP-01…09 must produce the
   exact selected segment/scheduler/cache equation and full-cap measurement;
   DC2-F12 closes only after external approval of those final values.
4. **DC2 ledger update.**
   Every `partial/open` row must receive a source-bound test and its factual
   status. The R1CS parts of F11/F13/F20/F22 are closed by the T2 relations
   below, but the T1 evidence must be updated as well.

## Historical complete T2 gap analysis

1. **Production running state, anchors, and public-input layer**

   Exact-P prefix is typed: 12 digest anchors, including the pre-witness
   `update_trace_digest`, plus version/height/predecessor, seven semantic counts,
   and 17 per-opcode counts are built from the sole authority/statement/bundle
   path, range constrained, and consumed by constrained count/P SHA. All 17
   consumed counters are compared at the sole final successor. Generation,
   commitment roots, `X_h`, prior finalized IVC state and the independently
   derived successor are typed and bound without including `z_N` in its own
   statement. Focused one-field R1CS/envelope mutations reject. Recomputed
   Model C now proves a second complete satisfying statement under the same
   PP/VK, is accepted for its own state, and is rejected by the target all-limb
   comparator. No second public-state representation remains.

2. **Full uniqueness theorem**

   The live element is the complete 100-byte semantic row: definition ID,
   serial ID, terminal ID, and leaf-value hash. Replay derives this row from the
   authenticated canonical payload, and the immediately adjacent Original row
   is byte-equal across all four fields. Original and Sorted SHA commitments and
   both product pairs consume all fifty little-endian `u16` limbs. Per-set
   terminal ordering is strict; a globally equal terminal is admitted only for
   one spent/output pair on the exact same definition/serial path.

   The one 353-byte acyclic transcript still derives `P`, both `U` values, and
   eight independent mapped challenges through the shared FIPS lane. Circuit
   spec version 6 binds `(50 limbs, degree 49, 248 challenge bits, log2(q_U)=128)`
   together with the live JMT-header/Promote relation;
   the repository profile binds `n_max=16,000` per set. Symbolic formal-
   polynomial and exhaustive toy-field tests cover nonzero unequal multisets,
   degree `<=49n`, duplicate and zero-factor cases, and squared two-pair
   collision accounting. The correct local bound is
   `epsilon_perm <= q_U * (49*n_max / 2^248)^2`.

   Closed in native/R1CS code:

   - exact replay → adjacent Original full-row equality;
   - four list commitments over the complete row;
   - two independent full-field product pairs per set;
   - strict per-set/global ordering and exact-path replacement;
   - semantic Net consumption of the same Product-Sorted rows;
   - delete-only and insert-only cardinalities.

   Still required before the global uniqueness theorem can be accepted:

   - an authority-provided operational `q_V`, kept separate from theoretical
     `q_U`;
   - final authority selection and review convergence.

3. **Semantic Net relation**

   Closed in the current native and R1CS slice. The sole 134-byte codec carries
   exact `Delete`, `Insert`, `Replace`, `Unchanged`, and `Close` rows. One
   pending spent/output pair is populated by authenticated Product-Sorted rows;
   the adjacent effect must consume the exact path and old/new leaf hashes.
   Delete/insert require a zero absent side, replacement requires unequal
   hashes, unchanged requires equal hashes, and changed-path replacement
   rejects. `Close` directly binds the derived precommit, `P`, spent `U`, and
   output `U`, then checks effect and non-unchanged mutation counts against the
   declared Net/JMT anchors.

   Focused release tests reject relabelled kinds, changed pending rows, changed
   Close transcript limbs, changed semantic-row fields, cardinalities, order,
   and products at their direct gates. Recomputed Model C also reaches the
   unchanged compressed verifier and target comparator; semantic Net must not
   be called full T2 closure by itself while authority/T1/review gates stay
   open.

4. **Full JMT transition in R1CS — semantic relation closed**

   The canonical source contains one 39-byte header followed by the sole bounded
   `JmtMicroOp` transcript. R1CS now proves exact old leaf/null membership
   framing, leaf/value and parent raw-SHA preimages, sibling order/direction,
   split-prefix count/direction and former-leaf/null prelude, insert/update/split,
   delete-to-empty, delete-coalesce-leaf, delete-preserve-internal cases, exact
   declared new root, and old-root continuity across operations. The initial
   old root remains bound to the authority-selected pre-definition root.

   Real pinned-JMT fixtures cover all six cases and a two-operation chain.
   Focused mutations reject split count/direction/former leaf/parent, semantic
   active/case aliasing, and declared new root. The historical JMT-only release
   ShapeCS pin was `C=325,091/V=206,541/NZ=1,221,306/G=2,097,153`; the current
   complete semantic base-shape pin is recorded in the live disposition below.
   Canonically ordered prior
   value blocks are now raw-SHA constrained and presence/key/hash-bound to the
   authenticated old proof leaf. This closes only the
   per-update JMT transition relation. The Net→terminal-operation bridge and
   hierarchy consumption and the final unchanged compressed-verifier Model C
   are now live.

5. **Hierarchy and settlement-root induction — semantic relation closed**

   R1CS now proves the canonical terminal → bucket → serial → definition →
   optional path-index stage order, strict role order, changed roots, exact
   prior/new parent child-root bytes, and unused-child rejection through two
   independent products plus exact counts at all three promotion levels.

   Exact SettlementV2 Goldilocks Poseidon2 `serial_key` and `definition_key`
   parent-operation keys are constrained in R1CS. The authority layout/policy
   plus pre/post definition roots are absorbed by the settlement-root SHA jobs;
   the pre-root is compared with the immutable snapshot and the post-root with
   the application statement. Focused key, child, role/order and root
   substitutions reject. Real-verifier Model C passed on the complete mixed
   relation.

6. **Typed commitments, counters, and canonical flow path — semantic relation closed**

   Delta root, witness/unchanged root, journal digest and checkpoint-link digest
   are four ordered typed `CommitTypedEvent` rows and are compared directly with
   the X_h-derived public bytes. Exact declared/consumed counts, no
   skip/trailing/reorder and finalized transient clearing are constrained.
   Replay/commit equality is no longer an obligation: the redundant second flow
   transcript was deleted. `ReplayInput`/`ReplayOutput` are the sole flow-item
   representation, while `CommitTypedEvent` rejects every non-typed payload in
   both the native decoder and the R1CS first-chunk gate.
   A retained producer regression asserts exactly one replay row per flow item
   and exactly the four canonical commitment kinds.

7. **`X_h`, bundle/parameter identity, prior IVC, and exact final successor — semantic relation closed**

   `RecursiveTransitionStatementV2` remains only the transition commitment.
   The next typed layer binds checkpoint/link, context/network/genesis/config,
   predicate/suite/backend revision, profile/spec/grammar, PP generation,
   bundle digest and prior finalized IVC state into `X_h`. The envelope carries
   both prior and successor digests, and strict load/verify recomputes the sole
   expected successor independently before accepting every returned `z_N`
   limb. Final unchanged-verifier identity, one-field endpoint mutations, and
   the recomputed Model C fixture have retained release evidence.

8. **Final fixed-shape/control/SHA-work theorem**

   After connecting every relation, the evidence must re-prove:

   - the same shape for every opcode/JMT case;
   - absence of witness-dependent Rust control flow;
   - one production transition table shared by trace/evaluator/circuit;
   - one successor or typed rejection for every state/opcode;
   - `FINALIZE_BLOCK` as the only path to `done=1`;
   - zero inactive lanes/candidate results.

   The SHA lane must have compile-time width `k`, be used by all
   content/JMT/commitment hashes, and bind role, DST, part lengths, ordinal,
   active count, FIPS padding, chaining, and final digest. The source currently
   freezes `NOVA_SHA_BATCH_WIDTH_V2 = 1`, and both material and bundle headers
   bind and strictly decode that value. This is a measured implementation
   candidate and was not yet an authority selection:
   [nova.rs (line 20505)](/home/vadim/Projects/z00z/crates/z00z_storage/src/checkpoint/nova.rs:20505).

9. **Nova transcript, dependency, entropy, and A-17**

   The backend/feature/lock checksum and `ck_floor` are pinned. The exact
   polynomial-depth result is now ePrint 2024/232 revision 2026-02-13,
   Theorem 5 (Definitions 3 and 7). Its EAGM, GZT and separate compression
   premises are not demonstrated by the concrete Poseidon/Spartan candidate,
   so A-17 remains an explicit residual assumption in
   `069-051-A17-APPLICABILITY.md`. The complete retained transcript/entropy/
   dependency guard corpus is now implemented; A-17 is not an open T2 coding
   task under the Plan's explicit-residual branch.

   The retained guards pin and test:

   - Pallas/Vesta, Pedersen SHAKE256 generators, Spartan IPA;
   - embedded Poseidon modes/rounds/constants/MDS/squeeze lengths;
   - Keccak transcript;
   - the difference between ordinary and relaxed NIFS transcripts;
   - production `OsRng` sampling sites and the absence of caller RNG/test seed;
   - exact Cargo source/features/lock identities;
   - applicability of the specific polynomial-depth Nova theorem.

   A release proof cannot convert the EAGM/GZT/compression premises into an
   unconditional result. A-17 therefore remains explicit, and the project must
   not claim unconditional “128-bit cumulative IVC security.”

10. **Authority operating budget, streaming/concurrency measurement, and selection**

   Only test-only emergency caps exist today, not an operating budget:
   [nova.rs (line 20554)](/home/vadim/Projects/z00z/crates/z00z_storage/src/checkpoint/nova.rs:20554).

   Authority must provide a numeric, versioned tuple for:

   - setup/fold/compression/verifier-load/verifier-check latency and RSS;
   - HJMT segment bytes, worker threads, in-flight jobs/bytes, queue and spool caps;
   - independent prover concurrency and the sequential-per-chain rule;
   - PP/PK/VK/bundle/envelope/cache/recovery sizes;
   - cancellation/hard-kill and reservation/temporary-file cleanup;
   - PP/PK cache cold/warm behavior and VK cold-load/fanout/restart;
   - a finite set of permitted `k` values;
   - operational `q_V` and maximum cumulative Nova steps `N`;
   - the decision when no candidate passes.

   This was an external blocker: the coder had no authority to derive the
   operating budget from the 24 GiB emergency cap.

   The historical resident candidate `k=1` has cache-bypassing complete-relation
   evidence: base `C=533,794/V=401,550/NZ=2,036,733`, augmented
   primary/secondary constraints `991,488/10,349`, a 1,727-step proof of
   122,288 bytes, a 342,353-byte envelope, and bounded-worker peak RSS below
   24 GiB. These values are comparison measurements, not permission to select
   the candidate and not evidence for the final streaming source. Authority
   still had to provide finite segment/thread/concurrency/`k` candidate sets, the
   operational tuple, `q_V`, `N`, and the no-candidate decision.

11. **Final prover/verifier artifacts and strict parsers**

   `NovaProverMaterialV2` now has a finite private PP/PK header, caps, strict
   canonical codec/loader, complete generation/source/shape identity, payload
   digests and deterministic PK regeneration from the decoded PP.
   `VerifierBundleHeaderV2` carries the same identity, and its sole load entry
   additionally requires the exact nonzero authority-selected bundle content
   digest before header/key/proof decode. The keyless proof envelope enforces
   the selected activation interval. `validate_pinned_verifier_key_wire`
   additionally walks the exact primary/secondary Pedersen and IPA key ranges,
   rejects identity/default/order drift, and checks setup-derived blinders and
   derandomization-key copies before proof decode.

   Retained baseline status:

   - generation/source-bound PP+PK format and roundtrip — implemented;
   - VK-only bundle format and strict selected-digest field — implemented;
   - verifier-only clean-process test without PP/PK — passed with setup path forbidden;
   - strict cap/decode/canonical-reencode — implemented;
   - invalid scalar/curve/identity, swapped primary/secondary, duplicate/trailing/
     default-key corpus — passed under recomputed selected-bundle digests;
     Pallas/Vesta cofactor one makes a separate nonidentity wrong-subgroup point
     inapplicable;
   - evidence that rejection occurs before proof decode — passed.

   Preserve the current absence of key material from the proof envelope. Final
   authority generation, selected streaming/profile/cache identities and bundle
   digest remained pending and required GAP-16 regeneration.

12. **Complete real-Nova mixed-block proof**

   The older heavy diagnostic still uses `canonical_hash_control_fixture` and
   proves only a working backend/prefix relation:
   [nova.rs (line 28743)](/home/vadim/Projects/z00z/crates/z00z_storage/src/checkpoint/nova.rs:28743).
   A separate complete mixed test now constructs the canonical transition,
   runs the independent evaluator and every TestCS step, covers every opcode,
   folds/compresses, loads the VK-only bundle, compares exact endpoints, and
   frames/verifies the keyless envelope:
   [nova.rs (line 29203)](/home/vadim/Projects/z00z/crates/z00z_storage/src/checkpoint/nova.rs:29203).

   One canonical mixed block containing every opcode class is required:

   `replay → uniqueness → net → JMT → hierarchy → commitments → finalize`

   The fresh cache-bypassing bounded release run passed all 1,727 steps,
   uncompressed verification, compression, strict VK-only verification, exact
   endpoint comparison, envelope load/check, and the clean verifier-only child.
   Exact timing, RSS, and byte accounting are in `069-051-BENCHMARKS.md`.

13. **Mutation ledger, Models A/B/C, and retained evidence**

   The theorem/code, mutation, and benchmark ledgers now exist. The mutation
   ledger has zero open mutant rows without claiming global T2 closure. The two required canonical-owner integration
   guards now exist as `test_recursive_v2_nova_step.rs` and
   `test_recursive_v2_nova_adversarial.rs`. The structured complete bounded
   report and recomputed Model C passed: the unchanged VK accepts the second
   proof for its own statement and the target all-limb comparator rejects it.

   Every security-critical constraint must have a killed mutant that still
   satisfies unrelated framing. Model C must reach the unchanged
   verifier/endpoint comparator rather than stopping at a checksum or native
   decoder.

14. **Final closure convergence**

   Only after all items above are complete:

- bootstrap;
- targeted and full release storage/workspace test/build;
- crypto/security/constant-time/spec/dependency/public-type/feature checks;
- at least three review passes until two consecutive globally clean passes;
- two independent doublechecks: theorem mapping and Model A/B/C falsification.

These gates passed on the prior 2026-07-18 evidence tree: the release bootstrap
improved from `1609.07 s` to `120.48 s` (`13.36x`); the curated Nova packet
passed in `77.56 s`; the all-target workspace release build passed in
`136.95 s`; and full workspace release tests passed in `2662.15 s` without
executing the explicitly ignored milestone TestCS/full-proof cases. The
canonical milestone runner guarded one owner, zero legacy owner, exactly 41
ignored milestone tests, one unignored canonical-plus-mutation R1CS smoke, and
`1332/1332` coverage. Three reviews ended in two consecutive significant-clean
passes, and both doublechecks passed. This retained baseline does not satisfy
T2-GAP-18/T2-GAP-19 after streaming, cache, concurrency, profile and authority
binding changes; those gates must run again on the unchanged final tree.

## Live hierarchy/Net/final-state disposition (2026-07-19)

The canonical stage/order and two-sided promotion subgap is now implemented in
R1CS. Terminal, bucket, and serial child transitions and their consuming
bucket, serial, and definition operations use one fixed 133-byte row, two
independent products, and exact per-level counts. This rejects reordered or
duplicate roles, equal old/new hierarchy roots, unused children, duplicate
consumers, and mismatched prior/new child roots. The real four-level fixture
passes through `PromoteChildRoot` in release mode.

Exact-P codec version 2 includes the update-trace digest as digest anchor 12,
and the authenticated JMT header must equal it. Therefore neither hierarchy nor
the two Net→terminal-JMT product pairs choose challenges after seeing witness
rows. Parent operation keys, SettlementV2 roots, four typed commitments,
`X_h`, prior IVC and exact successor are constrained in the same fixed shape.
Current exact static evidence is
`C=533,794/V=401,550/NZ=2,036,733/G=1,048,577`, with
PP/VK/bundle/Pedersen lower bounds
`127,834,984/127,834,984/523/201,326,784 B`.

The semantic families named in T2-DONE-02…07 are live. Complete
compressed-verifier, clean-process, invalid-key and recomputed Model C evidence
are retained as a resident-path baseline. The remaining blockers are the
production-first streaming/cache/concurrency work in GAP-01…09, reopened T1
residual acceptance, the external authority budget/`q_V`/candidate decision,
final regeneration, review convergence and Git reproducibility.

## Deferred integration after the T2 boundary is frozen

- **PP/PK setup cache activation** — GAP-07 implements and verifies the sole
  safe cache now. Production runner activation may occur later, but no later
  phase may create a second cache identity/codec. Fresh authority measurements
  always bypass the cache; separate warm-mode measurements are informational.
- **Continuous folding recovery activation** — GAP-08 freezes and tests the sole
  deterministic-replay or validated-accumulator recovery boundary now. T3 owns
  continuous per-block orchestration and policy activation, not a new format.
- **Nova publication in Celestia** — not T2. Live DA still produces the old
  `celestia-local.payload.v1`:
  [da.rs (line 511)](/home/vadim/Projects/z00z/crates/z00z_rollup_node/src/da.rs:511).
  Publication cadence/integration belong to Plans 06/09/12. T2 requires only
  the final provider-neutral keyless envelope.
- **PP/PK/VK physical retention and GC service** — later plans own lifecycle
  scheduling, but they must consume GAP-07 identities/caps/private atomic files.
- **Continuous per-block IVC runner, receipt, and recovery** — this is the T3
  definition:
  [069-051-PLAN.md (line 2077)](/home/vadim/Projects/z00z/.planning/phases/069-Recursive-Proof/069-051-PLAN.md:2077).

## Historical documentation reconciliation

The prior live reconciliation completed the following formerly inconsistent
items:

- Plan frontmatter and both appendices now bind the current TODO SHA
  `621666f4…`;
- the continuation now names exactly 57 inherited atoms, with the 31-row
  recovery appendix and all 26 version/migration rows accounted for;
- prior STATE/ROADMAP text then reflected T1-reopened/T2-active without
  rewriting historical measurements; GAP-17 later replaced that live status;
- DC2-F12/F23/F24 were explicitly partial while code-addressable work remained
  separated from external residuals; the final authority decision above later
  accepted their exact dispositions without widening the claims;
- this ledger already used the canonical A–Z/GAP-01…20/CLOSE-21 structure;
  final version scope and clean-clone truth are now owned solely by
  `069-051-VERSIONING-MANIFEST.md`.

## Verified directly

- planning coverage: `1332/1332`, exit `0`;
- theorem, mutation, benchmark and A-17 ledgers exist; the mutation ledger has
  zero open mutants while A-17 remains explicit and conditional;
- the five-seam release cutover crash/reopen corpus, six-outcome release
  secret-process corpus, log/telemetry owner scan, and clean-process core-zero
  test pass; no internal redb or dependency-Nova zeroization claim is made;
- the historical native residency equation is `69,337,178 B`; its focused
  release test and one-real-HJMT transition pass, the latter at `574,040 KiB`
  process peak RSS; these are baseline rather than final streamed limits;
- focused uniqueness order/count/product release test passes and reaches the direct per-set order, global cross-set, four product-equality and cardinality gates;
- real T1 trace/HJMT suite: `6/6` passed;
- the mandatory bootstrap after the F23 assertion fix reaches terminal
  `BOOTSTRAP COMPLETE`; the exact strengthened F23 corpus passes 1/1. The older
  all-target/workspace failure timings remain historical;
- the wallet golden-owner repair preserves all 79 HEAD mappings inline, passes
  the four release domain tests and its dedicated canonical-owner guard, and
  restores a green all-target workspace release build in `2:11.23` at
  `3,458,900 KiB` peak RSS;
- the egui archive repair preserves exact HEAD blob/member-list provenance in
  the sole rename-guard owner, forbids all five former archive paths, and
  passes both the focused owner gate and broad runtime rename-guard 1/1;
- the latest three-pass baseline-tree review completed with passes 2 and 3
  consecutive significant-clean, followed by two passing doublechecks; GAP-19
  must repeat convergence after the production-first final diff;
- in this final audit, review pass 1 corrected only a docs-level recovery-wire/
  setup-cache misstatement; passes 2–3 were consecutive significant-clean.
  Doublecheck 1 revalidated theorem/source/Model-C/T3-lock claims; Doublecheck 2
  recomputed resource arithmetic and revalidated external authority,
  `VERIFIED`, and then-current wallet deletion blocker; the later wallet-owner
  repair supersedes only that blocker;
- code, Plan 051, current STATE/ROADMAP, the final T1/DC2 ledger, DA boundary,
  active authority records, and test inventory were checked directly;
- the complete mixed bounded release report, clean verifier-only process,
  strict invalid-key corpus, PP/PK recovery roundtrip, and recomputed Model C
  are retained as baseline evidence; none substitutes for the final streaming,
  full-cap, authority-selection and clean-clone gates;
- live code and STATE/ROADMAP/Plan/authority surfaces were checked directly.
  Exact Git provenance is retained in `069-051-VERSIONING-MANIFEST.md`. The
  complete runtime packet is retained by `v1.14.0`; the
  clean-clone guard repair is retained by `v1.14.1`/`37ece6c…faad2`; detached
  bootstrap/curated/build/workspace gates pass. The ordinary GitHub push of
  `v1.14.1` and its final normal retry both failed DNS resolution. The writable
  versioning clone's local `main`/tag remain `37ece6c…faad2`, `origin/main`
  remains `0256a85…6483`, and remote transport is not reported as synced.

Completion order was satisfied: GAP-01…19 closed on the final T2 relation and
`v1.14.1` clean-clone verification closed GAP-20. T3 subsequently completed on
the continuous 1/3/5-block accumulator. The current T4 source-bound evidence
and final plan disposition are owned by `069-051-PROFILING-REPORT.md` and
`069-051-SUMMARY.md`.

EXPANDABLE: details available for the line-by-line T2 theorem-to-code matrix.
