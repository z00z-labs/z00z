---
phase: 067
plan: 067-08
plan_id: 067-08
title: Network And Signature Adapter
type: implementation
wave: 4
depends_on: [067-04, 067-05, 067-06, 067-07]
task_ids: [PHASE-7]
files_modified:
  - crates/z00z_runtime/aggregators/src/signature.rs
  - crates/z00z_runtime/aggregators/src/transport.rs
  - crates/z00z_runtime/aggregators/src/evidence.rs
  - crates/z00z_runtime/aggregators/src/service.rs
  - crates/z00z_runtime/aggregators/src/lib.rs
  - crates/z00z_runtime/aggregators/tests/test_signature_adapter.rs
  - crates/z00z_runtime/aggregators/tests/test_transport_adapter.rs
  - crates/z00z_runtime/aggregators/tests/test_equivocation_evidence.rs
  - crates/z00z_simulator/tests/test_scenario_11.rs
autonomous: true
requirements:
  - REQ-067-003
  - REQ-067-005
  - REQ-067-006
  - REQ-067-007
  - REQ-067-008
  - REQ-067-009
  - REQ-067-010
---

# 067-08 Network And Signature Adapter

<objective>
Prepare the quorum path for real distributed operation by replacing
simulator-only signatures with a production-ready signature trait, introducing
a vote transport trait, and emitting anti-equivocation or payload-withholding
evidence without changing the replay-first local semantics.
</objective>

<plan_fields>
plan_id: `067-08`
task_ids: `PHASE-7`
copied_task_rows:
- `Goal: prepare for real distributed operation without changing core semantics.`
- `Actions: Replace simulator signatures with production signature trait; add anti-equivocation evidence format; add transport trait for vote exchange; keep local deterministic harness as the canonical conformance test.`
- `Acceptance: local tests run without network; network adapter cannot bypass replay verifier; equivocation produces evidence.`
- `067-verdict.md Gates 6, 9, and 14 require a production-signature seam, a deterministic local signer, network fault delivery without vote injection, and structured safety evidence.`
source_refs:
- `.planning/phases/067-Sharded-Concensus/067-TODO.md`
- `.planning/phases/067-Sharded-Concensus/067-verdict.md`
- `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
- `.planning/phases/090-New-Scenarios/90-TODO.md`
inputs:
- `ShardVote` and `ShardQuorumCertificate` from earlier slices
- validator-aware certificate binding from `067-07`
- local deterministic `scenario_11` harness
- existing service and consensus seam boundaries
outputs:
- production-facing signature trait with deterministic local signer implementation
- transport trait for vote exchange with in-memory local adapter
- anti-equivocation and payload-withholding evidence artifacts and tests
dependencies:
- `067-04`
- `067-05`
- `067-06`
- `067-07`
acceptance_tests:
- `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_signature_adapter -- --nocapture`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_transport_adapter -- --nocapture`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_equivocation_evidence -- --nocapture`
- `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`
simulation_gate:
- local deterministic tests must run fully without external network dependencies
- in-memory transport must preserve message identity, delay, reorder, and retry semantics without bypassing replay verification
- conflicting same-voter same-term different-subject votes must emit evidence artifacts
- payload withholding or missing-payload delivery by a peer must emit deterministic evidence or degraded-state signals without creating synthetic votes
negative_tests:
- transport-delivered vote without replay verification rejects
- duplicate or replayed vote messages remain idempotent or reject without double counting
- equivocation evidence triggers on conflicting signed votes
- signature verification rejects wrong signer, wrong membership digest, or wrong subject digest
- payload withholding or missing batch material triggers evidence or degraded status instead of silent success
plan_artifacts:
- this plan
- `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
plan_tests:
- `rg -n "PHASE-7" .planning/phases/067-Sharded-Concensus/067-COVERAGE.md .planning/phases/067-Sharded-Concensus/067-08-PLAN.md`
- `rg -n "signature trait|transport trait|equivocation" .planning/phases/067-Sharded-Concensus/067-08-PLAN.md`
plan_results:
- distributed seams exist without weakening local replay-first semantics
- local harness remains the canonical conformance test for future real transport implementations
- local evidence exists for both equivocation and payload withholding before any slashing or live-network claim
task_artifacts:
- task_id: `PHASE-7`
  implementation_depth: `simulated-full`
  artifacts:
    - `crates/z00z_runtime/aggregators/src/signature.rs`
    - `crates/z00z_runtime/aggregators/src/transport.rs`
    - `crates/z00z_runtime/aggregators/src/evidence.rs`
    - `crates/z00z_runtime/aggregators/src/service.rs`
    - `crates/z00z_runtime/aggregators/src/lib.rs`
    - `crates/z00z_runtime/aggregators/tests/test_signature_adapter.rs`
    - `crates/z00z_runtime/aggregators/tests/test_transport_adapter.rs`
    - `crates/z00z_runtime/aggregators/tests/test_equivocation_evidence.rs`
    - `crates/z00z_simulator/tests/test_scenario_11.rs`
task_tests:
- task_id: `PHASE-7`
  commands:
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_signature_adapter -- --nocapture`
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_transport_adapter -- --nocapture`
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_equivocation_evidence -- --nocapture`
    - `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`
  scenarios:
    - `local conformance run with in-memory transport only`
    - `equivocation evidence from conflicting same-voter votes`
    - `payload-withholding evidence or degraded-state path`
    - `network adapter cannot create or count unreplayed votes`
task_results:
- task_id: `PHASE-7`
  expected_results:
    - `local tests run without real network transport`
    - `signature and transport seams preserve replay-first semantics`
    - `equivocation and payload-withholding produce deterministic evidence`
anti_placeholder_gate:
- a trait stub with no exercising tests does not close this plan
- network delivery that can inject votes without replay verification does not close this plan
- evidence that records only strings and no conflicting vote material does not close this plan
current_code_refs:
- `crates/z00z_runtime/aggregators/src/consensus_adapter.rs`
- `crates/z00z_runtime/aggregators/src/service.rs`
- `crates/z00z_runtime/aggregators/src/dist_sim.rs`
- `crates/z00z_runtime/aggregators/src/lib.rs`
blockers:
- transport and signature seams must stay behind current runtime-owned types and must not leak external provider types into the core subject path
- this slice must reuse existing `z00z_core`, `z00z_crypto`, `z00z_storage`, and `z00z_utils` primitives and must not add a parallel HJMT, crypto, or utility layer
- equivocation evidence must remain deterministic and locally serializable
- any new adapter, evidence, or test homes listed above are proposed targets if they do not yet exist in the worktree; implementation must prefer a tighter existing owner when one already exists
evidence_gate:
- targeted signature, transport, and equivocation tests pass
- `scenario_11` remains green using only in-memory transport and deterministic local signers
- payload-withholding evidence or degraded-state assertions pass on live local harness paths
- replay verifier remains the only path to vote creation
not_recommendation_gate:
- this slice is complete only when future transport hooks exist and the current local harness still proves core semantics unchanged
- third-party transport or signature crates from `067-TODO.md` Section `8.1` stay blocked unless the repository lacks the primitive and the adapter remains outside the core replay path
</plan_fields>

<tasks>
<task type="auto" id="067-08-T1">
  <read_first>
  - `.planning/phases/067-Sharded-Concensus/067-TODO.md`
  - `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
  - `.planning/phases/090-New-Scenarios/90-TODO.md`
  - `crates/z00z_runtime/aggregators/src/consensus_adapter.rs`
  - `crates/z00z_runtime/aggregators/src/service.rs`
  - `crates/z00z_runtime/aggregators/src/dist_sim.rs`
  </read_first>
  <action>
  Replace simulator-only signatures with a production-facing signature trait,
  add an in-memory transport trait implementation for votes, and record
  anti-equivocation and payload-withholding evidence. Keep the local
  deterministic harness as the canonical conformance test and block every path
  that would bypass replay verification.
  </action>
  <acceptance_criteria>
  - local tests run without real network transport
  - transport-delivered votes still require replay verification
  - conflicting same-voter votes emit evidence
  - payload withholding emits evidence or degraded state rather than a silent pass
  - signature validation rejects wrong signer and wrong digest bindings
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, rerun, and only then continue.
  2. Create and run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_signature_adapter -- --nocapture`.
  3. Create and run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_transport_adapter -- --nocapture`.
  4. Create and run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_equivocation_evidence -- --nocapture`.
  5. Run `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`.
  6. Run `cargo test --release` if broader Rust or test-affecting surfaces changed.
  7. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times; fix all issues and continue until at least 2 consecutive runs show no significant code issues. If the runner is unavailable, perform the equivalent workspace-first manual review and record the blocker and clean reruns in the summary.
  8. Use nested `.github` skills/prompts as needed, especially `doublecheck`, `spec-to-code-compliance`, smart tests, and the relevant Z00Z verification gates.
  9. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
- signature and transport seams are proven without real network dependency
- equivocation evidence exists as structured runtime-owned artifacts
- replay verifier remains mandatory even after transport abstraction is added
</verification>

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | `PHASE-7` | `067-08` | `.planning/phases/067-Sharded-Concensus/067-TODO.md`, `.planning/phases/067-Sharded-Concensus/067-verdict.md`, `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`, `.planning/phases/090-New-Scenarios/90-TODO.md` | vote or certificate path plus local conformance harness | signature, transport, and evidence seams | signature, transport, equivocation, and scenario tests | distributed hooks without semantic drift | in-memory transport and deterministic signer simulation | no trait-only or bypassable replay path | planned |
