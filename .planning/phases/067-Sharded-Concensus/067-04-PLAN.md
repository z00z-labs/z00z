---
phase: 067
plan: 067-04
plan_id: 067-04
title: Local Quorum Certificate Integration
type: implementation
wave: 2
depends_on: [067-01, 067-02, 067-03]
task_ids: [PHASE-3]
files_modified:
  - crates/z00z_runtime/aggregators/src/consensus_adapter.rs
  - crates/z00z_runtime/aggregators/src/lib.rs
  - crates/z00z_runtime/aggregators/src/types.rs
  - crates/z00z_runtime/aggregators/tests/test_hjmt_consensus.rs
  - crates/z00z_runtime/aggregators/tests/test_hjmt_failover_same_lineage.rs
  - crates/z00z_runtime/aggregators/tests/test_local_quorum_certificate.rs
autonomous: true
requirements:
  - REQ-067-003
  - REQ-067-005
  - REQ-067-006
  - REQ-067-007
  - REQ-067-009
  - REQ-067-010
---

# 067-04 Local Quorum Certificate Integration

<objective>
Connect the existing shard-local `ConsensusAdapter` seam to the new
`CommitSubject`, `ShardVote`, and `ShardQuorumCertificate` artifacts so the
live commit path returns a real quorum proof while preserving current freeze,
term, and generation safety rules.
</objective>

<plan_fields>
plan_id: `067-04`
task_ids: `PHASE-3`
copied_task_rows:
- `Goal: connect existing ConsensusAdapter to the new artifact.`
- `Actions: Add certificate-producing commit path; preserve same-term split-brain freeze; preserve generation-bound membership changes; bind certificate membership digest to placement view.`
- `Acceptance: current consensus tests still pass conceptually; new tests assert certificate fields; conflicting same-term subjects cannot both commit.`
- `067-verdict.md requires local quorum certificates to be real executable artifacts, not labels, and to feed downstream validator gates.`
source_refs:
- `.planning/phases/067-Sharded-Concensus/067-TODO.md`
- `.planning/phases/067-Sharded-Concensus/067-verdict.md`
- `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
- `.planning/phases/090-New-Scenarios/90-TODO.md`
inputs:
- `CommitSubject`, `ShardVote`, and `ShardQuorumCertificate` from `067-02`
- `SecondaryReplayVerifier` vote-eligibility output from `067-03`
- existing `ConsensusAdapter` same-term freeze and membership-change logic
- live placement generation and recovery lineage invariants
outputs:
- certificate-producing consensus commit path
- `ConsensusCommit` or sibling result that carries or references quorum-certificate proof
- regression coverage for honest parity, split-brain freeze, and generation-bound membership
dependencies:
- `067-01`
- `067-02`
- `067-03`
acceptance_tests:
- `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_consensus -- --nocapture`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_failover_same_lineage -- --nocapture`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_local_quorum_certificate -- --nocapture`
simulation_gate:
- honest primary plus ready secondaries must produce the same local CFT decision through both the legacy majority seam and the new certificate-producing path
- same-term conflicting subjects must still freeze or reject before two commits can exist
- membership digest in the certificate must match one shard and one placement generation only
negative_tests:
- duplicate or removed voters reject before commit
- joined-but-not-ready secondary voters reject before commit
- mixed membership digest rejects
- mixed term rejects
- conflicting same-term subjects cannot both commit
plan_artifacts:
- this plan
- `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
plan_tests:
- `rg -n "PHASE-3" .planning/phases/067-Sharded-Concensus/067-COVERAGE.md .planning/phases/067-Sharded-Concensus/067-04-PLAN.md`
- `rg -n "simulation_gate|anti_placeholder_gate|evidence_gate|ShardQuorumCertificate" .planning/phases/067-Sharded-Concensus/067-04-PLAN.md`
plan_results:
- live shard-local consensus yields a real certificate artifact rather than an implicit majority-only result
- same-term freeze and generation safety remain unchanged in behavior
task_artifacts:
- task_id: `PHASE-3`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_runtime/aggregators/src/consensus_adapter.rs`
    - `crates/z00z_runtime/aggregators/src/lib.rs`
    - `crates/z00z_runtime/aggregators/src/types.rs`
    - `crates/z00z_runtime/aggregators/tests/test_hjmt_consensus.rs`
    - `crates/z00z_runtime/aggregators/tests/test_hjmt_failover_same_lineage.rs`
    - `crates/z00z_runtime/aggregators/tests/test_local_quorum_certificate.rs`
task_tests:
- task_id: `PHASE-3`
  commands:
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_consensus -- --nocapture`
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_failover_same_lineage -- --nocapture`
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_local_quorum_certificate -- --nocapture`
  scenarios:
    - `honest local quorum returns one certificate-bound commit decision`
    - `same-term conflicting subject pair freezes or rejects before dual commit`
    - `generation-bound membership changes reject stale or mixed vote sets`
task_results:
- task_id: `PHASE-3`
  expected_results:
    - `the live commit path emits or references a ShardQuorumCertificate`
    - `conceptual parity with current consensus tests remains green`
    - `conflicting same-term subjects cannot both commit`
anti_placeholder_gate:
- wrapping the old majority result in a synthetic certificate after commit does not close this plan
- keeping `ConsensusCommit` as the only proof object does not close this plan
- split-brain freeze must be asserted by tests, not by comments
current_code_refs:
- `crates/z00z_runtime/aggregators/src/consensus_adapter.rs`
- `crates/z00z_runtime/aggregators/src/placement.rs`
- `crates/z00z_runtime/aggregators/src/recovery.rs`
- `crates/z00z_runtime/aggregators/tests/test_hjmt_consensus.rs`
- `crates/z00z_runtime/aggregators/tests/test_hjmt_failover_same_lineage.rs`
blockers:
- certificate integration must preserve current public crate exports through `z00z_aggregators`
- this slice must reuse existing `z00z_core`, `z00z_crypto`, `z00z_storage`, and `z00z_utils` primitives and must not add a parallel HJMT, crypto, or utility layer
- any touched consensus result shape must remain compatible with later local DA and validator binding work
- any new helper or test homes listed above are proposed targets if they do not yet exist in the worktree; implementation must prefer a tighter existing owner when one already exists
evidence_gate:
- targeted consensus tests pass on the certificate-producing path
- new tests assert certificate fields, membership digest, and same-term conflict rejection
- no active commit path can bypass certificate validation once this slice lands
not_recommendation_gate:
- this slice is complete only when the live local commit path returns real quorum-proof material and proves parity with current honest behavior
- adding a separate consensus crate or external backend dependency before local proof exists does not close this plan
</plan_fields>

<tasks>
<task type="auto" id="067-04-T1">
  <read_first>
  - `.planning/phases/067-Sharded-Concensus/067-TODO.md`
  - `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
  - `.planning/phases/090-New-Scenarios/90-TODO.md`
  - `crates/z00z_runtime/aggregators/src/consensus_adapter.rs`
  - `crates/z00z_runtime/aggregators/src/placement.rs`
  - `crates/z00z_runtime/aggregators/src/recovery.rs`
  - `crates/z00z_runtime/aggregators/tests/test_hjmt_consensus.rs`
  </read_first>
  <action>
  Extend the live shard-local consensus seam to consume the new replay-backed
  votes and emit a real `ShardQuorumCertificate`. Preserve same-term freeze,
  generation-bound membership, and existing honest majority semantics while
  making the certificate the canonical proof-bearing output.
  </action>
  <acceptance_criteria>
  - the certificate-producing commit path returns the same honest decision as the current local majority seam
  - conflicting same-term subjects cannot both commit
  - certificate membership digest matches the active placement view and generation
  - removed, stale, duplicate, and mixed voters reject before commit
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, rerun, and only then continue.
  2. Run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_consensus -- --nocapture`.
  3. Run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_failover_same_lineage -- --nocapture`.
  4. Create and run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_local_quorum_certificate -- --nocapture`.
  5. Run `cargo test --release` if broader Rust or test-affecting surfaces changed.
  6. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times; fix all issues and continue until at least 2 consecutive runs show no significant code issues. If the runner is unavailable, perform the equivalent workspace-first manual review and record the blocker and clean reruns in the summary.
  7. Use nested `.github` skills/prompts as needed, especially `doublecheck`, `spec-to-code-compliance`, smart tests, and the relevant Z00Z verification gates.
  8. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
- certificate-bearing commits preserve current local CFT behavior for honest inputs
- same-term split-brain freeze still blocks dual-commit outcomes
- placement generation and membership digest remain hard validation boundaries
</verification>

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | `PHASE-3` | `067-04` | `.planning/phases/067-Sharded-Concensus/067-TODO.md`, `.planning/phases/067-Sharded-Concensus/067-verdict.md`, `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`, `.planning/phases/090-New-Scenarios/90-TODO.md` | live consensus seam plus new subject/vote/certificate types | consensus adapter, exports, and consensus tests | targeted consensus and failover tests | local commit path returns real quorum proof | honest and conflicting local replay matrix | no synthetic after-the-fact certificate wrapper | complete |
