---
phase: 067
plan: 067-05
plan_id: 067-05
title: Scenario 11 End To End Harness
type: implementation
wave: 2
depends_on: [067-04]
task_ids: [PHASE-4]
files_modified:
  - crates/z00z_simulator/Cargo.toml
  - crates/z00z_simulator/src/lib.rs
  - crates/z00z_simulator/src/scenario_11/mod.rs
  - crates/z00z_simulator/src/scenario_11/report.rs
  - crates/z00z_simulator/tests/test_scenario_11.rs
  - crates/z00z_runtime/aggregators/src/types.rs
  - crates/z00z_rollup_node/src/da.rs
  - crates/z00z_rollup_node/tests/support/test_theorem_fixture.rs
  - crates/z00z_rollup_node/tests/test_da_local_sim.rs
  - crates/z00z_runtime/validators/tests/test_hjmt_publication_contract.rs
autonomous: true
requirements:
  - REQ-067-003
  - REQ-067-005
  - REQ-067-006
  - REQ-067-007
  - REQ-067-008
  - REQ-067-009
  - REQ-067-010
---

# 067-05 Scenario 11 End To End Harness

<objective>
Build the independent `scenario_11` local harness that proves a wallet-style
package can flow through ingress, routing, planning, secondary replay,
certificate formation, local DA publication, and validator verdict without
reusing `scenario_1` as the execution home.
</objective>

<plan_fields>
plan_id: `067-05`
task_ids: `PHASE-4`
copied_task_rows:
- `Goal: prove the complete local path.`
- `Actions: Build the wallet package to validator harness described above; run one shard happy path and at least one dual-primary owner path; run failure matrix with stale secondary, offline secondary, primary offline before dispatch, and primary crash cases.`
- `Acceptance: each shard can form 2-of-3 local certificate; no global 5-of-5 quorum is required; secondary replay, DA publication, and validator theorem all bind the same subject; post-quorum publication resume reuses the same certificate; route-bound publication preserves the same route authority.`
- `067-verdict.md requires one real end-to-end local path from wallet package to validator verdict and treats SIM-5A7S as the default local conformance topology unless a stronger BFT-valid profile is explicitly selected.`
source_refs:
- `.planning/phases/067-Sharded-Concensus/067-TODO.md`
- `.planning/phases/067-Sharded-Concensus/067-verdict.md`
- `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
- `.planning/phases/090-New-Scenarios/90-TODO.md`
inputs:
- `sim_5a7s` topology and planner configuration
- replay-backed certificate path from `067-04`
- live ingress, routing, placement, local DA, and validator primitives
- scenario_11 required artifact list and fault matrix
outputs:
- independent `scenario_11` target under `crates/z00z_simulator`
- happy-path, dual-primary, and failure-matrix scenario evidence
- JSON honesty artifacts proving package, route, subject, vote, certificate, DA, and validator alignment
- all-shard sweep, route or dispatch owner-path evidence, and crash or offline failure telemetry
- route-snapshot publication evidence proving the same route authority survives from request ingress through local DA publication
dependencies:
- `067-04`
acceptance_tests:
- `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`
- `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_dispatch -- --nocapture`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_shard_routing -- --nocapture`
- `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_da_local_sim -- --nocapture`
- `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_hjmt_topology -- --nocapture`
- `cargo test --release -p z00z_validators --test test_hjmt_publication_contract -- --nocapture`
simulation_gate:
- one-shard happy path must form a 2-of-3 certificate and accepted validator verdict
- one dual-primary owner path must prove per-shard quorum isolation for an aggregator that owns two shards
- primary-offline-before-dispatch must remain deferred on the lawful owner path rather than silently rerouting or synthesizing progress
- stale-secondary, primary-crash-before-quorum, and primary-crash-after-quorum-before-DA cases must produce deterministic fail-closed evidence
- all-shard sweep must prove that each of the seven shards resolves its owner and quorum group through the same live route and dispatch logic
- post-quorum pre-DA crash handling must prove exact-certificate publication resume rather than a recomputed or detached publication
negative_tests:
- no certificate before quorum
- no DA publication after pre-quorum primary crash
- no synthetic reroute or synthetic certificate while the primary owner is offline before dispatch
- no global 5-of-5 quorum counting
- wrong route, plan, or subject drift rejects
- wrong dispatch owner or shard-owner mismatch rejects
- one-secondary-offline path must either form honest 2-of-3 quorum or fail closed without synthetic votes
- one-secondary-stale path rejects the stale vote and either commits with the other secondary or fails closed if quorum is unavailable
- report honesty rejects BFT, Celestia finality, production signature, slashing, and public-finality claims
plan_artifacts:
- this plan
- `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
plan_tests:
- `rg -n "PHASE-4" .planning/phases/067-Sharded-Concensus/067-COVERAGE.md .planning/phases/067-Sharded-Concensus/067-05-PLAN.md`
- `rg -n "scenario_11|report_honesty|local certificate" .planning/phases/067-Sharded-Concensus/067-05-PLAN.md`
plan_results:
- `scenario_11` becomes the canonical local conformance harness for the quorum path
- the full path is proven without mutating `scenario_1`
task_artifacts:
- task_id: `PHASE-4`
  implementation_depth: `simulated-full`
  artifacts:
    - `crates/z00z_simulator/Cargo.toml`
    - `crates/z00z_simulator/src/lib.rs`
    - `crates/z00z_simulator/src/scenario_11/mod.rs`
    - `crates/z00z_simulator/src/scenario_11/report.rs`
    - `crates/z00z_simulator/tests/test_scenario_11.rs`
    - `crates/z00z_runtime/aggregators/src/types.rs`
    - `crates/z00z_rollup_node/src/da.rs`
    - `crates/z00z_rollup_node/tests/support/test_theorem_fixture.rs`
    - `scenario_11/quorum/package_ingress_report.json`
    - `scenario_11/quorum/route_plan_report.json`
    - `scenario_11/quorum/placement_membership.json`
    - `scenario_11/quorum/commit_subject.json`
    - `scenario_11/quorum/secondary_replay_votes.json`
    - `scenario_11/quorum/quorum_certificate.json`
    - `scenario_11/quorum/local_da_binding.json`
    - `scenario_11/quorum/validator_verdict_report.json`
    - `scenario_11/quorum/fault_matrix.json`
    - `scenario_11/quorum/report_honesty.json`
task_tests:
- task_id: `PHASE-4`
  commands:
    - `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_dispatch -- --nocapture`
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_shard_routing -- --nocapture`
    - `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_da_local_sim -- --nocapture`
    - `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_hjmt_topology -- --nocapture`
    - `cargo test --release -p z00z_validators --test test_hjmt_publication_contract -- --nocapture`
  scenarios:
    - `happy-path one-shard local quorum`
    - `dual-primary per-shard isolation`
    - `all-shard sweep over the full 5a7s topology`
    - `primary offline before dispatch deferred owner path`
    - `route and dispatch owner-path conformance`
    - `post-quorum pre-DA crash resume from the same certificate`
    - `one-secondary-offline path`
    - `one-secondary-stale path`
    - `stale secondary and primary crash fault cases`
task_results:
- task_id: `PHASE-4`
  expected_results:
    - `each shard resolves its own primary-plus-secondary 2-of-3 quorum group`
    - `secondary replay, local DA publication, and validator theorem all bind the same subject`
    - `offline owner paths defer dispatch instead of synthesizing reroute, certificate, or publication progress`
    - `scenario evidence proves local CFT quorum only`
anti_placeholder_gate:
- extending `scenario_1` with new stage or observability fields does not close this plan
- emitting report files without live vote, certificate, DA, and validator evidence does not close this plan
- any global-five-aggregator quorum count does not close this plan
current_code_refs:
- `config/hjmt_runtime/sim_5a7s/manifest.json`
- `crates/z00z_runtime/aggregators/src/ingress.rs`
- `crates/z00z_runtime/aggregators/src/batch_planner.rs`
- `crates/z00z_runtime/aggregators/src/placement.rs`
- `crates/z00z_runtime/aggregators/tests/test_hjmt_dispatch.rs`
- `crates/z00z_runtime/aggregators/tests/test_hjmt_shard_routing.rs`
- `crates/z00z_rollup_node/src/da.rs`
- `crates/z00z_rollup_node/tests/test_hjmt_topology.rs`
- `crates/z00z_runtime/validators/src/verdict.rs`
- `crates/z00z_simulator/src/scenario_1/runtime_observability.rs`
blockers:
- `scenario_11` must own its own evidence writer and target; it must not reuse `scenario_1` runtime report shapes
- this slice must reuse existing `z00z_core`, `z00z_crypto`, `z00z_storage`, and `z00z_utils` primitives and must not add a parallel HJMT, crypto, or utility layer
- artifact paths and schemas must remain repository-local and deterministic
- new scenario_11 module or test homes listed above are proposed targets if they do not yet exist in the worktree; implementation must prefer a tighter existing owner when one already exists
evidence_gate:
- the dedicated `scenario_11` target runs green with happy-path and failure-path assertions
- produced JSON artifacts show package digest, route digest, subject digest, vote set, certificate digest, DA binding, and validator verdict alignment
- all-shard sweep, owner-path, and crash/offline telemetry artifacts are emitted from live scenario runs
- honesty checks reject overclaims
not_recommendation_gate:
- this slice is complete only when the full local package-to-validator path is executable as independent `scenario_11` evidence
- introducing new consensus, storage, or crypto abstractions inside simulator-only code does not close this plan
</plan_fields>

<tasks>
<task type="auto" id="067-05-T1">
  <read_first>
  - `.planning/phases/067-Sharded-Concensus/067-TODO.md`
  - `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
  - `.planning/phases/090-New-Scenarios/90-TODO.md`
  - `config/hjmt_runtime/sim_5a7s/manifest.json`
  - `crates/z00z_runtime/aggregators/src/ingress.rs`
  - `crates/z00z_runtime/aggregators/src/batch_planner.rs`
  - `crates/z00z_rollup_node/src/da.rs`
  - `crates/z00z_runtime/validators/src/verdict.rs`
  </read_first>
  <action>
  Create the independent `scenario_11` local conformance harness and its report
  writer. Drive a wallet-style package through live ingress, routing, planning,
  secondary replay, certificate formation, local DA publication, and validator
  verdict, then emit deterministic evidence and fault-matrix artifacts.
  </action>
  <acceptance_criteria>
  - `scenario_11` exists outside `scenario_1`
  - each shard can form a local 2-of-3 certificate without any global quorum rule
  - happy-path and dual-primary paths prove per-shard isolation
  - all-shard sweep proves the seven live shard committees and owner paths
  - stale secondary and primary crash cases fail closed with report artifacts
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, rerun, and only then continue.
  2. Create the dedicated target and run `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`.
  3. Run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_dispatch -- --nocapture`.
  4. Run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_shard_routing -- --nocapture`.
  5. Run `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_da_local_sim -- --nocapture`.
  6. Run `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_hjmt_topology -- --nocapture`.
  7. Run `cargo test --release -p z00z_validators --test test_hjmt_publication_contract -- --nocapture`.
  8. Run `cargo test --release` if broader Rust or test-affecting surfaces changed.
  9. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times; fix all issues and continue until at least 2 consecutive runs show no significant code issues. If the runner is unavailable, perform the equivalent workspace-first manual review and record the blocker and clean reruns in the summary.
  10. Use nested `.github` skills/prompts as needed, especially `doublecheck`, `spec-to-code-compliance`, smart tests, and the relevant Z00Z verification gates.
  11. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
- an independent scenario target proves the full local quorum path
- per-shard quorum isolation is demonstrated for both ordinary and dual-primary ownership
- honesty artifacts block false BFT or Celestia claims
</verification>

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | `PHASE-4` | `067-05` | `.planning/phases/067-Sharded-Concensus/067-TODO.md`, `.planning/phases/067-Sharded-Concensus/067-verdict.md`, `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`, `.planning/phases/090-New-Scenarios/90-TODO.md` | `sim_5a7s` topology plus certificate path | independent `scenario_11` target and JSON evidence | scenario_11 plus DA and validator tests | full local package-to-validator proof path with route-bound publication and deferred offline-owner behavior | deterministic one-shard, dual-primary, offline-owner, and crash/stale replay matrix | no `scenario_1` reuse and no report-only closure | complete |
