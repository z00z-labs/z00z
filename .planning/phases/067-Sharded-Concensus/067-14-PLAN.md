---
phase: 067
plan: 067-14
plan_id: 067-14
title: Network Fault Matrix And Quorum Transport Conformance
type: implementation
wave: 10
depends_on: [067-08, 067-11, 067-13]
task_ids: [VERDICT-LCS-05]
files_modified:
  - crates/z00z_runtime/aggregators/src/transport.rs
  - crates/z00z_runtime/aggregators/src/evidence.rs
  - crates/z00z_runtime/aggregators/tests/test_transport_fault_matrix.rs
  - crates/z00z_runtime/aggregators/tests/test_transport_adapter.rs
  - crates/z00z_runtime/aggregators/tests/test_equivocation_evidence.rs
  - crates/z00z_simulator/tests/test_scenario_11.rs
autonomous: true
requirements:
  - REQ-067-009
  - REQ-067-010
---

# 067-14 Network Fault Matrix And Quorum Transport Conformance

<objective>
Expand the network simulator from a basic adapter into an exhaustive local
conformance layer for delay, reorder, duplicate, drop, replay, partition, heal,
restart, and reconnect behavior while proving that transport cannot create or
count unreplayed votes.
</objective>

<plan_fields>
plan_id: `067-14`
task_ids: `VERDICT-LCS-05`
copied_task_rows:
- Copied Task-To-Plan Coverage row: TASK-NNN=`none; VERDICT-LCS-05`; PLAN id=`067-14`; Source refs=`067-TODO.md`, `067-verdict.md`, `067-CONTEXT.md`, `067-COVERAGE.md`, transport/vote/QC anchors; Inputs=signature, transport, durable evidence, process harness; Artifacts=fault scheduler, transport evidence; Tests=transport fault, adapter, scenario tests; Expected results=network simulation cannot inject consensus truth; Simulation requirement=fake delivery timing only; replay/signature real; Anti-placeholder proof=no label-only fault simulation; Status=`planned`.
- `067-verdict.md` Gate 6 requires peer identity, envelope, delay, reorder, drop, duplicate, partition, heal, restart or reconnect, and replay protection.
- `067-verdict.md` Transport may deliver evidence; it must not manufacture consensus truth.
- `067-TODO.md` distributed HJMT simulation must include partition/heal and failure telemetry.
source_refs:
- `.planning/phases/067-Sharded-Concensus/067-TODO.md`
- `.planning/phases/067-Sharded-Concensus/067-verdict.md`
- `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
- `.planning/phases/090-New-Scenarios/90-TODO.md`
- `crates/z00z_runtime/aggregators/src/consensus_adapter.rs`
- `crates/z00z_runtime/aggregators/src/dist_sim.rs`
- `crates/z00z_runtime/aggregators/src/service.rs`
- `crates/z00z_runtime/aggregators/src/shard_vote.rs`
- `crates/z00z_runtime/aggregators/src/shard_quorum_certificate.rs`
inputs:
- signature and transport seam from `067-08`
- durable evidence store from `067-11`
- process harness from `067-13`
- existing secondary replay and certificate validators
outputs:
- deterministic transport fault scheduler
- multi-task or multithread local transport conformance tests
- evidence for replay, duplicate, partition, heal, and reconnect faults
- asserted per-fault structured logs and optional transport/fault metric assertions when `tracing`, `tracing-subscriber`, `metrics`, or `prometheus` are installed by this slice
- hard test that transport-delivered messages cannot bypass replay and signature verification
dependencies:
- `067-08`
- `067-11`
- `067-13`
acceptance_tests:
- `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_transport_fault_matrix -- --nocapture`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_transport_adapter -- --nocapture`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_equivocation_evidence -- --nocapture`
- `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`
simulation_gate:
- fake only packet timing, delivery, process boundary, and partition scheduler
- every counted vote must originate from replay result plus signature verification
- minority partitions, duplicate packets, and replayed envelopes must be deterministic and fail closed
- if this slice installs `tracing`, `tracing-subscriber`, `metrics`, or `prometheus`, the local fault harness must assert per-fault run ids, peer identities, and counter labels instead of relying on free-form log text
negative_tests:
- transport-injected vote without replay rejects
- replayed envelope does not double count
- duplicate delivery is idempotent or rejects
- partitioned minority cannot form QC
- healed network cannot commit stale conflicting subject
- restart/reconnect cannot replay old membership generation
- missing per-fault structured log or counter evidence rejects whenever this slice claims observability coverage
plan_artifacts:
- this plan
- transport fault evidence JSON
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
plan_tests:
- `rg -n "VERDICT-LCS-05|067-14" .planning/phases/067-Sharded-Concensus`
- `rg -n "partition|heal|duplicate|replay" crates/z00z_runtime/aggregators/src crates/z00z_runtime/aggregators/tests`
plan_results:
- local network simulation becomes exhaustive enough for final conformance claims
- process/devnet harness can reuse the same fault model without changing consensus truth
task_artifacts:
- task_id: `VERDICT-LCS-05`
  implementation_depth: `simulated-full`
  artifacts:
    - `crates/z00z_runtime/aggregators/src/transport.rs`
    - `crates/z00z_runtime/aggregators/src/evidence.rs`
    - `crates/z00z_runtime/aggregators/tests/test_transport_fault_matrix.rs`
    - `crates/z00z_runtime/aggregators/tests/test_transport_adapter.rs`
    - `crates/z00z_runtime/aggregators/tests/test_equivocation_evidence.rs`
    - `crates/z00z_simulator/tests/test_scenario_11.rs`
task_tests:
- task_id: `VERDICT-LCS-05`
  commands:
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_transport_fault_matrix -- --nocapture`
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_transport_adapter -- --nocapture`
    - `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`
  scenarios:
    - `delay/reorder/drop/duplicate matrix`
    - `partition minority cannot commit`
    - `heal rejects stale conflicting subject`
    - `restart reconnect rejects old membership generation`
task_results:
- task_id: `VERDICT-LCS-05`
  expected_results:
    - `transport faults are deterministic and evidence-backed`
    - `all counted votes are replay-verified and signature-verified`
    - `no network path can manufacture consensus truth`
anti_placeholder_gate:
- transport tests that call the certificate builder directly without envelopes fail this plan
- fake fault labels without scheduled delivery behavior fail this plan
- string-only evidence for partitions or replays fails this plan
current_code_refs:
- `crates/z00z_runtime/aggregators/src/consensus_adapter.rs`
- `crates/z00z_runtime/aggregators/src/dist_sim.rs`
- `crates/z00z_runtime/aggregators/src/service.rs`
- `crates/z00z_runtime/aggregators/src/shard_vote.rs`
- `crates/z00z_runtime/aggregators/src/shard_quorum_certificate.rs`
blockers:
- no `libp2p` dependency is allowed until this local transport contract passes
- multi-task scheduling must remain deterministic enough for repeatable tests
- `tracing`, `tracing-subscriber`, `metrics`, and `prometheus` remain optional until local fault runs emit asserted observability; if they are added here, they must stay behind the local transport contract and never be treated as proof of real external networking
- `crates/z00z_runtime/aggregators/src/transport.rs` and `crates/z00z_runtime/aggregators/src/evidence.rs` are proposed implementation targets, not current anchors; implementation must first attach them to the existing consensus adapter, vote, QC, service, and dist-sim seams above
evidence_gate:
- fault matrix test emits structured evidence rows
- when observability crates are installed here, the fault matrix emits asserted per-fault log or metric rows with peer identity, run id, and no false success counters
- report records number of votes produced by replay versus delivered by transport
- final conformance gate can consume transport fault artifacts
not_recommendation_gate:
- this plan closes only when the network simulator enforces the replay/signature boundary at runtime
</plan_fields>

<tasks>
<task type="auto" id="067-14-T1">
  <read_first>
  - `.planning/phases/067-Sharded-Concensus/067-TODO.md`
  - `.planning/phases/067-Sharded-Concensus/067-verdict.md`
  - `crates/z00z_runtime/aggregators/src/consensus_adapter.rs`
  - `crates/z00z_runtime/aggregators/src/dist_sim.rs`
  - `crates/z00z_runtime/aggregators/src/service.rs`
  - `crates/z00z_runtime/aggregators/src/shard_vote.rs`
  - `crates/z00z_runtime/aggregators/src/shard_quorum_certificate.rs`
  </read_first>
  <action>
  Implement the deterministic transport fault scheduler and assert every
  delivery path against replay and signature verification before vote counting.
  Cover delay, reorder, duplicate, drop, replay, partition, heal, restart, and
  reconnect cases.
  </action>
  <acceptance_criteria>
  - fault matrix covers all verdict-required network faults
  - transport cannot inject or double-count votes
  - partition/heal and restart/reconnect emit structured evidence
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, rerun, and only then continue.
  2. Create and run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_transport_fault_matrix -- --nocapture`.
  3. Run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_transport_adapter -- --nocapture`.
  4. Run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_equivocation_evidence -- --nocapture`.
  5. Extend and run `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`.
  6. Run `cargo test --release` if transport or evidence surfaces changed.
  7. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times; fix all issues and continue until at least 2 consecutive runs show no significant code issues.
  8. Use nested `.github` skills/prompts as needed, especially `doublecheck`, `spec-to-code-compliance`, smart tests, and Z00Z gates.
  9. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
- transport fault scheduler is deterministic and exhaustive for local claims
- no vote is counted without replay and signature verification
- partition/heal evidence is structured and scenario-consumable
</verification>

## Coverage Appendix

| TASK-NNN | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| none; `VERDICT-LCS-05` | `067-14` | `067-TODO.md`, `067-verdict.md`, transport/evidence anchors | signature, transport, durable evidence, process harness | fault scheduler, transport tests, evidence JSON | transport fault, adapter, scenario tests | network simulation cannot inject consensus truth | fake delivery timing only; replay/signature real | no label-only or direct-builder tests | planned |
