---
phase: 067
plan: 067-03
plan_id: 067-03
title: Secondary Replay Verifier
type: implementation
wave: 1
depends_on: [067-01, 067-02]
task_ids: [PHASE-2]
files_modified:
  - crates/z00z_runtime/aggregators/src/secondary_replay.rs
  - crates/z00z_runtime/aggregators/src/lib.rs
  - crates/z00z_runtime/aggregators/tests/test_secondary_replay_verifier.rs
  - crates/z00z_runtime/validators/Cargo.toml
  - crates/z00z_runtime/validators/src/verdict.rs
autonomous: true
requirements:
  - REQ-067-003
  - REQ-067-005
  - REQ-067-006
  - REQ-067-009
  - REQ-067-010
---

# 067-03 Secondary Replay Verifier

<objective>
Make every secondary vote mean an independent deterministic replay over live
route, placement, recovery, publication, and theorem inputs rather than fixture
copying or primary-owned precomputation.
</objective>

<plan_fields>
plan_id: `067-03`
task_ids: `PHASE-2`
copied_task_rows:
- `Goal: make secondary-aggregator votes meaningful.`
- `Actions: add a secondary verifier that recomputes the commit subject from local deterministic inputs; reuse IngressBoundary, BatchPlanner, route table digest, placement view, recovery record, publication binding, and theorem digest; return structured accept/reject verdicts.`
- `Acceptance: secondary accepts exact primary subject; secondary rejects wrong route, plan, root, lineage, proof, policy, or publication binding; reject reason is stable enough for simulation assertions.`
- `Addendum: SecondaryReplayVerifier must recompute the subject from live route, placement, planning, recovery, publication, and theorem inputs; it must not compare only fixture bytes or reuse the primary's precomputed subject without recomputation.`
- `067-verdict.md requires transport and quorum paths to prove votes came from replayed subjects, not injected bytes or fixture-only comparisons.`
source_refs:
- `.planning/phases/067-Sharded-Concensus/067-TODO.md`
- `.planning/phases/067-Sharded-Concensus/067-verdict.md`
- `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
- `.planning/phases/090-New-Scenarios/90-TODO.md`
inputs:
- ingress-normalized package and digest
- work-item route key and planner output
- placement view and membership digest
- recovery record and local state metadata
- publication binding and theorem digest
outputs:
- `SecondaryReplayVerifier`
- structured accept/reject result type with stable reason codes
- replay fixture helpers and drift tests
dependencies:
- `067-01`
- `067-02`
acceptance_tests:
- `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_secondary_replay_verifier -- --nocapture`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_dist_journal -- --nocapture`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_recovery_failover -- --nocapture`
simulation_gate:
- replay the exact primary subject from real ingress, route, planning, recovery, publication, and theorem fixtures
- mutate route, plan, root, lineage, proof, policy, publication binding, and theorem digest independently and verify fail-closed rejection
negative_tests:
- wrong route rejects
- wrong plan digest rejects
- wrong root or lineage rejects
- wrong proof version or policy generation rejects
- wrong publication binding or theorem digest rejects
- membership drift rejects
- wrong data-availability digest rejects
- wrong term rejects
- stale secondary recovery state rejects before vote creation
plan_artifacts:
- this plan
- `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
plan_tests:
- `rg -n "PHASE-2" .planning/phases/067-Sharded-Concensus/067-COVERAGE.md .planning/phases/067-Sharded-Concensus/067-03-PLAN.md`
- `rg -n "SecondaryReplayVerifier|publication binding|theorem digest" .planning/phases/067-Sharded-Concensus/067-03-PLAN.md`
plan_results:
- secondary replay is a real runtime-owned verification step
- reject reasons are stable enough for scenario fault-matrix assertions
task_artifacts:
- task_id: `PHASE-2`
  implementation_depth: `full`
  artifacts:
    - `crates/z00z_runtime/aggregators/src/secondary_replay.rs`
    - `crates/z00z_runtime/aggregators/src/lib.rs`
    - `crates/z00z_runtime/aggregators/tests/test_secondary_replay_verifier.rs`
    - `crates/z00z_runtime/validators/Cargo.toml`
    - `crates/z00z_runtime/validators/src/verdict.rs`
task_tests:
- task_id: `PHASE-2`
  commands:
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_secondary_replay_verifier -- --nocapture`
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_dist_journal -- --nocapture`
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_recovery_failover -- --nocapture`
  scenarios:
    - `exact primary subject is accepted by a ready secondary`
    - `every named drift axis prevents vote creation with stable reason text`
task_results:
- task_id: `PHASE-2`
  expected_results:
    - `secondary vote creation depends on real subject recomputation`
    - `replay verdicts expose stable accept/reject outputs for simulator evidence`
    - `stale or drifted local state cannot vote`
anti_placeholder_gate:
- fixture-byte comparison without recomputing the subject does not close this plan
- vote creation from hard-coded expected digests does not close this plan
- reject paths must be asserted by tests, not only described
current_code_refs:
- `crates/z00z_runtime/aggregators/src/ingress.rs`
- `crates/z00z_runtime/aggregators/src/batch_planner.rs`
- `crates/z00z_runtime/aggregators/src/placement.rs`
- `crates/z00z_runtime/aggregators/src/recovery.rs`
- `crates/z00z_runtime/aggregators/src/dist_sim.rs`
- `crates/z00z_rollup_node/src/da.rs`
- `crates/z00z_runtime/validators/src/verdict.rs`
blockers:
- any missing theorem/publication digest helper needed by replay must be exposed through current modules, not via duplicate DTOs
- this slice must reuse existing `z00z_core`, `z00z_crypto`, `z00z_storage`, and `z00z_utils` primitives and must not add a parallel HJMT, crypto, or utility layer
- replay verifier must stay runtime-owned and not move into scenario-only code
- new module and test homes listed above are proposed targets if they do not yet exist in the worktree; implementation must prefer a tighter existing owner when one already exists
evidence_gate:
- tests show exact accept and named reject paths
- later vote/certificate plans consume the verifier output instead of bypassing it
not_recommendation_gate:
- this slice is complete only when real replay logic gates vote creation
- third-party crates from `067-TODO.md` Section `8.1` stay blocked unless this slice proves the repository lacks the primitive and the dependency remains behind the existing runtime seam
</plan_fields>

<tasks>
<task type="auto" id="067-03-T1">
  <read_first>
  - `.planning/phases/067-Sharded-Concensus/067-TODO.md`
  - `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
  - `.planning/phases/090-New-Scenarios/90-TODO.md`
  - `crates/z00z_runtime/aggregators/src/ingress.rs`
  - `crates/z00z_runtime/aggregators/src/batch_planner.rs`
  - `crates/z00z_runtime/aggregators/src/recovery.rs`
  - `crates/z00z_rollup_node/src/da.rs`
  - `crates/z00z_runtime/validators/src/verdict.rs`
  </read_first>
  <action>
  Add the runtime-owned `SecondaryReplayVerifier` and the fixture helpers it
  needs to recompute a primary subject from live deterministic inputs. Expose
  stable accept/reject outputs and make drifted local state fail closed before
  any vote can be formed.
  </action>
  <acceptance_criteria>
  - the verifier accepts the exact primary subject
  - every named drift axis rejects before vote creation
  - stable result codes or messages exist for scenario assertions
  - later plans can consume replay-verifier output without reimplementing replay
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, rerun, and only then continue.
  2. Run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_secondary_replay_verifier -- --nocapture`.
  3. Run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_dist_journal -- --nocapture`.
  4. Run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_recovery_failover -- --nocapture`.
  5. Run `cargo test --release` if broader Rust or test-affecting surfaces changed.
  6. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times; fix all issues and continue until at least 2 consecutive runs show no significant code issues. If the runner is unavailable, perform the equivalent workspace-first manual review and record the blocker and clean reruns in the summary.
  7. Use nested `.github` skills/prompts as needed, especially `doublecheck`, `spec-to-code-compliance`, smart tests, and the relevant Z00Z verification gates.
  8. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
- real replay logic gates vote eligibility
- recovery and distribution tests still prove stale-state rejection
- reject outputs are precise enough for scenario fault evidence
</verification>

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | `PHASE-2` | `067-03` | `.planning/phases/067-Sharded-Concensus/067-TODO.md`, `.planning/phases/067-Sharded-Concensus/067-verdict.md`, `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`, `.planning/phases/090-New-Scenarios/90-TODO.md` | ingress, route, placement, recovery, publication, theorem inputs | replay verifier and tests | targeted replay, dist-journal, and recovery tests | secondary votes mean independent replay | local deterministic replay over real primitives | no copied primary bytes or hard-coded digests | complete |
