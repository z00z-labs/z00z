---
phase: 067
plan: 067-13
plan_id: 067-13
status: complete
completed_at: 2026-07-05
next_plan: 067-14
title: Multi Process Devnet Harness
type: implementation
wave: 9
depends_on: [067-08, 067-10, 067-11, 067-12]
task_ids: [VERDICT-LCS-04]
files_modified:
  - crates/z00z_rollup_node/Cargo.toml
  - crates/z00z_rollup_node/src/lib.rs
  - crates/z00z_rollup_node/src/main.rs
  - crates/z00z_rollup_node/src/process_devnet.rs
  - crates/z00z_rollup_node/tests/test_hjmt_process_devnet.rs
  - crates/z00z_simulator/tests/test_scenario_11.rs
  - scripts/hjmt_local_devnet.sh
  - docker/compose.hjmt-local.yaml
autonomous: true
requirements:
  - REQ-067-008
  - REQ-067-009
  - REQ-067-010
---

# 067-13 Multi Process Devnet Harness

<objective>
Prove that `sim_5a7s` process realism is executable, not just manifest text, by
starting multiple aggregator identities as separate local OS processes or a
Docker/local-task equivalent with separate ports, data directories, logs,
restart, kill, and partition evidence.
</objective>

<plan_fields>
plan_id: `067-13`
task_ids: `VERDICT-LCS-04`
copied_task_rows:
- Copied Task-To-Plan Coverage row: TASK-NNN=`none; VERDICT-LCS-04`; PLAN id=`067-13`; Source refs=`067-TODO.md`, `067-verdict.md`, `067-CONTEXT.md`, `067-COVERAGE.md`, `090-New-Scenarios/90-TODO.md`, `sim_5a7s` manifest/process anchors; Inputs=runnable binary, durable store, planner authority; Artifacts=local devnet script, optional Compose, process evidence; Tests=process tests, smoke script, scenario test; Expected results=process/devnet behavior is local simulated-full; Simulation requirement=local OS/Docker boundary only; consensus primitives real; Anti-placeholder proof=no manifest-only or Docker-file-only proof; Status=`complete`.
- `067-verdict.md` Gate 10 requires actual local process-level evidence or Docker/local task equivalent.
- `067-verdict.md` Gate 12 crash recovery must be exercised through process kill/restart with durable evidence reload, not regenerated fixtures.
- `067-verdict.md` Manifest-only `process_model` fields are insufficient for a process/devnet claim.
- `067-verdict.md` Stronger production approximation should combine deterministic transport exhaustiveness with a minimal multi-process smoke harness.
source_refs:
- `.planning/phases/067-Sharded-Concensus/067-TODO.md`
- `.planning/phases/067-Sharded-Concensus/067-verdict.md`
- `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
- `.planning/phases/090-New-Scenarios/90-TODO.md`
- `config/hjmt_runtime/sim_5a7s/manifest.json`
- `crates/z00z_rollup_node/src/runtime.rs`
- `crates/z00z_rollup_node/tests/test_hjmt_process.rs`
inputs:
- runnable aggregator binary from `067-10`
- durable evidence store from `067-11`
- deterministic planner authority from `067-12`
- signature and transport seam from `067-08`
- `sim_5a7s` manifest process identities
outputs:
- repo-owned local devnet script and optional Docker Compose harness
- separate aggregator process start, stop, restart, and log directories
- asserted per-process structured log evidence and optional process/devnet metric assertions when `tracing`, `tracing-subscriber`, `metrics`, or `prometheus` are installed by this slice
- local smoke test proving multi-process quorum, kill/restart, and artifact collection
- explicit skip or degraded marker when Docker is unavailable, while non-Docker process mode remains testable
dependencies:
- `067-08`
- `067-10`
- `067-11`
- `067-12`
acceptance_tests:
- `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`
- `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_hjmt_process_devnet -- --nocapture`
- `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_hjmt_process -- --nocapture`
- `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`
- `bash scripts/hjmt_local_devnet.sh --profile sim_5a7s --smoke --timeout 30`
simulation_gate:
- OS process boundary may be local, but each aggregator must have a separate identity, port, data directory, and log directory
- network partitions may be simulated through the transport seam or process harness controls, but vote creation still requires replay and signature verification
- process kill and restart must reload durable evidence instead of rebuilding state from fixtures
- if this slice installs `tracing`, `tracing-subscriber`, `metrics`, or `prometheus`, every claimed process/devnet observability surface must be asserted with per-process identity or run-id context and remain local-only evidence rather than production-overclaim
- Gate 12 crash recovery evidence must prove the same QC, publication state, DA/Celestia-local anchor, and validator decision path recover or explicitly downgrade after restart
negative_tests:
- duplicate ports reject before start
- missing process binary rejects before claiming devnet
- killed primary before quorum produces no QC or DA publication
- killed primary after quorum before DA resumes exact stored QC
- partitioned minority cannot synthesize quorum
- stale process data dir rejects on restart
- missing per-process identity or run-id observability rejects whenever this slice claims structured logs or metrics
plan_artifacts:
- this plan
- `scripts/hjmt_local_devnet.sh`
- `docker/compose.hjmt-local.yaml`
- per-run devnet evidence JSON
plan_tests:
- `rg -n "VERDICT-LCS-04|067-13" .planning/phases/067-Sharded-Concensus`
- `rg -n "Gate 12|crash recovery|durable evidence" .planning/phases/067-Sharded-Concensus/067-13-PLAN.md`
- `test -x scripts/hjmt_local_devnet.sh`
plan_results:
- devnet/process behavior becomes a tested local claim or fails closed
- final report can distinguish deterministic in-process simulation from process-level evidence
task_artifacts:
- task_id: `VERDICT-LCS-04`
  implementation_depth: `simulated-full`
  artifacts:
    - `scripts/hjmt_local_devnet.sh`
    - `docker/compose.hjmt-local.yaml`
    - `crates/z00z_rollup_node/Cargo.toml`
    - `crates/z00z_rollup_node/src/lib.rs`
    - `crates/z00z_rollup_node/src/main.rs`
    - `crates/z00z_rollup_node/src/process_devnet.rs`
    - `crates/z00z_rollup_node/tests/test_hjmt_process_devnet.rs`
    - `crates/z00z_simulator/tests/test_scenario_11.rs`
task_tests:
- task_id: `VERDICT-LCS-04`
  commands:
    - `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_hjmt_process_devnet -- --nocapture`
    - `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_hjmt_process -- --nocapture`
    - `bash scripts/hjmt_local_devnet.sh --profile sim_5a7s --smoke --timeout 30`
  scenarios:
    - `start five aggregator processes from manifest`
    - `kill primary before quorum`
    - `kill primary after quorum before DA`
    - `minority partition cannot commit`
task_results:
- task_id: `VERDICT-LCS-04`
  expected_results:
    - `each aggregator runs with distinct identity, port, data dir, and log dir`
    - `process restart uses persisted consensus evidence`
    - `minority and stale process paths fail closed`
anti_placeholder_gate:
- Docker file without a runnable smoke script does not close this plan
- process manifest validation without launching processes does not close this plan
- a harness that bypasses replay, signature, or storage by direct vote injection fails this plan
current_code_refs:
- `crates/z00z_rollup_node/src/runtime.rs`
- `crates/z00z_rollup_node/src/status.rs`
- `crates/z00z_rollup_node/tests/test_hjmt_process.rs`
- `config/hjmt_runtime/sim_5a7s/manifest.json`
blockers:
- if host Docker is unavailable, local OS process smoke remains required
- process harness must be deterministic enough for CI or clearly gated as a local conformance script with testable unit coverage
- `tracing`, `tracing-subscriber`, `metrics`, and `prometheus` remain optional until the harness emits asserted process evidence; if they are added here, the emitted rows and counters must stay tied to this local devnet seam
evidence_gate:
- per-process logs and JSON evidence are emitted
- when observability crates are installed here, per-process logs or metrics include aggregator identity, run id, and reject false-success counters
- smoke run proves start, quorum, kill/restart, and cleanup
- final report classifies devnet as local simulated-full, not public production devnet
not_recommendation_gate:
- this plan closes only with a runnable process harness and tests
</plan_fields>

<tasks>
<task type="auto" id="067-13-T1">
  <read_first>
  - `.planning/phases/067-Sharded-Concensus/067-TODO.md`
  - `.planning/phases/067-Sharded-Concensus/067-verdict.md`
  - `config/hjmt_runtime/sim_5a7s/manifest.json`
  - `crates/z00z_rollup_node/src/runtime.rs`
  - `crates/z00z_rollup_node/tests/test_hjmt_process.rs`
  </read_first>
  <action>
  Build a manifest-driven local devnet runner that launches multiple aggregator
  process identities, collects per-process evidence, and exercises kill,
  restart, and partition cases without bypassing the replay/signature/storage
  path.
  </action>
  <acceptance_criteria>
  - five `sim_5a7s` identities can be started locally
  - process kill/restart scenarios produce deterministic evidence
  - minority partitions cannot form a certificate
  - final report labels this as local simulated-full devnet
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, rerun, and only then continue.
  2. Create and run `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_hjmt_process_devnet -- --nocapture`.
  3. Run `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_hjmt_process -- --nocapture`.
  4. Run `bash scripts/hjmt_local_devnet.sh --profile sim_5a7s --smoke --timeout 30`.
  5. Extend and run `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`.
  6. Run `cargo test --release` if runtime, binary, or harness surfaces changed.
  7. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times; fix all issues and continue until at least 2 consecutive runs show no significant code issues.
  8. Use nested `.github` skills/prompts as needed, especially `doublecheck`, `spec-to-code-compliance`, smart tests, and Z00Z gates.
  9. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
- multiple local processes can be started from the manifest
- process kill and restart exercise durable state
- process/devnet claim is local simulated-full and evidence-backed
</verification>

## Coverage Appendix

| TASK-NNN | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| none; `VERDICT-LCS-04` | `067-13` | `067-TODO.md`, `067-verdict.md`, `sim_5a7s` manifest, process tests | runnable binary, durable store, planner authority, manifest | script, optional compose, process tests, evidence JSON | process devnet tests, smoke script, scenario test | local process/devnet behavior is executable | process boundary local; network faults may use transport seam | no manifest-only or Docker-only claim | complete |
