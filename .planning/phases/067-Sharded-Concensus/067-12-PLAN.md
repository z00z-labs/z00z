---
phase: 067
plan: 067-12
plan_id: 067-12
title: Planner Authority And Failover Claim Boundary
type: implementation
wave: 8
depends_on: [067-05, 067-06, 067-07, 067-10, 067-11]
task_ids: [VERDICT-LCS-03]
files_modified:
  - crates/z00z_runtime/aggregators/README.md
  - crates/z00z_runtime/aggregators/src/batch_planner.rs
  - crates/z00z_runtime/aggregators/src/lib.rs
  - crates/z00z_runtime/aggregators/src/types.rs
  - crates/z00z_runtime/aggregators/tests/test_hjmt_dispatch.rs
  - crates/z00z_runtime/aggregators/tests/test_live_guardrails.rs
  - crates/z00z_runtime/aggregators/tests/test_planner_authority.rs
  - crates/z00z_simulator/src/scenario_1/runtime_observability.rs
  - crates/z00z_simulator/src/scenario_11/mod.rs
  - crates/z00z_simulator/src/scenario_11/report.rs
  - crates/z00z_simulator/tests/test_scenario_11.rs
  - crates/z00z_wallets/src/redb_store/mod.rs
  - .planning/phases/067-Sharded-Concensus/067-GLOSSARY-CLAIMS.md
autonomous: true
requirements:
  - REQ-067-009
  - REQ-067-010
---

# 067-12 Planner Authority And Failover Claim Boundary

<objective>
Resolve the planner SPOF question by implementing the safer Phase 067 model:
planner truth is deterministic replicated computation over canonical config and
route-table digest. Remove any planner primary/secondary HA claim unless a real
planner-service protocol is implemented and tested.
</objective>

<plan_fields>
plan_id: `067-12`
task_ids: `VERDICT-LCS-03`
copied_task_rows:
- Copied Task-To-Plan Coverage row: TASK-NNN=`none; VERDICT-LCS-03`; PLAN id=`067-12`; Source refs=`067-TODO.md`, `067-verdict.md`, `067-CONTEXT.md`, `067-COVERAGE.md`, `090-New-Scenarios/90-TODO.md`, planner code/config anchors; Inputs=planner config, route digest, placement generation; Artifacts=planner authority checks, claim-registry row; Tests=planner drift and dispatch tests; Expected results=planner HA is implemented or removed as a live claim; Simulation requirement=each aggregator recomputes canonical plan locally; Anti-placeholder proof=no docs-only HA removal or planner stub; Status=`complete`.
- `067-verdict.md` Gate 11 requires planner HA implementation or claim removal.
- `067-verdict.md` The safer current path is deterministic replicated planner truth over canonical route-table config.
- `067-TODO.md` secondary replay must recompute from live planner and route inputs rather than copying primary bytes.
source_refs:
- `.planning/phases/067-Sharded-Concensus/067-TODO.md`
- `.planning/phases/067-Sharded-Concensus/067-verdict.md`
- `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
- `.planning/phases/090-New-Scenarios/90-TODO.md`
- `config/hjmt_runtime/sim_5a7s/planner/planner-config.yaml`
- `crates/z00z_runtime/aggregators/src/batch_planner.rs`
- `crates/z00z_runtime/aggregators/src/dist_dispatch.rs`
- `crates/z00z_runtime/aggregators/tests/test_hjmt_planner.rs`
inputs:
- canonical planner config
- shard route table digest and routing generation
- placement generation and membership digest
- `scenario_11` planner and dispatch evidence
outputs:
- deterministic planner authority API or validation wrapper
- config-drift and route-drift rejection tests across aggregators
- claim registry entry marking planner HA as `live-claim-removed` unless a real HA service is built
- report guard preventing implicit planner primary/secondary claims
dependencies:
- `067-05`
- `067-06`
- `067-07`
- `067-10`
- `067-11`
acceptance_tests:
- `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_planner_authority -- --nocapture`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_planner -- --nocapture`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_dispatch -- --nocapture`
- `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`
simulation_gate:
- every aggregator in the simulation must recompute the same plan from the same canonical inputs
- stale planner config, stale route table, or mixed planner generation must reject before vote creation or dispatch
- planner HA may appear only as `live-claim-removed` unless implemented as a separate tested service
negative_tests:
- one aggregator with drifted planner config rejects
- stale route-table digest rejects
- mixed planner generation rejects
- report containing unqualified planner HA claim rejects
- secondary replay using primary-precomputed plan bytes instead of recomputation rejects
plan_artifacts:
- this plan
- `.planning/phases/067-Sharded-Concensus/067-GLOSSARY-CLAIMS.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
plan_tests:
- `rg -n "VERDICT-LCS-03|067-12" .planning/phases/067-Sharded-Concensus`
- `rg -n "planner HA|primary planner|secondary planner|live-claim-removed" .planning/phases/067-Sharded-Concensus`
plan_results:
- planner authority is explicit and test-backed
- the phase no longer leaves a silent planner HA or SPOF claim
task_artifacts:
- task_id: `VERDICT-LCS-03`
  implementation_depth: `live-claim-removed`
  artifacts:
    - `crates/z00z_runtime/aggregators/README.md`
    - `crates/z00z_runtime/aggregators/src/batch_planner.rs`
    - `crates/z00z_runtime/aggregators/src/lib.rs`
    - `crates/z00z_runtime/aggregators/src/types.rs`
    - `crates/z00z_runtime/aggregators/tests/test_hjmt_dispatch.rs`
    - `crates/z00z_runtime/aggregators/tests/test_live_guardrails.rs`
    - `crates/z00z_runtime/aggregators/tests/test_planner_authority.rs`
    - `crates/z00z_simulator/src/scenario_1/runtime_observability.rs`
    - `crates/z00z_simulator/src/scenario_11/mod.rs`
    - `crates/z00z_simulator/src/scenario_11/report.rs`
    - `crates/z00z_simulator/tests/test_scenario_11.rs`
    - `.planning/phases/067-Sharded-Concensus/067-GLOSSARY-CLAIMS.md`
task_tests:
- task_id: `VERDICT-LCS-03`
  commands:
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_planner_authority -- --nocapture`
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_planner -- --nocapture`
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_dispatch -- --nocapture`
    - `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`
  scenarios:
    - `all aggregators recompute identical plan`
    - `planner config drift rejects`
    - `report rejects planner HA overclaim`
task_results:
- task_id: `VERDICT-LCS-03`
  expected_results:
    - `planner truth is deterministic replicated config`
    - `no unimplemented planner primary/secondary failover claim remains`
    - `mixed planner inputs fail before vote, dispatch, or validator acceptance`
anti_placeholder_gate:
- documenting planner HA removal without a report guard does not close this plan
- recomputation tests that compare fixture strings instead of planner outputs do not close this plan
- adding a planner service stub without failover tests does not close this plan
current_code_refs:
- `crates/z00z_runtime/aggregators/src/batch_planner.rs`
- `crates/z00z_runtime/aggregators/README.md`
- `crates/z00z_simulator/src/scenario_11/mod.rs`
- `crates/z00z_simulator/src/scenario_11/report.rs`
- `.planning/phases/067-Sharded-Concensus/067-GLOSSARY-CLAIMS.md`
blockers:
- a real planner HA service is out of scope unless it gains durable plan state, activation boundary, and stale-plan tests in this plan
- all final reports must use deterministic-replicated planner wording unless the stronger service is actually implemented
evidence_gate:
- planner drift tests pass
- scenario evidence records planner config digest and route-table digest
- glossary/report registry marks planner HA honestly
not_recommendation_gate:
- this plan closes only by code tests plus claim-registry enforcement
</plan_fields>

<tasks>
<task type="auto" id="067-12-T1">
  <read_first>
  - `.planning/phases/067-Sharded-Concensus/067-TODO.md`
  - `.planning/phases/067-Sharded-Concensus/067-verdict.md`
  - `config/hjmt_runtime/sim_5a7s/planner/planner-config.yaml`
  - `crates/z00z_runtime/aggregators/src/batch_planner.rs`
  - `crates/z00z_runtime/aggregators/src/dist_dispatch.rs`
  - `crates/z00z_runtime/aggregators/tests/test_hjmt_planner.rs`
  </read_first>
  <action>
  Implement deterministic replicated planner authority checks and remove the
  unimplemented planner HA claim from executable reports. Add drift tests that
  fail before vote or dispatch when planner or route inputs diverge.
  </action>
  <acceptance_criteria>
  - every simulated aggregator recomputes identical plan output from identical inputs
  - planner config or route digest drift rejects
  - final reports cannot claim planner HA unless a tested planner service exists
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, rerun, and only then continue.
  2. Create and run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_planner_authority -- --nocapture`.
  3. Run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_planner -- --nocapture`.
  4. Run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_hjmt_dispatch -- --nocapture`.
  5. Extend and run `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`.
  6. Run `cargo test --release` if shared planner or dispatch surfaces changed.
  7. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times; fix all issues and continue until at least 2 consecutive runs show no significant code issues.
  8. Use nested `.github` skills/prompts as needed, especially `doublecheck`, `spec-to-code-compliance`, smart tests, and Z00Z gates.
  9. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
- planner authority is deterministic, replicated, and digest-bound
- planner HA is either real and tested or removed as a live claim
- planner drift fails before consensus or validation
</verification>

## Coverage Appendix

| TASK-NNN | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| none; `VERDICT-LCS-03` | `067-12` | `067-TODO.md`, `067-verdict.md`, planner config and planner code anchors | planner config, route digest, placement generation | planner authority checks, drift tests, claim registry | planner, dispatch, scenario tests | no silent planner HA or SPOF ambiguity | planner is recomputed locally by every aggregator | no docs-only HA removal or stub planner service | complete |
