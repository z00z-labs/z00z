---
phase: 067
plan: 067-09
plan_id: 067-09
title: Bft And Celestia Backend
type: implementation
wave: 5
depends_on: [067-07, 067-08]
task_ids: [PHASE-8]
files_modified:
  - config/hjmt_runtime/sim_7a7s/manifest.json
  - config/hjmt_runtime/sim_7a7s/planner/planner-config.yaml
  - config/hjmt_runtime/sim_7a7s/aggregators/agg-*/aggregator-config.yaml
  - crates/z00z_runtime/aggregators/src/bft_committee.rs
  - crates/z00z_runtime/aggregators/src/bft_engine.rs
  - crates/z00z_runtime/aggregators/src/lib.rs
  - crates/z00z_runtime/aggregators/tests/test_bft_committee_rules.rs
  - crates/z00z_rollup_node/src/celestia_local.rs
  - crates/z00z_rollup_node/tests/test_celestia_local_binding.rs
  - crates/z00z_simulator/tests/test_scenario_11.rs
autonomous: true
requirements:
  - REQ-067-003
  - REQ-067-005
  - REQ-067-006
  - REQ-067-007
  - REQ-067-008
  - REQ-067-009
  - REQ-067-010
  - REQ-067-011
---

# 067-09 Bft And Celestia Backend

<objective>
Implement the external-backend layer through local deterministic simulation
first: BFT-valid committee sizing, 3f+1 membership and 2f+1 quorum rules, and a
Celestia-compatible local DA backend that resolves the same artifact contracts
already accepted by the certificate-aware validator path.
</objective>

<plan_fields>
plan_id: `067-09`
task_ids: `PHASE-8`
copied_task_rows:
- `Goal: implement the external networked BFT and Celestia layer.`
- `Actions: Increase committee sizing to BFT-valid sets; use 3f+1 membership and 2f+1 quorum certificates; add HotStuff or another BFT backend only behind the already-proven commit-subject interface; add Celestia DA backend after local DA certificate binding is complete.`
- `Acceptance: BFT claims are backed by 3f+1 committee tests; Celestia blob resolution produces the same artifact accepted by local DA tests; validator acceptance does not depend on trust in the primary.`
- `067-verdict.md Gates 4, 5, 7, and 8 require artifact-complete Celestia-local simulation, Celestia negative tests, BFT math, and HotStuff-like semantics only behind proven seams.`
source_refs:
- `.planning/phases/067-Sharded-Concensus/067-TODO.md`
- `.planning/phases/067-Sharded-Concensus/067-verdict.md`
- `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
- `.planning/phases/090-New-Scenarios/90-TODO.md`
- legacy non-canonical aggregator-consensus references `(stale drift only; do not synthesize a second authority file)`
inputs:
- commit-subject, replay, certificate, and validator-binding seams from `067-04` through `067-08`
- BFT-valid committee math and quorum rules
- allowed fake boundaries: external transport, remote process boundary, external DA transport, wall-clock, and unavailable third-party network
outputs:
- local BFT-valid simulator config with 3f+1 membership sets
- backend trait or adapter that stays behind the proven commit-subject interface
- Celestia-compatible local blob binding path that resolves to the same artifact contract as local DA
- config, metrics, alert, proposal-validation, and operator-runbook carry-forward for the simulated external backend layer
dependencies:
- `067-07`
- `067-08`
acceptance_tests:
- `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`
- `cargo test --release -p z00z_aggregators --features test-params-fast --test test_bft_committee_rules -- --nocapture`
- `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_celestia_local_binding -- --nocapture`
- `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_hjmt_topology -- --nocapture`
- `cargo test --release -p z00z_validators --test test_hjmt_publication_contract -- --nocapture`
- `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`
simulation_gate:
- only external transport or external DA transport may be faked; routing, placement, replay, certificate, publication, validator, and state paths must stay real
- 3f+1 membership and 2f+1 quorum behavior must be proven by local deterministic tests before any BFT claim is made
- Celestia-compatible local blob resolution must return the same batch artifact contract currently accepted through local DA
- challenge-window, unanchored-height, payload-retention, and degraded-mode behavior must be asserted through local config and test evidence rather than future-only prose
negative_tests:
- below-3f+1 membership rejects BFT mode
- below-2f+1 vote set rejects BFT certificate
- wrong blob commitment or wrong namespace rejects Celestia-local resolution
- validator acceptance cannot rely on primary trust when certificate or blob binding mismatches
- missing payload during the challenge window rejects or enters degraded mode
- commit-certificate verification failure or state-root mismatch rejects
- unanchored-height or settlement-delay limits enter degraded mode rather than silently overclaiming finality
plan_artifacts:
- this plan
- `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
- `.planning/phases/067-Sharded-Concensus/067-COVERAGE.md`
plan_tests:
- `rg -n "PHASE-8" .planning/phases/067-Sharded-Concensus/067-COVERAGE.md .planning/phases/067-Sharded-Concensus/067-09-PLAN.md`
- `rg -n "3f\\+1|2f\\+1|Celestia|simulated-full" .planning/phases/067-Sharded-Concensus/067-09-PLAN.md`
plan_results:
- external backend claims become locally test-backed instead of aspirational wording
- validator trust shifts from proposer trust to proof-bearing quorum and blob binding
task_artifacts:
- task_id: `PHASE-8`
  implementation_depth: `simulated-full`
  artifacts:
    - `config/hjmt_runtime/sim_7a7s/manifest.json`
    - `config/hjmt_runtime/sim_7a7s/planner/planner-config.yaml`
    - `config/hjmt_runtime/sim_7a7s/aggregators/agg-*/aggregator-config.yaml`
    - `crates/z00z_runtime/aggregators/src/bft_committee.rs`
    - `crates/z00z_runtime/aggregators/src/bft_engine.rs`
    - `crates/z00z_runtime/aggregators/src/lib.rs`
    - `crates/z00z_runtime/aggregators/tests/test_bft_committee_rules.rs`
    - `crates/z00z_rollup_node/src/celestia_local.rs`
    - `crates/z00z_rollup_node/tests/test_celestia_local_binding.rs`
    - `crates/z00z_rollup_node/tests/test_hjmt_topology.rs`
    - `crates/z00z_simulator/tests/test_scenario_11.rs`
task_tests:
- task_id: `PHASE-8`
  commands:
    - `cargo test --release -p z00z_aggregators --features test-params-fast --test test_bft_committee_rules -- --nocapture`
    - `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_celestia_local_binding -- --nocapture`
    - `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_hjmt_topology -- --nocapture`
    - `cargo test --release -p z00z_validators --test test_hjmt_publication_contract -- --nocapture`
    - `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`
  scenarios:
    - `3f+1 membership and 2f+1 quorum local proof`
    - `Celestia-compatible local blob resolution returns the same artifact contract`
    - `challenge-window, unanchored-height, and degraded-mode behavior`
    - `validator acceptance is independent of trusting the primary`
task_results:
- task_id: `PHASE-8`
  expected_results:
    - `BFT claims are backed by local 3f+1 and 2f+1 tests`
    - `Celestia-local blob resolution yields the same artifact shape accepted by the validator path`
    - `no external network dependency is required to prove backend correctness claims locally`
    - `config, metrics, alerts, and operator runbook boundaries stay truthful and local-backend-specific`
anti_placeholder_gate:
- renaming a 2-of-3 local quorum as BFT does not close this plan
- feature flags or adapter stubs without 3f+1 or 2f+1 tests do not close this plan
- a Celestia-local resolver that returns constant or detached artifacts does not close this plan
current_code_refs:
- `crates/z00z_runtime/aggregators/src/consensus_adapter.rs`
- `crates/z00z_runtime/aggregators/src/placement.rs`
- `crates/z00z_rollup_node/src/da.rs`
- `crates/z00z_rollup_node/tests/test_hjmt_topology.rs`
- `crates/z00z_runtime/validators/src/verdict.rs`
- `config/hjmt_runtime/sim_5a7s/manifest.json`
blockers:
- the scenario_11 review basis still carries legacy non-canonical aggregator-consensus drift; planning must continue from `067-TODO.md` and the live repo instead of inventing replacement authority
- this slice must reuse existing `z00z_core`, `z00z_crypto`, `z00z_storage`, and `z00z_utils` primitives and must not add a parallel HJMT, crypto, or utility layer
- if real third-party BFT or Celestia dependencies are unavailable, local deterministic adapters are mandatory rather than optional
- any new config, backend, adapter, doc, or test homes listed above are proposed targets if they do not yet exist in the worktree; implementation must prefer a tighter existing owner when one already exists
evidence_gate:
- 3f+1 committee and 2f+1 quorum rules are proven by local tests
- Celestia-compatible local binding tests pass and resolve the same artifact contract as local DA
- validator rejection covers detached certificate or blob binding even in simulated backend mode
- local config, degraded-mode, and topology evidence proves metrics or alert or runbook claims against the simulated backend layer
not_recommendation_gate:
- this slice is complete only when BFT and Celestia claims are backed by executable local proof, not by naming or deferred design notes
- a new production consensus crate, parallel state path, or bypass around repository-owned cryptographic and HJMT primitives does not close this plan
</plan_fields>

<tasks>
<task type="auto" id="067-09-T1">
  <read_first>
  - `.planning/phases/067-Sharded-Concensus/067-TODO.md`
  - `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`
  - `.planning/phases/090-New-Scenarios/90-TODO.md`
  - `crates/z00z_runtime/aggregators/src/consensus_adapter.rs`
  - `crates/z00z_rollup_node/src/da.rs`
  - `crates/z00z_runtime/validators/src/verdict.rs`
  - `config/hjmt_runtime/sim_5a7s/manifest.json`
  </read_first>
  <action>
  Add the BFT-valid committee and backend layer behind the proven
  commit-subject interface, plus a Celestia-compatible local blob adapter.
  Prove the backend claims through local deterministic simulation, using real
  routing, replay, certificate, publication, and validator primitives while
  faking only the allowed external boundaries.
  </action>
  <acceptance_criteria>
  - BFT mode uses 3f+1 membership and 2f+1 quorum certificates
  - BFT claims are backed by local deterministic tests
  - Celestia-compatible local blob resolution returns the same artifact contract as local DA tests
  - config and degraded-mode behavior stay truthful and locally test-backed
  - validator acceptance does not depend on trusting the primary
  </acceptance_criteria>
  <verify>
  1. Run `./.github/skills/smart-tests-bootstrap/scripts/bootstrap_tests.sh`; if it fails, stop, fix, rerun, and only then continue.
  2. Create and run `cargo test --release -p z00z_aggregators --features test-params-fast --test test_bft_committee_rules -- --nocapture`.
  3. Create and run `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_celestia_local_binding -- --nocapture`.
  4. Run `cargo test --release -p z00z_rollup_node --features test-params-fast --test test_hjmt_topology -- --nocapture`.
  5. Run `cargo test --release -p z00z_validators --test test_hjmt_publication_contract -- --nocapture`.
  6. Extend and run `cargo test --release -p z00z_simulator --features test-params-fast --features wallet_debug_tools --test scenario_11 -- --nocapture`.
  7. Run `cargo test --release` if broader Rust or test-affecting surfaces changed.
  8. Run `./.github/prompts/gsd-review-tasks-execution.prompt.md` (`/GSD-Review-Tasks-Execution`) in YOLO mode at least 3 times; fix all issues and continue until at least 2 consecutive runs show no significant code issues. If the runner is unavailable, perform the equivalent workspace-first manual review and record the blocker and clean reruns in the summary.
  9. Use nested `.github` skills/prompts as needed, especially `doublecheck`, `spec-to-code-compliance`, smart tests, and the relevant Z00Z verification gates.
  10. If committing, use `./.github/skills/z00z-git-versioning/scripts/version-manager.sh`.
  </verify>
</task>
</tasks>

<verification>
- BFT-valid committee sizing and quorum math are proven locally
- Celestia-compatible local binding resolves the same artifact contract as current DA paths
- validator trust is shifted to proof-bearing artifacts rather than proposer trust
</verification>

## Coverage Appendix

| TASK-NNN count | Required group | PLAN id | Source refs | Inputs | Artifacts | Tests | Expected results | Simulation requirement | Anti-placeholder proof | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | `PHASE-8` | `067-09` | `.planning/phases/067-Sharded-Concensus/067-TODO.md`, `.planning/phases/067-Sharded-Concensus/067-verdict.md`, `.planning/phases/067-Sharded-Concensus/067-CONTEXT.md`, `.planning/phases/090-New-Scenarios/90-TODO.md`, legacy non-canonical aggregator-consensus references noted in-plan | proven commit-subject/certificate/validator seams plus allowed simulated external boundaries | BFT-valid config, backend seam, Celestia-local binding, and tests | BFT committee, Celestia binding, validator, and scenario tests | local proof-backed BFT and Celestia-compatible behavior | deterministic backend simulation with only external transport or DA boundaries faked | no renamed CFT claims, no stub adapters, no constant blob path | planned |
